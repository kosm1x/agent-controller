# V8.3 Phase 6 §12/§14 consent-linkage audit — 2026-07-06

Verdict: PASS (0 Critical / 1 Warning / 4 Info). Scope: NEW `judgment-linkage.ts` + 15-line pipeline.ts block (the §12 demote) + reworked pipeline.test + new judgment-linkage.test. tsc clean, 143 v8-3 tests pass (per prompt). SAFETY-CRITICAL gate: decides if an L≥3 autonomous action runs without operator confirm.

ENFORCEMENT AIRTIGHT (traced): order = max_level cap → L0 refuse → ODD demote(≥3) → seam(b) reversibility demote(≥3, !sql_inverse→L2) → **§12 linkage demote(≥3, !ok→L2)** → route(`≤2||ux_confirm→confirm`). Nothing mutates effectiveLevel between linkage(L262) and route(L274). So route=autonomous ⟹ still ≥3 ⟹ linkage.ok=true ⟹ all 3 conditions held. No bypass. checkJudgmentLinkage is a plain SELECT that RETURNS a reason (never throws by design) for every failure → fail-closed on each: null id→no_linked_judgment, no row→judgment_not_found, confidence∉{green,yellow}→demote (CHECK-constrained to green/yellow/red/NULL so exact-match safe, red+NULL fail), criticVerdict≠'approved'→demote (returns "—" for null/malformed trail = fail-closed), is_prior≠1→demote.

DATETIME FIX = LOAD-BEARING + VERIFIED EMPIRICALLY (ran better-sqlite3): `pb.delivered_at < datetime(?)` wraps ONLY the param (RHS). Raw lexical `'…18:00:00' < '…T12:00:00Z'` = 1 (space 0x20 < 'T' 0x54 → future wrongly passes) — the fix's `datetime(?)`→'YYYY-MM-DD HH:MM:SS' makes future→0. delivered_at always normalized in prod (storage.ts:154 `datetime('now')`). is_prior returned as SQLite 1/0/NULL; `!== 1` handles all (same-second→0, NULL→0, undelivered→0 all fail-safe). briefing_id is `TEXT UNIQUE`+judgments.briefing_id `NOT NULL FK` → LEFT JOIN = exactly-1 row, no nondeterminism.

DOCTRINE — ASYMMETRIC datetime() STILL FAILS SAFE: wrapping only one side of a lexical date compare is safe IFF the unwrapped side (delivered_at) shares the space-format date PREFIX. For the SAME date an ISO ('T') delivered_at ALWAYS sorts AFTER space-format now → is_prior=0 → OVER-demote (fail-safe), never a wrong-pass. The only wrong-pass would need a non-prior delivered_at sorting BEFORE now, impossible for same-date since 'T'>' '. So the "both sides" claim in the docstring/test-comment is INACCURATE but harmless — correctness rests on delivered_at already being normalized, not on symmetric wrapping.

CANARY UNAFFECTED: jarvis_file_delete = level 1 / max_level 2 / tri_restore (seed.ts:94) → effectiveLevel=min(1,2)=1 → `if(effectiveLevel>=3)` linkage block never runs. Only max_level=5 seeds are task_edit(sql_inverse) + schedule_task(delete_inverse); schedule_task's delete_inverse≠sql_inverse → seam(b) demotes it before linkage, so task_edit is the ONLY cap that reaches linkage — and it's not the armed canary. NO wired L≥3 caller (only caller = executeGatedCapability←router.ts confirm-accept, builds L1-L2 no-sqlMutation triggers). Fully dormant/forward-looking, same as seams a/b.

W1 (forward-looking): degrade-on-throw. executeGatedCapability (trigger.ts:~163) catch → `if(output===undefined) return toolRegistry.execute()` = DIRECT UNLOGGED execute if runDecisionPipeline throws BEFORE execute ran. checkJudgmentLinkage runs before insertDecision/execute, so a DB-layer throw (table missing/SQLITE_BUSY) in the linkage SELECT for a WIRED L≥3 decision would bypass the gate entirely (worse than demote-to-confirm). Not live (canary L1, no L≥3 caller). SAME handoff caveat as reversibility-seams + phase-5 audits — Phase 6 adds one more throw-capable stage to that surface. Handoff: an L≥3 wired call site must degrade CLOSED (block/confirm), never direct-execute.

INFO: (I1) docstring L50-53 + test-comment L109 say "datetime() on BOTH sides" — only the param is wrapped; fix comment (harmless, see doctrine). (I2) gate verifies judgment VALIDITY not judgment→action RELEVANCE — any green/approved/prior judgment_id green-lights ANY autonomous action; caller trusted to link the judgment that proposed THIS action. Pin at handoff. (I3) "prior" = delivered_at strictly < now at 1s granularity, NOT a distinct-cycle identity; a brief delivered seconds before the decision counts as prior (acceptable given daily 06:00 cadence + "operator has seen it" intent). (I4) no pipeline-integration test for the not_prior_brief demote path (standalone linkage.test covers it; integration covers no-judgment/red/unfixable only).

TEST INTEGRITY — NOT MASKING: reworked L≥3 tests swapped arbitrary caps (c_l4/c_ux/c_l5) → task_edit + sqlTrigger(sql_inverse) + seedLinkedJudgment(green/approved/prior via delivered_at=datetime('now','-1 day')) = STRONGER (now needs 3 conditions to route autonomous). seedLinkedJudgment produces a GENUINELY valid linkage (verified: date-prior, green, {verdict:'approved'}), not a wrong-reason pass. Negative tests (no-judgment/red/unfixable → L2 confirm) + positive test (valid → autonomous, incl. L4-out-of-ODD→L3 which exercises linkage post-ODD-demote) constrain BOTH directions → an always-pass or always-fail linkage bug would be caught.

Demote-vs-block = spec-correct: §12 says red can't AUTONOMOUS-execute, not can't-execute; demote-to-confirm keeps operator in loop (mirrors ODD/reversibility demotes). Caveat: on the CURRENT wiring the confirm hand-off is still the Phase-2 mock auto-approve (pipeline.ts:359-361) + executeGatedCapability's execute cb runs the tool regardless of route, so demote-to-confirm changes route/ledger/events but not whether the tool runs — teeth depend on the deferred real router-confirm hand-off. Zero live impact (canary never reaches linkage).
