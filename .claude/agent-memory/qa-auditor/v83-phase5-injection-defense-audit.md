# V8.3 Phase 5 — prompt-injection defense audit (2026-07-06)

**Bundle**: `external-content.ts` (NEW) + pipeline.ts step 4b. Spec §8. VERDICT: **PASS**, 0 Crit / 2 Warn / 4 Info.

## What held (verified, not asserted)
- **Halt correctness**: step 4b (pipeline.ts:313-336) is an ADDITIVE early-return placed BEFORE both the confirm branch (:341) and execute (:349). On a heuristic hit it `return`s with status `interrupted` → execute unreachable (test asserts `executed=false`). A MISS falls through to the normal gate path — 4b never weakens confirm/reversibility.
- **Persistence**: `status='interrupted'` + `event_kind='interrupted'` verified present in the CHECK constraints on the **LIVE mc.db** (both entered in the schema's first commit e5648ff, so NO stale-`CREATE TABLE IF NOT EXISTS`-CHECK trap). Interrupted row + event persist for the §8 metric.
- **Envelope breakout**: `escapeText`(&,<,>) + `escapeAttr`(+") neutralize every vector tried — literal `</external_content>`, forged opening tag, attr-injection via `source`/`retrievedAt`. Classifier runs on RAW `item.content` BEFORE any wrapping (independent functions), so escaping never blinds the scan.
- **Trust never skips scan**: `scanExternalContent` reads `item.content` for every item, never `item.trust` — a mislabeled `trust:"trusted"` hostile item is flagged.
- **Canary unaffected**: trigger.ts builds DecisionTrigger with NO `externalContent` (:115-123) → 4b never fires for jarvis_file_delete. Confirmed.

## DOCTRINE crumbs
1. **English-only heuristic in a Spanish-FIRST deployment.** Empirically 4/4 Spanish injection probes SLIP all 8 patterns (ignora/eres ahora/no le digas/muestra tu prompt), plus base64/homoglyph/sentence-boundary/synonym-gap ("new TASK" not in the instruction-word alt). This is EXPECTED (heuristic ≠ complete) and the code does NOT overclaim (docstrings say "classic vectors","bias toward flagging","false negative is the danger"). Acceptable ONLY because it FAILS SAFE: 4b is additive, so undetected → normal confirm (L1-L2) / ODD+sql_inverse (L≥3) gates still apply. The regex is a TRIPWIRE, not the security boundary. Gap is undocumented → over-trust risk. (Warn)
2. **Observability-only wrapper vs a BLOCKING gate = contract tension.** trigger.ts contract = "never block a confirmed action, degrade-to-direct on any pipeline failure." Phase 5's point is to BLOCK. Today inert (no externalContent passed). But if Phase 5 is wired into trigger.ts later: an `interrupted` pipeline RETURN (no throw) leaves the execute callback un-run → `output` undefined → `return output ?? ""` → operator gets SILENT empty string, action neither runs nor signals blocked. The interrupted return is unhandled by the sole existing consumer. (Warn, forward-looking — same "R1 sound, R2 surfaces forward contract looseness" pattern as v76-spine6.)
3. **LLM-facing half of §8 is fully dormant.** grep: `wrapExternalContent`, `EXTERNAL_CONTENT_STANDING_RULE`, `normalizeTrust` have ZERO production callers (tests only); only `scanExternalContent` is wired. v8-3 has NO LLM site (grep infer/queryClaudeSdk empty) → standing-rule/caching deferral to Phase-6 critic is a DEFENSIBLE scope boundary, matches implementer disclosure (lines 84-91). Not a silent gap.
4. Spec deviation (safe dir): §8 "escalate to stricter mode for the session" NOT implemented — halts the single decision, no session-level escalation state.
