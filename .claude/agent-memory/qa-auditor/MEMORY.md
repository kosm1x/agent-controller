# QA Auditor Memory Index

> One line per entry: link + date + verdict + one doctrine crumb. Full detail lives in each topic file — Read the file, don't inline paragraphs here.
> Retention: hooks ≤180 chars. Never inline a paragraph. Cap ~17KB.

## Project Knowledge

- [gate-settleable-expect-r2-audit](gate-settleable-expect-r2-audit.md) - R1 folds verified, same bundle (09-12): PASS-W-NOTES, 0 Crit, 8/8 R1 bypasses refused, 136/136, 4/5 mutations RED, 1,415-pattern fuzz clean. The regex branch is closed BY CONSTRUCTION (probe and runner share `safeRegexTest` + the same 6 failure strings) - so the hole is the EARLY RETURN above it: `if (parsed.kind === "cmp") return null;` (expect.ts:162) skips the probe at :188, and `gte 0` / `gt -1` / `neq -1` sail through the real CLI door (exit 0) and are MET on `0\n`. And a refusal REASON can be false while the refusal is defensible: `/^[1-9][0-9]*$/m` demonstrably FAILS on its own check's count-0 output (`n\n-\n0`), all 12 live MET rows carry evidence `- | 1`, zero false METs - the live ritual now reads `Gates: 0/1 met · ABANDONED` nightly until the operator runs the prepared set-ritual.
- [gate-settleable-expect-r1-lens-a-audit](gate-settleable-expect-r1-lens-a-audit.md) - same bundle, CORRECTNESS lens (09-12): FAIL, 2 Crit, 85/85 green. A char-class strip that deletes `()?:` as "machinery" hands the bypass back: `(?:` `(?=` `(?<n>` leave `:`/`=`/`<n>` behind, so `/^(?:[1-9][0-9]*)$/m` - the live top expect plus 2 chars - is ADMITTED and MET on `echo 3`. And a failure-output set written as `["","0"]` is not what the shell EMITS (`"0\n"`): `/\D/`, `/[^0-9]/`, `/[\s]/` pass on any output. Fix replays 34/61 → 34/61, zero cost. Also: a 256KB HEAD cap starves a comparator that needs the TAIL; one trailing space downgrades a regex to a never-matching substring; write-time regex has no vm/deadline (451 ms on a 1-char haystack).
- [gate-settleable-expect-r1-audit](gate-settleable-expect-r1-audit.md) - Honest-Done settleable `expect` grammar, uncommitted (09-12): PASS-W-WARN, 9/9 mutations RED, 85/85. A write-time refusal is ALSO a READ-time refusal when specs are STORED: `scheduleGates` catches `parseGateSpecs` and returns `[]`, so the ONE active gated ritual (`/^[1-9][0-9]*$/m`, 20 rows) runs UNGATED after deploy - invisible surrender, no test. A layered refusal hides its unpinned layer: restricting the failure-output loop to substrings stays 85/85 GREEN, yet `/(PASS)?/`, `/x*/` need its regex arm. `ledgerVerdict` of an ALL-abandoned ledger is `met` (met=0) - abandoning a refused gate is a verdict-level pass; only `failed` demotes.
- [gate-settleable-expect-r1-audit](gate-settleable-expect-r1-audit.md) - Honest-Done write-time `expect` refusal, uncommitted (09-12): FAIL, 1 Crit, tsc clean + 224/224 v8-4 green, corpus replay 296 rows. A per-source refusal is only as visible as the CALLER's catch: the plan path emits an ABANDONED row, but `scheduleGates` (dynamic.ts:206-216) already swallowed parse errors "so a bad gates column never stops the ritual" — so the throw silently drops the WHOLE array (first-bad-entry), and the ONE live ritual gate (`/^[1-9][0-9]*$/m`, 20/20 ritual rows in 30d, active nightly 19:00) becomes 0 ledger rows. A write-time metric wired into the VALIDATOR counts the read path: `countGateRefusal("spec")` at gates.ts:137 ticks nightly forever from one stale row (proved 1,2,3). A module-scope prom-client Counter throws under a 2nd module identity (11 importers incl. both container workers) — latent, prom-client was already in the pre-change dist worker graph. A 28-char evidence PREFIX sits inside `substr(...,1,80)` (mc-ctl:3093). Abandoned-ONLY ledger ⇒ verdict `met` (`Gates: 0/1 met`), unpinned.
- [tool-desc-ratchet-r2-audit](tool-desc-ratchet-r2-audit.md) - R1 folds verified, 641f09a (09-12): PASS-W-NOTES, 0 Crit, 5/5 R1 Criticals fixed, 740/740, 6/7 mutations RED, tree clean. Muting a risk BAND mutes every producer: `risk==="low"` now returns unchanged, and `suspicious_formatting` (`---`+ignore/bypass/instruction) shares that band with `high_entropy` - an injection payload passes byte-identical with NO log line (the warn sits after the return). Corpus: 379/1096 KB docs hit low, 379/379 entropy-only, so the narrow fix is free. Reverting the A1 fix left guards.test.ts 60/60 GREEN. `isUntrustedTool` short-circuits: `jarvis_file_read` scores `none` on a textbook injection. A sibling guard scoped to `DO NOT USE` skips 11/162 tools that say `NOT FOR:`.
- [tool-desc-ratchet-r1-audit](tool-desc-ratchet-r1-audit.md) - not-for description ratchet + harness safety invariants, commits 5192508/62517d2 (09-12): FAIL, 2 Crit, 40/40 green, tree clean. A sibling pointer is EXECUTABLE: `northstar_index` doesn't exist and `findClosest` (Levenshtein <=30%) repairs it to `northstar_sync` at d=5/max=5 - a read-intent line lands on the destructive sync; `paper_trade` resolves to null. A ratchet over ONE source array (`BUILTIN+CRM+GWS+WP`, 162) misses google/memory/skills (32 tools) incl. 3 backfilled by the same commit. `USE ... INSTEAD` admits a POSITIVE recommendation (jarvis_file_move). A source-grep wiring pin fails on a pure `err`->`caught` rename and PASSES a guarded rethrow. A closed-set test that iterates the set can't see a new member.
- [security-approvals-r1-audit](security-approvals-r1-audit.md) - 685d313+54c64bc+fd0c2e1 injection-set widening + durable tool_approvals + memory_forget (09-12): FAIL, 2 Crit, 217/217 green, 2/3 mutations RED. A `low` risk tier still MUTATES: one `high_entropy` flag (>5.0) → `mediumFlags>=1 → "low"` → sanitizer prepends the ⚠️ banner because its early-return keys on `"none"` — corpus replay 100% of real HTML (http_fetch), 37.3% jarvis_files, 31.0% KB md, 19.5% runner JSON. A tool gated on `hasMemory: backend === "hindsight"` is UNREACHABLE (HINDSIGHT_ENABLED=false; 0 `memory_*` rows in 9,975 trace events). A thread-scoped `WHERE decision='pending'` UPDATE stamps rows the user never saw; approver = the GROUP JID, not `senderJid`. A negative test both the anchored AND unanchored regex fail pins NOTHING (dropping `^$` stayed green).
- [observability-tuning-r1-audit](observability-tuning-r1-audit.md) - a4a55d0+de3d7e7 termination_reason + cache gauges + variant fingerprint (09-12): FAIL, 2 Crit, 158/158 green, 2 mutations RED. A fingerprint hashed code-vs-code can still certify a payload snapshotted from the MUTATED singleton (`applySandbox` copies post-activation `DEFAULT_SCOPE_PATTERNS`) - the guard green-lights the drift it was written to catch, permanently. A new return field added at the BOTTOM return is inert when the live provider early-returns at the top (fast-runner:1284 claude-sdk branch; a maxTurns cap reports `completed`). `"stop"` is the claude-sdk SUCCESS exitReason and is NOT in the enum. Live April variant replaces 14 current code patterns across 11 groups.
- [email-verify-r5-audit](email-verify-r5-audit.md) - email_verify R5 delta, uncommitted (09-11): FAIL, 1 Crit, tsc clean + 574 tests green, 3x no flake. R4's W-1 fix INVERTED R3's Critical: widening `RE_NO_ACCOUNT` with a free `[\w.-]* ?` slot + `do not` and NO subject anchor makes SENDER-side/relay 5.7.x refusals read `invalid`/`mailbox_rejected` and CACHE 24h (proved e2e; control `Sender verify failed` still `blocked`). A shape-only guard misses its own new source's likeliest bad value (`hostname -f` -> `localhost.localdomain` HAS a dot -> probes). A NEW alternation branch does not inherit its sibling's `(?=\?|$)` lookahead. Anchoring RE_ENHANCED un-masks the phrase table for the trailing-`(#5.x.x)` family -> `unknown`, never cached, re-charged every run.
- [email-verify-r3-audit](email-verify-r3-audit.md) - email_verify R3, uncommitted (09-11): FAIL, 1 Crit, tsc clean + 546 tests green. R2 C-1/W-A..W-F all HOLD (43-reply corpus; maxVerifyMs=700 -> 705ms; `mx.fake.test` is NXDOMAIN so the SSRF `lookup` is finally mutation-RED). NEW: a phrase written FOR one reply that cannot match it - INVALID_PHRASES `"doesn't have an account"` never matches Yahoo's `"doesn't have a yahoo.com account"` (domain sits mid-phrase) -> every dead Yahoo/AOL box = `unknown`, never cached, re-charged every run. Moving a charge past DNS != past "no packets sent": paceHost+deadline still sit after `chargeConnection` (used 1->2, server saw 1 TCP conn). A 4xx CAN reach a cached verdict (`452 4.3.1 Insufficient system storage` -> risky/full_inbox, 24h).
- [email-verify-r2-audit](email-verify-r2-audit.md) - email_verify R2, uncommitted feature (09-11): FAIL, 1 Crit, tsc clean + 538 tests green. A safety check HOISTED above a structurally-correct classifier inherits its phrase list's FPs: bare "relay"/"access denied" in SENDER_SIDE_PHRASES make Postfix `relay_recipient_maps` and Office365 DBEB rejections read `blocked` - 5 ordinary invalid mailboxes open the breaker for 30 min (proved e2e vs the LOCAL-table control). A per-STEP timeout is not a wall-clock bound (maxVerifyMs=500 -> durationMs=1255). The SSRF enforcement line (custom `lookup`) has ZERO coverage: every test host is an IP literal and Node SKIPS lookup for those.
- [perf-usefulness-batch-r1-audit](perf-usefulness-batch-r1-audit.md) - perf/usefulness batch D6/D13/D5/U10/U16/U7 (09-10): FAIL, 3 Crit, tsc clean + 235 tests green. A length bound placed DOWNSTREAM of a truncator is INERT: `countWords<=120` sits after `capWords(...,EMAILED_PUSH_WORD_CAP=120)`, so on the emailed path it never binds - live replay of all 36 budget deferrals rescued 0/5 real failure notices (motivating tweet failure = 179w) and flipped exactly 1 FALSE POSITIVE (a success digest saying "digest storage failed"). "Every consumer reads <=90d" is a claim about mc-ctl too: `recall-modes` + `jme-stats` have NO window clause.
- [security-batch-sec01-18-r4-audit](security-batch-sec01-18-r4-audit.md) - shell.ts REVERTED + additive re-patch (09-10): FAIL, 1 Crit, tsc clean + 406 tests green, loosened=8/230. "Strictly additive" describes the CODE PATH, not the VERDICT: re-homing HEAD's `.env` rule into the shared `isBlockedEnvFile` inherited file_read's FP exemption, so `.env.secrets.json`/`.env.bak.txt`/`.env.prod.yml`/`.env.old.md` go BLOCK→ALLOW; and swapping `\b` for `(?![\w-])` drops the whole `.env-prod` family. `&`/`\n` separators cost ZERO FP on 55 realistic shapes — the only FP is `\n`-splitting UNQUOTED heredoc prose (`<<'EOF'` stays clean).
- **Archived: security-batch SEC-01..18 R1–R3 (09-10, all FAIL, pre-revert token/cwd pipeline)** → grep `security-batch-sec01-18-r[1-3]-audit.md`. Hottest crumbs: a per-rule sanitiser needs the whole PIPELINE receiver (`cat <<EOF | bash`); a wrapper-stripper that skips `sudo` but not `-n` returns `-n` AS the base command; quote splicing walks every path rule; deleting heredoc bodies from the scan input deletes them for EVERY rule; moving `mcpRateLimit` before `mcpAuth` 500s every MCP request.
- [logic-sql-batch-r1-audit](logic-sql-batch-r1-audit.md) - logic+SQL batch F1-F24, 40 files (09-10): FAIL, 2 Crit, tsc clean + 669 scoped tests green. Declaring `ESCAPE '\'` without escaping `\` FIRST makes `\d` match a bare `d` (proved on mc.db); and `prompt_tokens` INCLUDES `cache_read_tokens`, so switching a $0 claude row to list rate overstates a 94%-cached row 6x. Also: `grep -c` + `--max-count N` caps the reported COUNT per file; a short-circuit guard (`head.length>300`) leaves its sibling fold (`{0,4}`) unpinned; `fd` is MISSING on this box so file_find's live path is `find`.
- [kb-citation-searchoutput-r3-audit](kb-citation-searchoutput-r3-audit.md) - task_history keyword-AND + output-search ON BY DEFAULT (09-09): PASS-W/WARN, 0 Crit, 138/138. `matchSnippet` anchors on the EARLIEST token, which is the one the TITLE already matched - 12.2% of output-hits show the discriminating term in NEITHER outputMatch NOR outputPreview (incl. the motivating query). `matchedIn` is recomputed in JS while SQL `LIKE` selected the row, so a `_` in a token (`shell_exec`, 27 live) mislabels title hits as output. better-sqlite3 does NOT throw on UNUSED named params (only missing).
- **Archived: kb-citation R1 · diag-four-fixes R1 · stuck-watchdog R2 · psql-flailing R1 (09-03→09-09)** → grep the file by name. Hottest crumbs: `content TEXT NOT NULL` held BLOBs (TEXT affinity is not a constraint) and one row muted 15 hits; RAISING a cap changes the POPULATION it admits; `eventBus.emit()` reaches `.on()` and NO subscriber; hardening a `-c` BODY leaves the FLAGS loop as a second payload.
- [diag-four-fixes-r2-audit](diag-four-fixes-r2-audit.md) - R1 folds verified (09-06): PASS-WITH-NOTES, 0 Crit, 3/3 mutation-RED. A substring keyword score has NO word boundary (`con` inside `mexi[con]ecesario` still pulls a bearer token in 2/20 ordinary Spanish sentences); swapping `appendChunk` (no-ops when finalized) for a callback that calls `reset()` (UN-finalizes + repaints ⏳) hands a late SSE delta the power to erase a delivered reply; a whitespace-bounded secret regex masks `espn_s2` and LEAKS the `{SWID}` two lines below it.
- [strategy-relay-guard-r11-audit](strategy-relay-guard-r11-audit.md) - R11 allow-by-shape rewrite verified (09-05): PASS-WITH-NOTES, 0 Crit, 11/11 folds RED, 0/220 corpus mismatches. Ten deny-list rounds lost; anchoring BOTH ends and closing EVERY slot won. A case-SENSITIVE marker (`RECIPIENT_NAME`) is safe only because its `(a|to)` is lowercase - replay the corpus UPPERCASED and Title-Cased to prove it (0/140 flip).
- **Archived: strategy-relay-guard R1–R8 (09-05, 8 rounds, deny-list → allow-by-shape)** → grep `strategy-relay-guard-r[1-8]-audit.md`. Hottest crumbs: JS `\b` is ASCII-only so a bare `a` fires inside «Enví|a»; capping ONE alternation branch leaves its SIBLING uncapped; the `re-` prefix hides every DESIGN verb stem; a fold can be 0-RED over the corpus yet live-load-bearing; inverting a deny-list moves the leak from the VERB axis to the SEPARATOR axis.
- [trustr-remote-deadend-r2-audit](trustr-remote-deadend-r2-audit.md) — R1 folds verified (09-01): SHIP, 0 Crit/0 High. Answer "did the anchoring lose a shape?" with a printed OLD-vs-NEW table over 22 real spellings (the only losses were shapes OLD got WRONG); count allow-list inputs the extractor can't extract (0/11); pin a hoisted path guard with `not.toHaveBeenCalled()` on the subprocess mock; prettier drift only counts against the HEAD pre-image; the PROSE half of a two-half fix is the unpinned half.

- [sdk-bump-0-3-245-r1-audit](sdk-bump-0-3-245-r1-audit.md) — claude-agent-sdk 0.3.207→0.3.245 + npm-12 `allowScripts` (09-01): PASS W/WARN, 0 Crit. An `else if` chain with NO trailing `else` makes every added stream frame inert by construction; the image's `mc.lock-sha256` label matching `sha256sum package-lock.json` PROVES npm 10.9.9 handled the new `libc` arrays (only glibc installed); npm 10 IGNORES `allowScripts`, so the denial is unenforced in the image; a cost multiplier is Critical only if a threshold is ARMED (`enabled=false`, but daily sat at 89.7%).

- [nanoclaw-upstream-review-r1-audit](nanoclaw-upstream-review-r1-audit.md) — host dist/ mounts + image lock label (09-01): PASS W/WARN, 0 Crit. A live `docker run` with the new mount proved the motivating break (`Cannot find package undici` — a PROD dep newer than the image) and that the drift guard catches it; probe EACH worker entrypoint (nanoclaw OK, heavy fatal). A lockfile label proves the LOCKFILE matched, not that every import RESOLVES under `--omit=dev`; `{{index .Config.Labels "X"}}` on a missing label exits 0 with an empty line; re-authoring an allow-list does not fix `..` it never checked.

- [hermes-ssrf-standing-orders-r3-audit](hermes-ssrf-standing-orders-r3-audit.md) — R2 folds verified (09-01): PASS W/WARN, 0 Crit, closure-ready. A guard pinned at the FUNCTION level can be unpinned at every CALL SITE — grep tests for the door's REFUSAL MESSAGE, not the guard's name (3 disk doors revert GREEN); a generated `it()` shows `added_it=0` in the diff; a command-text write guard inherits its EXTRACTOR's blind spots (quoted path + `sed -i` skip it — and skip the pre-existing mission-control deny too, so pre-existing).
- **Archived: superseded earlier rounds (hermes-ssrf R1–R2 · trustr-remote-deadend R1 · memory-arch-v2 R1–R3 · v85-opensandbox R1 · v85-rule-of-two R1 · v82-combinator R2)** → grep the file by name. Hottest crumbs: a `..`-guard must normalize against a VIRTUAL ROOT; anchoring a heading parser can INERT it (its only caller feeds JSON); a DOCKER-USER `-i eth0` rule guards only the EXTERNAL edge; an "exact set" pinned over a registry the test never BUILDS is vacuous.
- [piotr-identity-fix-r1-audit](piotr-identity-fix-r1-audit.md) — Piotr=Jarvis in P1 + consolidator Names rule (08-31): PASS W/WARN, 0 Crit. Spanish `te llama` is unambiguous and the literal keeps the cache prefix static; but a prose rule that ALREADY failed once (anti-echo) got no code twin, and a new self-name in P1 un-covers the CM signature deny-list that enumerates only Jarvis/IA/asistente. A positive pin elsewhere is what proves a narrowed `not.toContain` still discriminates.
- [memory-arch-v2-r4-audit](memory-arch-v2-r4-audit.md) — R3 folds verified (08-28): PASS W/WARN, 0 Crit, closure-ready. Both Criticals mutation-RED; identity replay re-derived 3032/3032 plus a clean bijection. NEW: shieldCode's `CODE0` placeholder is SPELLABLE by prose (a `| CODE0 |` table cell restores as `undefined`), 0 live. And: `git checkout --` to undo a planted mutation DELETES the uncommitted bundle — use `cp` + `md5sum -c`.
- [budget-ruling-5-1400-audit](budget-ruling-5-1400-audit.md) — reading budget 4/700→5/1400 (08-27): PASS W/WARN, 2 Crit. Raising a cap DELETED it — `slots x per-push cap` (3x255+anchors=1381) < WORD_CAP=1400, so the word branch is inert (needs a 636w sync; live max 357); and reverting the constant to its OLD value left 51/51 GREEN — mutate to the old value, not just to absurd.
- **Archived: 19 jarvis-usability audits P0–P5 (08-23/24, all SHIPPED)** → grep `usability-phase*-audit.md` by name. Hottest crumbs: score a text filter against the LIVE CORPUS not fixtures; a budget whose EXEMPT members still count starves everything else; GNU option BUNDLING defeats per-binary flag regexes (flag deny-lists never converge — allow by membership); an ANCHORED verifier behind an UNANCHORED admitter admits any prefix; a cap that trims the payload BEFORE the dedup ledger records it starves the filter.
- **Archived: 10 audits 2026-08-02 → 08-17 (v83 seam-origin · v85 opensandbox R2 · v84 honest-done · v85 rule-of-two R2 · unicode absence probe · v83 background seam · v82 sync-surfacing · oom scope-wrap · v82 combinator R3 · v82 §17 6a removal)** → grep the file by name. Hottest crumbs: `sent>0` ≠ delivered (a `return "error"` RESOLVES); a 2nd exec path around a guarded tool WIDENS the envelope; widening an ALS store fresh→inherit-parent turns every in-context callback into a cross-task bleed; a `grep -c` readout aborts under `set -e` exactly when the count is 0; deleting the check that PINNED a gate un-pins the `||` combinator; a throw-capable resolve OUTSIDE the try defeats "never blocks"; a label whose producer serves EVERY inbound sender is not "operator".
- **Archived: 18 audits 2026-07-05 → 07-27 (planner sizing · graded-down delivery · v85 §3.3 metering · v82 6a affordance · v81 §13 · skill fast-route · v83 phases 5/6/7 · hardening sweep · efficiency refactor ×2 · support-subsystems · hotpath perf · phase0-1a · git-shell prefix · v83 seams · loop-unlimited)** → grep the file by name. Hottest crumbs: a decision-table row is DORMANT until a producer emits its trigger token; a batched delete violates a self-ref parent FK when a subtree straddles a batch boundary; broadening a write allow-list to the PARENT prefix grants the parent's non-project children; an explicit-token allow-list only helps if the tokens are RARE outside the intended act.
## Older audits

- **Archived: 16 v6.x sprint audits (2026-04)** → [archived-v6-sprint-audits](archived-v6-sprint-audits.md) — grep for any v6.0/v6.2/v6.3/v6.4 sprint audit.
- **Archived: 20 audits 2026-04-08 → 05-15** (CCP1-5, F7-F9, Google Workspace, security sweep, v7.3/v7.6, northstar strict-mirror, prompt-enhancer, sdk-wrapper, skill-evolution, community-manager, evolution-log ×2) → [archived-pre-may-audits](archived-pre-may-audits.md) — grep by name.
- [loop-unlimited-r1-audit](loop-unlimited-r1-audit.md) — /loop unlimited task (08-27): FAIL, 2 Crit. A command feature shipped behind an adapter that drops every "/"-prefixed message (the sibling /rituales regex makes the slash optional — that IS the tell); lifting an inner timeout leaves the OUTER abandon timer, which owns the only AbortController.
