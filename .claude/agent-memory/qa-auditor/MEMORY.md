# QA Auditor Memory Index

> One line per entry: link + date + verdict + one doctrine crumb. Full detail lives in each topic file — Read the file, don't inline paragraphs here.
> Retention: hooks ≤180 chars. Never inline a paragraph. Cap ~17KB.

## Project Knowledge

- [dep-trust-shell-gate-r16-audit](dep-trust-shell-gate-r16-audit.md) - R15 folds C15-1/W15-2/N15-1 ONLY, uncommitted (09-16): PASS-WITH-WARNINGS, 0 Crit, 2 Warn, 4 Notes, 362/362, tsc 0, corpus 0/454 + 0 verdict diffs vs BOTH the pre-fix build and HEAD. C15-1 is CLOSED: the R15 exploit matrix replays 136/136 refuse (0 flips), my own 17x7 rerun 0/119, and all 5 payloads that R15 EXECUTED (3 unshimmed `cp npm` copies + a deleted dir) are now blocked at the real `shellTool.execute` boundary with a fake npm on a temp PATH. The widened word-start class `[\s();&|]` swallows no name: 227 command-shaped names x 6 leading separators = 0 flips AND 0 new FPs / 1135 cells; 90/108 cross-product pins are mutation-RED against the R14 normalizer; every shape linear on doubling. W15-2 16/16 (and it fixed 3 HEAD FPs: `.data/`, `..data/`, `a.data/`). N15-1 closes `>|`/`2>|`/`{fd}>|`/spaced while the append exemption survives (pinned + mutation-RED). W16-1: doc §5 says "13 of the auditor's 17 payloads EXECUTED against a fake binary" - 13 passed the GATE, 4 executed; that conflation is the only in-scope inaccuracy. W16-2 (pre-existing at HEAD, NOT on the disclosed-open list): `^[({]+` is stripped once at the SEGMENT HEAD, so after a `COMMAND_WRAPPERS` keyword the token `(rm` becomes the base command - `for f in *.log; do (rm -rf build); done` ALLOWs (bash-verified to run); it bypasses only the pre-existing DENY list, every dependency-trust rule still refuses. And R15's own reach list mis-attributed 3 rows to C15-1 - all 3 ALLOW with NO fd at all and are identical at HEAD. (detail in file)
- **Archived: dep-trust-shell-gate R15 (09-16, FAIL, "digit run is an fd only after whitespace" era)** -> grep `dep-trust-shell-gate-r15-audit.md`. Hottest crumbs: moving a normalizer's ANCHOR moves its blind spot (R13 anchored before the digits and lost a name, R15 anchored after them and lost every fd starting a word at `(`/`;`/`&&`/`|`); a fix made OUTSIDE a 3-strike-stopped class is still gradeable, and must be replayed against that class's previously CLOSED spellings; a pinned row pins a SPELLING, not the property.
- **Archived: dep-trust-shell-gate R14 (09-16, FAIL, "redirectionSpan equality test" era)** -> grep `dep-trust-shell-gate-r14-audit.md`. Hottest crumbs: a span function is a GRAMMAR not an equality test (`op === token && !/&[\d-]*$/` - both halves proxies, so `>& 2` and `<<- TAG` handed command position to the next word, 3rd failure = 3-strike stop); a normalizer that inserts separators can DELETE a name (`sqlite3>o` -> `sqlite` + `3>o`); closing an operator in the TOKENIZER does not close it in the REGEXES (`>|` still wrote to /etc and src/).
- **Archived: dep-trust-shell-gate R13 (09-16, FAIL, "a redirection is not a word" era)** -> grep `dep-trust-shell-gate-r13-audit.md`. Hottest crumbs: the closure was graded against the spellings R12 HAPPENED to use - the `&` of `2>&1` got `(?<![<>])` in all three splitters and the `|` of `>|` got nothing; both tokenizers split on WHITESPACE ONLY while bash also delimits on `<>|&;()`, so `cp>/tmp/zz` was ONE token whose basename was `zz` - silencing the whole DENY_COMMANDS list.
- **Archived: dep-trust-shell-gate R11-R12 (09-16, both FAIL, bounded-lookahead then hoisted one-pass eras)** -> grep `dep-trust-shell-gate-r1[12]-audit.md`. Hottest crumbs: `LOOKAHEAD=64` counted RAW tokens so 64 `-v` flags pushed the manager name out of view; `fresh` ("seen a real command word?") was minted by a wrapper's OPTION VALUE (22/23 known basenames work as decoys); then the hole that outlived four rewrites of the rule BODY lived in the two lines that FEED it - a REDIRECTION took command position and the splitters cut `2>&1` in half at the bare `&`.
- **Archived: dep-trust-shell-gate R7-R10 (09-16, all FAIL, glob-parser + sticky/window/known-command-word eras)** -> grep `dep-trust-shell-gate-r(7|8|9|10)-audit.md`. Hottest crumbs: a glob->regex translator leaks BOTH ways and re-opens ReDoS; POSIX leading `]` is a literal member (`n[!]]m` -> npm); killing a catastrophic regex can move the DoS to the CALLER (any wrapper word mints a free command position, 18 KB = 33 s); enumerating wrapper VALUES leaves every non-numeric value open - model the SURVIVAL of command position, never the shapes that precede it; sticky made every token a command position and the 3 rest-of-segment lookaheads went O(n^2) (58 KB = 13.2 s).
- **Archived: dep-trust-shell-gate R3-R6 (09-16; R3/R4/R5 FAIL, R6 PASS-W-WARN)** -> grep `dep-trust-shell-gate-r[3456]-audit.md`. Hottest crumbs: the TOKENIZER is the attack surface once the regex layer dies (`${P:-npm}`, a heredoc's receiver is the whole PIPELINE); a PATH-shim gate is beaten by changing the NAME or the PATH, and `npm run <script>` bodies had ZERO scanners; a script-body checker that models `npm run` misses `npm restart` (synthesized stop+start) and the pnpm/yarn/bun SHORTHAND; a path-NAME match dies on ONE glob char (`cp /usr/bin/np? zz`). Prove "allowed" with a FAKE binary on PATH; mc-guard blocks a Bash command whose TEXT contains the install verb - assemble it at runtime.
- **Archived: dep-trust-shell-gate R1-R2 (09-16, both FAIL, pre-rewrite token/regex layers)** -> grep `dep-trust-shell-gate-r[12]-audit.md`. Hottest crumbs: a per-SEGMENT token gate dies on anything that RE-ENTERS a shell (`bash -c`, `eval`, `xargs`, a QUOTED HEREDOC the sanitizer deleted first); `-{1,2}[\w=/.:@-]+` = 29 s on 229 chars; "0 refused" over a corpus means nothing without a diff vs the PRE-change validator.
- [agent-memory-t1-p1-r2-audit](agent-memory-t1-p1-r2-audit.md) - agent-memory Tier1+P1 shadow logger, uncommitted (09-16): PASS-W-WARN, 0 Crit, 7 Warn, 283/283, tsc clean. A one-file exclusion fix missed its SIBLING reader 43 lines below; "unreachable from router.test.ts" was FALSE (76 executions - count the fn's own console.log); a metric recorded BEFORE the budget loop reports a tax never paid.
- [skills-scope-group-r1-audit](skills-scope-group-r1-audit.md) - `skills` added to CLASSIFIER_SYSTEM_PROMPT + VALID_GROUPS, uncommitted (09-13): PASS-W-WARN, 0 Crit, 380/380. Measured: 0 `skills` lines in 216 semantic classifications/30d and 0 skill_run/load/describe calls, and the regex fallback fires `skills` on only 2/29 of the certified skills' own trigger phrases (one lands on `video` via `pel[ií]culas?`) - so … (detail in file)
- [screenwriting-corpus-r2-audit](screenwriting-corpus-r2-audit.md) - R1 3-lens folds verified, uncommitted (09-12): PASS-W-NOTES, 0 Crit, 19/19 + tsc clean, 16/16 live certification. SQLite `length()` counts CHARACTERS not bytes (a 174-byte "drift" was em-dashes). Both src folds are 100% UNPINNED (no test names `timeoutMs`/`maxTokens`, 0 `toHaveBeenCalledWith`) - and `maxTokens: 4096` is INERT on the live provider … (detail in file)
- **Archived: gate-settleable-expect R1/R1-lens-A/R2 · tool-desc-ratchet R1/R2 (09-12)** -> grep the file by name. Hottest crumbs: a write-time refusal is also a READ-time refusal when specs are STORED (the caller's catch swallows it and the gate runs UNGATED); a char-class strip that deletes `()?:` as "machinery" hands `(?:`/`(?=`/`(?<n>` back; muting a risk BAND mutes every producer in it; a sibling pointer is EXECUTABLE (fuzzy repair lands a read-intent line on a destructive sync).
- **Archived: email-verify R2/R3/R5 · perf-usefulness-batch R1 · logic-sql-batch R1 · observability-tuning R1 · security-approvals R1 (09-10→09-12, all FAIL)** -> grep the file by name. Hottest crumbs: a phrase list HOISTED above a correct classifier inherits its FPs, and widening it later INVERTS the prior round's Critical; a length bound placed DOWNSTREAM of a truncator is INERT; `ESCAPE '\'` without escaping `\` first makes `\d` match a bare `d`; `prompt_tokens` INCLUDES `cache_read_tokens`; a fingerprint hashed code-vs-code still certifies a payload snapshotted from the MUTATED singleton; a `low` risk tier still MUTATES.
- [security-batch-sec01-18-r4-audit](security-batch-sec01-18-r4-audit.md) - shell.ts REVERTED + additive re-patch (09-10): FAIL, 1 Crit, tsc clean + 406 tests green, loosened=8/230. "Strictly additive" describes the CODE PATH, not the VERDICT: re-homing HEAD's `.env` rule into the shared `isBlockedEnvFile` inherited file_read's FP exemption, so `.env.secrets.json`/`.env.bak.txt`/`.env.prod.yml`/`.env.old.md` go … (detail in file)
- **Archived: security-batch SEC-01..18 R1–R3 (09-10, all FAIL, pre-revert token/cwd pipeline)** → grep `security-batch-sec01-18-r[1-3]-audit.md`. Hottest crumbs: a per-rule sanitiser needs the whole PIPELINE receiver (`cat <<EOF | bash`); a wrapper-stripper that skips `sudo` but not `-n` returns `-n` AS the base command; quote splicing walks every path rule; deleting heredoc bodies from the scan input deletes them … (detail in file)
- [kb-citation-searchoutput-r3-audit](kb-citation-searchoutput-r3-audit.md) - task_history keyword-AND + output-search ON BY DEFAULT (09-09): PASS-W/WARN, 0 Crit, 138/138. `matchSnippet` anchors on the EARLIEST token, which is the one the TITLE already matched - 12.2% of output-hits show the discriminating term in NEITHER outputMatch NOR outputPreview (incl. the motivating query). `matchedIn` is recomputed in JS … (detail in file)
- **Archived: kb-citation R1 · diag-four-fixes R1 · stuck-watchdog R2 · psql-flailing R1 (09-03→09-09)** → grep the file by name. Hottest crumbs: `content TEXT NOT NULL` held BLOBs (TEXT affinity is not a constraint) and one row muted 15 hits; RAISING a cap changes the POPULATION it admits; `eventBus.emit()` reaches `.on()` and NO subscriber; hardening a `-c` BODY leaves the FLAGS loop as a second payload.
- [diag-four-fixes-r2-audit](diag-four-fixes-r2-audit.md) - R1 folds verified (09-06): PASS-WITH-NOTES, 0 Crit, 3/3 mutation-RED. A substring keyword score has NO word boundary (`con` inside `mexi[con]ecesario` still pulls a bearer token in 2/20 ordinary Spanish sentences); swapping `appendChunk` (no-ops when finalized) for a callback that calls `reset()` (UN-finalizes + repaints ⏳) hands a late SSE delta the power … (detail in file)
- [strategy-relay-guard-r11-audit](strategy-relay-guard-r11-audit.md) - R11 allow-by-shape rewrite verified (09-05): PASS-WITH-NOTES, 0 Crit, 11/11 folds RED, 0/220 corpus mismatches. Ten deny-list rounds lost; anchoring BOTH ends and closing EVERY slot won. A case-SENSITIVE marker (`RECIPIENT_NAME`) is safe only because its `(a|to)` is lowercase - replay the corpus UPPERCASED and Title-Cased to prove it (0/140 flip).
- **Archived: strategy-relay-guard R1–R8 (09-05, 8 rounds, deny-list → allow-by-shape)** → grep `strategy-relay-guard-r[1-8]-audit.md`. Hottest crumbs: JS `\b` is ASCII-only so a bare `a` fires inside «Enví|a»; capping ONE alternation branch leaves its SIBLING uncapped; the `re-` prefix hides every DESIGN verb stem; a fold can be 0-RED over the corpus yet live-load-bearing; inverting a deny-list moves the leak from … (detail in file)
- [trustr-remote-deadend-r2-audit](trustr-remote-deadend-r2-audit.md) — R1 folds verified (09-01): SHIP, 0 Crit/0 High. Answer "did the anchoring lose a shape?" with a printed OLD-vs-NEW table over 22 real spellings (the only losses were shapes OLD got WRONG); count allow-list inputs the extractor can't extract (0/11); pin a hoisted path guard with `not.toHaveBeenCalled()` on the subprocess mock; prettier drift only … (detail in file)

- [sdk-bump-0-3-245-r1-audit](sdk-bump-0-3-245-r1-audit.md) — claude-agent-sdk 0.3.207→0.3.245 + npm-12 `allowScripts` (09-01): PASS W/WARN, 0 Crit. An `else if` chain with NO trailing `else` makes every added stream frame inert by construction; the image's `mc.lock-sha256` label matching `sha256sum package-lock.json` PROVES npm 10.9.9 handled the new `libc` arrays (only glibc installed); npm 10 IGNORES … (detail in file)

- [nanoclaw-upstream-review-r1-audit](nanoclaw-upstream-review-r1-audit.md) — host dist/ mounts + image lock label (09-01): PASS W/WARN, 0 Crit. A live `docker run` with the new mount proved the motivating break (`Cannot find package undici` — a PROD dep newer than the image) and that the drift guard catches it; probe EACH worker entrypoint (nanoclaw OK, heavy fatal). A lockfile label proves the LOCKFILE matched, … (detail in file)

- [hermes-ssrf-standing-orders-r3-audit](hermes-ssrf-standing-orders-r3-audit.md) — R2 folds verified (09-01): PASS W/WARN, 0 Crit, closure-ready. A guard pinned at the FUNCTION level can be unpinned at every CALL SITE — grep tests for the door's REFUSAL MESSAGE, not the guard's name (3 disk doors revert GREEN); a generated `it()` shows `added_it=0` in the diff; a command-text write guard inherits its EXTRACTOR's … (detail in file)
- **Archived: superseded earlier rounds (hermes-ssrf R1–R2 · trustr-remote-deadend R1 · memory-arch-v2 R1–R3 · v85-opensandbox R1 · v85-rule-of-two R1 · v82-combinator R2)** → grep the file by name. Hottest crumbs: a `..`-guard must normalize against a VIRTUAL ROOT; anchoring a heading parser can INERT it (its only caller feeds JSON); a DOCKER-USER `-i eth0` rule guards only the EXTERNAL edge; an "exact set" pinned … (detail in file)
- [piotr-identity-fix-r1-audit](piotr-identity-fix-r1-audit.md) — Piotr=Jarvis in P1 + consolidator Names rule (08-31): PASS W/WARN, 0 Crit. Spanish `te llama` is unambiguous and the literal keeps the cache prefix static; but a prose rule that ALREADY failed once (anti-echo) got no code twin, and a new self-name in P1 un-covers the CM signature deny-list that enumerates only Jarvis/IA/asistente. A positive pin … (detail in file)
- [memory-arch-v2-r4-audit](memory-arch-v2-r4-audit.md) — R3 folds verified (08-28): PASS W/WARN, 0 Crit, closure-ready. Both Criticals mutation-RED; identity replay re-derived 3032/3032 plus a clean bijection. NEW: shieldCode's `CODE0` placeholder is SPELLABLE by prose (a `| CODE0 |` table cell restores as `undefined`), 0 live. And: `git checkout --` to undo a planted mutation DELETES the uncommitted bundle — use … (detail in file)
- [budget-ruling-5-1400-audit](budget-ruling-5-1400-audit.md) — reading budget 4/700→5/1400 (08-27): PASS W/WARN, 2 Crit. Raising a cap DELETED it — `slots x per-push cap` (3x255+anchors=1381) < WORD_CAP=1400, so the word branch is inert (needs a 636w sync; live max 357); and reverting the constant to its OLD value left 51/51 GREEN — mutate to the old value, not just to absurd.
- **Archived: 19 jarvis-usability audits P0–P5 (08-23/24, all SHIPPED)** → grep `usability-phase*-audit.md` by name. Hottest crumbs: score a text filter against the LIVE CORPUS not fixtures; a budget whose EXEMPT members still count starves everything else; GNU option BUNDLING defeats per-binary flag regexes (flag deny-lists never converge — allow by membership); an ANCHORED verifier behind an UNANCHORED admitter … (detail in file)
- **Archived: 10 audits 2026-08-02 → 08-17 (v83 seam-origin · v85 opensandbox R2 · v84 honest-done · v85 rule-of-two R2 · unicode absence probe · v83 background seam · v82 sync-surfacing · oom scope-wrap · v82 combinator R3 · v82 §17 6a removal)** → grep the file by name. Hottest crumbs: `sent>0` ≠ delivered (a `return "error"` RESOLVES); a 2nd exec path around a guarded tool WIDENS the envelope; widening an ALS … (detail in file)
- **Archived: 18 audits 2026-07-05 → 07-27 (planner sizing · graded-down delivery · v85 §3.3 metering · v82 6a affordance · v81 §13 · skill fast-route · v83 phases 5/6/7 · hardening sweep · efficiency refactor ×2 · support-subsystems · hotpath perf · phase0-1a · git-shell prefix · v83 seams · loop-unlimited)** → grep the file by name. Hottest crumbs: a decision-table row is DORMANT until a producer emits its trigger … (detail in file)
## Older audits

- **Archived: 16 v6.x sprint audits (2026-04)** → [archived-v6-sprint-audits](archived-v6-sprint-audits.md) — grep for any v6.0/v6.2/v6.3/v6.4 sprint audit.
- **Archived: 20 audits 2026-04-08 → 05-15** (CCP1-5, F7-F9, Google Workspace, security sweep, v7.3/v7.6, northstar strict-mirror, prompt-enhancer, sdk-wrapper, skill-evolution, community-manager, evolution-log ×2) → [archived-pre-may-audits](archived-pre-may-audits.md) — grep by name.
- [loop-unlimited-r1-audit](loop-unlimited-r1-audit.md) — /loop unlimited task (08-27): FAIL, 2 Crit. A command feature shipped behind an adapter that drops every "/"-prefixed message (the sibling /rituales regex makes the slash optional — that IS the tell); lifting an inner timeout leaves the OUTER abandon timer, which owns the only AbortController.
- [authored_doc_citation_is_a_claim](feedback_authored_doc_citation_is_a_claim.md) — When auditing an authored (non-distilled) doc that cites decision IDs or a sibling doc, open the cited file — IDs, enum/family names and figures drift or ship as placeh
- [certified_skill_flake_budget](feedback_certified_skill_flake_budget.md) — Auditing a mission-control S5 skill (seed/skills/*/SKILL.md) — a green certification is n=1 evidence; check latency headroom and LLM-judgment assertions before trusting
- [skill_error_envelope_strike](feedback_skill_error_envelope_strike.md) — In mission-control S5 skills, a body-level {"error":...} return is a wrong_output failure that burns an anti-list strike; required:true inputs make the body's INPUT_REQUI
- [verification_claims_need_the_live_store](feedback_verification_claims_need_the_live_store.md) — A log or status doc saying artifacts were "registered"/"deleted" must be checked against the live store (jarvis_files, filesystem) before it counts as done.
