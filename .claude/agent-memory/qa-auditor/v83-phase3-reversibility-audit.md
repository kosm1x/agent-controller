# V8.3 Phase 3 — reversibility (reversal.ts) audit — 2026-06-26

Verdict: PASS WITH WARNINGS. Scope: reversal.ts (NEW), pipeline.ts/decisions-store.ts/pipeline.test.ts (EDITED), reversal.test.ts (NEW).

DORMANCY = YES (load-bearing, confirmed). No production caller of runDecisionPipeline / revertDecision / insertDecision / markReverted / appendDecisionEvent (grep empty outside v8-3 + tests). index.ts only calls seedV83Capabilities + assertV82Dependencies (Phase 0/1). `V83_ENABLED` still unconsumed (only flags.ts defines isV83Enabled). Nothing in prod can write a `decisions` row.

STRUCTURAL SAFETY = PRESERVED. max_level cap applied (pipeline.ts:164) BEFORE the new §7 gate; §7 gate (L≥3 ⇒ non-sql_inverse demotes to L2, pipeline.ts:207-211) is DEMOTE-ONLY, never grants autonomy. northstar_sync/gmail_send (max_level=2) can't reach autonomous branch.

DOCTRINE — 3 recurring holes when a dormant reversibility skeleton binds an undo primitive:
1. **Strategy comes from the TRIGGER, not the seeded capability** (pipeline.ts:200 reads `trigger.sqlMutation.strategy`). capability_autonomy persists only `reversible_default` (bool), NOT `reversal_strategy` (seed.ts:190-194 drops it). So the "NorthStar → compensating-only / excluded from SQL-inverse" invariant (§7.1, 2026-05-12 resurrection incident) has ZERO code enforcement at this seam — a northstar_sync trigger could pass strategy:'sql_inverse' and build a replayable local SQL-inverse op. Saved from autonomy by max_level=2, but revertDecision/applyReversal would still REPLAY it locally (resurrection class). Enforcement delegated to a not-yet-written caller. → A capability-bound invariant must be enforced against the PERSISTED capability row, not trusted from a per-call trigger arg.
2. **Two undo paths, divergent not-restored handling.** revertDecision (operator): restored=false ⇒ status stays committed, CRITICAL "reversal_failed_state_not_restored", do NOT mark reverted (reversal.ts:421-429) — correct, tested (reversal.test.ts:386). Pipeline auto-revert (execution-failure): restored=false ⇒ STILL emits 'reverted' + markReverted, status='reverted' (pipeline.ts:288-292) — mislabels an inconsistent DB as cleanly reverted; no freeze. Untested. → when the same primitive has 2 entry points, the safety branch (verify-failed) must behave identically in both.
3. **The §7 reversibility gate only fires on sqlMutation triggers.** An L≥3 trigger with NO sqlMutation ⇒ reversalOp=null, executes autonomously, gateConfig.reversible_required (documented types.ts:105 as "no reversal_op ⇒ cannot execute") is NEVER enforced. max_level cap doesn't catch schedule_task (max_level=5; delete_inverse counts as reversible_default=true) — the spec's "natural first-flipper" to L3, whose real reversal (delete_inverse) is DEFERRED/unreplayable in v1. Dormant (no promotion path yet) but the structural guard is incomplete.

TEST ADEQUACY: undo INSERT/UPDATE/DELETE round-trips (reversal.test.ts:88/112/133), blast-radius reject at build (165) + replay (355), compensating proposed-not-executed (190/309), §7.4 irreversible both-conditions (210), deferred (227), operator not-restored CRITICAL (386) — all genuine, no tautologies. GAPS: pipeline auto-revert not-restored path untested; auto-revert at L≥3 (autonomous) untested (only L1 at 406).

STRENGTHS: SAFE_IDENT identifier poka-yoke (reversal.ts:40), single-transaction atomic replay (263-301), key-order-independent SHA256 fingerprint, §7.4 gate checks (level>=3 || reversibleRequired). Demote-event refactor (single emit → demotions[] loop) preserved ODD-only-path ordering/payload (one out_of_odd demotion = one event); backward-compat with Phase-2 no-sqlMutation path (mock preState, reversal:null, execute{ok:false}→pending) intact.

---
RE-AUDIT 2026-06-26 (correctness+security lens, focused). Verdict ELEVATED to FAIL on this lens. Hole #2 (auto-revert marks status='reverted' even when applyReversal fails OR verifyRestored=false, pipeline.ts:283-293) is the Critical: markReverted + status="reverted" run UNCONDITIONALLY; honest restored:false is only in the event payload; PipelineResult never surfaces restored. Note `applied.ok===false` short-circuits restored→false (inverse never ran) yet still marks reverted. Untested. TWIN-PATH DOCTRINE: when a bundle ships the same op via two call sites (careful ledger path revertDecision + convenience/auto path), diff them line-by-line — identical compute (`restored = applied.ok && verify`) but only the operator path branches on it before the terminal write; the auto path is where the guard was dropped. INDEPENDENTLY VERIFIED CORRECT (do not re-flag): ident() on EVERY dynamic identifier (capture/delete/exists-probe/UPDATE/INSERT, param names interpolated only after preceding ident throws); param-collision-free (pk_<col> vs col_<col> distinct namespaces; {...rowParams,...pkParams} matches UPDATE's named-param set exactly — 40 round-trip tests green under better-sqlite3 strict binding); atomic transaction with clean rollback→{ok:false}; revertDecision lifecycle (committed-only, no double-revert). New W2 sharpening: verifyRestored is target-scoped → false-positive on pk-altering UPDATE (inverse re-INSERTs old pk, orphan new-pk row survives, verify recaptures old pk=match→true) and on under-declared targets.
