# v7.7 — Ordered to-do list

> **Source**: `docs/V7.7-GUIDE.md` (operational) + `docs/V7.7-PREPLAN.md` (decision record, READ-ONLY).
> **Order**: dependency chain. Spines 1-3 are independent and parallel-eligible; Spines 4-6 layer on top; Spine 7 closes.
> **Recommended entry**: Spine 1 (S2 self-audit) — fastest first-win; its `verified-against:` discipline catches the rest of v7.7's metric claims.

---

## Spine 1 — S2: Self-audit before reporting (~5d)

**Spec**: `docs/planning/v8-substrate-s2-spec.md` · **Already shipped (partial)**: `mc-ctl audit-claim` primitive (commit `095647b`).

### Phase 1 — schema + critic harness ✅ **Shipped 2026-05-19**

- [x] Add `reports` table migration in `src/db/index.ts` (additive, per spec §6 storage block)
- [x] `src/audit/report-schema.ts` — Zod `DataSourceCitationSchema` (8-variant discriminated union, **strict**) + `ReportDraftSchema` + `ReportSchema` + `validateReportInvariants` (freshness + window ordering + evidence-index bounds)
- [x] `src/audit/critic.ts` — `runCritic(report, options) → {verdict, critique, costUsd}` using `infer()` with critic system prompt from spec §4. Balanced-paren JSON walker + pre-aborted-signal short-circuit.
- [x] `src/audit/submit-report.ts` — boundary fn: schema-validate → run critic → retry ≤3 → persist to `reports` table. Honors `critic_skip_for` allowlist (empty for Phase 1). 256KB size cap, reviseFn-throw safety, collision warning.
- [x] Tests: `report-schema.test.ts` (28 tests), `critic.test.ts` (18 tests), `submit-report.test.ts` (18 tests). 107 audit tests passing.
- [x] R1 qa-auditor: 0 Crit / 8 Warning / 4 Info → 11 in-bundle folds + 1 queued (W6 prepared-statement hoisting → triggers at Phase 2 morning_brief wire-in)

### Phase 2 — production retrofit

- [ ] Retrofit `morning_brief` (`src/rituals/morning.ts`) to emit the contract — `verified_against:` lines on every aggregate-metric claim
- [ ] Wire critic-as-write-gate into community-manager email reply path (`comunidades@mexiconecesario.org.mx`)
- [ ] Pin schema on closure-doc scoreboard rows (codifies the v7.6 closure-audit pattern that caught 4 fidelity bugs)
- [ ] qa-auditor pass + fold findings

### Closeout

- [ ] Spec-delta artifact: `docs/planning/v8-substrate-s2-impl.md` (records what shipped vs what specced; answer Qs that bit during impl)
- [ ] Activation gate met: zero "Audited?" cycles in next sprint of v7.7 reports
- [ ] Spine 1 row in `V7.7-GUIDE.md` marked **Closed** with closure commits, audit log, finds

---

## Spine 2 — S3: Out-of-band drift detector (~5d) — parallel-eligible

**Spec**: `docs/planning/v8-substrate-s3-spec.md` · **Already shipped (partial)**: S3 skeleton (commit `3089057`).

- [ ] Tables: `drift_signals`, `drift_alerts`, `baseline_history` (additive migration)
- [ ] Cron evaluator process
- [ ] Seed 13 signals across S1 / S2 / V8.2 / V8.3 / S5 / infra (incl. `mc_whatsapp_disconnects_total` per `aed876d`)
- [ ] Correlated-burst dedupe (3+ alerts in 5min → bundled root-cause)
- [ ] V8.1 morning-brief delivery hook (delivers alerts inline)
- [ ] Tests for evaluator, dedupe, seed signals
- [ ] qa-auditor pass + fold findings

### Closeout

- [ ] Spec-delta artifact: `docs/planning/v8-substrate-s3-impl.md`
- [ ] Activation gate met: env edit bypassing deploy script triggers visible alert within 60s of next interaction
- [ ] Spine 2 row marked **Closed**

---

## Spine 3 — S5: Skills-as-stored-procedures, all 5 phases (~10d) — parallel-eligible

**Spec**: `docs/planning/v8-substrate-s5-spec.md` · **Already shipped (partial)**: 57-skill shim at `src/db/skills.ts`, v7.6 Spine 5's `src/skills/catalog.ts`.

- [ ] Phase 1 — registry + frontmatter contract (unlocks the rest)
- [ ] Phase 2 — Voyager description-embedding for skill discovery
- [ ] Phase 3 — critic-as-gate on skill output (composes Spine 1's critic)
- [ ] Phase 4 — failed-tasks anti-list (Voyager pattern)
- [ ] Phase 5 — `skill_run(name, args)` end-to-end + register 5 production skills (HTML compose suite + 4 from current rituals — REGISTER, do not author)
- [ ] qa-auditor pass per phase + fold findings

### Closeout

- [ ] Spec-delta artifact: `docs/planning/v8-substrate-s5-impl.md`
- [ ] Activation gate met: ≥5 production-grade skills with green test runs in prior 7 days
- [ ] Spine 3 row marked **Closed**

---

## Spine 4 — Conway Pattern 1: General-events middle layer (~3d) — depends on Spines 1-3

**Reference**: `~/.claude/projects/-root-claude/memory/reference_conway_2005_sms.md`

- [ ] Derive `general_events` view/table over journal + cost_ledger + recall_audit + decision_events
- [ ] Time-windowed aggregations: per-day, per-week, per-project
- [ ] Acceptance test: V8.1 Phase 1 spec can run against this layer without further design
- [ ] qa-auditor pass + fold findings

### Closeout

- [ ] Spine 4 row marked **Closed**

---

## Spine 5 — Conway Pattern 2: Self-defining cohort + ⚠️ operator-profile skeleton (Q3) (~3-4d)

**Reference**: `reference_conway_2005_sms.md` + V8-VISION §4

- [ ] Derive cohort membership from existing data (NorthStar projects + frequent CRM contacts + recurring threads)
- [ ] Daily roll-up job
- [ ] **Q3 carve-out — operator-profile skeleton (anti-mission compliance critical)**:
  - [ ] Schema-only table `operator_profile (cohort_member_id, attribute_key, attribute_value, written_at, written_by)`
  - [ ] Write-path test: only cohort roll-up may populate (NO inference / NO LLM-derived fields)
  - [ ] Anti-mission compliance test: V8.2 can redefine value-side semantics without data migration
- [ ] qa-auditor pass + fold findings (operating rule 9 — Q3 vigilance applied to every PR)

### Closeout

- [ ] Spine 5 row marked **Closed**

---

## Spine 6 — Conway Pattern 3: Named recall modes + correspondence audit (~3d)

**Reference**: `reference_conway_2005_sms.md` + V8-VISION §9 trap warnings

- [ ] Add `recall_audit.mode` column (`coherence` | `correspondence`)
- [ ] Weekly correspondence-audit cron: re-validates sample of recently-cited memories against current reality
- [ ] Emit findings to `drift_signals` (composes with Spine 2)
- [ ] Stale-rate baseline measured + recorded
- [ ] qa-auditor pass + fold findings

### Closeout

- [ ] Spine 6 row marked **Closed**

---

## Spine 7 — Closure ritual + continuity tooling, Q4 absorption (~4-5d) — closes the phase

### Part A — Closure ritual (3rd use of the template; v7.5, v7.6, now v7.7)

- [ ] `docs/V7.7-CLOSURE.md` — spine-by-spine evidence, scoreboard, residuals, three patterns observed
- [ ] `docs/planning/v7.7-leftovers-queue.md` — post-closure watchlist (NOT actionable backlog)
- [ ] Annotated git tag `v7.7-closed`
- [ ] qa-auditor pass on closure doc (expect 33%-ish finding rate per v7.5+v7.6 pattern)
- [ ] **Update `docs/V8-VISION.md` §10 one-page summary** — set S2, S3, S5, Conway 1-3 to ✅
- [ ] Recommend v8.0 entry point (almost certainly V8.1 Phase 1)
- [ ] Write `docs/planning/v8-capability-1-entry-brief.md` (V8.1 Phase 1 handoff)

### Part B — Continuity tooling (Q4 absorption)

- [ ] `mc close-phase <name>` — drafts `<NAME>-CLOSURE.md` from spine table in `<NAME>-GUIDE.md`, drafts watchlist reframe, creates annotated git tag draft. **MUST NOT** push/commit.
- [ ] `mc resume <phase>` — reloads phase's open queue + watchlist + last commit ledger into session brief
- [ ] `mc audit-closure <closure-doc>` — wraps the qa-auditor invocation pattern from v7.5 + v7.6 (cross-check spine commits, sum scoreboard math, verify anti-mission held, validate memory-pin existence)
- [ ] Git-op gate test (operating rule 10): try to invoke without confirmation → tool must abort with no side effects
- [ ] **Acceptance**: v7.7's own closure uses these commands at least once end-to-end

### Closeout (the phase itself closes)

- [ ] Spine 7 row marked **Closed**
- [ ] All 11 closure criteria below ticked

---

## Phase-wide closure criteria (from V7.7-GUIDE.md)

v7.7 is closed when:

- [ ] S2 shipped — `verified-against:` lives in production reports, schema-validated; community-manager email reply path enforces critic-as-write-gate
- [ ] S3 shipped — drift detector running with 13 seed signals, ≥1 real drift caught and surfaced
- [ ] S5 shipped — all 5 phases (Q1), ≥5 production skills registered with green test runs
- [ ] Conway Pattern 1 — general-events layer queryable
- [ ] Conway Pattern 2 — cohort definition stable + V8.1-consumable + **operator-profile skeleton table shipped (Q3 carve-out, schema-only)**
- [ ] Conway Pattern 3 — recall-mode tagging live + correspondence-audit cron firing
- [ ] **Continuity tooling shipped (Q4)** — `mc close-phase` / `mc resume` / `mc audit-closure` working; v7.7 closure used them end-to-end as acceptance
- [ ] `V8-VISION.md §10` one-page summary updated to reflect new shipped state
- [ ] qa-auditor audited closure doc
- [ ] **No regression in v7.6 baselines** (re-benchmark 2026-05-22 — Tier-G watchlist from v7.6 closure)
- [ ] Tag `v7.7-closed` pushed
- [ ] V8.1 Phase 1 entry brief written (smooth handoff to v8.0)

---

## Operating rules to apply throughout

From `V7.7-GUIDE.md`:

1. Instrument first, decide later (perennial)
2. Audit every bundle — qa-auditor pass on every ship-it (calibrated 33% real-bug catch rate across 3 phases)
3. Catalog-then-execute for multi-domain backlogs
4. Risk carve-outs everywhere — every ergonomic-relief gate has a high-risk override
5. Producer/consumer coupling tests — every observability surface pinned at both ends in one test
6. Closure ritual every phase
7. No new features mid-phase — extends to "no v8 capabilities mid-substrate"
8. **Spec-first delta tracking (new)** — every spine lands `docs/planning/v8-substrate-s{2,3,5}-impl.md` recording shipped vs specced
9. **Q3 carve-out vigilance (new)** — every `operator_profile`-touching PR reviewed against the anti-mission compliance test (can V8.2 redefine value-side semantics without migration?). If no, REJECT.
10. **Continuity-tooling git-op gate (new)** — every git-op codepath in `mc close-phase` / `mc audit-closure` has explicit "operator confirmed?" gate. Test: invoke without confirmation → abort with no side effects.

---

## Timeline

| Metric                      | Value                                                    |
| --------------------------- | -------------------------------------------------------- |
| Sum of focused spine effort | ~33-35d                                                  |
| Realistic elapsed estimate  | 4-5 weeks (audit-driven follow-ups)                      |
| v7.6 reference compression  | 4-week projection → 10 days actual (3× via independence) |
| v7.7 target tag             | 2026-06-30 → 2026-07-05                                  |
| v8.0 (V8.1 Phase 1) start   | ~2026-07-06                                              |
| Stability re-benchmark gate | 2026-05-22 (4 days post-v7.6-closure, Tier-G watchlist)  |

---

## Cross-references

- Operational guide: `docs/V7.7-GUIDE.md`
- Decision record (READ-ONLY): `docs/V7.7-PREPLAN.md`
- Predecessor closure: `docs/V7.6-CLOSURE.md`
- v7.6 watchlist (Tier G = 2026-05-22 re-benchmark): `docs/planning/v7.6-leftovers-queue.md`
- Source roadmap: `docs/V7-ROADMAP.md`
- Source specs: `docs/planning/v8-substrate-s{2,3,5}-spec.md`
- V8 vision (§10 updated at Spine 7 close): `docs/V8-VISION.md`
- V8.1 customer spec: `docs/planning/v8-capability-1-spec.md`
- Conway 2005 reference: `~/.claude/projects/-root-claude/memory/reference_conway_2005_sms.md`
- Spine 1 entry brief (post-/compact handoff): `docs/planning/v7.7-spine-1-entry-brief.md`
