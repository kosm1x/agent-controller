# v7.7 — Post-Closure Watchlist

> **Status**: Watchlist, NOT actionable backlog.
> **Origin**: Created at v7.7 closure (2026-05-20), drafted by `mc-ctl close-phase v7.7` then hand-curated.
> **Siblings**: `docs/planning/v7.6-leftovers-queue.md`, `docs/planning/v7-leftovers-queue.md`.
>
> Items here have explicit re-open triggers. Until a trigger fires, nothing here requires engineering effort. If a trigger fires, the item moves to the active queue (`next-sessions-queue.md`) or the relevant phase plan. `next-sessions-queue.md` stays the canonical execution-ordered list.

---

## Tier A — open queue items inherited from `next-sessions-queue.md`

| Item                         | Status      | Re-open trigger                                                                                                                                                                                                                                                                                                                                                                |
| ---------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `S6-recall-audit-dormant`    | P2, open    | `recall_audit` receives no new rows under the Hindsight-demote routing — recall goes through `SqliteMemoryBackend`, which never calls `logRecall`. Re-enable the `recall_coherence_suppression_rate` signal when recall logging resumes (`HINDSIGHT_ENABLED=true`, or `logRecall` wired into `SqliteMemoryBackend`). Gates Spine 6's correspondence audit producing live data. |
| #2 — AV key rotation         | P0, dormant | Operator provisions a new AlphaVantage API key. ~1-2h once unblocked. Independent of everything else.                                                                                                                                                                                                                                                                          |
| `S4-B1-W4` — auto-discovery  | P3 / V8.1   | The general-events layer's auto-discovery population path (nightly LLM clustering) — deferred from Spine 4 as capability-flavored. V8.1 capability work, OR the manual-seed cohort proves too sparse for briefing retrieval.                                                                                                                                                   |
| `S4-B1-I1` — recency index   | P3          | `general_events` row count exceeds a few hundred (auto-discovery lands) OR a retrieval-latency regression.                                                                                                                                                                                                                                                                     |
| `S4-B3-R2` — mc-ctl id guard | P3          | An `mc-ctl` injection-hardening pass — `cmd_status`/`cmd_schedule_delete` interpolate `${id}` unguarded (pre-existing, surfaced by Spine 4).                                                                                                                                                                                                                                   |
| S1 / S2 / S3 P3 hygiene      | P3          | The remaining P3 hygiene items from the substrate spines (S1 3 · S2 8 · S3 21), each with a trigger in `next-sessions-queue.md`. Most load-bearing: the 3 schema-CHECK-enum gaps below.                                                                                                                                                                                        |

## Tier B — v8.0 schema-reset batch

Three CHECK-constraint enum gaps cannot be fixed without a destructive table recreate; all wait for the v8.0 schema reset:

| Item          | Gap                                                          |
| ------------- | ------------------------------------------------------------ |
| `S5-P2-I1`    | `skills` table — `created_by` missing `'operator-override'`. |
| `S5-P4-B1-I1` | `skill_failures.resolution` missing `'self_recovered'`.      |
| `S5-P5-B1-W4` | `skill_failures.error_class` missing `'archived'`.           |

## Tier C — anti-mission deferrals (v8.0+)

v7.7 declared these out-of-scope by anti-mission (V7.7-GUIDE §"Anti-mission"). Each has a forward-anchor.

| Item                                                | Forward-anchor / re-open trigger                                                             |
| --------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| V8.1 / V8.2 / V8.3 capability work                  | v8.0 — the substrate v7.7 built is now complete; capability work begins.                     |
| New runners (6th+)                                  | Not planned. "Five is enough."                                                               |
| New skill SOURCE authoring                          | v8.0+ — Spine 3 registered the substrate; authoring new skills is capability work.           |
| Operator-profile semantics (`attribute_*`)          | V8.2 — Spine 5 shipped the skeleton only; V8.2 owns the value-side meaning (zero-migration). |
| general-events auto-discovery / `details_embedding` | V8.1 — see `S4-B1-W4` above.                                                                 |

## Tier D — Hindsight escape hatches (dormant, mechanism preserved)

Carried from v7.6. `RecallOptions.withRerank: true` and `HINDSIGHT_RECALL_DISABLED_BANKS` remain inert opt-ins under the Hindsight demote; `S6-recall-audit-dormant` (Tier A) is the live decision point on whether to revisit the routing.
