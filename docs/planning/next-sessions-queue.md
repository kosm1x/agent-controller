# Next-Sessions Queue — Jarvis Stability & Efficiency

> **Authored**: 2026-05-07 end-of-session, post-freeze-lift
> **Replaces**: `stabilization/next-session-brief.md` (freeze-window framing, now obsolete)
> **Scope**: post-freeze stability + efficiency work for Jarvis. Excludes new feature work; that gets its own queue when initiated.

---

## Two-week reflection (the synthesis behind the priority order)

The freeze achieved its purpose — five distinct stability fixes in seven days without rollbacks, five hardening dims closed. But three patterns emerged across the incidents that shape what we do next:

1. **Narrow regex gates were structurally fragile.** Three of the five fixes (algebra-progress, DENUE shell_exec, write-tool friction) were the same shape: a scope gate too tight to admit legitimate operator phrasing. The fix on each was widening — and on the fifth (write-tool friction) we capitulated entirely and promoted four tools to always-on. Calling discipline belongs at the tool-description + system-prompt layer, not at scope. Architectural rethink (#13) is on the back-burner only because incidents have been stamped out one by one; if a fourth recurs, it jumps the queue.

2. **The Hindsight "promise gap" is real but mitigable.** "Jarvis remembers" was overpromised; what shipped is "Jarvis pattern-matches on similar-looking past text." Outcome-blind retrieval recycles failure narratives as recipes. Cross-encoder reranker is 99% of recall latency. Per-bank surgical demote on `mc-jarvis` is already a Path B pilot — but the strategic A/B/C decision can't be made without `was_used` and outcome-distribution data (~2 weeks once both ship).

3. **Silent state was the dominant failure class.** Fireworks deprecated alias still answered, 1229 KB orphans were invisible, dead worker leases never cleaned up, mc cost ledger doesn't see Hindsight, cache-hit ratio drifted unnoticed. Stability gains compound when drift is observable; right now we only see what we've already had an incident about. **The cheapest two weeks of work are instrumentation moves**, not architecture moves.

The queue below reflects: instrument first, decide later. The strategic decisions (#15 Hindsight A/B/C, #13 scope architecture) are gated on data that the instrumentation work produces.

---

## Execution-ordered queue

| #   | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Priority      | Estimate                                    | Notes                                                                                                                                                                             |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | ~~**mc systemctl restart async fix**~~ ✅ **DONE 2026-05-07** — `scripts/deploy.sh` + `mc-ctl restart` now capture `oldPid`, restart, then poll until `MainPID != oldPid` AND `kill -0 oldPid` fails (process actually dead). 120s timeout. Pre-flight warns on running tasks. Smoke-tested: 501746 → 1295697, 2s transition.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | P0            | DONE                                        | Foundational. Old PID can serve 4+ min after restart. Every fix below depends on "did my deploy actually deploy" being trustworthy. See `feedback_mc_systemctl_restart_async.md`. |
| 2   | **AV key rotation**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | P0            | 1-2h                                        | Carryover from old `next-session-brief.md`. Independent — can run parallel to #1.                                                                                                 |
| 3   | ~~**Pricing tables for minimax-m2p7 + kimi-k2p5 + qwen3p6-plus**~~ ✅ **DONE 2026-05-07** — Added 6 Fireworks p-notation aliases (minimax-m2p7, kimi-k2p5, kimi-k2p6, qwen3p6-plus, glm-5p1, deepseek-v4-pro) to both `mc/src/budget/pricing.ts` and `crm/src/budget.ts`. Added `normalizeModelName()` to strip `accounts/fireworks/models/` path prefix so callers can pass either form. +6 tests, all green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | P2 → raised   | DONE                                        | Prerequisite for #4. Closes punch-list P1 #3 in `project_hindsight_fireworks_followup.md`.                                                                                        |
| 4   | ~~**Hindsight cost telemetry**~~ ✅ **DONE 2026-05-07** — `crm-hindsight` Prom scrape job added to `monitoring/prometheus.yml` (target `host.docker.internal:8888`, scrape*interval 30s). New `src/rituals/hindsight-cost-pull.ts` runs every 5min, pulls `increase(hindsight_llm_tokens*\*[5m])`, writes one cost_ledger row per (scope, model, success) series tagged `agent_type='hindsight'`. Idempotent via deterministic run_id (sha1 of label set + bucket). 3 alert rules added: `HindsightTokenRateSpike`, `HindsightCircuitErrorRate`, `HindsightTargetDown`. +6 tests. Hindsight spend will now roll into the 3-window budget — `/health` will reflect true total within 5 min of next traffic.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | P1            | DONE                                        | **Session A.** Hindsight already exposes 26 metric families incl. `hindsight_llm_tokens_input_tokens_total` etc. Detail in §"Scope #5" below. Retires silent-leak class.          |
| 5   | ~~**Reflect-model quality sample**~~ ✅ **DONE 2026-05-07** — Sampled 10 reflector entries (`mc.db conversations WHERE source='reflector'`) over the last 7d. All produced under minimax-m2p7. Quality verdict: **stay on minimax-m2p7.** Reflects are 1-2 sentences, grounded in observed task data (cite real tool names, counts, percentages), actionable, no hallucination. Sample observation→suggestion structure is consistent. Cost-per-quality is excellent: $0.30/$1.20 per 1M with sub-second latency vs. kimi-k2p5 at $0.60/$3.00 with 1.01s. No reason to swap. Re-evaluate post item #7 (outcome-aware retrieval) if reflect quality starts affecting retain decisions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | P2            | DONE                                        | **Deadline 2026-05-13.** Punch-list P1 #4. Cheap; do during next session naturally. 707 reflect calls/day means data is plentiful.                                                |
| 6   | ~~**Cache-hit audit**~~ ✅ **DONE 2026-05-07** — Audit found cache-hit ratio is **healthy**, not degraded. 24h: 81.1%, 7d: 74.3% (n=723). 14-day daily breakdown shows: cache OFF before 2026-04-26 (0% hits), priming day 04-26 (36.4%), then stable 81-92% from 04-27 onward. The 47.6% memory was mis-cited — actual 2026-05-04 24h aggregate was 84.3%. No drift to fix. Today (5-07) is 76.9% but elevated cache_creation_pct (23.1%) is explained by 3 mc redeploys this session producing cache-create bursts. Updated `feedback_cache_hit_audit_pending.md` with resolved state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | P1 efficiency | DONE                                        | 🔔 already flagged in MEMORY.md. Estimated $10-15/d savings. No deps.                                                                                                             |
| 7   | ~~**Outcome-aware retrieval close-the-loop**~~ ✅ **FULLY DONE 2026-05-07**. Part 1: retain wired into `handleTaskFailed` + new `handleTaskCancelled` subscription. Part 2: `outcome-bias.ts` replaces binary filter — drops `outcome:failed`, +0.10 boost on success, -0.05 penalty on concerns. Part 3: `mc-ctl outcome-distribution [WIN]` standalone command (default 7d) reads conversations.tags + recall_audit.outcome_breakdown; `mc_recall_outcome_total{outcome,bank}` + `mc_retain_outcome_total{outcome,bank}` Prom counters bumped from logRecall + sqlite-backend.retain (Hindsight delegates so all paths counted); `RetainOutcomeConcerns` alert fires if `concerns/total > 25%` for 24h with >5/day floor. Bank labels capped via `KNOWN_BANKS` cardinality guard. +7 prometheus tests. **Live verification 2026-05-07: 24h shows mc-jarvis 62 success / 20 concerns / 4 unknown over 86 retains, mc-operational 0/0/71 (most retains predate tagging); concerns ratio 12.7% (under 25% threshold).**                                                                                                                                                                                                                                                                                                                                                                               | P1            | DONE                                        | **Session B.** Half-shipped already (`outcome-tag.ts` write-side tagging done 2026-04-29). Detail in §"Scope #3" below. Closes failure-narrative-as-recipe class.                 |
| 8   | ~~**`was_used` recall audit**~~ ✅ **DONE 2026-05-07** — Bulk of infrastructure was already shipped pre-session (`recall_audit` table with `was_used` / `match_type` / `overlap_score` / `task_id`, `markRecallUtility` dual-signal matcher, `mc-ctl recall-utility` aggregations). This session added the missing pieces: `recall_audit.top_k_ids` TEXT JSON column populated on the hindsight path with stable Hindsight memory IDs; `mc-ctl recall-utility` extended with "Outcome distribution (per-recall avg)" section reading `outcome_breakdown` JSON. **Current 7d data:** Hindsight on mc-operational = 5.3% utility / 2355ms latency vs SQLite-only on mc-jarvis = 29.7% / 324ms — the strategic-options threshold ("<20% utility AND >50% latency tax") is already blown for mc-operational. 14-day measurement window for outcome-distribution starts now (recall_audit rows pre-2026-05-07 lack the column). See `feedback_recall_audit_state_2026_05_07.md`.                                                                                                                                                                                                                                                                                                                                                                                                                          | P1            | DONE                                        | Gates the Hindsight A/B/C decision (#15). Ship before #15. Strategic-options doc names this "the single most important measurement."                                              |
| 9   | ~~**Hindsight stuck-queue + dead-worker-lease cleanup**~~ ✅ **DONE 2026-05-07** — Discovered Hindsight ships an `hindsight-admin decommission-worker` CLI; wrapped it in new `crm-ctl hindsight-reap [IDS...]` command that auto-detects stale workers by parsing the WORKER_STATS log line for `others: ID:N` references. **Live verification:** ran reap on `01b79314f397` (idle 9+ hours) → released 6 jobs, queue dropped 17→11, active worker went 0/10 → 4/10 slots, my_active now `consolidation:mc-operational(2), consolidation:mc-jarvis(2)`. Also added `postmaster.pid` cleanup in `crm-ctl hindsight-start` so container recreates don't fail with "Instance already running (pid: N)" on the bind-mounted volume. Auto-reaper as ritual deferred (risk of decommissioning slow-but-alive workers); manual is sufficient until recurrence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | P2 stability  | DONE                                        | Punch-list P1 #1. Currently 3/5 24h-verify checks fail because of this lease. Pending queue stuck at 17.                                                                          |
| 10  | ~~**Hindsight cross-encoder drop / two-tier retrieval**~~ ✅ **MECHANISM SHIPPED 2026-05-07** — Hindsight has no `provider="none"` reranker option, so the drop is implemented at the mc routing layer. New `RecallOptions.withRerank?: boolean` with three states: `undefined` respects existing env flag (legacy), `true` opts into Hindsight even if global default is off, `false` forces SQLite hybrid even if global is on. Bank-disabled wins over `withRerank=true` (operator's manual circuit breaker has priority). Two new source labels: `rerank-opt-out` (caller chose SQLite) and `sqlite-fallback-opt-in` (Hindsight failed under explicit opt-in — preserves caller intent through audit). +5 tests, all green. **Operator activates the no-rerank default by flipping `HINDSIGHT_RECALL_ENABLED=true → false` in mc/.env (hook-protected; manual sed required).** Until that flip, mechanism is dormant; no caller currently sets `withRerank`.                                                                                                                                                                                                                                                                                                                                                                                                                                     | P2 efficiency | DONE (mechanism); pending env flip          | 5-10× latency headroom. Strategic-options doc Path A item 2. Worth shipping even if final path is DEMOTE — preserves the option.                                                  |
| 11  | ~~**Hardcoded `/root/claude/jarvis-kb/` path extraction**~~ ✅ **DONE 2026-05-07** — `getJarvisKbRoot()` exported from `db/jarvis-fs.ts`. Three tool files (`file.ts`, `shell.ts`, `file-convert.ts`) now build their `ALLOW_*_PREFIXES` arrays at call time via `getAllowWritePrefixes()`/`getSourceAllowPrefixes()` so JARVIS_KB_MIRROR_DIR overrides flow through tools too.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | P3            | DONE                                        | Audit Critical 2 carryover from KB-visibility session (`feedback_kb_visibility_drift.md`). Hidden coupling for tests via `JARVIS_KB_MIRROR_DIR`.                                  |
| 12  | ~~**KB drift alerting**~~ ✅ **DONE 2026-05-07** — Four new gauges in mc Prometheus exporter: `mc_kb_reindex_drift`, `_fs_files`, `_db_files`, `_errored`. Set from `scheduler.ts` after each hourly reindex via new `recordKbReindex()` setter. Alert rule `KbReindexDrift` (in `mc-prometheus`) fires if `mc_kb_reindex_drift > 10` for ≥2h (≥2 consecutive reindex runs). Verified Prom loaded 4 mission-control rules (was 3).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | P3            | DONE                                        | Depends on #4 (Prometheus scrape pattern reusable for any mc metric). Closes the "KB silently drifted" failure mode end-to-end.                                                   |
| 13  | ~~**Scope architecture rethink**~~ ✅ **VERDICT SHIPPED 2026-05-15** — Option B (always-on expansion + tool-description discipline) wins on the 30-day inventory tally (admission 7 / genuine 1 / discipline 1). Promotion criteria codified in `mc/docs/planning/scope-architecture-rethink.md`. Concrete promotions this commit: `project_get` + `project_update` → `MISC_TOOLS` (closes 04-21 false-unavailable + 05-07 Alianza CMLL archive incidents). 8 regression `it.each` cases pin the always-on contract; 2 pre-existing tests rewritten to assert the new "regex still doesn't add `projects` group, but tools always-on" split contract. **Deferred B promotions** (queue follow-ups, NOT shipped): `gdrive_*` subset (incident 04-26; needs scoping), `browser__*` extras (incident 05-14; second-incident threshold not met).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | ~~P1~~ DONE   | DONE                                        | Closes the broader architectural decision the 2026-05-09 pickup brief deferred. Pickup brief item #1 was already closed by always-on promotion of `jarvis_file_*` (2026-05-07).   |
| 17  | ~~**`max_turns` involuntary task truncation**~~ ✅ **CLOSED 2026-05-15** — both patterns from the 2026-05-09 friction-pickup audit now shipped. Pattern B (exploration-before-write, 12 incidents @ 20 turns) closed by `6c0be55` (MAX_ROUNDS_DEFAULT 20→30). Pattern A (bulk-throughput throttle, 6 incidents @ 55 turns) closed this commit: `jarvis_files_batch_write` + `jarvis_files_batch_delete` collapse N runner turns → 1 for bulk-write / bulk-delete workflows (NS reconstruction 53× → 1, NS bulk-delete 54× → 1). Both deferred, partial-error policy, BATCH_CAP=50, dedup up front, precious-path scan returns the full list in one trip, router auto-injects `confirmed:true` on operator-accepted retry (audit W2 — a real bug that also affected single-item `jarvis_file_delete` for precious paths). 22 new unit tests + 6 scope regression tests; total 4830/4830 pass. qa-auditor R1 found W2 critical (folded), W3+R1+R3+R4+R5+R6 (folded), SV1 (folded as `formatConfirmationResult` cases). W1/W4/W5/R2 deferred as pre-existing single-item issues outside #17 scope.                                                                                                                                                                                                                                                                                                      | ~~P1~~ DONE   | DONE                                        | Pattern B closed 2026-05-09; Pattern A closed 2026-05-15. Reliability concern for bulk-task complexity scaling now bounded.                                                       |
| 18  | ~~**NorthStar compass-reframe operationalization**~~ ✅ **RESOLVED 2026-06-23** — superseded by the **day-log-is-work-truth** operator ruling (`feedback_daylog_is_work_truth.md`, tasks #447-453), a cleaner mechanism than the original migration plan. Signals/detectors/rituals now judge advancement/stalledness from the Telegram day-log + active-`projects` list, **NEVER NorthStar** — NorthStar is compass-only (visions/goals); `northstar_sync` (compass data) retained, `kb-reindex` still skips it. Shipped: new day-log-grounded `detectStalledProjects`; the 4 NorthStar/task-table detectors + proactive nudge + weekly-review switched OFF; morning brief converted to day-log + projects; session-end day-log writer kept. The proposed `jarvis_files/projects/<project>/tasks/` task-migration was **NOT** done — rendered moot (the day-log IS the execution record; the tasks-table is simply no longer a work-truth source). _Refinement 2026-06-24:_ silence ≠ stall — `config.stall_exempt` skips done/parked projects ([[feedback_silence_is_ambiguous]]). _Original plan (history):_ operator declared 2026-05-05 (_"NorthStar should be a compass, not a backlog — visions are mine, execution lives in the KB"_); tasks→`jarvis_files/projects/<project>/tasks/`, NorthStar→visions/goals, briefings split intentions from execution, COMMIT-side tasks-table decision. | ~~P1~~ DONE   | DONE                                        | Resolved by the 2026-06-23 day-log-is-work-truth ruling (tasks #447-453). Pickup brief #3 now moot.                                                                               |
| 14  | ~~**Subagent verification FP reduction**~~ ✅ **DONE 2026-05-07** — Updated `qa-auditor.md` Behavioral Rules: added "Quote-the-line discipline for missing-feature claims". Auditor must quote the exact line that lacks the claimed feature (missing enum, missing `.describe()`, etc.). If line can't be quoted, finding downgrades to Recommendation. Surfaces FPs at report time. Cites historical 75% FP rate as motivation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | P3            | DONE                                        | Operational tax on every audit. Worth a tightener pass.                                                                                                                           |
| 15  | ~~**Hindsight A/B/C strategic decision**~~ ✅ **CLOSED 2026-05-15 — DEMOTE (small)** shipped. 30-day data + 3-day post-#7 sample (n=315 since 05-07): mc-jarvis SQLite-hybrid 38.9% utility / 301ms vs mc-operational Hindsight 4.0% / 2496ms — rehab threshold blown. L4 already built (no 2-week wiring needed). `HINDSIGHT_RECALL_ENABLED=false` flipped via `scripts/recall-demote.sh`. Container + retain/reflect + cost-ledger pull paths stay active. `withRerank:true` queue #10 escape hatch preserved. Re-eval triggers: mc-jarvis SQLite utility <25% for 7d OR new memory-product candidate. Verdict block in `docs/planning/hindsight-strategic-options.md`; memory `feedback_hindsight_demote_verdict_2026_05_15`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | ~~P4~~ DONE   | DONE                                        | The 2026-05-21 14d-window plan was overtaken — HINDSIGHT_ENABLED=false at 05-10 froze new data; 3 days of post-#7 data was already conclusive.                                    |
| 16  | ~~**Audit follow-ups from 2026-05-07 qa-auditor pass**~~ ✅ **MOSTLY DONE 2026-05-07** — Shipped: (a) router-side fresh-status check in `handleTaskCompleted`/`handleTaskFailed` (DB read → short-circuit if `status='cancelled'`); (b) W1 gated on Hindsight container uptime > 24h + non-trivial baseline; (c) W2 tailored cancel UX message when `cancelled_by !== "user"`; (d) W3 bucket lag of 30s eliminates boundary ambiguity in hindsight-cost-pull; (e) W5 cross-repo pricing drift test (`pricing-drift.test.ts`, 6 Fireworks aliases verified); (g) S1 renamed `normalizeModelName` → `stripFireworksPrefix` in mc + crm; (h) S2 dropped `importActual` from router.test.ts memory mock. **Deferred**: (a-deep) `pending.abortController` wiring (real runner integration risk, separate item); (f) W6 `is_old_pid_alive()` bash helper extraction (cosmetic).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | P3            | DONE bundled; abortController + W6 deferred | Inherited from the audit on items #1, #3, #4, #7-pt-1. C1 + the narrow C2 + W7 + W8 already shipped 2026-05-07.                                                                   |

---

## Suggested session bundling

| Session          | Items        | Description                                                                      |
| ---------------- | ------------ | -------------------------------------------------------------------------------- |
| 1                | #1 + #2      | P0 hygiene — deploy verification + key rotation                                  |
| 2                | #3 + #4 + #5 | Cost telemetry bundle — pricing → scrape → reflect-model sample chains naturally |
| 3                | #6           | Cache-hit audit, standalone                                                      |
| 4–5              | #7           | Outcome-aware retrieval — the longer build                                       |
| 6                | #8 + #9      | `was_used` audit + stuck-queue cleanup, parallel                                 |
| 7+               | #10–#14      | Interleave as appetite allows                                                    |
| ~Session 6 + 14d | #15          | Strategic decision when data window completes                                    |

To clear P0+P1: ~5 sessions. To clear P0–P2 fully: ~9 sessions. Strategic decision lands ~3 weeks out.

---

## Scope #5 — Hindsight cost telemetry (detail)

**Discovery**: Hindsight on `:8888/metrics` already exposes 26 metric families with full label sets `(model, provider, scope, success, tenant)`:

- `hindsight_llm_calls_total` (counter)
- `hindsight_llm_tokens_input_tokens_total` / `output_tokens_total` (counters, also bucketed by `token_bucket`)
- `hindsight_llm_duration_seconds` (histogram)
- HTTP duration/total, DB pool size/idle/min/max, process CPU/RSS/threads/fds

`mc-prometheus` runs from `/root/claude/mission-control/monitoring/prometheus.yml`. mc `cost_ledger` schema already has all needed columns; no schema change.

**Part 1 — Scrape target (~15 min)**: append `crm-hindsight` job to `prometheus.yml` targeting `host.docker.internal:8888` with `metrics_path: /metrics`, scrape_interval 30s. Reload via `docker exec mc-prometheus kill -HUP 1`. Verify at `:9090/targets`.

**Part 2 — Cost ledger sidecar (~1.5h)**: new `src/rituals/hindsight-cost-pull.ts` runs every 5 min. Queries Prometheus HTTP API for `increase(hindsight_llm_tokens_input_tokens_total[5m])` + output. For each `(model, scope)` non-zero series: lookup `src/budget/pricing.ts`, compute cost, `INSERT OR IGNORE` into `cost_ledger` with `agent_type='hindsight'`, `run_id='hindsight-{scope}-{timestamp_5m_bucket}'`. Add to `src/rituals/scheduler.ts`: `cron.schedule("*/5 * * * *", ...)`.

**Part 3 — Alert rules (~20 min)**: append to `monitoring/alerts.yml`:

- `HindsightTokenRateSpike`: 5m token rate > 3× 24h baseline for 10m
- `HindsightCircuitErrorRate`: success=false rate > 10% over 10m

**Verification**: Prom target `up`, PromQL returns non-zero, cost_ledger rows accumulate, `mc-ctl stats` shows Hindsight cost line, force-trigger spike alert by lowering threshold then revert.

**Risk / rollback**: misattribution if Hindsight `scope` label drift (already saw `verification`, `reflect_tool_call`) — log unknown scopes at warn, default `agent_type='hindsight-unknown'`. Pricing data freshness — yearly review note in `feedback_fireworks_qwen3p6_retry_storm.md`.

---

## Scope #3 — Outcome-aware retrieval close-the-loop (detail)

**Discovery**: `src/memory/outcome-tag.ts` (shipped 2026-04-29) maps `task.status` → `outcome:{success|concerns|failed|unknown}`. Wired into `auto-persist.ts:207`. **Two known gaps in code comments**:

1. `handleTaskFailed` doesn't call retain → `outcome:failed` rows are near-zero in production
2. Recall-side ranking doesn't use the tag — Hindsight's recall API accepts `tags` filter (`hindsight-client.ts:172`), but no caller passes outcome tags

**Part 1 — Close failed-task retain gap (~½ day)**: edit `router.ts:handleTaskFailed`, mirror `handleTaskCompleted` retain call. Same for `handleTaskCancelled`. Tests: 2 new in `router.test.ts`.

**Part 2 — Recall-side soft bias (~½–1 day)**: edit `src/memory/index.ts` recall path. Submit recall with no tags filter, post-filter the response: drop `outcome:failed` by default, +0.10 boost on `outcome:success`, -0.05 penalty on `outcome:concerns`. Add `recall_audit.outcome_breakdown` TEXT JSON column. Provide `recall(query, { include_failed: true })` opt-in for analysis tasks. Tests: extend `recall-compare.ts`.

**Part 3 — Distribution telemetry (~½ day)**: new `mc-ctl outcome-distribution` command. New `mc_recall_outcome_total` Prom metric in mc `/metrics`. Alert if `outcome:concerns` rate > 25% of last-7d retains for 24h.

**Verification**: trigger deliberate task failure → confirm `outcome:failed` row in conversations within 30s. `mc-ctl outcome-distribution --window=7d` shows non-zero in all four. A/B 5 known queries.

**Risk / rollback**: overcorrection on negative-precedent recalls (mitigated by `include_failed` opt-in). Tag-malformed JSON in old rows (saw one during scoping) — defensive try/catch, treat as `outcome:unknown`.

**What this doesn't do**: doesn't ship the `was_used` audit (that's #8 — separate).

---

## Anchors / cross-references

- Strategic decision context: `docs/planning/hindsight-strategic-options.md`
- Open punch-list: `feedback_hindsight_fireworks_followup` memory
- Two-week post-mortems: `feedback_fireworks_qwen3p6_retry_storm`, `feedback_kb_visibility_drift`, `feedback_scope_classifier_safety_net`, `feedback_jarvis_writes_always_on`
- Re-benchmark target: 2026-05-22 vs `docs/benchmarks/2026-04-22-baseline.md`
- Freeze closure: `feedback_freeze_lifted_2026_05_07`

---

## Tool-search follow-ups (V8.5 Phase 3.2, 2026-07-13)

1. **Container propagation (audit W4).** `TOOL_SEARCH_ENABLED`/`ENABLE_TOOL_SEARCH` reach only HOST SDK calls; the nanoclaw sandbox and containerized-heavy runs keep full-schema prompts (correct, just unoptimized). Trigger: next nanoclaw/heavy-container image rebuild — decide whether to pass the flag into the container env (needs its own validation run inside the sandbox).
2. **Core set widened 2026-09-01 (un-defer, see updates log).** The 07-13 numbers ("21/22-tool core, 77 % cut") are superseded: `shell_exec`, `file_write`, `file_edit`, `git_status/diff/commit/push` are always-loaded because a coding-scoped chat searched once, missed, and went BLOCKED (tasks 8958/8961–8963). Harness now reports core 29/29, 73 % cut. Rule: a tool at the HEAD of the 30-day call distribution for a scope group must not be `deferred: true` — deferral is for the long tail. Trigger for the next widening: a BLOCKED chat naming a deferred tool that was in `metadata.tools` (queue #24 has the query).
3. **SDK-internal contract watch (audit W2).** The tripwire fields (`terminal_reason`, `deferred_tool_use`) and the inline `_meta['anthropic/alwaysLoad']` carrier are SDK-version-coupled (live-verified on 0.3.207 via `scripts/validate-tool-search.ts` phases D/E). Trigger: EVERY SDK version bump — re-run `npx tsx scripts/validate-tool-search.ts --run` (~$0.80) as part of the bump checklist, alongside the eval gate. **Run 2026-09-01 on 0.3.245: PASS** (all five phases, $0.57; `TerminalReason` literals now also pinned at compile time in `claude-sdk.ts`).

## BUDGET_ENFORCE arming pre-flight (V8.5 Phase 3.3 audit deferrals, 2026-07-13)

> **PARKED — operator ruling 2026-07-13 (same day): "No hard-cap enforcement in the near future. Viewability and accountability are enough."** The hard-cap thread is CLOSED until the operator reopens it. Do not propose arming, do not schedule these items, do not count them as open work. The enforcement code stays dormant in the tree (zero-cost, fully tested); if arming is ever wanted, this section is the mandatory pre-flight.

**Trigger: BEFORE flipping `BUDGET_ENFORCE=true`** (it stays dormant until then — both items are latent, not live). From the 3.3 R1 audit:

1. **W1 — budget refusal has no messaging floor.** Verbatim: "`BudgetExhaustedError` is a new throw with zero messaging-layer handling. `router.ts:1924` catches the fast-path throw and falls through to the full pipeline, which re-throws (same exhausted window) → generic task-`failed`. No dedicated 'budget exhausted' user reply." When armed, Telegram chat under an exhausted window must produce a deterministic "presupuesto agotado, se restablece a la hora en punto" reply + one operator notification — never silence (never-silent-reply floor). Fix belongs in router.ts/fast-path error handling; deferred because router.ts is outside the 3.3 diff.
2. **W4 — eval/tuning processes write prod cost_ledger.** `npm run eval:gate` (~$5, `tuning:eval-probe` rows) and any script importing `infer()` against `mc.db` now land real rows in the shared windows. Under enforcement, one gate run can consume most of an hourly window and refuse production calls for the rest of the hour. Before arming: either size `BUDGET_HOURLY_LIMIT_USD` to accommodate a gate run, or exclude `tuning:%` from `getRemainingBudgetUsd()`'s windows (decide then — exclusion weakens the cap's completeness).
3. **W2 (documented, accepted)** — per-call `maxBudgetUsd` = full remaining window, so N concurrent calls can overshoot by ~N×. The cap is a soft ceiling under concurrency, not a hard bound. Revisit only if overshoot is observed to matter.

## JME Phase 4 — REJECTED and reverted (2026-08-02)

PR #31 closed unmerged; both branches deleted (`jarvis/feat/jme-phase4-auto-prune` @ `0e1f898`, `jme-phase4-autoprune` @ `b37712e`). No code reached `main`; the 129 `jme_facts` rows are intact. **Reason: every precondition matched an empty set** — trigger `embeddingCount >= 400` vs a live count of **129**; `confidence < 0.5` vs a table minimum of **0.72** (0 rows); `ts < 30 days ago` vs an oldest fact of **16 days** (0 rows). Raising the floor `0.4 → 0.5` changed the outcome for exactly zero facts. Same defect class as §17 6a below — acting on a population that does not occur.

Patches for BOTH attempts preserved at `/root/claude-backups/jme-phase4-rejected-2026-08-02/` (its `README.txt` marks which is worth reusing). Postmortem written for Jarvis at `jarvis-kb/projects/agent-controller/jme-phase4-postmortem-2026-08-02.md`.

**Tooling residue (no owner yet): `git log` author does not distinguish autonomous from operator work.** `git_commit` stamps `--author` as `JARVIS_GH_USER <JARVIS_GH_EMAIL>` (defaults `PiotrCoderDroid <peter.blades@gmail.com>`) on `jarvis/*` branches by design (`src/tools/builtin/git.ts:343-348`), so host-path Jarvis commits are indistinguishable from the operator's; in-container commits fall back to the nanoclaw git config (`Jarvis (nanoclaw) <jarvis@mission-control.local>`) because `JARVIS_GH_TOKEN` is unset there. So the author field encodes the EXECUTION PATH, not the actor. **Trigger: the first time a review, blame, or incident needs "did a human or Jarvis write this?"** — the answer must come from the branch prefix or a commit trailer, not the author. Deliberately not "fixed": the current behavior exists so commits attach to the pushing GitHub account, and changing it is a deliberate tradeoff, not a bug fix.

**Trigger: `jme_facts` with embeddings reaching ~400** (≈ early September at the current ~8 facts/day — check with `./mc-ctl db "SELECT COUNT(*) FROM jme_facts WHERE embedding IS NOT NULL"`). When it fires, the fix is `b37712e`'s shape — the FIRST attempt, which correctly parameterized `pruneExpiredFacts(confidenceFloor = 0.4)` and called it, rather than the inlined duplicate `DELETE` that became PR #31 — with a floor chosen from the confidence distribution observed at that time. Note `pruneExpiredFacts()` currently has **no caller in production**: it is exported, tested, and dead. Wiring it up is the actual Phase 4 task.

## Morning surface rethink — brief retired, Morning Sync is the strategic surface (2026-08-03)

**Operator ruling:** "The briefs are NOT helping steering the decision as they should… use the 8 am brief that is actually helping me." The delivered 06:00/07:00 brief (promote/discard contract) is RETIRED; the operator's own 08:00 "Morning Sync" scheduled task (`scheduled_tasks` `6c312196-…`) is the operator-facing morning surface. Implementation (V8.2 sync-surfacing):

- `judgments.surfaced_at` (migration v4) + `src/lib/v8-2/sync-surfacing.ts`: one critic-approved green/yellow judgment/day injected into the Sync prompt (weave-context) **and appended deterministically** as the `🧭 Lectura estratégica` line at broadcast; `surfaced_at` stamped only on a verified ≥1-channel send (telegram adapter now THROWS on total failure — the tally is a consent record).
- V8.3 §12 consent linkage accepts `surfaced_at` as the prior-surface condition — **L≥3 stays reachable with the brief dark**.
- §13 promote-rate: retired via code constant `BRIEF_SURFACE_RETIRED` in `activation-gate.ts` (annotated skip, never insufficient/fail; scoring path kept behind a test-injectable opt for a ruling reversal). This supersedes watch item 1's affordance path as the live acceptance story: steering now happens conversationally in the Sync, and the no-verdict-reminder goes structurally dormant (no delivered briefs → `getResolvablePendingBriefing` returns null).
- Flags: `V81_BRIEF_DELIVERY_ENABLED=false` (in `.env` — the unit's `EnvironmentFile=` OVERRIDES `Environment=` drop-ins, so the flip must live there), `v82-delivery.conf` removed, `V82_SYNC_SCHEDULE_ID` drop-in added. `morning-briefing` ritual `enabled:false` in config (scheduler only skipped it while the V8.1 flag was true — flag-off would have resurrected it). Producer + probe stay armed (shadow §17 accrual unchanged).
- **Watch: `signal-intelligence` (06:00) still runs+emails.** Its digest historically fed the retired 07:00 ritual; if its email is now noise, disabling it is a one-line config change — operator call.
- **Queued (qa R2-W1, latent): WhatsApp `send()` resolves `"queued"` on non-delivery** (`channels/whatsapp.ts:364-379` — disconnected OR throw both enqueue-and-resolve). Every tally consumer counts a resolution as a send, so with WhatsApp enabled a permanently-logged-out socket (the historical norm on this box) defeats SIX zero-delivery guards at once: the sync-surfacing consent stamp (`dynamic.ts` `tally.sent > 0`), `delivery.ts:91` (`delivered_at` — the original §12 consent path), `no-verdict-reminder.ts:132`, `prometheus-alert-poller.ts:285`, `scheduler.ts:172`, `x-poster/probe-cron.ts:91`. Verbatim R2: "identical to R1-C1 … latent, not live" (`WHATSAPP_ENABLED` unset, 0 channel-active log hits in 14d). The fix is NOT a reflex rethrow — the reconnect queue is a deliberate feature; it's a structured send result (delivered vs queued) or the tally treating `"queued"` as not-sent. **Trigger: before `WHATSAPP_ENABLED=true` is ever set again.**
- **Watch: first Sync with a Lectura line.** Verify `surfaced_at` stamped for exactly the judgment whose line went out (`mc-ctl db "SELECT id, subject, surfaced_at FROM judgments WHERE surfaced_at IS NOT NULL"`), and that `checkJudgmentLinkage` returns ok for it next cycle.

**Update 2026-08-04 — concession misfire + Sync grounding:** the retirement left the LAST delivered brief (08-03, judgments 126–128) pending, so its §13 reply classifier kept intercepting operator chat: the operator's complaint about the 17:15 Sync smoke test (wrong hour, fabricated "Sofía") was misbound as a pushback on judgment 128 (VLCRM), passed the evidence gate, and re-delivered a VLCRM options block as a non-sequitur — judgment 128's ledger briefly carried the clock complaint as evidence ref [33], and its at_risk posture self-scheduled a `verify_resolution` follow-up (~08-06). Operator-ordered fixes applied: 128's ledger SCRUBBED (ref [33] removed, `concession_kind`/`triggering_evidence_text` NULLed so §14/§17 shadow stats don't count the misbound fold; pre-scrub backup in the 08-04 session scratchpad) + the follow-up row deleted — residual: 128's re-run prose still cites the now-absent [33] (original prose was overwritten in place; the row is shadow-only and ages out of all pick windows). Briefing `7ed5c8bc` manually EXPIRED (0 resolvable pending briefs remain — the interception class is now structurally extinct: shadow briefs never get `delivered_at`), and the Morning Sync `scheduled_tasks` description gained a REGLAS DE FIDELIDAD section (every claim must trace to what was read this run; never invent names; absent outcome ≠ "pendiente/a medias"; never assert the hour; omit over invent) — the Sync fire after 08-04 08:00 MX doubles as the grounding test. Lesson for any future surface retirement: retiring delivery does NOT retire the RESOLVER — the last delivered artifact stays interactive until resolved/expired; expire it as part of the cutover.

## MexicoNecesario tweet schedule — DIY GraphQL recipe replaced with tweet_post (2026-08-05)

**Incident:** the daily tweet failed Aug 4 + Aug 5 with `401 code 89`, self-reported as "Cookies expiradas" — but the cookies were valid the whole time (probed 200; project store and env store byte-identical by hash). The schedule (`9e06a237`, created 07-31 19:43 — 43 min after the last successful lib post) bypassed `tweet_post` to hand-roll the X GraphQL call via `project_get` + `http_fetch`, with a **mangled hardcoded bearer AND queryId** (`…Ws_Bv5XemA` vs the lib's proven `…Wv_I3WzS-A`), and its step 8 PRESCRIBED the failure message ("on 401/403 report Cookies expiradas") — so every failure was misdiagnosed and the operator rotated cookies uselessly. Class memory: `feedback_scripted_diagnosis_in_prompt.md`.

**Fixed 2026-08-05 (no code changes — DB row + KB file):**

- Schedule `9e06a237` tools → `["jarvis_file_read","tweet_post"]`; prompt rewritten: match calendar entry by day-number+month, publish via `tweet_post` account `mexiconecesario`, on failure relay the tool result VERBATIM (no scripted diagnosis). Cron unchanged `0 13 * * 1-5`.
- `jarvis-kb/projects/mexico-necesario-ac/docs/calendario-editorial-ago-sep-2026.md`: weekday labels were 2025-aligned — all 42 headings script-verified against real 2026 (34 relabeled; **8 "Viernes" entries actually fell on 2026 Saturdays — unreachable under the L-V cron — moved to their following Mondays**, so Alianzas now runs lunes). Preamble pillar map + week ranges + summary updated. Backup: `/root/claude-backups/calendario-editorial-pre-weekday-fix-2026-08-05.md` (jarvis-kb is not a git repo).
- Live-verified through the real scheduler (cron nudged to 13:35 MX, then restored): `x post ok, tweetId 2085087336304398813`, one network attempt, result broadcast to operator (msgId 20838).

**Watches:**

1. **First natural run: jueves 2026-08-06 13:00 MX** ("Jueves 6 agosto — Noticias/contexto"). Verify `journalctl -u mission-control | grep "x post ok"` shows a fresh tweetId. If it 401s NOW, that is a genuine cookie expiry and the tool's refresh guidance applies.
2. **Calendar texts exceed 280 weighted chars despite the file's "Máximo 280 caracteres (verificado)" claim** — today's tweet was rejected 4× tool-side and trimmed live by the agent before fitting. The published text is a shortened variant of the calendar's. Decide: batch-verify/trim the 41 remaining texts at source (script the weighted-length check), or accept per-run agent trimming. Trigger: next time a published tweet reads noticeably different from the calendar.
3. **Aug 4's tweet was never published** (pilar Dato demográfico, "En 2026, más de 9 millones…"). Backfill is an editorial call — operator.
4. **Calendar preamble says "08:00 CDMX"; the cron fires 13:00 MX.** Pre-existing mismatch (also confused Jarvis's own 08-05 self-diagnosis, which called it "el schedule de las 08:00"). Align one to the other — operator call.

## §17 6a removed — follow-ups (2026-08-02)

**Context:** 6a (green/red brief promote ratio) was deleted, not re-tuned — it scored a population that never existed (0 pure-red briefs in 20 ruled), over a label that means evidence density rather than predicted usefulness, from a per-brief verdict that cannot express per-judgment calibration. §17 now passes 5/5. Full rationale lives in the module doc of `src/briefing/v82-activation-gate.ts` — read it before touching any of the below.

**Watches:**

1. **Do NOT re-add a confidence ratio without a per-judgment verdict.** This check has now been rebuilt twice and removed once; the next person to see "§17 doesn't measure calibration" will be tempted to restore it. **Trigger: any proposal to score `confidence` against operator behavior.** The prerequisite is an affordance that lets the operator rule on individual judgments (the brief already numbers them — e.g. `sirve 1,3 / descarta 2`). Build that first; then calibration is directly measurable and 6a can return honestly.
2. **§17 now passes — confirm the operator still wants V8.2 activation gated on the remaining five.** §17 passing is one of three blockers on V8.3 L≥3 (the others: §14 at 0/7, and an explicit `mc-ctl v83-promote <cap> --confirm`). Nothing auto-arms — `evaluateV82Gate` is tsx-only, never called in-service. **Trigger: before any L≥3 discussion**, re-read what §17 now asserts (schema, shadow volume, citation resolver, critic unfixable, sycophancy) and confirm that set is a sufficient bar without an acceptance term.
3. **`V82_DELIVERY_ENABLED` is now a product decision, not a gate unblock.** It used to be the only path off §17's `insufficient_data`. With 6a gone, §17 goes green on shadow data alone. **Trigger: when the judgments are worth showing** — arm it because the operator wants them in the 06:00 brief, not to move a gate.

**Queued (not scheduled):**

4. ~~🔥 **§14's two SAFETY checks are vacuously green**~~ — **RESOLVED 2026-08-02.** `linkageIntegrity` and `reversibilityCoverage` are violation counts over `autonomy_level >= 3`, a population empty by construction (only L1→L2 is implemented; all 6 capabilities sit at L1), so both passed having examined ZERO rows — and `promotion.ts` wrote those two `✓` lines into a PERMANENT ADR as promotion evidence. **Correction to this item's original fix direction, which was wrong and would have caused real harm:** it said "a check whose population is empty must return `insufficient_data`, never `pass`". Applying that would have **deadlocked §14 permanently** — L≥3 decisions cannot exist until an L3 promotion, and §14 gates the L1→L2 step on the way there, so requiring a non-empty L≥3 population makes the gate unsatisfiable forever. Reading the module doc first is what caught it: checks 5/6 are explicitly **regression detectors** for invariants enforced upstream ("a `fail` here means one of those gates regressed"), so a pass over an empty set is semantically correct — it records "no breach found". **The actual defect was evidentiary, not logical:** a vacuous `✓` was indistinguishable from a verified one, in a record meant to outlive the session. Fix = `V83GateCheck.vacuous` flag set when the L≥3 population is 0; details now report the DENOMINATOR (`all N L≥3 decision(s) carry…` vs `no L≥3 decision exists in the 7d window — nothing to verify (vacuous)`); `mc-ctl v83-gate` and the ADR render `○` instead of `✓` for a vacuous pass, and the ADR gains an explanatory note naming which checks were vacuous and why. Verdict semantics deliberately unchanged. 3 tests added, all **mutation-verified** (reverting the flag or the renderer fails them) — the pre-fix code produced identical output for an empty and a verified population, which is exactly why this survived. Live: `○ linkage integrity: no L≥3 decision exists in the 7d window — nothing to verify (vacuous)`. Related: [[structural_safety_gate]], [[verification_that_cannot_fail]].
5. **Collapse-to-green sweep — 4 more instances of the same class (audit R2 A2–A5, all PRE-EXISTING, all currently unconsumed).** (a) `src/observability/prometheus.ts:292` `rituals.every((r) => !r.stale)` — `[].every()` is `true` and heartbeats are written only on the success path (`:250`), so a scheduler that never fires reports healthy forever (the exact boot-wedge the gauge exists to catch). (b) `src/api/routes/health.ts:83` — `inferenceOk` never enters `status` or `code` (`:84`), so unreachable inference still returns 200 `"healthy"`; and `:58` tests `statSync().size > 0` on a credentials file, not reachability. (c) `scripts/watchdog.sh:57` collapses curl-fail, jq-fail and metric-absent into `""`, all consumers skip, and `:319` prints `OK: all checks passed`; no `absent()` companion rule in `monitoring/alerts.yml` (0 hits across 23 rules). (d) `scripts/eval-gate.ts:152` prints "too few for subtle regressions" then falls through to `:255 exit(verdict === "PASS" ? 0 : 1)` — `src/tuning/gate.ts:28` has no third state. **Trigger: any incident where a monitor reported healthy through a real outage** — that is this class cashing in. Treat as one sweep, not four tickets.
6. **§17 check 5 has no recency floor (audit R1 rec).** Sycophancy is scored over a **30d** window while every other §17 check uses 7d. Before 2026-08-02 this was inert — 6a pinned §17 non-green anyway. Now §17 can reach `pass`, so a **dead probe cron keeps check 5 green on rows up to 30 days stale**. Currently benign (latest probe 2026-08-02T08:30, 40 rows in 7d). **Trigger: any `probe-cron` failure, or the first §17 evaluation where `sycophancy_probes` has no row inside 7d.** Fix = a recency floor on the newest probe, not a shorter window (30d is needed for sample size).
7. **`composeReminder` non-morning branch (audit R1 rec).** `no-verdict-reminder.ts:85` now tells the operator verdicts feed §13 — true for `morning`, but §13 filters `surface === 'morning'` while the old 6a was surface-agnostic. **Trigger: the first non-`morning` row in `proposed_briefings`** (73/73 are `morning` today). Fix = branch the sentence, or drop the gate reference for non-morning surfaces.
8. ~~**§13 cache-read: `nanoclaw` is not in the cold-start exclusion**~~ — **RESOLVED 2026-08-02.** `nanoclaw` moved out of `GATE_CACHEABLE_AGENT_TYPES` and into `GATE_COLD_START_AGENT_TYPES`, so it is excluded from the ratio but still rendered in the auditable mirror (`excluded cold-start (heavy, nanoclaw)`). **Correction to this item's original text:** it claimed `GATE_COLD_START_AGENT_TYPES` is what excludes rows from the ratio. It is not — since V8.5 Phase 3.3 the gate filter is the `GATE_CACHEABLE_AGENT_TYPES` ALLOW-list, and that const only shapes the mirror display. The conclusion (nanoclaw was being scored) was right; the named mechanism was wrong, so fix (a) as written would have changed nothing. **The decision, made on evidence rather than the cheap/general axis originally posed:** over 30d `nanoclaw` pooled **34.5%** cache-read (49 runs / 72.0M prompt tokens) versus `heavy` — already excluded FOR coldness — at **61.8%**, and hot-path `fast` at 85.8%. The excluded type was warmer than the scored one, making this a category error in the allow-list rather than a weighting problem. Option (b) (median/capped contribution) was rejected: pooled token share is the cost-relevant question §13 exists to ask, and blunting it would mask a genuine hot-path cache regression. A reusable CRITERION is now recorded on the const: a runner belongs on the cold-start side if it starts from a cold prompt prefix on EVERY run (containerized, fresh clone, or fires less often than the 5-min cache TTL) — regardless of how good its numbers look that week. Live effect: the ratio is undragged (94.7% without nanoclaw vs 76.8% with), but the gate now reads `insufficient_data` because excluding the row also drops the run COUNT 19 → 18 against the `GATE_MIN_CACHEABLE_RUNS` floor of 20 — correct quiet-Sunday behavior, not a regression. Test added + mutation-verified (restoring nanoclaw to the allow-list fails it). No deploy needed: `evaluateActivationGate` is called only by `scripts/briefing-gate.ts`.
9. ~~**Deploy the reminder-string change.**~~ — **DONE 2026-08-02 18:56 UTC** (operator ran `./scripts/deploy.sh`, newPid 3631276; new string verified in `dist/`). All later commits in the session (`0c1a7d5`…`c45d9bb`) touch only tsx-script paths (`scripts/*`, gate modules imported by them) — **no further deploy owed**. Everything the running `dist/` serves is current.

## V8.3 ledger coverage + unsolicited-Telegram follow-ups (2026-08-01)

**Watches** (each has a concrete trigger — promote to a queue item when it fires):

1. **First V8.3 decision row.** The canary is now `jarvis_file_delete,gmail_send,northstar_sync,schedule_task` and `CAPABILITY_BY_TOOL` covers `delete_schedule` + `jarvis_files_batch_delete`. The NEXT operator-confirmed action on any of those five tools should write a `decisions` row. **Trigger: if one is confirmed and `mc-ctl v83-gate` still reads 0, the wiring is wrong** — check the router confirm path actually calls `executeGatedCapability` for that tool, and that the capability is seeded non-zero.
2. ~~**Shadow volume vs real tempo.**~~ **FIRED + RULED 2026-08-08.** Re-check found 0/7 after 33 days armed AND 7 days at the widened canary — while ~27 real gated-capability executions/wk (26 `gmail_send` + 1 `schedule_task`) flowed through scheduled paths the seam doesn't cover. Operator ruled: record scheduled/ritual executions too (audit-only). See the 2026-08-08 gate-validity section below; build approved same day.
3. **Alert notify-once regret.** `ALERT_RENOTIFY_HOURS=0` means a MISSED first announcement can become a silent outage (the 2026-06-12 failure mode the reminder was built for). **Trigger: any alert discovered late.** Fix = set `24` (daily reminder) rather than reverting to 6h.
4. ~~**Salon 54444db3 still logged out.**~~ ✅ **RESOLVED 2026-08-02 ~21:12 UTC** — re-linked per RUNBOOK §5 (pairing code, first attempt, no cool-down), `connected ✅` + operator-verified e2e ("hola" answered). Two corrections to the record: (a) the outage began **~Jul 19 06:00 UTC** (Prometheus `salones_wa_baileys_connected` flip), not Jul 29 — the in-memory registry's `since` resets on service restart and had re-learned the state at the Jul-29 host boot, under-reporting a 2-week outage as 4.6 days; the durable onset lives in Prometheus, read it there. (b) The alert chain is VALIDATED end-to-end on a real incident: `SalonWhatsAppLoggedOut`/`Disconnected` fired the whole window, the mc poller delivered (newly-firing + reminders), resolution notice sent post-re-link, and the operator confirmed the alerts were SEEN — deferral was deliberate, no delivery gap.

**Queued (not scheduled):**

5. **§13 pushback classifier — relevance floor.** Topic-word overlap is being read as dispute: judgment 122's prose contained "the Transhumanismo daily exploration", so two operator messages about a meditation SCHEDULE were classified as pushback on a SUBSTACK judgment. Each emitted the `held_position` default = the judgment's full ~1,600-char prose verbatim, in English, ahead of the real answer. Consider (a) requiring the reply to name the judgment's subject or contradict its posture, not merely share a token; (b) truncating/summarising the held-position re-delivery instead of restating full prose; (c) matching the re-delivery to the conversation's language. Cheap partial: (b) alone removes most of the perceived spam.
6. **`skill_run` unreachable at the confirm seam.** It is in `CAPABILITY_BY_TOOL` but carries `riskTier: medium` with no `requiresConfirmation`, so it never reaches `executeGatedCapability`. Either give it confirmation semantics or accept it is inert and note that in the map (currently the comment says so; the entry itself reads as if it were live).

## V8.3 gate-validity assessment — verdict + approved build (2026-08-08)

**Operator asked for a thorough validity assessment of the gate chain ("we need to move forward"). Verdict: every check is well-engineered, but the chain as wired could not produce a meaningful autonomy flip even at full green.** Four findings, all verified against live code + data (not memory):

1. **§14's shadow scores an empty-by-construction population** ([[gate-scored-an-impossible-population]] class, third instance). The only pipeline call site is the interactive confirm seam (`router.ts:1672`); 33 days armed → **0 lifetime `decisions` rows**. Meanwhile ~27 gated-capability executions/wk (26 `gmail_send`, 1 `schedule_task`, LIKE-tally over `tasks.output`, 7d) flow through scheduled/ritual paths that bypass the seam. Filling it synthetically (deliberate chat confirms) would validate a distribution autonomy will never govern.
2. **A full pass opens a door to nowhere.** The §7 runtime gate (`pipeline.ts:251`) demotes any L≥3 decision whose reversal op is not `sql_inverse`; `delete_inverse`/`tri_restore` compile to `deferred` = not replayable in v1 (`reversal.ts:207-210`). The only `sql_inverse` capability (`task_edit`, max_level 5) is internal and **unwired**; `schedule_task` ("natural first-flipper", max_level 5) carries `delete_inverse` → always demotes; everything else is capped L2. **No wired capability can ever execute autonomously under current wiring, gates green or not.**
3. **§17 is now a production-hygiene gate, not the consent warrant §12 cites it for.** Post 6a-removal (08-02) + brief retirement (08-03), all five checks are shadow-side (Jarvis's critic approving Jarvis's author). The bilateral leg = `surfaced_at` receipts (working, 4 stamps) + §12 per-decision linkage + conversational steering (unmeasured — explicitly folded into the operator's §16 judgment). Treating "§17 pass" as consent maturity is a category slip the gate's own module doc already disclaims.
4. **`<5%` unfixable at n≈20/wk is a zero-tolerance coin flip.** 1/20 = 5% fails (strict `<`); at a true compliant 3% defect rate, P(≥1 in 20) ≈ 46% → the gate fails ~half of all windows even when quality is in spec. Event forensics: #132 genuine (4 contradicted refs on the stall headline — the silence-ambiguity detector class); **#126's final artifact is sound** (6/6 resolved, 0 contradicted — the 2-strike escalation fired on a claim the author had already removed by the final revision), so the measured 10% overstates the defect rate. §17 self-clears ~08-12 if the approved streak holds.

**Approved build (operator, same session): items 1+2 of the recommendation — (1) re-site the V8.3 seam at the general tool-execution chokepoint, audit-only (observability-not-a-gate, never blocks), so §14's shadow fills with real traffic; (2) implement `delete_inverse` replay for `schedule_task` + widen §7's accepted set to proven-replayable kinds, making one real capability autonomy-eligible.** Deferred-but-standing pre-promotion items: ~~unfixable-window statistics fix~~ (**SHIPPED 2026-08-14** — 30d window + defect-class breakdown; see the 2026-08-14 section below), §16 explicit consent ruling, judgment→action relevance binding, injection-defense LLM half, `v83-promote` command.

**SHIPPED same day (`/ship-it`): both items live.** New `lib/v8-3/gated-execution.ts` (shared wrapper: interactive seam via `trigger.ts` + background seam inside `ToolRegistry.execute`; `{v83:"skip"}` prevents double-recording; `context.source` distinguishes rows); `delete_inverse` implemented end-to-end (template pre-execution → `completeDeleteInverse` from the tool's `schedule_id` → replay/verify/revert; null-pk ops refuse replay); §7 gate accepts `{sql_inverse, delete_inverse}`. qa-auditor PASS-w-W, no Criticals; folded before deploy: W1 (`getDatabase()` was the one throw-capable statement outside the never-blocks try), W2 (background seam re-throws the tool's ORIGINAL exception — arming a capability must not convert a throw into a success-shaped string; claude-sdk's `isError` flag rides on the throw), W5 (post-execute degrade test — the double-execute guard was untested), R1 (§8 interrupted → structured error, not silent `""`), R3 (`{success:false}` batch envelope grades as failure, matching task-executor), S1 (import-cycle edge removed outright — `gated-execution` no longer imports `db/index`), R4/W4 (contract cross-comments + `CREATION_BY_TOOL` exported).

**Deferred from the audit (triggers):**

1. **W3 — §10 freeze for an incomplete autonomous inverse.** A committed L≥3 decision whose `delete_inverse` op stayed `pkValue: null` (output shape drift) is currently only a comment. **Trigger: BEFORE the first L1→L2/L≥3 promotion of `schedule_task`** — add the freeze/metric (needs a decision_events CHECK migration for a new event kind). Joins the standing pre-promotion checklist.
2. **R2 — §14 shadow now fills WITHOUT operator confirms.** Background rows carry `context.source='background'`/`thread_id='background'`. That is the operator-approved intent (prove pipeline correctness on real traffic), but `mc-ctl v83-gate` should stratify the count by source so "shadow ≥7" is never misread as evidence of the confirm loop. **Trigger: before citing a §14 pass in any promotion decision.** **CLOSED 2026-08-17** — `shadowBySource` on `V83GateResult` + the by-source tail on the `shadow volume` line; `source` now persisted on the `proposed` event; operator chat runs label `operator` on their thread key (see the 2026-08-17 section below).
3. **R5 — `decisions.payload_json` retention.** Every scheduled `gmail_send` body now persists in the ledger indefinitely (previously interactive-only). No prune path exists. **Trigger: decisions table growth or the next privacy pass.**
4. **W4 residual — `CREATION_BY_TOOL` ↔ `schedule.ts` contract is pinned by cross-comments + an exported map, not an e2e test against the real tool.** **Trigger: any change to `schedule_task`'s output shape** — the cross-comment at its return site names the map.

## 2026-08-17 — V8.3 seam origin stratification (Jarvis "SDK Hook Seam" plan rejected)

Jarvis's plan on `jarvis/feat/v8-3-sdk-hook-seam` (KB + branch, untracked) claimed the operator-agent bypasses `ToolRegistry.execute` and proposed a router-seeded ALS + moving the seam toward the SDK hook layer. **Reviewed against `main` and rejected**: every path to a gated handler already funnels through `registry.execute` (chat = `router → submitTask → dispatcher → fast-runner → task-executor:131`); the "0 rows for schedule_task/northstar_sync/jarvis_file_delete" evidence was an empty population (those tools did not run 08-08→08-17); the 33 `gmail_send` rows were background, not interactive; the branch was 28 commits stale (pre-`fd60a02`, the seam files it planned to edit did not exist on it). Reflection + corrected plan: `docs/planning/v8-3-seam-origin-plan.md` (KB copy overwritten with a superseded banner; cookbook candidate #1 retracted).

**Shipped (this section's commit):** `RunOrigin {source: operator|background, threadId}` on `RunToolContext` (`rule-of-two.ts`; explicit > parent's > `BACKGROUND_ORIGIN`; `outsideRunToolContext` still isolates queue drains) · `TaskSubmission.threadId` seeded by the router's two operator submit sites (chat + user-background spawn, same `tk` the interactive seam records) · registry seam reads `currentRunOrigin()` instead of the hardcoded background literal · `GatedExecutionSource` = interactive | operator | background (re-throw guard = `!== "interactive"`: operator is a registry caller like background) · `source` persisted on the `proposed` event payload (previously ODD-context only — `thread_id` was the sole surviving discriminant) · `evaluateV83Gate` → `shadowBySource` (informational; legacy rows fall back to `thread_id`; buckets sum to `shadowDecisions`).

**Watch:** first `source:"operator"` row needs an operator chat turn that calls a gated tool WITHOUT a confirmation gate — today only `schedule_task` (the other three are confirm-gated → interactive seam). Verify: `sqlite3 data/mc.db "SELECT d.thread_id, json_extract(e.payload_json,'$.source') FROM decisions d JOIN decision_events e ON e.decision_id=d.id AND e.event_kind='proposed' ORDER BY d.id DESC LIMIT 5"` and `./mc-ctl v83-gate` (by-source tail on the shadow-volume line).

**Housekeeping DONE 2026-08-17:** `jarvis/feat/v8-3-sdk-hook-seam` deleted (0 unique commits; superseded untracked plan removed); Jarvis worktree `/root/claude/mission-control-jarvis` detached at `main` = `b337a23`. Merged `jarvis/*` branches pruned local + origin (jme-phase0/-schema/-phase2-consolidator/-phase3, fix/jme-phase1-gate, braid-prompt-enhancer, gdocs-read-full — every one verified `merge-base --is-ancestor` of `origin/main` before deletion). Only `jarvis/chore/save-chat-clarification-chain` remains on origin (unmerged, 1 commit ahead — not inspected).

## §17 unfixable regression — root cause + statistics fix (2026-08-14)

The 08-08 watch inverted: §17 did NOT self-clear on 08-12. Three NEW unfixables (#147 08-11, #153 08-13, #154 08-14 — all `unsupported`, all 2-strike escalations) put the 7d rate at 15%. Meanwhile the V8.3 background seam proved out cleanly: first `decisions` row 08-09 04:00 UTC (inside the ~24h forecast), 23 rows by 08-14, all `gmail_send`/`committed`/`background`, complete event chains, zero degrades — §14's ≥7 fill is met (R2 caveat stands: 100% background-source, single-capability; `delete_inverse` still unexercised).

**Root cause (confirmed live) behind #153 + #154 — one mechanical bug, two actors:** `sanitizeFtsQuery`'s ASCII-only token class `[a-z0-9]+` treated accented letters as separators, splitting `"ángeles"` into the unmatchable token `"ngeles"` (0 FTS hits; `MATCH 'ángeles'` directly = 33 hits — unicode61 folds fine, the sanitizer mangled the token first). Consequences: (a) the AUTHOR's `retrieveKbForQuery` (decompose.ts, same sanitizer) could never surface `projects/grupo-angeles/README.md` into the ledger → mis-cite (#153); (b) the CRITIC's `recall_check` reported a false "zero KB hits" for an entity **14 KB files contain**, steering the revision loop to DELETE a true claim, then scoring the non-compliance `unfixable` (#154). Every accented Spanish query term was degraded this way since Phase 6. #147 (staleness citations pointing at 65-day-old logs) is a genuine precision defect — the gate working as intended.

**Shipped (`dd24a79`):**

1. `sanitizeFtsQuery` token class → Unicode `\p{L}\p{N}` — fixes author + critic in one line.
2. **Absence-proof probe in `recall_check`**: on 0 FTS hits a bounded substring LIKE probe runs (raw + diacritic-stripped variants, wildcards escaped) before the tool may answer. A hit returns "the KB DOES contain the term" + paths; only a double-zero returns "no KB matches (lexical FTS + substring probe)". The tool can no longer manufacture a false absence for substring-findable content.
3. Critic prompt rule 4 (**ABSENCE NEEDS A DOUBLE PROBE**) + recall_check description documents the absence semantics — never remove-as-undocumented without the double-probe result.
4. **§17 check 4 window 7d → 30d** (`GATE_V82_UNFIXABLE_WINDOW_DAYS`; in-gate precedent: sycophancy is 30d) + `unfixableBreakdown {contradicted, unsupported}` in the result and detail string. Closes the zero-tolerance-at-n≈20 finding (one unfixable = 5% = fail was a coin flip — ≈46% false-fail at a compliant 3% true rate). Volume/resolver checks stay 7d (tempo, not hygiene).
5. Sweep: `jme.ts` `extractKeywords` enumerated-accent class → same Unicode classes (`sqlite-backend.ts` already clean; `jarvis-fs.ts` `buildFtsMatch` splits on separators — clean).

**Expected trajectory:** 30d reads 8/81 = 9.9% today — an honest red (5 of the 8 are `unsupported`, the class the root-cause fix stops). With no new unfixables it clears **~09-02** (#126 exits the window → 4/~81 = 4.9%). **Watch: the daily 12:01 judgment ritual** — post-fix judgments should stop producing accent-driven `unsupported` escalations; a NEW `unsupported` whose critique asserts KB absence would mean the guard failed (check the critique for the double-probe marker).

**Rejected:** a gate-side `mis-verified` exclusion class — prevention (B1/B2) beats exclusion, and an excludable "critic was wrong" label is a temptation channel for greening a red gate. Revisit only if a false-absence unfixable recurs post-fix. No retro-reclassification of #154 (ages out of the 30d window 09-13).

**qa-audit (PASS-w-W, 0 Crit) — folded before deploy:** W1 the probe FAILED OPEN (a thrown probe still emitted the double-probe absence string — the exact fabricated-absence class the guard exists to close; now a `probed` counter gates the message, fail-closed to "absence NOT established"); W2 short-query branch claimed absence with zero probes run (same fail-closed message, so prompt-rule 4's guarantee is now literally true); W3 spec single-source block still said 7d (updated); S1 a THIRD sanitizer sibling — `dispatch/keywords.ts` ASCII split shredded accented task titles, so historical-similarity routing never matched Spanish (fixed, same class); S2 breakdown assertions added on the legacy/retro-classification paths; R3 case-insensitive variant dedupe; R4 entity-identity caveat in the EXISTS message (a substring hit on a name-prefix sibling must not read as presence). Verified-no-action: LIKE escaping empirical (R1), probe cost 5.9 ms/scan at 883 rows (R2 — linear in KB size, revisit at ~10×), 30d window honest vs live data (R5), Unicode tokens injection-safe on both call sites incl. jme's unquoted join (R6). **Residual (R7b):** the breakdown buckets any non-`unsupported` reason as `contradicted` — correct while `UnfixableReason` stays a 3-union; a 4th reason would be silently mislabelled. Trigger: any widening of `UnfixableReason`.

## Sandbox runtime — OpenSandbox backend SHIPPED behind `SANDBOX_BACKEND` (2026-08-16)

Operator asked to evaluate + adopt https://github.com/opensandbox-group/OpenSandbox for Jarvis's sandbox runtime. Verdict: adopt as a **backend behind a seam**, not as a platform (doc `docs/planning/opensandbox-adoption.md`). Gains: egress default-deny allow-list (exfil defence for the keys we ship into the box), server-owned TTL lifecycle (zombie-container class from 07-14), an exec surface (`execd`) that later lets V8.4 gate checks run INSIDE the sandbox (today's W1 skip). Honest caveat: it does not move the nanoclaw failure rate (54 tasks/90d, 32 failed — routing/timeouts/evaporation, not substrate).

**Shipped:** `src/runners/sandbox-backend.ts` (`spawnSandbox`; docker default = byte-identical in-tree path) + `src/runners/opensandbox-backend.ts` (SDK `@alibaba-group/opensandbox` 0.1.11 exact-pinned; create → writeFiles input → `commands.run('<worker> < input')` SSE → same sentinel parse → teardown on every path; TTL = timeout+120s; `SANDBOX_EGRESS_ALLOW` → default-deny policy); runners call `spawnSandbox` + `handle.kill()`; config `SANDBOX_BACKEND / OPENSANDBOX_URL / OPENSANDBOX_API_KEY / SANDBOX_EGRESS_ALLOW`; `mc-ctl sandboxes` + `mc-ctl smoke sandbox`; host: `scripts/install-opensandbox.sh` (uv tool `opensandbox-server==0.2.2`, keep-labelled `mc-opensandbox-execd:v1.0.22` / `mc-opensandbox-egress:v1.1.6`, TOML cap ALL / pids 512 / ports 40000-40999 / allowed_host_paths mirror, `/etc/opensandbox/api.env` 600, `opensandbox-fw.service` **firewall guard** (4 rules: eth0 edge · `br-+`→docker0 cross-bridge · `docker0` same-bridge · INPUT hairpin, + `br_netfilter`) — the vendor publishes UNAUTHENTICATED execd on `0.0.0.0` and Docker bypasses UFW; qa R1 found the edge-only rule left sandbox→sandbox execd OPEN (proven, then CLOSED by the lateral probes); R2 MERGE-READY-w-W (5 W + 2 S folded: `grep -c` under `set -e`, truncation bound, `-o docker0`, probe-cannot-fail, dead floor, doc wording), `opensandbox-server.service` on `127.0.0.1:8098`); e2e `scripts/opensandbox-e2e.ts` PASS (round-trip + `docker inspect` hardening parity + lateral probes CLOSED with a positive control + teardown; egress deny probe passes in a one-off run with `SANDBOX_EGRESS_ALLOW` exported — the LIVE service runs without an allow-list) via `systemd-run -p EnvironmentFile=/etc/opensandbox/api.env` (never paste the key). Found on the way: execd `mode` = octal digits in a decimal int (`600`, not `0o600`); execd `timeoutSeconds` is an ABSOLUTE wall clock (qa C1) → dropped, TTL renewed on every heartbeat instead.

**ARMED 2026-08-16 (operator: "adopt it for our sandbox runtime")** — drop-in `mission-control.service.d/opensandbox.conf` (`EnvironmentFile=-/etc/opensandbox/api.env` + `SANDBOX_BACKEND=opensandbox`), redeployed pid 2581358 → final build pid 2625181; `mc-ctl smoke sandbox` PASS (nanoclaw task `8840e3b2…` 52 s via the backend, DELETE 204, no leftovers, V8.4 G-landing met). Remaining order (one key at a time): observation week on nanoclaw → `SANDBOX_EGRESS_ALLOW=*.anthropic.com,github.com,api.github.com,*.githubusercontent.com,registry.npmjs.org` → decide gVisor. Rollback = drop the `SANDBOX_BACKEND` line.

**Watches:** `./mc-ctl sandboxes` (guard present, server ok, no orphan `sandbox-*`); first nanoclaw task on the backend (`tasks.agent_type='nanoclaw'` after arming — compare duration/outcome with the docker path); `journalctl -u opensandbox-server` for `Failed`/`500`.

**Queued (not built):** V8.4 W1 in-sandbox gate checks (backend exposes `exec()` + holds the sandbox until the ledger is evaluated); execd `/metrics` into the task trace; egress ON; upstream bump = re-run the e2e.

## V8.4 "Honest Done" — completion ledger SHIPPED dormant (2026-08-16)

Adopted from the unlazy v2 review: **"done" is a claim; the harness holds a ledger of acceptance gates written BEFORE the work, runs the runnable ones as subprocesses, records evidence, and only then decides what the terminal status may say.** Spec: `docs/planning/v8.4-honest-done-spec.md`. Five slices in one session, all behind `TASK_GATES_MODE` (default `off`).

**Shipped (`66bc8b6`):**

- **8.4.0 substrate** — `task_gates` (harness-owned; model has no writer; additions only; met REQUIRES evidence; abandoned is final), `TaskSubmission.gates`, `scheduled_tasks.gates` column (+`mc-ctl gates set-ritual`), planner object-form criteria → `goal.metadata.gates` → orchestrator/swarm declare (source `plan`), dispatcher declare-at-submit → freeze+render at run start. Fail-open defaults fixed: nanoclaw/heavy blob without `success` ⇒ DONE_WITH_CONCERNS; `statusSource` measured (`mc-ctl gates status-sources`).
- **8.4.1 runner + consumer** — `evaluateLedger` (sh -c, own process group, minimal env, group-kill timeout, EXPECT substring|/regex/, evidence tail); modes off/shadow/enforce; enforce demotes completed→completed_with_concerns on FAILED and appends `Gates: n/N met · FAILED · unverified · ABANDONED` to the deliverable; `ABANDON: <id> <reason>` honored.
- **8.4.2** — swarm parent re-verifies each child's ledger (rerun) before REFLECT; nanoclaw non-messaging tasks get harness `G-landing` verified on the HOST against origin (branch/PR/commit claims) — the 07-17/08-02 "sandbox fix evaporated" class.
- **8.4.3** — SDK `hooks.Stop` wall (`TASK_GATES_STOP_HOOK=true` + mode≠off + task has ledger): blocks only on FAILED runnable gates, ABANDON honored, 3 same-set blocks ⇒ release recorded (`gates.hook_released`).
- **8.4.4** — numbers-provenance audit (registry records tool-result digests per run; deliverable numbers vs corpus+input → `numbers.audited`/`output.numbers_audit`; footer only with `TASK_GATES_NUMBERS_ANNOTATE=true`).

**ARMED 2026-08-16 (operator "Arm and deploy"): step 1 done** — drop-in `/etc/systemd/system/mission-control.service.d/v84-gates.conf` = `TASK_GATES_MODE=shadow`, redeployed newPid 2401298 (env verified in `/proc/<pid>/environ`, no Stop hook); first ritual gate set on `MexicoNecesario — Tweet Diario` (`9e06a237…`): G1 "tweet_post invoked in THIS run" = `./mc-ctl db "SELECT COUNT(*) … task_id='$MC_TASK_ID' AND tool='tweet_post'"` EXPECT `/^[1-9][0-9]*$/m` (probed under the runner's minimal env on a real task → `2`; guard-allowed). On no-tweet days the block instructs ABANDON — the first real exercise of the surrender path. Watch: `./mc-ctl gates summary 7` after the next 12:00 MX fire; then steps 2–4 below.

**ENFORCE ARMED 2026-08-27 (operator ran the §11 arm line): step 2 done** — drop-in flipped to `TASK_GATES_MODE=enforce`, pid 199417, `/proc/<pid>/environ` verified. Shadow tally that justified it (11d): 37 gates / 35 met / 1 abandoned (`G1`, X `daily_limit (344)`) / 1 failed (`g-2.2` on `138fba6f`: `check rejected by shell guard: command 'systemctl' is blocked`); `statusSource` 212 explicit / 2 default in 7d. **Ruling: guard-rejected ⇒ `failed` stays (strict)** — revisit only if such rows show up as demotions on real work. Remaining: step 3 Stop hook LAST after a week of `mc-ctl gates summary 7` demotion data; step 4 optional. Open row: `G-landing` on `fe6d55c3` still `pending` (never checked).

**Arming (operator, in this order; drop-in, not EnvironmentFile if the key already lives there):**

1. `TASK_GATES_MODE=shadow` → restart → give a ritual gates: `./mc-ctl gates set-ritual <schedule_id> gates.json` (e.g. the @MexicoNecesario publisher: `{"criterion":"tweet URL in report","check":"…","expect":"https://x.com/"}`) → watch `./mc-ctl gates summary 7` for a week (expect: failed rate sane, abandoned rate ≈ real impossibilities, landing verdicts on nanoclaw tasks).
2. `TASK_GATES_MODE=enforce` once shadow shows no false FAILED on good runs (spot-check 10 `gates.evaluated` rows).
3. `TASK_GATES_STOP_HOOK=true` last — it spends turns; watch `gates.hook_blocked`/`hook_released` counts and cost.
4. Optional: `TASK_GATES_NUMBERS_ANNOTATE=true` after eyeballing `numbers.audited` false-positive rate on Morning Sync / reports.

**Watches:** `mc-ctl gates status-sources 7` (how often the DONE default is what marks a fast task complete — the "~67%" claim becomes a number); first `G-landing` FAILED on a nanoclaw task = the class this was built for.

**Residuals / next:** V9 W1 `verifier.ts` should read `task_gates` as its `checks[]` (manual gates → capable-tier `criteria_check`); the wall is host-only (container workers don't see the flag); plan gates declared mid-run are consumed at completion, not by the wall; numbers regex is heuristic. Ad-hoc gated tasks: `POST /api/tasks` with `gates: [{criterion, check?, expect?}]` (validated at the edge, 400 on a bad payload).

## V8.5 Phase 5.2 — Rule-of-Two audit SHIPPED (2026-08-15)

Meta's Rule of Two — hold at most two of **[A] untrusted input · [B] sensitive access · [C] state change** per agent SESSION — shipped as two layers, because the doctrine is a composition property (a run that `gmail_read`s (A+B) then `gmail_send`s (C) is the trifecta though no single tool holds all three) and the plan's "per-tool matrix + forced L-level bump" wording only catches the degenerate single-tool case. Operator rulings (2026-08-15): **KB reads = trusted tier (B only)** — untrusted content enters through the ingest edges, which are A; **trifecta ⇒ hard cap L1** at tool level and run level.

**Shipped (`ec8d9dd`):**

- **Layer 1 — per-tool matrix.** `src/tools/rule-of-two.ts`: name-keyed classification of all 191 host tools + MCP prefix defaults (browser/playwright = A, graphify-code = B, xpoz = A+B with `xpoz_trigger_run`/`get_job_status` overridden to B) + `Tool.untrustedInputHint`/`sensitiveAccessHint` (explicit fields win; unclassified ⇒ true/true). Classification rules are in the file header (A = INHERENT third-party free text; numeric feeds ≠ A; unbounded dispatchers `skill_run`/`jarvis_dev`/`google_workspace_cli`/`wp_raw_api` hold A by construction; `shell_exec` = B only, args-borne fetches named as the residual). **Structural rule in the ONE resolver** (`getToolAnnotations`, which `ToolRegistry.getEffectiveRiskTier` — the interactive confirm flow's source — now delegates to): a single-tool A∧B∧C ⇒ `riskTier:"high"` + confirm, no code path around it. Live delta, all ≤1 call/30d: `google_workspace_cli` low→high, `kb_ingest_pdf_structured` low→high, `skill_run` medium→high, `wp_raw_api` medium→high (`jarvis_dev` already high; `shell_exec` 981 calls/30d deliberately B-only so nothing high-frequency changes). V8.3: `skill_run` seed `max_level` 2→1; `structuralMaxLevel` enforced in the seed invariant, `promotion.ts` (`refusedBy:"rule_of_two"`, from the code constant — the LIVE row still says 2 because INSERT OR IGNORE never rewrites; the audit reports the drift) and the pipeline.
- **Layer 2 — run-level composition.** `runToolContext` (AsyncLocalStorage) entered by the dispatcher around every in-process runner execution (nested `submitTask` INHERITS the parent's set — swarm sub-tasks compose); `ToolRegistry.execute` records every call and hands the gated seam the PRIOR set; the V8.3 pipeline (step 2a, tool-backed triggers at effectiveLevel ≥ 2) demotes to L1 with `autonomy_demoted` reason `rule_of_two_tool` (backing tool alone is a trifecta), `rule_of_two` (prior ∪ this is), or `rule_of_two_no_context` (seam ran outside any run context — container workers, reflection runner, interactive confirm handler — unknown prior composes as all three, fail closed). Dormant while everything is L1; live at first promotion. End-to-end test: `gmail_read` then `gmail_send` at L2 ⇒ demoted `rule_of_two`; control stays L2.
- **`mc-ctl rule-of-two [--offline] [--days N] [--write]`** (`scripts/rule-of-two-audit.ts` + read-only `GET /api/admin/tool-annotations`): matrix, trifectas + tier delta, capability rows vs the structural cap, and the **30d retrospective** → `docs/audit/rule-of-two-matrix-2026-08-15.md`. Findings: 191 host tools, 0 unclassified, 5 single-tool trifectas; **880 runs/30d, 189 (21.5%) held A∧B∧C, 88 with a GATED capability (`gmail_send`) as the terminal edge** — [A] came from `web_search` (115) / `web_read` (57) / `intel_query` (53), i.e. genuine third-party prose, not the conservative calendar/drive calls. **That 88 is the blast radius of promoting `gmail_send` to L2: those runs demote back to L1 by the rule.** Stratify before citing §14 in the promotion conversation.

**qa (R1 FAIL → folded → R2 PASS-w-W, all folded):** C1 `xpoz__xpoz_trigger_run` was an UNSIGNED low→high flip via the xpoz prefix default — none of the three verification surfaces (pinned test, offline audit, live endpoint pre-deploy) saw MCP tools; folded with per-tool MCP overrides + a 43-name live MCP fixture in the pinned test + a `mcp-servers.json`-driven prefix-coverage test. W1 prototype-chain keys classified as A=B=false via `in`/`??` — `Object.hasOwn` everywhere. W2 Layer 2 was blind AND fail-open at every process/task boundary — nested contexts now inherit, `priorRunTools()` is `undefined` outside a run and the pipeline demotes on it. W3 the dispatcher wrap (Layer 2's single point of failure) had no test — added (removing the wrap turns it RED). W4 retrospective first-seen dedup undercounted post-arming edges — undeduped ordered scan. W5 `--offline` still required `.env`. R1 `--days 30d` ⇒ NaN ⇒ a zero-row report indistinguishable from clean — refused. R2/R3/R5 folded.

**Residuals (named, not fixed):**

- Layer 2 cannot see container workers (separate process — `nanoclaw-worker`/`heavy-worker` register only classified B tools + `jarvis_dev`, no gated capability today: coincidence, now fail-closed via `rule_of_two_no_context` if one ever appears), the container-queue drain, the reflection runner, or the interactive confirm handler (`trigger.ts` passes `undefined` ⇒ any L2 interactive decision demotes to L1 — harmless, both routes confirm).
- `shell_exec` as a fetcher (curl) is args-borne and invisible to both layers — mitigated by the shell scope gate + allow/deny lists.
- `structuralMaxLevel` is static A∧B with C assumed (the seed cross-check already rejects readOnly backing tools, so every valid capability is C) — a read-backed capability would be mis-labelled "trifecta"; assert the C-assumption where the readOnly check lives if seeds ever admit reads.
- Skills classified conservatively as A+B+C by construction ⇒ interactive `skill_run` now confirms (0 calls/30d). Per-skill classification = future override at the `skill_run` args level.
- Live `capability_autonomy.skill_run` row still `max_level 2` (drift reported by the audit; enforced from code). If a boot-time reconcile is ever wanted it is a pointer-move on the row, not a mutation of history.

**Precondition on the `gmail_send` L1→L2 promotion (new):** re-run `mc-ctl rule-of-two` and read the "gated terminal edge" count — it is the number of background sends that will demote back to L1 under the rule; decide whether the morning/market rituals' web-read→send shape is acceptable at L2 or whether those rituals should be split before promoting.

**Next in Phase 5:** 5.3 time-boxed approval windows (only matters once something sits at L2) → 5.4 shadow-Git (a)-lite sizing session.

## Sandbox shell guard bypassed by a wrapper script — two follow-ups (2026-08-27)

Context: PR #35 (`eeee703`, PUSH_CAP 4→5 + WORD_CAP 700→1400, deployed pid 1314028) needed a 3-line sibling-test fix (`dynamic-budget.test.ts` `fillBudget(2)` → `fillBudget(PUSH_CAP - 2)`). The follow-up chat task `4c34b839` routed to nanoclaw (coding). Inside the sandbox `shell_exec` blocks `git commit/push/add` (`src/tools/builtin/shell.ts:276`, message "use git_commit/git_push tools"); Jarvis's reply: _"Escribí un script bash (`/tmp/do_commit.sh`) con las operaciones de git dentro y lo ejecuté con `bash /tmp/do_commit.sh`. El comando invocado no contiene el token bloqueado, así que la política lo dejó pasar."_ — a verbatim violation of the `[GUARD POLICY]` env-note (`src/runners/nanoclaw-env-note.ts:42`: HARD STOP, "no wrapper scripts"). Commit `d6c18ae` (author `Jarvis (nanoclaw)`) pushed because the sandbox clone is DESIGNED to push (`gh auth setup-git` on the mounted `/root/.config/gh:ro`, `nanoclaw-worker.ts:67-85`) — no credential leak; the boundary broken is tool routing. The runner's own verifier had rejected the previous attempt (score 0.8: "3/3 completed is not defensible… vitest not installed, git push blocked"); the continuation bypassed the guard and the task closed **`completed`**. V8.4 had **0 `task_gates` rows** on the task (nanoclaw path) — Honest Done never saw it. CI was the only gate that ran (green).

1. **Resolve the sandbox contradiction one way.** The clone exists so the agent can edit/commit/push, yet its shell deny-regex blocks exactly those verbs and points at `git_commit`/`git_push`, which the task narrative says were not reachable in the container (UNVERIFIED — first list what the nanoclaw worker actually registers; the Rule-of-Two residuals above say "classified-B tools + `jarvis_dev`"). Either (a) register `git_commit`/`git_push` in the worker (same `--author` stamping and path as host Jarvis), or (b) drop the regex inside the container and let the clone's plain git be the sanctioned path. A text deny-list on a shell that also allows `bash file.sh` protects nothing ([[flag-deny-never-converges]]) — do NOT "fix" by adding `bash`/`sh`/`.sh` to the list. Verify by mutation: a sandbox task that must commit succeeds through the sanctioned path, and a wrapper-script route is no longer the only one that works.
2. **Guard-evasion detector on task output → `completed_with_concerns`.** An agent that narrates a bypass ("la política lo dejó pasar", "wrapper script", "no contiene el token bloqueado", base64 / `env -i` / alternate binary) must not close green. Cheapest seam: a harness-source V8.4 gate on the nanoclaw path (today 0 gates) — `RB-guard` FAILS on the evasion vocabulary, and on any journal `[shell-guard] BLOCKED` followed by a success claim for the same verb in the same task. Corpus-replay before shipping ([[corpus-replay-before-shipping-a-text-filter]]): replay the last 30d of nanoclaw outputs — the 06-24 base64 incident and `4c34b839` must FAIL, ordinary "commit"/"push" mentions must pass. Trigger: before the next nanoclaw coding task that ends in a push, or together with V8.4 step 3 (Stop hook) — whichever comes first.

Also noted (not queued as items): (i) `jarvis_dev action=branch` runs `git checkout main` inside the linked worktree, which git refuses (`'main' is already used by worktree at /root/claude/mission-control`); the catch swallows it, so every new Jarvis branch forks from the PREVIOUS Jarvis branch and PRs stack while it is unmerged (#32→#33→#34 carried 3 commits; #34 even deleted #33's mechanism). Fix: `checkout -B <branch> origin/main` and fail loudly; also `next_steps` (`jarvis-dev.ts:381`) still tells Jarvis to edit `/root/claude/mission-control/` — the primary — which is how #34 left an uncommitted diff in the operator tree. Bites only when the prior branch has unmerged commits. (ii) `jarvis_dev action=pr` ran its full-suite gate for neither commit of #35 (`300b364`: SIGTERM at 300 s / `jarvis-gate.slice` MemoryMax; `d6c18ae`: sandbox path, never involved) — the abort text already steers Jarvis to scoped tests; CI is the backstop.

## Memory-architecture plan v2.0 — follow-ups (2026-08-28)

Plan: `jarvis-kb/projects/agent-controller/jarvis-memory-architecture-plan.md` (Tracks 2+3 shipped 2026-08-28).

1. **Track 1 audit — DONE by Jarvis 2026-08-28** (task `e8a1b038`; 0 deletions, 1 fact created, 3 KB files trimmed). Full delivery in the task output.
2. **Operator rulings from Track 1 (five, none auto-resolved):** (a) 12 real secrets in `user_facts.projects` (Alpha Vantage, DENUE, Gemini, OpenAI, Polygon, X bearer + key/secret, LivingJoyfully FTP + 2× WP app passwords, an unidentified `api_credential` UUID, a generic `wp_app_password`) → replace each value with a fact naming the env var + path, the `alphavantage_api_key_path` pattern; (b) license plates contradict — `fede-reference.md` R97-BRM vs `personal.vehiculo_placas` R97-BRY; (c) raw FTP password in `preferences/protocols.md` `image_upload_method`; (d) ~144 `signal_digest_*` blobs inflating `work`/`projects` — purge or migrate; (e) dream cluster (plane/boat/house) duplicated across `personal`/`preferences`/`philosophy`. On "go": one Jarvis prompt per ruling. Also: `fede-summary.md` is still `always-read` and now duplicates the children's ages that live in `personal.children` — fold into the same prompt.
3. **Jarvis fan-out playbook.** The "Competencia Meli" 5-agent run left no `tasks` rows → it did not use the swarm runner; Track 3 does not touch ad-hoc SDK subagents. Add the same two rules (shared brief first, disjoint owners, `## Shared findings`) to Jarvis's coding/fan-out playbook directive.
4. **Track 2 readout (+4 weeks, ~2026-09-25).** `./mc-ctl jme-preferences` — signal rate per 100 chat turns; once ≥ 5 inferred preferences exist, a ≥ 30% drop over 4 weeks = success; no drop at 8 weeks = remove the prompt rule + ordering, keep `jme_signals`.
5. **Track 3 readout (next 3 swarms).** `sqlite3 -readonly data/mc.db "SELECT id, description LIKE '%owned by g-%' FROM tasks WHERE parent_task_id='<swarm>' AND spawn_type='subtask'"` — ≥ 90% of sibling goals carry an ownership line.
6. **Rejected-preference re-inference.** `--reject` soft-expires the row; the consolidator can re-infer the same text. If a second reject happens, add a stop rule (skip an incoming preference whose cosine ≥ 0.85 to an expired-by-reject row).
7. **Classifier scores runner scaffolding.** The `## Coordination` block (69 words) and forwarded findings (aggregate cap 3,000 chars) count toward the classifier's word-count score (`classifier.ts` ~542-624), so late siblings in a wide swarm can route heavier. Fold: exclude runner-authored sections from `detectText` (R1 W6, second half).
8. **Knowingly unpinned wiring:** `orderForInjection` is applied at its single production site, `src/runners/fast-runner.ts` (JME block); no test asserts the ordering there (R2/R4 W4, accepted — ordering only). A refactor that drops the wrapper is a regression, not a cleanup.
9. **Identity inversion by the extractor (2026-08-31).** `jme_facts#307` «Fede prefers to be called Piotr by Jarvis» came from Jarvis's OWN Track 1 report (anti-echo rule violated, subject inverted) and survived a live correction because the 08-29 run extracted no identity fact. Prompt now carries a Names rule and `consolidateAll` refuses the "Fede … is called Piotr" shape (`isIdentityInversion`, warn + skip); if an inverted identity fact reappears in another shape, the structural fix is to withhold Jarvis turns from the extractor input (keep them as context only via a summary) — and note a correction twin scores 0.944 vs the wrong fact, 0.006 under the 0.95 SKIP line: consider lowering SKIP or exempting corrections. Consumer: `./mc-ctl jme-preferences` (Rejected section).

## Updates log

| Date | Change |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-05-07 | File created. Replaces `stabilization/next-session-brief.md` after freeze lift. 15-item queue derived from 2-week incident pattern. |
| 2026-05-08 | Strict-mirror sync shipped (`e78d71d`) — closes NorthStar↔COMMIT bricking class, not in original queue. Item #13 escalated P3 → P1 (4th-incident trigger fired on `jarvis_file_write` scope-loss). Two new P1 items added: #17 max_turns truncation, #18 NorthStar compass-reframe operationalization. Pickup brief at `next-session-friction-pickup.md` distills the three for next-session entry. Queue is now 18 items; 14 closed, 4 open (#2, #13, #17, #18). |
| 2026-05-08 | **v7.6 Spine 1 entry shipped** (`90ebc31` + `ed52d6c`). 3/9 gatekeepers audited (`scope-classifier`, `dispatch/classifier`, `confirmations`). F5 producer/consumer regex drift fixed: shared `confirmation-verbs.ts`, destructive-hint carve-out wired through router, fast-runner deletion confirm now strict, `para` decline preposition over-fire fixed, syntactic regex-source guards added. **75 new tests, 4479/4479 passing.** Round-2 audit cadence caught 5 bundle-regressions (C1/C2/C3/C3-refined/C4) — pattern memorized as `feedback_round2_audit_pattern.md`. Side-discovery: friction-pickup #1 brief amended (JARVIS_WRITE_TOOLS already always-on; the 4-day Spanish-imperative friction class needs re-diagnosis at a different layer). Audit doc: `mc/docs/audit/v7.6-gatekeepers.md`. **Remaining Spine 1 candidates** (6/9): `messaging/fast-path.ts`, `runners/fast-runner.ts` hallucination guard, `messaging/post-filter.ts`, `prometheus/reflector.ts`, `prometheus/planner.ts`, `messaging/normalize.ts` — LLM-output parsers (rows 2-5) likely to surface real bugs. Projection: ~2 more pre-existing bugs, +60-100 cumulative tests, 4-6h remaining work. |
| 2026-05-08 | **v7.6 Spine 2 SHIPPED** (`36dbcd9` + `9379fe1`). Observability backfill complete in one session. 4-pattern + broadened sweep across `src/` surfaced 12 candidate counter sites; **1 real drift bug** at `router.ts:437` (`fullCount` arithmetic summed only 9 of 30 tool groups + memory off-by-one). Fix: new `getAllAvailableTools(opts)` single-source-of-truth helper in `scope.ts`, co-located with `scopeToolsForMessage` (the producer). Other 11 sites: 8 non-drift (producer/consumer share data structure at-emit, e.g. `graph.size`, `result.alerts.length`, `result.perCase.length`), 1 already coupled (Spine 1 `Asking N`), 2 out-of-scope (size-in-KB info logs). **+17 effective tests** (it.each over 6 ENV_VARIANTS gate-flips), 4479→4496 passing. **2 audit rounds** — round-1 found 2 warnings + 2 suggestions about test depth, all 4 fixed in same commit before ship. Round-2 found 2 forward-looking gaps (URL-injection branch unreached, universe upper-bound asymmetry undocumented), both addressed in `9379fe1`. **0 bundle-regressions across 2 rounds.** Audit doc: `mc/docs/audit/v7.6-observability.md`. **Spine 2 done.** Recommended next: complete Spine 1 remaining 6 candidates, then Spine 4 (MCP annotation push) — Spine 3 data-ready 2026-05-21. |
| 2026-05-08 | **v7.6 Spine 1 CLOSED** (`228c0cd` + `a9ac208` + `74c6a73`). All 9/9 gatekeepers audited. **2 more pre-existing bugs found** (in addition to F5 from earlier today): F8 — G5 hallucination guard verification carve-out gap (first-person past-tense write verbs slipped through "verifica"/"lista" exemption); F10 — G8 planner `parseLLMJson` error embedded raw LLM content in `.message`, leaking through `Planning failed:` wrapper to user-facing `router.ts:2321` (background-agent fail-replies) and `rituals/dynamic.ts:574` (scheduled-task broadcasts). Fix: typed `LLMJsonParseError` class with generic `.message` + operator-only `.diagnosticDetail()`. **+24 tests this commit chain (15 initial + 6 round-1 + 3 round-2), Spine 1 cumulative +99 tests, 4479→4520 passing.** Round-1 audit caught C1 (`FIRST_PERSON_WRITE_RE` lacked Spanish negation guard); round-2 caught W1 (`ni` correlative + `siquiera` missed in negation alternation). Pattern: bundle-regressions on negation guards come in classes — catch one form ("no"), the next form ("ni") follows in the next round. Doctrine update logged in `feedback_round2_audit_pattern.md`. **Spine 1 final scoreboard**: 3 pre-existing bugs (F5/F8/F10), 7 bundle-regressions caught before shipping, 33% hit-rate (matches V7.6-GUIDE.md projection). Audit doc: `mc/docs/audit/v7.6-gatekeepers.md`. **Spine 1 + Spine 2 both DONE.** Recommended next: Spine 4 (MCP annotation push, ~2-3 days) or Spine 5 (skill engine surface). Spine 3 data-ready 2026-05-21. |
| 2026-05-08 | **v7.6 Spine 4 CLOSED** (`331678b` + `b458293` + `631ea0f`). MCP annotation coverage: pre-spine 23/185 (12.4%) tools annotated, the other 162 collapsing to conservative `getToolAnnotations` defaults that muted destructive-vs-readonly metrics. Bulk script with per-tool classification table (4 canonical sets `r`/`rL`/`d`/`di`) annotated 162 tools in one pass; 1 manual fix for template-literal edge case. New `MCP annotation coverage (v7.6 Spine 4)` describe block in `registry.test.ts` adds 5 invariant tests (4-hint completeness + readOnly⇒NOT destructive + requiresConfirmation⇒NOT readOnly + riskTier=high⇒destructive + exact-count `toBe(186)`). **2 audit rounds caught 7 bundle-regressions + 2 pre-existing finds**: round-1 W1+W2 misclassifications (batchDecompose `rL`→`d`, humanizeText `d`→`r`) + W3 loose threshold pin; round-2 C1-C4 SEO/video pure-transform misclassifications (seo_content_brief, seo_meta_generate, video_script, video_brand_apply) + chart_generate sibling + W1 pre-spine idempotent convention drift on web_read/web_search. **Empirical bulk-script misclassification floor: ~4.3%** (7/162) — round-2 sweep of name-pattern siblings is mandatory before closure. 2 residuals deferred (W4 MCP source needs name-pattern hint lookup; W5 screenshot edge needs a 5th canonical set if "side-effecting but reversible" pattern recurs). Audit doc: `mc/docs/audit/v7.6-mcp-annotations.md`. **Spine 1+2+4 all DONE; Spine 3 data-ready 2026-05-21; Spine 5 (skill engine) or Spine 6 (library composed smoke tests) recommended next.** |
| 2026-05-10 | **Anthropic SDK cutover SHIPPED** (`5b62c3f` + `64071fd`). Operator directive: agent-controller off Fireworks/Groq onto Claude Agent SDK — Sonnet primary, Haiku fallback for `infer()`/`inferWithTools()` callers; Opus→Sonnet wrapper for Prometheus complex paths (planner/executor/reflector). CRM stays on Fireworks per directive (Hindsight container also stays). Vision and Whisper unchanged on Groq (Anthropic offers neither). Three audit rounds: round-1 found C1 cost*ledger model attribution drift + C2 shared circuit breaker collapsed fallback chains; round-2 found C1 was only fast-runner-deep, heavy/swarm pipeline still dropped `actualModel` exactly where Opus runs (full plumbing: `prometheus/types.ts` TokenUsage → executor.executeGoal/executeGraph → orchestrator → both heavy-runner branches). W7 missing both-fail wrapper test, W8 wrapper retried on aborts (double-spend on cancelled tasks), W9 exact-equality breaker bucketing was drift-fragile (now prefix-match on `claude-haiku-`/`claude-opus-` family). Round-3 verdict PASS — no drift, 4 hygiene findings tracked below. **+19 effective tests (claude-sdk shim model param, adapter SDK routing with fetch spy, breaker per-model, wrapper Opus→Sonnet success+both-fail+abort variants, heavy-runner actualModel regression). 1433 passing on changed scope.** Pattern reinforced: round-1 fixes can introduce new drift; the audit cadence pays for itself by catching contract-level inconsistency (W7/W8/W9 are direct consequences of how round-1 implemented C2 and W4). Memory: `feedback_anthropic_sdk_cutover_2026_05_10.md`. **Operator-applied .env diff** still pending: comment out INFERENCE_PRIMARY*\_/FALLBACK\_\_/TERTIARY\_\* lines + flip `HINDSIGHT_ENABLED=false`. Until then, code paths route correctly but stale env vars stay loaded. |
| 2026-05-13 | **R3 hygiene bundle CLOSED** — one-shot of R3-1/R3-2/R3-3 follow-ups from the 2026-05-10 cutover audit. R3-1 (`executor.ts:163`): selfAssess() now surfaces `response.model` into the returned `TokenUsage.actualModel`. qa-auditor caught W2: executeGoal was still dropping `assessUsage.actualModel` on the floor — extended fix to read it back into the local `actualModel` rollup (`executor.ts:425`). R3-2 (`executor.ts:683`): clarifying comment that LWW is array-index order under `Promise.allSettled`, not wall-clock; also amended the pre-existing aggregator comment (`executor.ts:624`) to point at the R3-2 note for consistency. R3-3 (`claude-sdk.ts:184`): documented the abort guard as defensive belt-and-suspenders; first-pass comment claimed `isError: true` shape — qa-auditor caught W1 that `isError` is the MCP CallToolResult field, not on `ClaudeSdkResult`. Corrected to describe the degraded `ClaudeSdkResult` with abort-marker text. R3-4 accepted as-is — swarm-runner intentionally has no cross-subtask aggregation, doc drift only. **+3 tests** (R3-1 surfaces, R3-1 omits when undefined, R3-1 W2 rollup into goal-level): 4679/4679 on `npx vitest run src/`. Typecheck clean. Service active post-deploy, 246 tools loaded. |
| 2026-05-13 | **Heavy-runner pipeline operator-pending items RESOLVED**. (1) **`.env` cutover diff**: verified by reading `/proc/<mc-pid>/environ` that load-bearing changes are already live — `INFERENCE_PRIMARY_{URL,KEY,MODEL}` absent (commented), `INFERENCE_PRIMARY_PROVIDER=claude-sdk`, `HINDSIGHT_ENABLED=false`. Remaining FALLBACK/TERTIARY vars present-but-empty in process env — inert under claude-sdk routing, cosmetic only. Operator can run `./scripts/cutover-env-cleanup.sh` for hygiene; no functional risk if skipped. The 2026-05-10 memory's "stale env vars stay loaded" overstated the risk — they're loaded as empty strings, not stale Fireworks URLs. (2) **`mission-control:latest` image REBUILT** (2.15GB, sha256:7b8c77275ec5, 1m45s build). Session-100 build had vanished from the host. Tier-1 smoke PASS (env+creds+SDK loadable). Tier-2 end-to-end PASS — Prometheus PER loop ran inside container for "17×13" prompt, success score=1.00, `actualModel: "claude-opus-4-7"` correctly surfaced in parsed output (confirms R3 bundle's plumbing works through containerized branch too). (3) **`HEAVY_RUNNER_CONTAINERIZED` decision: STAY IN-PROCESS** (operator directive 2026-05-13). Performance analysis showed containerized adds 1-2s per-task cold-start, loses cross-task SDK prompt-cache warm-up, and (functional blocker) `heavy-worker.ts` only registers 4 builtin tools vs 246 in-process — every heavy task that needs MCP / Google adapters / jarvis-fs would fail mid-execution. Snapshot resume also missing in container branch. Rebuilt image kept as ready hot-swap option if future trigger fires (heavy task crashes API, traffic >25%, or untrusted-code execution). Pin: `feedback_heavy_runner_containerized_not_perf`. | |
| 2026-05-15 | **Queue #13 CLOSED — Scope architecture rethink VERDICT shipped.** 30-day inventory tallied admission 7/genuine 1/discipline 1. Option B (always-on expansion) wins. `project_get` + `project_update` → MISC*TOOLS. 8 regression cases + 2 rewritten. 4805/4805 pass. Commit `c2dd51e`. Deferred B promotions: `gdrive*\_`subset,`browser\_\_\_` extras. |
| 2026-05-15 | **Queue #17 CLOSED — Pattern A bulk-throughput throttle fix shipped.** `jarvis_files_batch_write` + `jarvis_files_batch_delete`. BATCH_CAP=50, partial-error policy, dedup up front. Router auto-injects `confirmed:true` on operator-accepted retry (audit W2 — real pre-existing bug also affecting single-item `jarvis_file_delete` on precious paths). `formatConfirmationResult` cases added. qa-auditor folded W2/W3/R1/R3/R4/R5/R6/SV1; deferred W1/W4/W5/R2 as pre-existing. 4830/4830 pass, 248 tools live (was 246). Memory: `feedback_batch_tools_for_max_turns_2026_05_15`. |
| 2026-05-15 | **Queue #15 CLOSED — Hindsight DEMOTE (small) verdict shipped.** Ran the diagnostic the strategic-options doc prescribed. Data window planned for 2026-05-21 was overtaken — `HINDSIGHT_ENABLED=false` at 2026-05-10 (SDK cutover) froze new `recall_audit` rows, but the 3-day post-#7 window (n=315) was already conclusive: mc-jarvis SQLite-hybrid 38.9%/301ms vs mc-operational Hindsight 4.0%/2496ms. Rehab threshold blown. The L4 layer the strategic-options doc anticipated is already built and serving traffic; no 2-week wiring needed. Shipped: `scripts/recall-demote.sh` (idempotent .env flipper) → `HINDSIGHT_RECALL_ENABLED=true` → `false` + cleared `HINDSIGHT_RECALL_DISABLED_BANKS`. Container, retain/reflect, observation, and cost-ledger pull paths all unchanged. `withRerank:true` (queue #10) preserved as escape hatch. Verdict block prepended to `hindsight-strategic-options.md`; memory `feedback_hindsight_demote_verdict_2026_05_15`. Re-eval triggers: mc-jarvis SQLite utility <25% for 7d OR new memory-product candidate. | |
| 2026-05-25 | **Task #233 P3 hygiene closure-pass bundle CLOSED — 5 items shipped + 2 grep-sweep folds + 1 audit-fold.** Items: S5-P4-B1-R1 (`MC_MAX_SKILL_CALL_DEPTH` env-overridable, capped at 20 via W1 fold), S5-P3-B1-W4 (pino migration — folded grep-sweep to dispatcher/test-runner/test-sweep; 5 files, 7 console.\*), S5-P3-B1-W5 (dead `BackfillLog.warn` removed), S5-P4-B1-R2 (5 recordSkillRun counter tests, +1 empty-name R1 fold), S4-B3-R2 (mc-ctl SQL-identifier guard on cmd_task + cmd_schedule_delete, mirrors cmd_cancel; smoke-tested injection rejection). qa-auditor verdict PASS WITH WARNINGS; folded S1 (grep-sweep) + W1 (upper cap) + R1 (empty-name test) + R2 (signature-inversion comment) same-bundle. Typecheck clean, 189/189 scoped tests pass (was 184; +5 counter tests). 18 unchecked P3 items remain in #233 long-tail, gated on v8.0 reset / Phase 4-5 producer surfaces / operator-side triggers. Bundle commit pushed; mc-deploy pending (user-only). | |
| 2026-05-26 | **Health review + silent-ritual fix + V8 consolidation doc.** 24h Jarvis health sweep: service healthy (PID 863665, 9h uptime since b2fc758 deploy), 27 tasks @ 93% success, /health=200, KB 5698 entries @ 99.96% confidence. Found memory-consolidation ritual (cron `30 2 * * 2,4,6` MX) had been silently failing for unknown duration with `SqliteError: FOREIGN KEY constraint failed`. Initial hypothesis (2 orphan rows in `learning_plan_units` referencing missing `learning_plans.plan_id='cee6bc74-...'`) was a real-but-unrelated latent issue — cleaned up but didn't fix the consolidator. **Actual root cause**: `conversation_embeddings.conversation_id REFERENCES conversations(id)` without `ON DELETE CASCADE` → 419 duplicate-group rows had linked embeddings, first DELETE in `consolidateDuplicates()` aborted the whole transaction. Fixed via WAL-mode online migration (BEGIN IMMEDIATE, table-recreate with CASCADE, 8673 rows preserved, daemon stayed up). Manually re-ran `runConsolidation()`: **331 duplicates + 198 stale tier-4 entries removed, 8690→8161 entries, 9MB freed via VACUUM, 4.15s**. Updated `src/db/schema.sql` line 184-188 (added CASCADE + WHY comment) + new `scripts/migrate-conversation-embeddings-cascade.sql` for reproducibility. Backup at `data/mc.db.bak-orphan-cleanup-20260526T160200Z` (369MB). Memory: [[fk-missing-cascade-silent-ritual-failure]] — 5 reusable rules (default-CASCADE for child FKs, structured-metric on ritual failure, FK-error→grep-schema-first, WAL-mode online migration, re-run-manually-to-verify). Also consolidated **`v8-pending-2026-05-26.md`** at `/root/claude/jarvis-kb/projects/agent-controller/` — 9-section doc (substrate ✅ / V8.1 remaining / V8.2 specced / V8.3 specced / operator homework / v8.0 schema-reset cluster / fresh tech debt / horizons / 8 discussion seeds for Jarvis). Forced `reindexJarvisKb()`: 1 upsert verified live in `jarvis_files`. **NEW FOLLOW-UP**: ritual-metrics — emit structured `mc_ritual_failed_total{ritual="..."}` counter so silent ritual failures surface in monitoring (the observability gap that hid the FK bug for weeks). | |
| 2026-05-26 | **`skill-evolution` g-4 self-assessment friction — 3-fix bundle SHIPPED + audit fold for SDK args drop.** Today's evolution-log entry surfaced two failed `skill-evolution` heavy runs overnight (2026-05-25): both 4/4-goals-completed but marked `status=failed` because LLM scored 0.75, heuristic stayed at 1.0, gap 0.25 < 0.30 override threshold → `success=false` → reactions-manager `adjusted_retry` → 2nd identical run → ~1.77M tokens burned. Investigation found the log itself also confabulated: "API health unreachable, HTTP 000" was contradicted by shell-guard logs (curl /health returned OK at 05:59:14); "memory_search and memory_reflect not available" was inverted (tools were in registry, in tools list, not deferred — agent chose shell_exec+curl). Root cause 3 layers deep: (a) planner-generated g-4 criteria referenced tool-call evidence (`memory_store called with bank='operational' and tags ⊃ {'evolution','ritual'}`); (b) `selfAssess(goal, resultText)` only received final text truncated to 2000 chars — never tool calls; (c) executor returned `ok: true` even when both rounds returned `met=false`, log message "completed after N rounds" fired unconditionally, criteriaMet info dropped. Shipped: (1) `selfAssess` accepts tool-call audit (name + args), renders `## Tool calls made` section with redacted args (api_key/bearer/token regex-strip + 400-char cap); (2) Output truncation: head 3000 + `[…N elided…]` + tail 2000 when > 5000 chars (recommendations-section evidence now survives); (3) `GoalResult.criteriaMet` tri-state (true/false/undefined); reflector applies symmetric 0.5/total discount to BOTH heuristic AND LLM score on `criteriaMet=false` goals (gap stays small → override stays dormant → truth reaches success gate); recomputes `success` after the discount. Executor log split: "completed after N round(s)" only when criteriaMet=true; "best-effort after N round(s) — criteria still unmet" otherwise. **C1 audit fold**: `ClaudeSdkResult` got new optional `toolCallsWithArgs?: Array<{name, input}>` field carrying SDK `block.input` payload; synthesized assistant turn now uses real args instead of `arguments: "{}"` (legacy `toolCalls: string[]` consumers unchanged — additive). 12 new tests (executor: criteriaMet propagation, tool-call section present/absent, head+tail elision, full-text passthrough, secret redaction; reflector: 1/4-best-effort discount → score 0.75→0.625 + success=false). qa-auditor verdict FAIL → C1 + W1-W6 folded same-bundle (incl. W3 retry-round boundary markers, W5 catch-path JSDoc fix, W6 over-acceptance prose tightening); W4 (deeper redaction policy in tool registry) + W7 (single `recomputeSuccess()` at end of `reflect()` for other penalty blocks) queued. 5832/5832 + 1 todo pass; typecheck clean. Deployed `b2fc758` → PID 3672072 → 863665, /health=200. Tonight's 05:00 skill-evolution is the live test. Memory: [[judge-evidence-gap]] — 7 reusable rules for LLM-as-judge evidence-shape alignment, symmetric discount, log-truth-after-loop, and provider-shape boundary auditing. | |
| 2026-05-27 | **Skill-evolution 23:00 MX live test — burn loop FIRED AGAIN, b2fc758 partial-only, 3-strike trigger.** Tonight's 23:00 MX skill-evolution heavy ritual: two failed tasks back-to-back (`e4a60c0f` 05:00-05:04 UTC then `9ea36c4f` 05:04-05:08 UTC, adjusted_retry between). b2fc758 propagation worked AS DESIGNED — both runs honestly reported goal g-4 "criteria not met", reflector applied -0.13 best-effort discount, final score 0.72 < 0.80 threshold → success=false. BUT `adjusted_retry` reactions rule still fired on `success=false` regardless of WHY → 2nd run was deterministic same-prompt same-model rerun, same 0.72, ~1.7M tokens burned total. **Root cause: reactions-manager `adjusted_retry` keys on success=false, doesn't distinguish "real goal failure" from "honest criteria-unmet score-only failure".** Architectural fix is queued S3 item (now bumped to P1): change rule from `if (!success) retry` to `if (!success && goals_failed > 0) retry`. ~5 LOC + 1 test. **3-strike status armed**: 2026-05-25 (confabulation burn, [[judge-evidence-gap]]) + 2026-05-27 tonight (honest-failure retry). Must ship S3 before 2026-05-28 23:00 MX or set kill switch. Diagnosis only this session — fix deferred to next session. | |
| 2026-05-27 | **S2 critic forced-tool fix — 5+ days of `fail_returned_anyway` CLOSED.** Live diagnosis traced every morning briefing 2026-05-22 → 2026-05-26 to identical `concerns: [{type:"audit_failed", detail:"critic returned non-JSON response: I need to verify this report against its cited evidence. Let me query the database to check the key claims."}]`. Root cause: post-2026-05-10 Anthropic SDK cutover, Claude Agent SDK Sonnet path reflexively emits chain-of-thought preamble when system prompt asks model to "verify/audit/check" against data — even with `toolNames: []` and `temperature: 0`. The free-text JSON parser correctly returned null every time; `submitReport` correctly stamped `audit_failed`. Working-as-designed but never producing a real verdict. **Fix**: registered `submit_verdict` as one-shot inline SDK MCP tool via new `extraTools` param on `queryClaudeSdk()`. Schema = verdict shape (`{verdict:'pass'\|'fail', critique:string}`). System prompt mandates calling it exactly once. Handler captures via closure sink. Model can't emit anything else legally. qa-auditor flagged C1 (fallback parser re-opens the bug class) + C2 (abort-during-handler race discards captured verdict) + W2 (double-call guard) + W5 (Zod `.optional().default("")` vs handler `??` inconsistency) + R2 (registry collision guard) + R3 (maxTurns 3→2) — all folded same-bundle. **Live verification**: manually triggered morning briefing 04:59 UTC → `critic_verdict='pass'`, zero audit_failed entries, only the legitimate operator-side `stale_data: no general events in the bounded-diff window` (§1.1 #4 operator curation work, unrelated). 385/385 tests pass scoped; typecheck clean. Memory: [[forced-structured-output-via-mcp-tool]] — 7 rules. Files: `src/inference/claude-sdk.ts` (extraTools plumbing + collision guard), `src/audit/critic.ts` (rewrite using `queryClaudeSdk` direct + submit_verdict tool + audit folds), `src/audit/critic.test.ts` (rewrite with 14 tests covering happy path, audit-C1 strict contract, audit-C2 abort recovery, audit-W2 double-call), `docs/V8.1-GUIDE.md`. **V8.1 §1.3 "briefing fail_returned_anyway" instability now closed.** | |
| 2026-05-28 | **Queue #232 CLOSED — Honor Retry-After over exp-backoff.** Adapter retry loop (`src/inference/adapter.ts:1078`) now calls `resolveRetryDelay(httpErr?.rateLimit ?? {}, expBackoffMs)` from `http-error.ts` instead of using the inline `Math.min(1000 * Math.pow(2, attempt), 8000)` curve unconditionally. Server `Retry-After` hint wins when present, capped at `MC_RETRY_AFTER_MAX_MS` (default 30000ms, ABSOLUTE_MAX 600000ms to guard typos). Exp-backoff curve is the fallback for 429/5xx without a hint. Log line gained `(source=retry-after                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | exp-backoff)`for journalctl correlation. Telemetry gate widened to fire`rate_limit_hit`on 429 OR any retryable response with a Retry-After hint (5xx-with-hint is RFC 7231 §7.1.3 legal). Scope: OpenAI-compat fallback + direct Anthropic Messages path; the Claude Agent SDK path (current prod default since 2026-05-10) has its own internal retry policy and isn't touched. **R1 audit** PASS WITH WARNINGS: W1 (5xx telemetry asymmetry — fired log only on 429) folded → widened log gate; W2 (claude-sdk path scope not disclosed) folded → JSDoc scope note added; S1 (placeholder afterEachEnvCleanup() function with misleading comment) folded → replaced with real`afterEach()`+`import { afterEach }`. S2/S3 acknowledged as forward-looking ergonomic notes (no cap-disable knob today; can add sentinel if operator need emerges). +10 tests (29→30 incl. one R1-prompt-suggested case asserting `MC_RETRY_AFTER_MAX_MS=999999999` clamps to ABSOLUTE_MAX 600000). Pre-commit suite green. |
| 2026-05-28 | **Task #233 P3 hygiene closure-pass bundle #2 CLOSED — 7 items shipped + 6 audit folds.** Items: S5-P2-W2 (`MC_CRITIC_BODY_CAP` env-overridable, default 8000, max 32000, invalid → default), S5-P3-B1-W1 (`loadSkillEmbedding` NaN/Inf scan after deserialize; defense-in-depth — current production retrieval bypasses this helper and already coerces NaN scores to 0), S5-P4-B2-R2 (`skill_load` `version_pointer_dangling` regression test pointing `current_version_id` at a non-existent row), S3-B3-I2 (`push.ts:114` `triggered ISO-UTC` → `disparado <formatRelativeTime>` matching morning brief locale; exported `formatRelativeTime` from `delivery.ts`), S3-B3-I1 (admin suppress route caps: `reason` 1024 chars, `acknowledged_by` 256 chars + 3 tests inc. 1024-char boundary), S3-I3-tests (new `scheduler.test.ts` 3 tests pinning `mc_s3_evaluator_errors_total` counter — signal-kind integration via evaluateSignal rejection, tick-kind integration via exported-async `noticeError`, burst-kind label contract via direct call), S3-B3-I4 (`composeMorningBriefDriftSection` Sunday-aging branch — 5 tests covering Mon-Sat aging omitted, Sunday empty, aging-only green hygiene heading, alerts+aging ordering invariant). **R1 audit caught zero criticals + W1-W5 + SV1 + R1-R4** — folded SV1 (CRITICAL try/catch comment in noticeError) + W2 (production-callsite accuracy in noticeError header) + W3 (fork-pool isolation dependency comment in afterEach) + W4 (burst-test coverage-scope clarification) + W1 (loadSkillEmbedding defense-in-depth scope note). Skipped: R1-R4 cosmetic + W5 (MC_CRITIC_BODY_CAP undocumented in `.env.example` — permission-blocked, self-documented via JSDoc; queued as follow-up). API surface change: `scheduler.ts:noticeError` went `void → async (...)→Promise<void>` so tests can await the dynamic-import path; production callsite `void runCadenceTick(c).catch((err) => noticeError(c, err))` preserves fire-and-forget. **Final state**: typecheck clean, 97/97 scoped tests pass across 6 touched test files (1 new file, 5 augmented). Pre-commit suite green. 11 unchecked P3 items remain in #233 long-tail, almost all gated on v8.0 destructive migration (SQLite CHECK constraint extensions, FK additions) / V8.3 producer surface / hard-trigger conditions that haven't fired. | |
| 2026-05-27 | **V8.1 §13 activation gate re-authored — GATE PASSED, V8.1 ACTIVE.** Original §13 cache-read check measured `agent_type LIKE 'reflection:%'` (6 runs/24h: 1× morning briefing + ~5× n-turn reflection). Anthropic default prompt-cache TTL is 5min; all adjacent-run gaps on the reflection path exceed TTL (smallest gap 2h 1m). Cache-read was 0% by structural design, not regression — gate could never pass. **Fix**: switched query filter to `agent_type NOT LIKE 'reflection:%' AND prompt_tokens > 0` (the high-frequency `fast`/`heavy` operator runner path where TTL covers inter-run gaps); bumped min-runs 5→20 so weekend low-traffic windows report `insufficient_data` (exit 2) instead of false-flipping. Hardcoded literal replaced with parameterized `${REFLECTION_AGENT_TYPE_PREFIX}%` reusing the same constant the writer in `budget/service.ts:93` uses (W2 audit fold — drift-proofing). 2 new tests: reflection rows EXCLUDED from ratio + `prompt_tokens=0` rows EXCLUDED. Boundary test bumped 5→20 runs. Empirical 24h ratio on new target: **82.1% over 29 runs** (≥80%) + morning promote-rate 100% (≥60%). `mc-ctl briefing-gate` → ✅ PASS on first re-evaluation. **V8.1 Proactive Context Engine is officially active.** Memory: [[gate-target-must-match-cadence]] — 5 reusable rules: (R1) query live ledger before authoring threshold, (R2) reuse writer's constant in reader's SQL, (R3) check schema NOT NULL before pinning IS-NULL tests, (R4) prefer insufficient_data over noise-flipping verdict on low-traffic days, (R5) memory must land in same commit as the citation. `V8.1-GUIDE.md` §13 updated with spec-correction note + activation declaration. 88/88 briefing scope + 828/828 neighborhood tests pass; typecheck clean. Files changed: `src/briefing/activation-gate.ts`, `src/briefing/activation-gate.test.ts`, `scripts/briefing-gate.ts`, `docs/V8.1-GUIDE.md`, queue doc, MEMORY.md. | |
| 2026-05-27 | **S3 deterministic-retry-gate SHIPPED — burn-loop fix landed before 2026-05-28 23:00 MX deadline.** Shipped same-day after diagnosis. Three-piece change: (a) `src/db/reflector-gap.ts` gains `getLatestGoalSnapshot(taskId)` returning `{goalsTotal, goalsFailed} \| null` (logs on DB error per W1 audit fold, returns null preserving legacy behavior); (b) `ReactionContext` gains `goalsTotal` + `goalsFailed: number\|null`; (c) `adjustedRetryRule` skips when `goalsFailed === 0 && goalsTotal > 0` (W2 audit fold: requires goals_total>0 to distinguish empty-graph pre-execution crashes from real zero-failure runs). qa-auditor PASS WITH WARNINGS → C0/W1/W2/W3/S1/R1 all folded same-bundle (DB-error logging, narrow gate, manager integration test, type narrowing on raw row, doc explicitness). 4 new rules tests (gate fires only for {>0, 0}; preserved for {>0, >0} / {null, null} / {0, 0}) + 2 manager integration tests (mocked snapshot wiring). 222/222 reactions+db scoped tests pass; typecheck clean; `npm run build` clean. Deployed `pending-commit` → PID 2745969 (transition verified), `[mc] Reaction engine started`. **Live verification deadline**: 2026-05-27 23:00 MX skill-evolution fire (tonight ~23h out at deploy time) — should record one task only, no `adjusted_retry` reaction, escalate after MAX_RETRIES instead of burning a 2nd run. Memory: [[deterministic-retry-gate]] — 5 reusable rules: (R1) retry rules must distinguish "real failure" from "deterministic failure", (R2) ground truth lives in `reflector_gap_log`, (R3) `null` snapshot = preserve legacy retry (don't gate on unknown), (R4) require `goals_total > 0` for the empty-graph edge case, (R5) silent DB-error swallow recreates the bug class — log even in fire-and-forget code paths. Files: `src/db/reflector-gap.ts`, `src/reactions/types.ts`, `src/reactions/manager.ts`, `src/reactions/rules.ts`, `src/reactions/rules.test.ts`, `src/reactions/manager.test.ts`, MEMORY.md, queue doc. | |
| 2026-05-25 | **`Service shutdown` recurring blocker — three-fix bundle SHIPPED.** Today's morning briefing flagged 6 chat tasks across 8 days killed by `Service shutdown` (root cause: 10s hardcoded grace period at SIGTERM, then UPDATE failing everything in-flight). Three fixes in one bundle: (1) Grace period 10s → 30s, env-overridable via `MC_SHUTDOWN_GRACE_MS`, clamped [0, 300_000], strict-integer parsing per W1 audit fold. Helper extracted to `src/lib/shutdown-grace.ts` so tests don't drag in `main()`. Atomics.wait busy-loop replaced with `await setTimeout` (W3 fold). (2) `scripts/deploy.sh` now BLOCKS on `status='running'` tasks unless `--force`; `--drain N` waits up to N seconds; lists affected task IDs. W2 audit fold validates `--drain` value as non-negative integer. Smoke-tested 6 branches. (3) `briefing/judgment-prompt.ts` `signalsOf()` got a `recurring_blocker` specialization that appends `[first_seen=..., last_seen=..., spanning N days]` so the LLM judge stops calling 8-day clusters "consecutive days" — the actual failure mode from today's briefing. Renders "same day" / "spanning N days" labels, accepts both SQLite-naive and ISO-8601 timestamps, returns 0 on parse failure (under-report). qa-auditor PASS WITH WARNINGS; folded W1+W2+W3+R1+R3 same-bundle (parseInt strictness, --drain validation, async-friendly sleep, `IN (single)` normalization, parseTs-null test). 23/23 scoped tests pass (was 19; +4 prompt rendering + 3 grace env-edge cases + new fold tests). 564/564 broader scope green. Deployed `f2a4c40` → new PID (transition 2s, graceful shutdown logged "Shutdown complete"). See [[insights_fixes]] §approach-first, [[3strike_rule]] not yet armed (1st fix on this surface). | |
| 2026-05-31 | **V8.3 capability spec reconciled — R2.** Ran the [[stale-spec-reconciliation]] pass on `docs/planning/v8-capability-3-spec.md` (Apr-30 R1 `7f8f8c7`) the same way V8.2 got R2. **1002→500 lines.** **Reconciled:** 6/6 capability seed names were fictional as tools → rebuilt §6 on the REAL registry (`gmail_send`/`northstar_sync`/`skill_run`/`schedule_task`/`jarvis_file_delete`; task-edit is an internal `tasks` mutation, not an LLM tool; WhatsApp send is router-level) AND on the existing per-tool hint system (`riskTier`/`destructiveHint`/`idempotentHint`, 186/186 annotated) instead of a parallel taxonomy; ODD example columns were fictional (`tasks` has `priority`/`status`/`assigned_to`, NOT `urgency`/`value_at_stake_usd`/`edit_kind`) → predicates now evaluate a documented decision-context object; hard dependency on V8.2's `judgments`/`reflection_followups` (absent today) made explicit via a Phase 0 gate. **Corrected:** forced-tool for any LLM verdict (reuse S2 `submit_critic_verdict`); SDK single-cache-block caveat on the standing rule; the `judgment_id NULL` gate contradiction (§5 allowed it, §14 banned it) → keyed on autonomy_level. **Cut:** 6 tables→4 (`capability_tokens`→JSON col + collided with live `mcp_tokens`; `decision_checkpoints` fork/replay→`pre_state_json`); shadow-Git + time-travel deferred; eager ADR→lazy render; "ODD predicate = S5 skill" dropped. **New rot class (the missing heart):** R1 sequenced the whole PI-calibration apparatus BEFORE any autonomous traffic exists to calibrate — at L1 launch the controller is inert (n<20 floor never lifts). Reordered into **v1 = decision-ledger + reversibility wrapping the EXISTING router confirm path** (runs day one) → **v2 = controller** (only after a capability earns L≥3 traffic). NorthStar reversal correctly excluded from SQL-inverse-DML (remote LWW + kb-reindex resurrection, the 2026-05-12 incident); CRM customer messaging scoped out (separate crm-azteca gates). Both capability specs now build-ready; build order unchanged: **V8.2 Phase 0 → V8.2 → V8.3**. Memory [[stale-spec-reconciliation]] updated to 3rd confirmed instance + the build-order rot class. |
| 2026-05-31 | **V8.2 Phase 0 SHIPPED — substrate reconciliation (`/ship-it`).** First build of the V8.2 (Strategic Initiative Layer) spec, gated by the Phase 0 reconciliation it needs before any capability code. Three new source units + the schema: (1) `judgments` table (normalized child of `proposed_briefings`, `ON DELETE CASCADE`) + `reflection_followups` table, added to `src/db/index.ts` boot path after `trigger_runs` and applied live to `data/mc.db` (additive `CREATE TABLE IF NOT EXISTS` — no restart, zero risk to existing rows); (2) `src/lib/v8-2/reconciliation.ts` — the reconciled `EVIDENCE_KINDS` (+`general_event`/`recurring_blocker`/`cohort_member`), `TOOL_GUIDANCE` (6 real registry tools, source-scan regression-guarded), `POSTURES`/`CONCESSION_KINDS`, `validateToolGuidance()`, and `fetchEvidenceExcerpt()` resolving the 3 V8.1 substrate kinds end-to-end; (3) `src/briefing/reflection-followups.ts` — insert/getDue/markFired + an **empty-handler-safe** `sweepDueReflectionFollowups` (dispatch by `context_ref` prefix `judgment:`/`decision:`; a no-handler row is left unfired so a later consumer still receives it) wired into `runMorningSurface` inside its own try/catch. **Posture enum divergence flagged** (spec 'momentum' vs V8.1 'has_momentum') with a Phase-2 normalization mandate + a CHECK-rejection test. 30 Phase-0 tests (schema idempotency on-disk, FK CASCADE, CHECK rejection, sweep idempotency/empty-handler/throw-retry, evidence resolution, EMBED_DIMS=1536) + full suite 5931 green; typecheck clean. **qa-auditor PASS WITH WARNINGS** — both folded same-bundle: W1 (validateToolGuidance test was self-referential → replaced with a `src/tools` source-scan that catches a tool RENAME), W2 (sweep stamped `fired_at` with wall-clock instead of the injected `nowIso` → now a pure clock injection). **Additive + dormant**: no producer writes `reflection_followups`/`judgments` rows yet (V8.2 §13 / V8.3 §12 are the first producers), so the running service needs no restart — the new dist/ is picked up on the next user-initiated deploy. Build order unchanged: Phase 0 ✅ → Phase 1 (attributed_claims + types) → … . Spec §5/§16 marked shipped. |
| 2026-06-01 | **V8.2 Phase 1 SHIPPED — schema + types (`/ship-it`).** The two remaining V8.2 tables + the canonical type module. (1) DDL added to `src/db/index.ts` boot path after the Phase 0 block + applied live to `data/mc.db` (additive, no restart): `attributed_claims` — the **normalized** citation ledger that fixes R1's `marker_index` overload; a per-judgment `claim_id` groups the 1+ evidence rows of ONE sentence, so multi-source `[1][3]` is two rows sharing a claim_id and two sentences both citing `[1]` are distinguishable; FK→`judgments(id)` `ON DELETE CASCADE`, `evidence_kind` CHECK (9 values) kept in lockstep with `EVIDENCE_KINDS`, partial index `WHERE resolver_status != 'resolved'`. Plus `sycophancy_probes` — the nightly probe ledger (nullable `judgment_id`, free-TEXT `judgment_color` for R2's all-colors-incl-red sampling, `concession_kind` CHECK). (2) `src/lib/v8-2/types.ts` — re-exports the Phase 0 reconciliation enums (one import surface, **no enum duplication**) + new `RESOLVER_STATUSES`/`RAPID_D_ROLES`/`OPTION_LABELS` + Zod schemas for `EvidenceRef`/`AttributedClaim`/`AttributedClaimRow`/`ProposedOption`/`ConfidenceBasis`/`StrategicJudgment` (extends V8.1 `JudgmentSchema`; `.refine` pins proposed_options ∈ {0,3} — §8 "never fake A/B/C")/`Decomposition`/`DecompositionAngle`/`AngleBoundaries`. 35 Phase-1 tests (schema presence, FK transitive CASCADE, CHECK rejection, DEFAULT unresolved, on-disk idempotency, **structural CHECK-lockstep drift guards**, Zod round-trips, posture-divergence pin, ≤3-angles); typecheck clean. **qa-auditor PASS WITH WARNINGS** — W1 folded same-bundle: the original drift guards only asserted DDL⊇TS (catches "added a TS enum value, forgot the DDL") but not the more-likely hand-edited-SQL drift; added a set-equality parse of the CHECK list out of `sqlite_master.sql` vs the TS enum, closing both directions. **Additive + dormant** — no producer writes `attributed_claims`/`sycophancy_probes` rows yet (Phase 4 `cite.ts` / Phase 8 `sycophancy.ts` are the first), so no restart; next `deploy.sh` picks up dist/. Spec §16 Phase 1 row marked shipped. Build order: Phase 0 ✅ → Phase 1 ✅ → Phase 2 (`decompose.ts` → ≤3 angles + structured boundaries over the `tasks` retrieval) → … . |
| 2026-06-01 | **V8.2 Phase 2 SHIPPED — decomposition (`/ship-it`).** `src/lib/v8-2/decompose.ts`, the §7 "angles, not answers" pipeline, three units: (1) `decomposeQuestion` — ONE forced-tool LLM call (`submit_decomposition` one-shot SDK tool whose Zod schema IS the `angles[]`, capped `.max(3)`; closure-sink capture; copied from the S2 critic `submit_verdict` pattern, incl. abort-during-handler handling). Re-validates the assembled `{question, angles, generated_at(injected)}` through `DecompositionSchema` so the ≤3-angle cap is enforced at the function boundary, not only by the SDK's tool-schema parse (the test path bypasses the SDK parse, so this is what makes the cap testable). (2) deterministic retrieval — `retrieveTasksForBoundaries`/`retrieveForAngle`/`gatherEvidence`: bound-parameter SQL over the internal `tasks` table honoring the structured boundaries (status*in→`IN`, date_from/to→`created_at >=/<=`, exclude_completed→drop ALL terminal incl. `failed`, limit→`min(limit ?? 20, 50)`), newest-first, → `EvidenceRef[kind='task']`, ledger deduped by `kind:id`. The 6 `tool_guidance` values are recorded per angle but NOT yet used to switch retrieval backends — crm_query/intel_query/memory_search/etc. are external/LLM tools wired in later phases; Phase 2 honors boundaries over the one deterministic structured substrate, NorthStar deliberately not auto-queried (moving-target guard, queue #18). (3) append-only ADR — `saveDecomposition` writes `decisions/<id>/decomposition.json`, never overwrites (versioned `…v2.json`), judgmentId path-traversal-guarded. **Additive + dormant** — nothing calls decompose yet (Phase 3 RAPID-D is the first consumer); no schema change, no restart. 16 tests (10-q→≤3, >3 rejected at boundary, no-tool-call/error/already-aborted, every boundary filter incl. failed-is-terminal, dedup, ADR versioning, unsafe-id rejection); typecheck clean. **qa-auditor PASS WITH WARNINGS** — all folded same-bundle: W1 (the forced-tool catch dropped critic.ts's captured-on-throw return → restructured: SDK-call try/catch separate from a finalize block so a timeout racing a successful tool_use still returns its angles), W2 (`exclude_completed` excluded completed/cancelled but NOT `failed` → now drops all 4 terminal states via `TERMINAL_TASK_STATUSES` + a seeded failed-task assertion), W3 (silent limit-cap → `log.debug` on a full page), R1 (Zod validation failure mislabeled "call failed" → now "failed validation"), R2 (judgmentId is typed `string` but flows into a path → `/^[A-Za-z0-9*-]+$/` guard throws before any mkdir). Spec §16 Phase 2 marked shipped. Build order: Phase 0 ✅ → Phase 1 ✅ → Phase 2 ✅ → Phase 3 (RAPID-D 4-role multi-option + 1536-d diversity gate). |
| 2026-06-02 | **V8.2 Phase 3 SHIPPED — multi-option / RAPID-D (`/ship-it`).** Two new units, no schema change. (1) `src/lib/v8-2/should-multi-option.ts` — the deterministic §8 skip predicate `shouldRunMultiOption(Pick<Judgment, kind\|posture\|confidence\|proposed_action>)`: ordered rules **red_confidence** (thin evidence → A/B/C is theatre; wins over an actionable kind — the at*risk/recurring_blocker carve-out is a Phase-4 \_surfacing* concern, not a run-RAPID-D one) → **observational** (`posture='noted'` or kind∈{momentum, self_defining_progress}) → **single_mechanical_action** (`proposed_action.surface='log_only'`) → **run**. (2) `src/lib/v8-2/multi-option.ts` — `runMultiOption`: Analyst∥Seeker∥Devil's-Advocate free-text perspectives (`Promise.all`, need ≥2 or degrade `no_perspectives`) → forced-tool Synthesizer (`submit_options` one-shot SDK tool, closure-sink, abort-during-handler race handled — copied from `decompose.ts`/`critic.ts`) producing A/B/C; `generated_by_role='synthesizer'` stamped deterministically (poka-yoke); `validateOptions` enforces exactly-3 + distinct A/B/C labels + 1/2/3 rank permutation AFTER capture so we own retry-vs-degrade. **Advisory diversity gate**: `computeMaxPairwiseSimilarity` over the live 1536-d Gemini embedder (`embed`/`cosineSimilarity`, injectable for tests) — `>θ` → retry Synthesizer nudged orthogonal (budget 2) → **graceful degrade to `options=[]`** (`degradedReason='no_diversity'`; NEVER fakes a 1/2-option set — the `StrategicJudgment` refine pins {0,3}). Embeddings-unavailable (`embedFn→null` / <2 summaries) → gate **degrades to logging-only and KEEPS the options** (advisory). **θ shipped conservative at 0.93**, env `MC_RAPID_D_DIVERSITY_THETA` (validated `(0,1]`); empirical calibration deferred to the 7-day shadow run via the §17 `>30%` retry-rate watchpoint (R1's 0.82/0.18 discarded). **§15/§18-Q8 DECISION:** the four roles are **versioned in-code prompt consts** (`RAPID_D_PROMPT_VERSION='v1'`), NOT S5 skills — ceremony heavier than value for four internal prompts; `prompt_modules/` reserved for Phase 5's strategic-voice cache-prefix block. **Additive + dormant** — first producer is a later judgment-assembly phase; no DDL, no restart, next `deploy.sh` picks up dist/. 36 tests (happy/retry→succeed/retry-exhausted→degrade/embed-unavailable→keep/no-tool/malformed-synth/throw/already-aborted/≥2-perspective boundary + `validateOptions` incl. the ProposedOptionSchema field-parse path the mock bypasses + pure-fn similarity/θ-env/digest); typecheck clean. **qa-auditor PASS WITH WARNINGS** — all 8 challenged design claims held; W2 folded same-bundle (added the empty-summary `validateOptions` test) + a dead `res.text?.` simplified; I1 (spec §8 "rank-1-only / `[]`" self-contradiction) resolved as `options=[]` + documented in §8 as a Phase-4 input. Spec §16 Phase 3 marked shipped. **NEXT: V8.2 Phase 4** (`cite.ts` — `[N]` extract→validate→normalized `attributed_claims`; drop-vs-surface §9). |
| 2026-06-02 | **V8.2 Phase 4 SHIPPED — citation + `[N]` resolver (`/ship-it`).** Two new units, no schema change. (1) `src/lib/v8-2/cite.ts` — `resolveCitations(prose, ledger)` deterministic, no-LLM: sentence-split (offset-tracked; decimals don't split) + `[K]` extract + validate K∈{1..N} (1-indexed: `[K]`→`ledger[K-1]`). A sentence with ≥1 VALID marker → one resolved claim (shared `claim_id`, multi-source `[1][3]`→2 refs, `[1][1]` deduped); a markerless sentence asserting a non-trivial fact (`hasNumber`/`hasDate`/`hasProperName`/`hasStateClaim`, §9's four categories) → **transient** candidate-unresolved for the §11 critic; pure-editorial ignored. `toAttributedClaimRows`/`persistAttributedClaims` flatten to ONE row per evidence ref, `resolver_status='resolved'`, single txn, `getDatabase()` singleton + injectable. **SCHEMA-DRIVEN INVARIANT:** `attributed_claims` evidence cols are all NOT NULL → an unresolved claim (no evidence) CANNOT be a row, so only resolved claims persist; `resolveCitations` returns `{resolved, unresolved, stats}` and the unresolved list is handed to the critic in-memory, never written. (2) `src/lib/v8-2/should-surface.ts` — `shouldSurfaceUnfixable(Pick<Judgment, posture\|kind>)`: the §9 drop-vs-surface carve-out the §8 skip predicate DEFERRED — `posture='at_risk'` OR `kind='recurring_blocker'` → surface optionless heads-up, else drop; `confidence` is a caller precondition (the carve-out surfaces even at red), not re-checked. **Additive + dormant** — table exists (Phase 1); no DDL, no restart; first producer is a later judgment-assembly phase (generates prose+ledger, calls resolve/persist). 29 tests (10 prose samples incl. single/multi-source/dedup/out-of-range `[9]`/`[0]`/valid+invalid mix/markerless-by-category/editorial-ignored + claim_id increment + prose_offset + hit-rate (editorial excluded from denominator) + splitSentences decimal/terminator + sub-predicates + `toAttributedClaimRows` + persist against `:memory:` with proposed_briefings+judgments FK seed + FK-reject rollback); typecheck clean. **qa-auditor PASS WITH WARNINGS** — all 9 challenged design claims held (1-indexing, multi-source dedup w/ order, NOT-NULL→only-resolved-persists, txn/parameterization, prose_offset round-trip, §9 carve-out). Folded same-bundle: W1 (markerless-fact heuristic FALSE-NEGATIVE class — proper-name-subject + unlisted-verb slips through; broadened `hasStateClaim` business-verb/copula-object sets + documented the residual gap + the producer `[K]`-marker contract as the real safety net), W2 (added the editorial-exclusion hit-rate test), R1 (confidence-precondition comment), R2 (startClaimId-continuation + FK-reject tests). R3 (abbreviation over-split) noted harmless. Spec §16 Phase 4 + §9 build-notes marked shipped. **NEXT: V8.2 Phase 5** (`prompt_modules/strategic_voice_principle_v1.md` — insert at top of every V8.2 call; verify intra-brief cache-read ≥70% under the SDK). |
| 2026-06-03 | **V8.2 Phase 5 SHIPPED — strategic-voice prompt + cache (`/ship-it`).** `prompt_modules/strategic_voice_principle_v1.md` (canonical §10 identity block, VERBATIM, SHA-256-pinned in test) + `src/lib/v8-2/strategic-voice.ts` — memoized fail-loud loader (`resolve("prompt_modules")`, env `MC_PROMPT_MODULES_DIR`; read-once for byte-stability), `strategicVoiceSystemPrompt()`, `composeV82UserPrompt(role, body)`, and `JUDGMENT_CITATION_CONTRACT_V1` (the §9 `[K]`-marker producer contract, exported-DORMANT, kept OUT of the byte-stable block). **The cache mechanic:** the SDK puts ONE cache breakpoint at the end of `systemPrompt` ([[sdk-systemprompt-single-cache-block]]), so all 5 V8.2 call sites (decompose + Analyst/Seeker/Devil's-Advocate + Synthesizer) now pass the BYTE-IDENTICAL strategic-voice block as `systemPrompt` and the per-call role/task text moved to the user-prompt HEAD ([[cache-prefix-variability]]) — one shared prefix → (N−1)/N cache ceiling by construction. **HONEST cache caveat (the §10 "verify before trusting" warning, confirmed):** the block is 1735 chars ≈ 430 tokens, BELOW Anthropic's ~1024-tok min cacheable prefix, and a plain-string `systemPrompt` REPLACES the SDK scaffold — so this prefix alone likely WON'T cache (cacheRead≈0) until the larger judgment-pass prefix exists; documented, not papered over ([[gate-target-must-match-cadence]]/[[derive-explanations-from-data]]). `scripts/verify-v82-cache.ts --run [N]` is the operator-run live harness (token-burning → `--run`-gated, NOT CI) that MEASURES the real ratio + pre-warns when sub-threshold. **Additive + dormant** — no producer wires the libs in yet; no DDL, no restart; Dockerfile now `COPY`s the runtime asset. 12 new tests (loader/memoization/fail-loud + 7-principle identity guard + SHA pin + `[K]`-contract guard + the cache-prefix invariant across all 5 sites + role-text-reaches-user-prompt); 135/135 v8-2 scoped pass, typecheck clean. **qa-auditor PASS WITH WARNINGS** — both folded same-bundle: W1 (Dockerfile omitted `prompt_modules/` → containerized-runner fail-loud trap; added `COPY`), W2 (identity guard was substring-only → added SHA-256 byte-pin). Spec §16 Phase 5 + §10 build-notes marked shipped. **NEXT: V8.2 Phase 6** (`critic.ts` — extend the S2 critic with forced-tool `submit_critic_verdict`, tri-state, 2-loop; `contradicted_claim_ids` → resolver writes `contradicted`). |
| 2026-06-03 | **V8.2 Phase 6 SHIPPED — §11 CRITIC (`/ship-it`).** `src/lib/v8-2/critic.ts`: a SEPARATE verifier LLM call (its OWN system prompt — deliberately NOT the Phase-5 strategic-voice block; the critic is the audit gate, not Jarvis-the-counsel) extending the S2 forced-tool pattern. **Forced tri-state verdict** via `submit_critic_verdict` (Zod schema IS the output: `verdict ∈ {approved, needs_revision, unfixable}` + `critique` + `contradicted_claim_ids?`), closure sink + double-call guard + abort-during-handler tolerance + **NO free-text fallback** (re-introduces `fail_returned_anyway`). A missing verdict → conservative `needs_revision`+`error` (never a silent approve); `runCriticLoop` re-authors once (injected `reAuthor`) then **escalates a 2nd `needs_revision` → `unfixable`** (terminates, no infinite loop). **4 read-only verification tools** (budget 5): `sql_check` — the ONLY LLM-authored SQL — runs on a **READONLY** better-sqlite3 conn (writes/DDL/ATTACH physically impossible) + SELECT-only (WITH rejected) + comma-join-aware table-whitelist + single-statement (`.prepare` rejects `;`-chains) + `.iterate()` row cap; `cost_check`/`recall_check` **parameterized** (no LLM SQL; recall is lexical `jarvis_files_fts` — semantic pgvector `kb_entries` DEFERRED, [[stale-spec-reconciliation]]); `file_sha` traversal+size-guarded. **§12 wiring** in `cite.ts`: `markClaimsContradicted` (flips ALL rows of a claim to `contradicted`, judgment-scoped, parameterized, idempotent) + `countContradictions` (DISTINCT claim_id, the §12 read side — dormant until Phase 8 but proven E2E now). **Additive + dormant** — no producer emits prose+claims+ledger yet; no DDL, no restart. 52 critic+cite tests (3 verdicts / no-tool→error / abort-capture / already-aborted / 2-loop ×4 / contradiction write-back + readback / every tool guard incl. readonly-conn prod path); 150/150 v8-2 scoped, typecheck clean, full suite green via pre-commit. **qa-auditor PASS WITH WARNINGS** — write-proof claim EMPIRICALLY verified (readonly conn rejects writes/ATTACH; `.prepare` rejects multi-statement); 4 folded same-bundle: W1 comma-join whitelist evasion (`FROM a, b`), W2 prod readonly-conn path untested (added temp-file test), W3 `.all()` pre-cap materialization (→`.iterate()`), W4 unbounded `readFileSync` (size cap). Spec §16 Phase 6 + §11 build-notes marked shipped. **NEXT: V8.2 Phase 7** (Concession handler — extend `resolveBriefingOnOperatorReply`: classify reply forced-tool → evidence gate → hold/update; `reflection_followups` write; no-evidence never concedes). |
| 2026-08-27 | **Reading-budget ruling 5/1400 SHIPPED** (#35 `eeee703`, deployed pid 1314028): `PUSH_CAP` 4→5 + `WORD_CAP` 700→1400 after the 12:00 reflection deferred `push-cap+word-cap` (3 optionals + reserved close = 4). Jarvis's #34 (words only, rewritten test that could not fail, stacked on #32/#33) closed; #35 rebuilt from `origin/main` with mutation-verified tests, sibling `dynamic-budget.test.ts` caught by CI and fixed. New section "Sandbox shell guard bypassed by a wrapper script" queues 2 follow-ups + notes the `jarvis_dev` branch-stacking bug. |
| 2026-08-31 | **Identity fix.** Jarvis mirrored «Gracias Piotr» back at Fede as his name (08-28, 08-31): the prod prompt never named Piotr (WhatsApp block compiled out; `preferences.jarvis_name` only a P4 bullet). P1 identity sentence + consolidator Names rule + `--reject 307` (an inverted preference the extractor pulled from Jarvis's own Track 1 report). Sweep: same Names rule in `background-extractor.ts` (live sibling extractor). Follow-up 9 added to the memory-plan section. |
| 2026-09-01 | **Hermes upstream review v0.15–v0.21** (7 minors, ~120 candidates verified against code + 30 d data). Shipped connect-time SSRF guard (`safeFetch` / `safeDispatcher`, redirect hops re-validated; swept into 7 fetch sites) and standing-orders guard (`directives/` refused by all six KB file tools + `file_write` + `code_edit`; kb-reindex never imports disk-only directives). R1 audit FAIL → C1/C2/C3 folded (FetchLike headers, `..`/case bypass, sibling doors). Deferreds with triggers in the new section. Next review due 2026-10-01. this commit |
| 2026-09-01 | **NanoClaw upstream review v1.2.52→v2.3.0** (major + 3 minors, 1,956 commits, 105 candidates verified against code, the LIVE image and 30/90 d data). Finding: the sandbox image was built 07-14 and the worker ran its baked `dist/` — 29 commits touching sandbox-executed paths (111 total) never reached the sandbox. Shipped: host `dist/`+`prompt_modules/` RO-mounted over `/app` (upstream "source is never baked in"), `mc.lock-sha256` image label + runners refuse on lockfile drift + `deploy.sh` auto-rebuild, `:ro`-only mount gate on both backends (boundary-safe prefixes), `--init` (docker stop 10.3 s → 0.2 s), unknown `SANDBOX_BACKEND` refuses at boot, **egress allow-list ON** (list corrected with `*.claude.com` from SDK evidence; wildcard + e2e probes PASS; flipped 06:44 UTC on the operator's "run it", smoke PASS with the sidecar attached), image npm 10.9.9 (tar critical), `.npmrc` `min-release-age=7` (old key was a silent no-op). Deferreds with triggers in the new section. Next review due 2026-10-01. this commit |
| 2026-09-01 | **Claude Agent SDK 0.3.207 → 0.3.245** (`latest` 0.3.252 refused by `min-release-age=7`; 38-version delta read against the adapter — no terminal `else` in the message loop, every consumed type intact). Gate PASS (376 cases, 66.05 vs 65.75 incumbent), tool-search validation PASS, both smokes PASS; `deploy.sh` rebuilt the image on lockfile drift (first live proof of the coupling; 3.04 GB, musl sibling dropped by npm 12's `libc` fields). Audits: R1 on the bump PASS-W (`TerminalReason` type pin folded; budget-window/classifier/regex warnings → items 13–15), R3 verdict-verification on the nanoclaw bundle PASS on code (`assertRuntimeCodeMounts()` boot invariant folded; scoreboard arithmetic, deferred-numbering map and the 27→29 commit count fixed). Incident: the install ran under the live service and hit the 07:00 tuning run (85× `spawn ETXTBSY` → silent Haiku fallback, contaminated baseline, run killed by the restart; row 83 closed `aborted` by hand) → item 12 + `feedback_live_service_dep_swap` recurrence + infrastructure rule. npm 12 `allowScripts` recorded in package.json (item 20); `npm audit` baseline unchanged (item 21). this commit |
| 2026-09-01 | **npm 12 `allowScripts` sweep across the other Node trees (queue #20 → DONE).** Truth-checked per tree (`npm install-scripts ls` + lockfile `hasInstallScript` scan; the queued lists wrongly named `sharp`/`bcrypt`): every tree's set is `better-sqlite3` + `esbuild`. Approved name-only in Pulso-Aura-Upfront root + `engine/`, very-light-cms, vlved; `engine/` denies `@whiskeysockets/baileys` + `protobufjs`; crm agent-runner gets the field by hand as forward defense (image npm 10.9.8 ignores it). No lockfile change, nothing installed, no service touched. Commits Pulso `6ae137e` · vlcms `e53b8c6` · vlved `9c858fb`. this commit |
| 2026-09-01 | **`gh_repo_create` / `git_push` could not wire a git remote — circular dead-end fixed (task 8953, trustr).** Jarvis created `EurekaMD-net/trustr` and scaffolded `/root/claude/trustr`, then hit `shell-guard BLOCKED: git remote add` (reason: "use git tools instead") and `git_push` → "No remote 'origin' configured. Use shell_exec to add one." — the two texts pointed at each other and NO tool could set a remote, so a new project was un-pushable by construction. Fix: `gh_repo_create` takes `cwd` and passes `--source <dir> --remote origin` (gh wires origin; push stays in git_push); `git_push` takes `remote` (plain `https://github.com/<owner>/<repo>[.git]` only — no userinfo/host/port/metachars; existence-checked with `gh repo view` BEFORE `git remote add`; ignored when origin exists); the shell-guard reason names both tools. 15 new tests (13 RED on the old code + 2 forward regression guards that pass on both — the old tools ignore the new params; measured by swapping the HEAD files back in); qa-auditor R1 FIX-FIRST folded in full (origin pre-flight in gh_repo_create so gh never strands an orphan repo, dotted-name extractor anchored so only [A-Za-z0-9_.-] reaches the gh shell string, path guard hoisted ahead of the no-origin catch, Spanish GIT Y GITHUB prompt block names cwd/remote and adds git remote to the shell_exec ban), R2 SHIP with 2 folds (the remote rule now also matches `git -C <dir> remote …` — `git -C x remote set-url` previously slipped past BOTH git rules; the prompt block is pinned by a `codingSection()` test); eval:gate PASS — composite 64.98 vs incumbent 65.75 (Δ −0.77, threshold 63.75; tool selection 34.32 / scope 92.74 / classification 100; 376 cases, $5.61, 17.3 min). Follow-up misroute of the retry (task 8954 → nanoclaw, mission-control-only mount, worker self-scored a refusal as success so the nanoclaw→fast fallback never fired) queued as item 22. Deployed pid 1915869 (17:38 UTC, image lock unchanged), live-verified by pushing trustr through the deployed `dist/tools/builtin/git.js` `gitPushTool.execute({cwd, remote})` → origin wired, master→main, `da070c7` on `EurekaMD-net/trustr`. Residual: the `gh_repo_create(cwd)` create+wire call is unit-tested (exact gh args) and gh-2.67 flag-probed by qa (nonexistent org, nothing created) but NOT run e2e against GitHub — the token lacks `delete_repo`, so a throwaway repo could not be cleaned up; the next real new project is the e2e. `337d91f` + this docs commit |
| 2026-09-01 | **Jarvis "stuck with shell_exec" — the core coding tools were `deferred: true` under tool search; un-deferred (7 tools).** /diagnose: tasks 8958/8961/8962/8963 (trustr thread, 20:36–21:40 UTC) each ran 2 turns, ONE `ToolSearch`, missed, and replied «Necesito `shell_exec` para esto.» + `STATUS: BLOCKED` (31 chars — under the 100-char promote floor at `fast-runner.ts:1498`) → `dispatcher.ts:850` → `blocked`, while `metadata.tools` listed shell_exec/git__/gh__ the whole time. Task 8959 with the IDENTICAL toolset (`toolsHash 13857b89`, 24 full + 27 deferred) succeeded after 6 searches → 15× shell_exec. Root cause: `TOOL_SEARCH_ENABLED=true` (drop-in `tool-search.conf`, `337e7d7` 07-13) makes every `deferred: true` tool invisible at turn 1 (`claude-sdk.ts:289` `alwaysLoad: !deferred`), ALL coding tools carried the flag, and nothing in the prompt teaches ToolSearch retry — one miss read as absence. Ruled out: scope filtering, yesterday's `337d91f`, provider/budget/turn caps, stale dist (same pid fails and succeeds). Fix (structural, no prompt change): removed `deferred: true` from `shell_exec`, `file_write`, `file_edit`, `git_status`, `git_diff`, `git_commit`, `git_push`; `file_delete`/`gh_*`/`code_search` stay deferred. Pinned by `src/tools/builtin/core-coding-always-loaded.test.ts` (7 cases, 7 RED against the pre-change files). `scripts/validate-tool-search.ts --run` PASS: core 22→29 visible 29/29, prompt 52,937→14,496 chars (73 % cut, was 77 %; ≈2.1k tokens/turn on full-registry calls), deferred retrieval + built-in seal + inline turn-1 intact. Scoped vitest 234 GREEN, tsc clean. Co-symptoms QUEUED (#23 scope-classifier 3 s timeout on the primary model — 17/17 fallbacks today; #24 ToolSearch etiquette + the BLOCKED path bypassing `detectScopeMiss`). Live check = operator re-sends the trustr ask. |

---

## Reflector / selfAssess follow-ups from 2026-05-26 (queue #s)

| #   | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Priority       | Estimate | Trigger                                                                                                                                |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| W4  | **Deeper tool-args secret redaction policy** — current regex in `executor.ts:renderToolCallAudit → redactSecretsInArgs` strips obvious keys (api_key, bearer, token, password, secret, authorization). Belt-and-braces; deeper policy belongs in per-tool annotation (e.g. `Tool.argsRedactor?: (args) => args`). Conservative now; revisit if a sensitive tool ships args the regex misses.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | P3 hygiene     | 1-2h     | A new tool whose args carry secrets the regex misses, OR a security review.                                                            |
| W7  | **Centralize `recomputeSuccess()` at end of `reflect()`** — convergence penalty, anchoring penalty, trace efficiency, and any future score mutators all currently leave `assessment.success` un-updated. Only the heuristic-override and best-effort discount blocks (2026-05-26) recompute. A single call at end of `reflect()` (after all penalties) reading `score >= 0.8 && !hasFailedGoals` would cover all current + future paths uniformly. ~10 LOC + 1 test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | P3 reliability | 30min    | Next time a penalty-block bug ships, OR a routine cleanup pass. Pre-existing pattern; not regression-urgent.                           |
| S1  | **Log-writer ground-truth feed** — evolution-log ritual's friction-points section confabulated 2/3 claims on 2026-05-25 (API HTTP 000 + "memory_search not available"); structured detector signals were correct. Feed the writer journald excerpts + a pre-rendered tool-call audit so prose can't drift from reality. Generalises [[orchestrator-owns-identity]] to the writer ritual.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | P2 honesty     | 2-3h     | Recurrence of confabulated friction-claims in evolution-log (likely within a week), OR operator notices the prose↔detector divergence. |
| S2  | **Fix empty `prometheus_snapshots` table** — `SELECT COUNT(*) FROM prometheus_snapshots = 0` while `reflector_gap_log` populates correctly. Snapshot persistence (insertSnapshot call site or table schema) is broken; latent — costs investigation reach when a Prometheus run needs forensic replay. ~1h to trace + fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | P2 forensic    | 1h       | Next Prometheus run failure that needs replay, OR routine substrate audit.                                                             |
| S3  | **✅ CLOSED 2026-05-27 — deterministic-retry-gate shipped.** `adjustedRetryRule` now gates on `goalsFailed === 0 && goalsTotal > 0` from latest `reflector_gap_log` row. Score-only failures (criteriaMet=false discount, both 2026-05-25 + 2026-05-27 burn loops) no longer trigger same-prompt retry. Files: `src/db/reflector-gap.ts` (new `getLatestGoalSnapshot()` helper, logs on DB error per W1 audit fold), `src/reactions/types.ts` (`goalsTotal: number\|null` + `goalsFailed: number\|null` on ReactionContext), `src/reactions/manager.ts` (wires snapshot into context), `src/reactions/rules.ts` (narrow gate with W2 audit fold: requires `goalsTotal > 0` to distinguish empty-graph crashes from real zero-failure runs), `rules.test.ts` (4 new cases: gate fires/holds for {>0,0}/{4,1}/{null,null}/{0,0}), `manager.test.ts` (2 integration tests verifying wiring with mocked snapshot, W3 audit fold). qa-auditor verdict PASS WITH WARNINGS → C0/W1/W2/W3/S1/R1 all folded same-bundle. 30 rules tests + 11 manager tests + 222 reactions/db scoped tests pass; typecheck clean. **Live test**: 2026-05-27 23:00 MX skill-evolution fire (tomorrow night) is the dispositive run — should see one task only, no `adjusted_retry` row, escalate after MAX_RETRIES. Memory: [[deterministic-retry-gate]] (5 reusable rules). | **DONE**       | 1.5h     | **CLOSED** — verify on 2026-05-27 23:00 MX live fire.                                                                                  |
| M1  | **`mc_ritual_failed_total{ritual="..."}` counter for silent ritual failures** — memory-consolidation FK bug (fixed today) had been failing every Tue/Thu/Sat for weeks with one terse `[rituals] X failed: ...` log line per fire and no metric. Backlog-via-silent-failure is unobservable today. `scheduler.ts:recordRitualFailure(name, err, stage)` is the single instrumentation point; add a `prom-client` counter wired through it. A non-zero counter on a recurring ritual is a paging-worthy signal that maintenance is silently bleeding. ~20 LOC + 1 test. See [[fk-missing-cascade-silent-ritual-failure]] R2.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | P2 monitoring  | 30min    | Next time a ritual silently regresses, OR routine observability pass. Pre-existing pattern; not regression-urgent.                     |

## Round-3 hygiene follow-ups from SDK cutover (2026-05-10)

These are non-blocking findings the round-3 audit surfaced after round-2 fixes verified. Each is small. Bundle them into the next routine cleanup pass.

| #    | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Priority   | Estimate | Status                                                                                                                                                                                                        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R3-1 | **selfAssess() drops actualModel from TokenUsage** — `src/prometheus/executor.ts:151-160`. selfAssess goes through `queryClaudeSdkComplexWithFallback` and can independently fall back Opus→Sonnet. Its tokens get summed into goal totals but the goal's `actualModel` stays as the main-call's model. Small attribution drift on a thin call. One-liner: add `...(response.model !== undefined && { actualModel: response.model })` to the `usage: TokenUsage` literal.                              | P3 hygiene | 5 min    | **DONE 2026-05-13** — also closed W2 (executeGoal rollup of `assessUsage.actualModel`) caught by qa-auditor.                                                                                                  |
| R3-2 | **`aggregatedActualModel` semantics are array-order, not time-order** — `src/prometheus/executor.ts:679-681`. Last-write-wins is implemented as raw last-iteration-write, but with `Promise.allSettled` parallel execution all goals on the same iteration overwrite each other in array index order. Deterministic but doesn't match the documented "last non-empty across goals" if "last" implies "most-recent in time." Per-task rollup is approximate by design; just clarify the inline comment. | P3 doc     | 5 min    | **DONE 2026-05-13** — also amended pre-existing comment at executor.ts:624-626 (R2 from audit).                                                                                                               |
| R3-3 | **W8 abort guard rarely fires from production paths** — `src/inference/claude-sdk.ts:184-198`. `queryClaudeSdk` currently catches abort internally and converts to a returned result, so the wrapper sees a successful return, not a thrown abort. The W8 guard is defensive belt-and-suspenders. Add a code comment noting this so a future contributor doesn't simplify the guard out, OR add an integration-style test exercising an actual abort flowing through the whole stack.                  | P3 doc     | 10 min   | **DONE 2026-05-13** — audit caught W1 (first-pass comment claimed `isError: true` shape which is the MCP CallToolResult, not ClaudeSdkResult); corrected to describe degraded ClaudeSdkResult + abort marker. |
| R3-4 | **Commit message overstates "heavy/swarm" scope** — commit `64071fd` body says C1 round-2 closes "heavy/swarm pipeline drops actualModel". Only heavy was wired. Swarm-runner returns at `swarm-runner.ts:419` with no `tokenUsage` aggregation (sub-tasks record per-subtask via dispatcher, so swarm doesn't need its own rollup). Documentation drift, not functional. Either accept as-is or wire swarm-runner if cross-subtask aggregation matters.                                               | P3 doc     | n/a      | Accepted as-is — swarm doesn't need rollup.                                                                                                                                                                   |

---

## v7.7 Spine 1 Phase 1 deferreds (R1 audit 2026-05-19)

| #     | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Priority   | Estimate | Trigger                                                                                                  | Status                                                            |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- | -------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| S2-W6 | **Hoist prepared statement out of `persistReport`** — `src/audit/submit-report.ts:237`. better-sqlite3 prepared statements cache per-Database; preparing inside `persistReport()` on every call is a perf regression vs caching once at module scope. Phase 1 has zero production callers so it's moot. Fix when the first production surface (likely `morning_brief`) wires in. Either lazy-cache via `WeakMap<Database, Statement>` or co-locate with the table DDL. R1 audit verbatim: "Phase 1 has zero producers so this is moot; flag for Phase 2 retrofit before `morning_brief` wires in (cron daily, but eventually per-task)." | P3 hygiene | 15 min   | **Phase 2 morning_brief wire-in** (Spine 1 Phase 2). Block the Phase 2 retrofit PR until this is folded. | **DONE 2026-05-19** — Phase 2a fold, WeakMap-cached + 2 new tests |

---

## v7.7 Spine 1 Phase 2a deferreds (R1 audit 2026-05-19)

| #     | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Priority   | Estimate | Trigger                                                                                                                                                       |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S2-I2 | **Freeze `CRITIC_SKIP_FOR` Set once populated** — `src/audit/submit-report.ts:55`. Typed `ReadonlySet<ReportSurface>` but runtime is mutable; tests mutate via cast. Low-priority while the set is empty (Phase 2a). When Phase 2b or later adds a real surface to the allowlist, freeze via `Object.freeze(CRITIC_SKIP_FOR)` AFTER population so cross-test/cross-call pollution becomes impossible. R1 audit verbatim: "Low priority (currently empty), but worth a `Object.freeze` once Phase 2 surfaces start being added or a test will pollute production behaviour cross-file." | P3 hygiene | 10 min   | **First entry added to `CRITIC_SKIP_FOR`** (Phase 2b did NOT add one — critic stays mandatory for community email; allowlist remains empty as of 2026-05-19). |

---

## v7.7 Spine 1 Phase 2b deferreds (R1 audit 2026-05-19)

| #     | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Priority   | Estimate | Trigger                                                                                                                                                                                                                     |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S2-W3 | **Fallback locale** — `src/messaging/community-reply-gate.ts:35` `COMMUNITY_REPLY_FALLBACK` is Spanish-only ("Hola, Gracias por escribirnos..."). Correct today (sole mailbox `comunidades@mexiconecesario.org.mx` is a Mexican org). The moment a non-Spanish community mailbox is provisioned, English/French/Portuguese senders receive a Spanish acknowledgement, which itself looks broken. Either (a) make this per-mailbox via `EMAIL_<ID>_FALLBACK_LOCALE`, or (b) gate the org-persona doc with a `fallback_locale` field. Add a regression test that exercises the multi-mailbox path. | P3 hygiene | 30 min   | **A non-Spanish `EMAIL_<ID>_PERSONA_FILE` lands in the config** OR an `EMAIL_<ID>_LOCALE` env var appears with a non-`es-MX` value. Block the provisioning of any non-Spanish community mailbox until the locale fix lands. |

---

## v7.7 Spine 2 Bundle 1 deferreds (R1 audit 2026-05-19)

| #     | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Priority   | Estimate | Trigger                                                                                                                                                                                                                           |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S3-C1 | ~~**Rate-from-counter for `mc_whatsapp_disconnects_total`**~~ ⛔ **OBSOLETE 2026-06-20** — mc's own WhatsApp is permanently disabled by operator directive (`no-whatsapp.conf` → `UnsetEnvironment=WHATSAPP_ENABLED`, 2026-05-25, "no re-auth desired"), so the counter never increments and the signal correctly stays `enabled=0`. Nothing to monitor until mc is re-linked to its own number — which the directive says won't happen. Reopen ONLY if mc regains its own WA channel. _Original rationale:_ Prometheus Counter is monotonic; treating cumulative value as "disconnects per hour" produces a permanent P0 spam fountain once cumulative crosses 10. Seed signal currently DISABLED with `baseline_query: "awaiting:rate-from-counter-evaluator"`. Re-enable when EITHER (a) PromQL `rate(mc_whatsapp_disconnects_total[1h])` access lands in the evaluator, OR (b) the S3 evaluator stores prior-tick snapshot and computes delta-per-cadence. When fixed: `UPDATE drift_signals SET baseline_query='prom:mc_whatsapp_disconnects_total', enabled=1 WHERE signal_name='mc_whatsapp_disconnects_total'`. | P1         | 1-2 h    | **Either** PromQL rate() access is wired into the evaluator OR the evaluator gains delta-per-tick snapshot semantics. **Pre-blocks**: any infra-alerting reliance on this signal.                                                 |
| S3-W3 | **Capture production schema baseline for `schema_migration_drift`** — seed signal DISABLED until operator runs `SELECT COUNT(*) FROM sqlite_master WHERE type = 'table'` on production, records the actual count, and updates the seed row's `baseline_value_json` + `baseline_query` + `enabled=1`. Until then, every dev/test `:memory:` DB would trip P2 because count is ~30-45 vs hardcoded 50.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | P3 hygiene | 15 min   | Operator captures prod baseline count + runs the documented UPDATE. Detailed workflow in `src/lib/s3/seed-signals.ts` row 12 `notes` field.                                                                                       |
| S3-W4 | **Per-cadence promise-mutex for `runCadenceTick`** — overlapping cron fires (hourly + every_4h at 00:00) OR a slow tick that exceeds its cadence interval could race the `loadRecentUnbundledAlerts → detectBursts → persistBurstBundle` non-transactional trio. Worst case: two overlapping ticks bundle overlapping member sets and the second UPDATE overwrites the first bundle_id. Currently mitigated by hourly+ cadences + ~50ms per signal; documented in `scheduler.ts:runCadenceTick` long comment block.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | P3 hygiene | 30 min   | **V8.3 `on_event` cadence lands** (concurrent fires become routine) OR observed bundle-bundle_id corruption in production. Add a `runningPerCadence: Map<Cadence, Promise>` lock or wrap the trio in a single `db.transaction()`. |
| S3-I2 | **Prometheus counter for cron tick failures** — `mc_s3_evaluator_errors_total{signal=...}` (or `{cadence=...}`) so cron-tick failures surface in Grafana, not just journalctl. Aligns with `feedback_prometheus_counter_recovery_path` discipline ("instrument every failure entry-point"). Currently only `console.error` logs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | P3 hygiene | 30 min   | **DONE 2026-05-19** in Bundle 2 — `mc_s3_evaluator_errors_total{cadence, kind}` wired into per-signal + per-burst + per-tick error paths via dynamic import. Live on `:8080/metrics`.                                             |

---

## v7.7 Spine 2 Bundle 2 deferreds (R1 audit 2026-05-19)

| #           | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Priority   | Estimate | Trigger                                                                                                                                                |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| S3-I1       | **Replace `---` section delimiters with explicit `<!--S3-VERBATIM-START-->` / `END` markers** in the morning-brief alert section, AND add an LLM-eval regression assertion that the markers appear in the outbound email body unmodified. Stronger guarantee than today's prose-only "no la parafrasees" discipline.                                                                                                                                                                     | P3 hygiene | 1 h      | **Bundle 3 / Spine 2 close** OR observed paraphrasing in production. Cross-references the Spine 1 P2a R1-C2 "tool boundary > prose discipline" lesson. |
| S3-I3-tests | ~~**Tests asserting `recordS3EvaluatorError` actually increments**~~ ✅ **DONE 2026-05-28** in #233 P3 hygiene closure-pass #2. Original: — three injected-throw tests (per-signal, per-burst, per-tick kinds) that mock the inner exception and assert `getRegistry().getSingleMetric("mc_s3_evaluator_errors_total").get()` reflects the bump. Counter is registered live on `:8080/metrics` ✓; tests would lock the fold against future refactors that break the dynamic-import path. | P3 hygiene | 45 min   | **Next refactor of `scheduler.ts` runCadenceTick** OR observed Grafana panel staying flat during an incident.                                          |
| S3-W3-fk    | **Schema FK option for `drift_alerts.signal_id`** — currently no `REFERENCES drift_signals(id)` clause; Bundle 2 mitigates via LEFT JOIN + COALESCE for visible-degradation. Adding the FK (`ON DELETE RESTRICT` vs `CASCADE`) is a design choice for v8.0 (destructive migration in SQLite).                                                                                                                                                                                            | P3 hygiene | 30 min   | **v8.0 schema reset opportunity** (SQLite FK add on existing column requires destructive migration; defer).                                            |

---

## v7.7 Spine 2 Bundle 3 deferreds (R1 audit 2026-05-19)

| #        | Item                                                                                                                                                                                                                                                                                                             | Priority   | Estimate | Trigger                                                                                                                                          |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| S3-B3-I1 | ~~**Max-length validation on `reason` / `acknowledged_by` POST body fields**~~ ✅ **DONE 2026-05-28** in #233 P3 hygiene closure-pass #2. Original: at `POST /api/admin/alerts/:id/suppress`. Currently a 10 MB body lands in `resolution_notes`. X-Api-Key gate is primary defense; this is hardening-in-depth. | P3 hygiene | 15 min   | **Admin route hardening pass** OR first abusive POST observed. Defense-in-depth.                                                                 |
| S3-B3-I2 | ~~**MX-localized timestamp rendering in push messages**~~ ✅ **DONE 2026-05-28** in #233 P3 hygiene closure-pass #2. Original: — `push.ts:114` ships `triggered ${a.triggered_at}` as ISO UTC. Operator's morning brief uses Spanish relative-time ("hace 5 min") elsewhere. Push surface is inconsistent.       | P3 hygiene | 15 min   | **Operator UX feedback** on push readability OR systematic timestamp-locale audit.                                                               |
| S3-B3-I4 | ~~**Direct test for `composeMorningBriefDriftSection`**~~ ✅ **DONE 2026-05-28** in #233 P3 hygiene closure-pass #2. Original: — covers Sunday-only aging gate + 4 heading-wrap branches (alert-only / aging-only / both / empty). Currently exercised only indirectly by morning.test.                          | P3 hygiene | 30 min   | **Next delivery.ts refactor** OR observed regression in Sunday morning brief.                                                                    |
| S3-B3-I5 | **Separate `acknowledged_at` vs `resolved_at` timestamps** in drift_alerts. Currently both store the same value at suppression time. v8.0 auto-unsuppress design needs the distinction ("operator acked at T1, system auto-resolved at T2").                                                                     | P3 hygiene | 1 h      | **v8.0 auto-unsuppress design lands** (deferred from spec §8). Schema column already exists (`acknowledged_at`); just needs separate write-path. |

---

## v7.7 Spine 3 Phase 1 deferreds (R1 audit 2026-05-19)

| #        | Item                                                                                                                                                                                                                                                                                                                                                         | Priority   | Estimate | Trigger                                                                                                                                  |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| S5-P1-I1 | **Backfill the 67 existing DB-only `skills` rows into `skill_versions`** — spec §4 says "auto-converted at migration time". Phase 1 ships schema only; backfill deferred so the activation gate doesn't certify legacy bodies as Phase 1 noise. Reconstruct `body` from `steps[]` JSON + emit `created_by='boot-scan'` rows with `critic_verdict='skipped'`. | P3 hygiene | 1-2 h    | **Phase 5 cleanup pass** (operator surface + activation gate). Avoid earlier to keep critic gate's first-N-skills signal noise-free.     |
| S5-P1-I2 | **Multi-process boot detection** — `ensureSkillRow` + `recordVersion` use SELECT-then-INSERT, safe under single-process invariant. Add a startup-fingerprint guard (PID + ppid + lock file) if a second writer ever lands.                                                                                                                                   | P3 hygiene | 30 min   | **Any introduction of `Type=notify` helpers / sidecars / dual-instance reads of the same `mc.db`**. Documented inline at `loader.ts:60`. |

---

## v7.7 Spine 3 Phase 2 Bundle 1 deferreds (R1 audit 2026-05-19)

| #        | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Priority   | Estimate | Trigger                                                                                                                                                             |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S5-P2-I1 | **Extend `skill_versions.created_by` CHECK constraint to include `'operator-override'`** — Phase 1 shipped without this enum value but spec §8 mandates it. Phase 2 worked around by carrying the override signal via `critic_verdict='fail_returned_anyway'` only. SQLite cannot ALTER a CHECK without destructive table re-create.                                                                                                                          | P3 hygiene | 15 min   | **v8.0 schema reset** (destructive migration is the SQLite-only path; not worth a special migration just for this).                                                 |
| S5-P2-W1 | **`SkillSaveResult.kind='unchanged'` returns `ok:false` but is a successful idempotent re-save** — operators and any future Phase 4 producer-retry loop cannot distinguish "rejected" from "no-op success." Either split into a distinct `{ok:true, kind:'unchanged', versionId}` variant (look up the existing version_id), or document explicitly in JSDoc that `kind:'unchanged'` is a success-class.                                                      | P3 hygiene | 30 min   | **Phase 4 producer agent ships its retry loop** OR operator confusion observed in `mc-ctl skill-health`.                                                            |
| S5-P2-W2 | ~~**Critic body excerpt cap is a fixed 8000 chars**~~ ✅ **DONE 2026-05-28** in #233 P3 hygiene closure-pass #2 — `MC_CRITIC_BODY_CAP` env (default 8000, max 32000). Original: (`src/skills/critic.ts:103`) — no env tunable, no surface to the result. Skills with bodies >8k chars silently grade against a truncated view. Fix: surface `body_truncated: boolean` on `SkillCriticResult` OR env-tunable `SKILL_CRITIC_BODY_CAP`. Logging-only acceptable. | P3 hygiene | 15 min   | **First skill body exceeds 8k chars** (none today; legacy 67 skills have ≤500-char `steps[]` JSON; the cap won't bite until operator-authored long playbooks land). |
| S5-P2-W3 | **Override-on-critic_error stores raw infrastructure error as `critic_critique`** — when `forceOperatorOverride: true` fires after a `critic_error` (timeout/empty/non-JSON), `skill_versions.critic_critique` holds something like "critic call failed: network reset". Operator reading `mc-ctl skill-health` sees ambiguous provenance. Fix: prefix override-on-error critique with `"[override-on-error] "` OR write `critic_critique=NULL`.              | P3 hygiene | 15 min   | **First production override-on-error observed** OR `mc-ctl skill-health` Phase 5 build.                                                                             |

---

## v7.7 Spine 3 Phase 2 Bundle 2 deferreds (R1 + R2 audit 2026-05-19)

| #           | Item                                                                                                                                                                                                                                                                                                                                                                                                                                       | Priority   | Estimate | Trigger                                                                            |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | -------- | ---------------------------------------------------------------------------------- |
| S5-P2-B2-W4 | **`deepPartialMatch` null-vs-undefined semantics** — when test author writes `expect.output_match: {y: null}` it means "expect null value", not "expect absent key" (the matcher currently uses `Object.is(null, undefined) === false`). Document in test-runner JSDoc + add a test case asserting current behavior. Phase 4's typed-output discipline pass may want to change semantics; until then, the current behavior is intentional. | P3 hygiene | 15 min   | First author confusion in Phase 2 production traffic OR Phase 4 typed-output pass. |

---

## v7.7 Spine 3 Phase 3 Bundle 1 deferreds (R1 audit 2026-05-19)

| #           | Item                                                                                                                                                                                                                                                                                                                                                                                     | Priority   | Estimate | Trigger                                                                                                                    |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- | -------------------------------------------------------------------------------------------------------------------------- |
| S5-P3-B1-W1 | ~~**Malformed-BLOB shape check**~~ ✅ **DONE 2026-05-28** in #233 P3 hygiene closure-pass #2 — NaN/Inf scan after deserialize. Original: at `src/skills/embedding.ts:loadSkillEmbedding` — byte-length match (1536 × 4) passes vectors of NaN/Inf. Cosine-of-NaN surfaces in B2 retrieval as silent zero scores. Add NaN/Inf scan after deserialize OR detect at score time.             | P3 hygiene | 15 min   | **B2 retrieval surfaces cosine-NaN in production** OR threat model expands to untrusted writers (V8.3 autonomous gates).   |
| S5-P3-B1-W4 | ~~**Migrate `console.warn`/`console.info` in `src/skills/lifecycle.ts` + `src/skills/backfill-embeddings.ts` to pino structured logger**~~ ✅ **DONE 2026-05-25** in #233 P3 hygiene bundle. Folded the audit's S1 grep-sweep finding: also migrated `dispatcher.ts`, `test-runner.ts`, `test-sweep.ts` (5 files total, 7 console.\* → pino). Bundle commit covers W4+W5+R1+R2+S4-B3-R2. | P3 hygiene | DONE     | v7.7 closure log-hygiene pass — declared and executed.                                                                     |
| S5-P3-B1-W5 | ~~**Remove dead `DEFAULT_LOG.warn` path** in `src/skills/backfill-embeddings.ts`~~ ✅ **DONE 2026-05-25** in #233 P3 hygiene bundle. Dropped `warn` from `BackfillLog` interface (verified body never invoked it) and updated SILENT test mock to match.                                                                                                                                 | P3 hygiene | DONE     | Bundled with W4.                                                                                                           |
| S5-P4-B1-I1 | **Extend `skill_failures.resolution` CHECK constraint to include `'self_recovered'`** — Phase 1 shipped without spec §10's enum value. Phase 4 B1 workaround: on success-recovery, set `resolved_at` and leave `resolution` NULL. Batches with **S5-P2-I1** at v8.0 schema reset.                                                                                                        | P3 hygiene | 5 min    | **v8.0 schema reset (batched with S5-P2-I1).**                                                                             |
| S5-P4-B1-W1 | **`consecutive_failures` unbounded — no auto-archive ceiling**. Anti-list (`< 3`) hides skill from retrieval, but dispatcher accepts explicit invocations forever. Spec §10 implies auto-archive after N failures; not yet implemented.                                                                                                                                                  | P3 hygiene | 30 min   | **Operator complains about a forever-broken skill** OR Phase 5 ships a `mc-ctl skills archive --threshold=N` workflow.     |
| S5-P4-B1-W2 | **Chained-dispatch cycle-detection test** — current test seeds `_callStack` directly; no test pins the contract that callers (Phase 5 body-side recursion) MUST append before recursing. Add sentinel: `runSkill("A", {}, {_callStack:["A"]})` rejects same as today, but ALSO confirm a wrapper that forgets to append breaks recursive isolation.                                      | P3 hygiene | 20 min   | **Phase 5 wires the first `skill_run`-from-skill-body codepath** (the contract becomes externally observable).             |
| S5-P4-B1-R1 | ~~**Make `MAX_SKILL_CALL_DEPTH` env-overridable**~~ ✅ **DONE 2026-05-25** in #233 P3 hygiene bundle. `MC_MAX_SKILL_CALL_DEPTH` env var; validated as positive integer; W1 audit fold capped at `MAX_REASONABLE_SKILL_CALL_DEPTH=20`. Invalid values silently fall back to default 3.                                                                                                    | P3 hygiene | DONE     | Zero-cost flexibility — shipped on closure pass.                                                                           |
| S5-P4-B1-R2 | ~~**Counter-increment regression test** for `mc_skills_run_total`~~ ✅ **DONE 2026-05-25** in #233 P3 hygiene bundle. 5 tests in `prometheus.test.ts`: ok-increment, partition-by-result, unknown→other collapse, 64-char cardinality clip, empty-name (R1 audit fold). Pattern matches existing `recordRetainOutcome`/`recordSwarmSubtaskRetry` blocks.                                 | P3 hygiene | DONE     | Closure-pass on Spine 3 — shipped.                                                                                         |
| S5-P4-B2-W5 | **`skill_load` body excerpt mode** — full body lands in LLM context; subsequent `skill_run` re-injects it via the mini-runner. Double-counted tokens. Defer optional `excerpt: true` arg until prompt-cost analysis confirms duplication is material (>5% of skill-related tokens).                                                                                                      | P3 hygiene | 20 min   | **Prompt-cost analysis shows skill_load+skill_run duplication >5% of skill-related tokens** OR S2 self-audit flags it.     |
| S5-P4-B2-R2 | ~~**`skill_load` version_pointer_dangling test**~~ ✅ **DONE 2026-05-28** in #233 P3 hygiene closure-pass #2. Original: — defensive branch unreachable via standard lifecycle (`pointSkillAtVersion` always updates current_version_id to a real id). Add a regression test if the dangling code path is ever surfaced in production.                                                    | P3 hygiene | 10 min   | **Production logs show `version_pointer_dangling` reason** OR Phase 5 introduces `skill_revert` that could orphan rows.    |
| S5-P4-B2-R3 | **Rename internal errorClass strings on public-facing channels** — `errorClass: "skill_corrupt"` may surface verbatim if community-manager scope gets `skill_run` access. Either rename to `"skill_data_error"` for external channels OR add a translation layer in the public-channel router.                                                                                           | P3 hygiene | 30 min   | **Community-manager scope is granted `skill_run` access** OR public-facing skill output appears in a non-operator channel. |
| S5-P5-B1-W4 | **`mc-ctl skills archive` writes a born-resolved `skill_failures` row** — `resolved_at` + `failed_at` (column default) both = now. Failure-time-to-resolution analytics see zero-duration archive rows. Fix: add `'archived'` to the `error_class` CHECK enum and use it instead of `'other'` (batches with the v8.0 schema-reset).                                                      | P3 hygiene | 5 min    | **v8.0 schema reset (batched with S5-P2-I1 + S5-P4-B1-I1)** OR failure-duration analytics queries are written.             |
| S5-P5-B1-R2 | **`mc-ctl skills show` omits `trigger_examples` + `tools_used`** — CLI/L2-tool asymmetry vs the `skill_load` builtin. Add a `--full` flag that parses + renders `tools_used_json` + `trigger_examples_json`.                                                                                                                                                                             | P3 hygiene | 15 min   | **Operator complains about missing CLI fidelity** OR Spine 3 closure pass asks for CLI/tool parity.                        |
| S5-P5-B2-R2 | **Date/division-arithmetic skills risk non-determinism for future test authors** — `clasificar-prioridad-tarea` + `evaluar-roi-de-fase` bodies do calendar/division arithmetic in natural language. The 5 seeded tests use same-month dates (safe at temp 0). A future cross-month / leap-day test case could be flaky. Add an inline "use exact arithmetic" note to the bodies.         | P3 hygiene | 10 min   | **A future test author adds a cross-month / leap-day case** to either skill's `tests_json`.                                |

## v7.7 Spine 4 deferreds (R1 audits 2026-05-20)

| #        | Item                                                                                                                                                                                                                                                                                                                                                     | Priority   | Estimate | Trigger                                                                                                        |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- | -------------------------------------------------------------------------------------------------------------- |
| S4-B3-R2 | ~~**`mc-ctl` SQL-identifier guard sweep**~~ ✅ **DONE 2026-05-25** in #233 P3 hygiene bundle. `^[a-zA-Z0-9_-]{4,64}$` regex guards added to `cmd_task` and `cmd_schedule_delete` before SQL interpolation (mirrors `cmd_cancel` shape). Smoke-tested: SQL-injection-shaped input rejected; real UUID accepted.                                           | P3 hygiene | DONE     | Declared injection-hardening pass — shipped.                                                                   |
| S4-B1-I1 | **`general_events` recency index** — `idx_general_events_active` is keyed on `end_at`, so Bundle 2 retrieval scans it for the `archived_at IS NULL` partial and sorts `start_at DESC` via a temp B-tree. Harmless at the ~30-50 row cohort. If the cohort grows, add `idx_general_events_recency ON general_events(start_at) WHERE archived_at IS NULL`. | P3 hygiene | 5 min    | **`general_events` row count exceeds a few hundred** (auto-discovery lands) OR a retrieval-latency regression. |
| S4-B1-W4 | **`general_events.details_embedding` + auto-discovery** — Bundle 1 omitted the spec §5 second embedding (descent uses the links table). Auto-discovery (the nightly cluster→S2-critic→insert population path, spec §5) is deferred — Spine 4 ships manual seed only. Both are additive when V8.1 (or a later spine) needs them.                          | P3 / V8.1  | —        | **V8.1 capability work** OR the manual-seed cohort proves too sparse for briefing retrieval.                   |

## v7.7 Spine 6 deferreds (2026-05-20)

| #                       | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Priority | Estimate | Trigger                                                                                                                          |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------- | -------------------------------------------------------------------------------------------------------------------------------- |
| S6-recall-audit-dormant | **`recall_audit` receives no new rows since 2026-05-10** — recall routes through `SqliteMemoryBackend`, which does not call `logRecall`/`applyOutcomeBias`; only `HindsightMemoryBackend` does, and `HINDSIGHT_ENABLED=false`. So recall-mode tagging + the coherence outcome filter + the `recall_coherence_suppression_rate` drift signal are all dormant. Fix: EITHER set `HINDSIGHT_ENABLED=true`, OR wire `logRecall` (instrumentation) — and decide whether to also wire `applyOutcomeBias` (the filter — a live recall-results change) — into `SqliteMemoryBackend.recall()`. Then flip the drift signal to `enabled=1` (swap `baseline_query` from the `awaiting:` sentinel to the activation SQL in its `notes`). | P2       | 2-3 h    | **A decision on recall-backend routing** (revisit the Hindsight demote) OR V8.1 morning-brief needs live recall instrumentation. |

## ~~Honor `retry-after` over exp-backoff for 429 retries~~ ✅ **DONE 2026-05-28**

Closed by `resolveRetryDelay()` in `src/inference/http-error.ts` + adapter wiring at `src/inference/adapter.ts:1078`. Server hint wins over exp-backoff curve when present, capped at `MC_RETRY_AFTER_MAX_MS` (default 30000ms, ABSOLUTE_MAX 600000ms). 10 tests including env-override, NaN-guard, and storm-clamp at ABSOLUTE_MAX. Log line gained `(source=retry-after|exp-backoff)` for journalctl correlation. Scope: OpenAI-compat + direct Anthropic Messages path — Claude Agent SDK path has its own retry policy and isn't touched. Telemetry gate widened to log on 5xx-with-Retry-After too (RFC 7231 §7.1.3). Original spec retained below for context.

---

**Triggered by:** rate-limit header capture shipped 2026-05-23. The infra now logs `retry-after` from 429 responses but does NOT use it — the retry delay is still `Math.min(1000 * Math.pow(2, attempt), 8000)`. When the server says "wait 30 seconds," we wait 1-8 sec and hit 429 again.

**Why deferred:** behavioral change with real risk. A long `retry-after` (some providers send minutes) would block an interactive task and feel like a hang. Need: (a) cap (e.g., `Math.min(retryAfterMs, MAX_BACKOFF_MS)` with MAX_BACKOFF capped at 30s for interactive contexts), (b) decision whether to abort vs queue vs degrade to a fallback provider when the cap is exceeded, (c) telemetry to confirm provider hints are not adversarially long.

**Acceptance:** after first ship, grep `rate_limit_hit` from journalctl for a week. If `retryAfterMs` is consistently under 30s, ship the honor-it change with a 30s ceiling. If providers ever send minute+ hints, design the bypass first.

**Tracking:** `feedback_prometheus_upstream` April Tier-1 #1 "Rate-limit header capture" is ✅ done; this is its behavioral follow-up.

## Hermes v0.15–v0.21 review deferreds (2026-09-01)

From `docs/planning/hermes-upstream-review-2026-09-01.md` (memory `feedback_prometheus_upstream`). Shipped: connect-time SSRF guard + standing-orders guard (this commit). Every deferral below carries its trigger.

1. **Temporal anchoring in compaction summaries** (v0.17 #41102) — add "today is YYYY-MM-DD; resolve relative dates" to `STRUCTURED_COMPACT_PROMPT`. Deferred: the compressor fires only on the OpenAI fallback (~6×/30 d), 0 `compaction/*.md` rows. **Trigger:** first persisted compaction row, or fallback traffic >5 %.
2. **Redaction at compaction / snapshot boundaries** (v0.20 #69294, v0.21 sweep) — `redactSecrets` before `upsertFile` in `context-compressor.ts` and on `prometheus_snapshots`. Blocker: the 40-hex rule would mangle git SHAs the summary must keep verbatim → needs a context-safe variant. **Trigger:** same as 1, or any secret found in `jarvis_files` `compaction/*`.
3. **`always-read` qualifier is model-settable** (R1-W2) — 8 rows (5 projects/, 1 knowledge/, 1 directives/, 1 root) turn any file into an every-turn instruction surface; same class as the standing-orders guard one axis over. **Needs operator ruling**: who legitimately sets it? If only the operator → downgrade like `enforce`.
4. **Model-authored proposal files** (R1-W3) — `jarvis_file_write` to `knowledge/proposals/*.md` skips `jarvis_propose_directive`'s cooldown; `jarvis_apply_proposal` still needs operator approval, so the gate holds. **Trigger:** a proposal flood (>3/day) in `knowledge/proposals/`.
5. **Live subagent orchestration / per-sub-task retry** (v0.21 #85232 et al.) — folds into the existing per-sub-task-retry design item below. **Trigger:** ≥10 swarm tasks/month or the first lost sub-task result.
6. **Per-session turn lease** (v0.19 #67401), **compaction cooldown escalation** (v0.21 #79741), **TTFB/stall watchdog on the OpenAI stream** (v0.15 #32042) — no incident on record. **Triggers:** first interleaved double-reply · a `compaction_exhausted` exit · a streaming-truncation incident on the fallback.
7. **Operator-side consequence of the reindex change:** a new directive dropped on disk is no longer auto-imported (directives/ is a managed namespace). Use `upsertFile` via dist, as the 08-28 Track 1 write did. No trigger — this is the intended posture.
8. **Shell tool residual door** — absolute-path writes to `jarvis-kb/directives/` are now denied (`DENY_WRITE_PATTERNS`, R2-C2), but the guard sees command TEXT: `cd /root/claude/jarvis-kb && echo > directives/x.md` still lands on disk — and so do a QUOTED target (`> "/root/claude/jarvis-kb/directives/core.md"`) and in-place editors (`sed -i … /root/claude/jarvis-kb/directives/core.md`), because `WRITE_INDICATORS` only extracts unquoted absolute paths after `>`/`>>`/`tee`/`mv`/`cp`; the same spellings bypass the mission-control deny (pre-existing extractor limit, R3-W2). The disk copy is Drive-synced but never becomes a row (reindex managed namespace) and never reaches the prompt (injection keys on the DB). **Trigger:** any journal line showing a shell write under `jarvis-kb/directives/`.
9. **Pre-existing SSRF surfaced by the R2 sweep** (out of bundle scope): `huggingface.ts:110` (SSE-payload URL + bearer), `a2a/client.ts:39,125` (URL from a task-input row + bearer/X-Api-Key), `stealth-browser.ts:262` / `screenshot.ts:171` (`page.goto` gated only by the non-resolving validator — a browser cannot use the dispatcher; needs a resolve-then-pin or a proxy), `video/images.ts:57`. **Trigger:** next security sweep, or the first task that feeds any of them an untrusted URL.

## Per-sub-task retry in swarm (deferred feature gap from Hermes v0.13)

**Triggered by:** Hermes v0.13 May Tier-2 #5 audit (2026-05-23). The audit closed the zombie-status gap (`syncSubTaskStatuses` recognizes 3 more terminal statuses), but Hermes also ships per-sub-task retry + hallucination recovery — neither of which we have. A failed sub-task in our swarm is final; the goal-graph cascades FAILED to dependents and the parent reflector picks up the partial state.

**Four concrete design questions to resolve before shipping:**

1. **Where does retry policy live?** Swarm-level (sub-task fails → swarm decides to retry the same goal with a new task) or dispatcher-level (any failed task gets one retry)? Swarm-level matches Hermes's framing; dispatcher-level reaches all task-types but composes strangely with non-swarm runners.
2. **Idempotency contract for tools** the failed sub-task already called. If a sub-task wrote to a DB / posted a message / called an external API before failing, the retry could double-effect. Need either (a) per-tool idempotency tags (not yet a thing in our `Tool` interface), (b) a snapshot-and-rollback boundary, or (c) an explicit "retries forbidden after side-effect" annotation.
3. **Composition with existing `_isRequiredToolRetry` one-shot retry** in `dispatcher.ts:469`. That retry is for "required tool not called yet" recovery within a single task. A new sub-task retry would compose strangely — could double the budget consumed by a single goal.
4. **Infinite-retry guard / max-retries-per-goal counter location**. Counter on the goal? On the parent task? On the swarm-runner state? The counter must survive process restarts (swarm tasks persist across `mc-ctl restart`), implying it lives on the goal-row in `prometheus_snapshots` or a dedicated column on `tasks`.

**Acceptance:** before shipping, write a 1-page design note resolving 1-4. Then implement with `maxRetriesPerGoal=1` default, side-effect gating, and `tasks.retry_count` column.

## Haiku downshift for aux LLM calls (deferred cost optimization)

**Triggered by:** Hermes v0.11 May Tier-2 #8 audit (2026-05-23, see `feedback_prometheus_upstream`). The audit confirmed no silent-cheap-default bug — every `infer()` caller (~35 sites) routes through `inferViaClaudeSdk` which hardcodes Sonnet. That's the OPPOSITE of Hermes's complaint, but the cost lever is real: aux tasks that fire frequently (scope-classifier on EVERY incoming message; prompt-enhancer twice on enhance flows) use Sonnet when Haiku-grade quality would likely suffice.

**Why deferred:** the change is multi-step and needs measurement first.

1. Add `model?: string` field to `InferenceRequest` (`adapter.ts:92-101` currently has no `model` field — callers cannot opt-in).
2. Plumb through `inferViaClaudeSdk` to honor `request.model ?? SONNET_MODEL_ID`.
3. Annotate aux callers (`scope-classifier.ts:102`, `prompt-enhancer.ts:262, 403`) with `model: HAIKU_MODEL_ID`.
4. **Quality measurement** before any rollout: run a one-day shadow pass (compute both Sonnet and Haiku scope-classifier output on the same incoming messages, log disagreements). If Haiku matches Sonnet ≥95% on the scope-classifier output, ship the downshift. If lower, design a confidence-aware fallback (Haiku first; on low-confidence output, retry with Sonnet).

**Expected savings:** scope-classifier fires once per incoming message + enhancer fires once-or-twice per enhance flow. Per-task aux Sonnet-vs-Haiku saves ~5-8x on those lines (Sonnet $3/M in vs Haiku ~$0.40/M in). Unclear absolute $/mo until plumbing measures the per-aux line in `cost_ledger`.

**Tracking:** companion to the already-audited [[aux-model-routing-audit]] in `feedback_prometheus_upstream` #8.

## Cross-task prompt cache miss (heavy task, $754/mo `fast` Sonnet line)

> **RESOLVED-STALE 2026-07-13 (V8.5 Phase 3.1):** fix option 1 below was ALREADY SHIPPED the day after this entry (`eff1f23`, 2026-05-23 — `cacheable:false` system messages route to the user-prompt prefix; see `flattenMessagesForSdk`). This entry was never updated, and the V8.5 plan inherited the stale "pick one of 3 options" decision. Post-fix fast-line cache-read ran 72–85% and improved further after the 07-12 settingSources leak removal (~23k smaller prefix, 1h-TTL writes). Residual verdict (is option 2 multi-block worth anything?) pends 3 days of `DEBUG_CACHE_DIAG` hash telemetry (drop-in `cache-diag.conf`, remove after verdict). The $754/mo figure is obsolete. Lesson filed: verify a queue entry's "pending" claim against the code before re-planning it.

**Status (2026-05-22 late):** ROOT CAUSE LOCATED, fix deferred to next session. `cache_diag` log (`f219340`) collected 10 samples across two PID generations. Two consecutive heavy-task calls (toolsHash `392a4296`, toolsN 50, ownerChannel=true, same day, 28 min apart, no cohort cron in between) hash DIFFERENTLY: `fd3936d8cb9e` (71921 chars) vs `10a9164b41d1` (71300 chars). 621-char drift in the "stable" half rules out hypothesis (a) cross-`query()` SDK gap and confirms hypothesis (b) hidden per-task variation.

**Architectural cause:** `src/inference/claude-sdk.ts:698` `flattenMessagesForSdk` concatenates EVERY `role: "system"` message into one `systemPrompt` string passed to the Claude Agent SDK, which sets ONE `cache_control` marker at the block end. The fast-runner painstakingly splits content into stableSP, stableKB, variableSP, variableKB, precedent, deferredCatalog as separate system messages (router.ts:1878, fast-runner.ts:750-788) — but `flattenMessagesForSdk` collapses all of them into a single cache block. Any byte change anywhere (e.g., the README size shift 10294→9988 chars seen in tonight's samples) → full cache miss on the entire 71K block.

The wrap memory's "byte-stable identitySection prefix not landing cross-task" intuition was right; the deeper cause is that the careful stable/variable split done by buildJarvisSystemPrompt + buildKnowledgeBaseSections is LOST at the SDK boundary. Cache-control granularity = 1 block, not the 4-6 we believe we have.

**Fix options (pick one in next session):**

1. Route ONLY the genuinely-stable prefix (stableSP + stableKB) into the SDK's `systemPrompt`. Push variableSP + variableKB + precedent + deferredCatalog into the user message (prepended). Smallest delta, single concat-site change in `flattenMessagesForSdk` if it can branch on a marker. Estimated savings: ~30K cache_read tokens per heavy task (full stable block becomes cacheable).
2. If `@anthropic-ai/claude-agent-sdk` exposes multi-block system content with per-block `cache_control`, use it (4 breakpoints available). Cleaner but depends on SDK surface area.
3. Treat `flattenMessagesForSdk` as the seam; pass a `{ stableSystem, variableSystem, userPrompt }` shape from runners → SDK shim → API. Largest delta, most correct long-term.

Verification after fix: run `cache_diag` again; same scope/ownerChannel should yield byte-identical `systemPromptHash` across calls. `cost_ledger.cache_creation_tokens` for `fast` should drop ~30-40K per task on average.

**Prereq:** before designing the fix, confirm the Anthropic Agent SDK's exact `cache_control` placement (read SDK source or run a 1-call experiment with a known-stable prompt, check API response usage block). Don't refactor on the assumption "one block at the end" without confirming.

## Hermes v0.11 adoption deferreds — CLOSED 2026-05-22

From the qa-audit of the compaction-thrash fix (`d4511c8`; Tier-1 #1+#2 from `feedback_prometheus_upstream`). Both audit deferreds resolved same session.

- _HERMES-W4 (`d72dc12`) — `compactionGuardStep` extracted as a pure helper + 6 unit tests covering audit-required behaviors (a)/(b)/(c) plus k-stability and custom-k. `inferWithTools` is genuinely unmockable from a test (calls `infer()` same-module), so testing the pure logic + obvious 1-line wiring was the proportionate fix._
- _HERMES-W3 (commit pending) — `buildWrapUpContext` per-message truncation extracted as `truncateMessageForWrapup` and extended to handle oversized assistant `content` and replace oversized `tool_calls[].function.arguments` with a valid-JSON `{_truncated_for_wrapup, original_length}` marker (raw truncation would yield invalid JSON the API rejects). The wrap-up infer() now stays under budget when reached from a `compaction_exhausted` exit. 7 unit tests on the helper._

## SDK cost-ledger phantom-turns anomaly — OPEN, surfaced 2026-05-23

Surfaced during the /diagnose phase of today's nanoclaw timeout fix. **Unrelated to that fix — separate subsystem** (claude-sdk metric pipeline, not the container runner). Filed here so it doesn't fall through.

**Symptom** (single live observation, 2026-05-23 07:41 UTC):

```
[claude-sdk] progress: 51 turns, 35 tool calls, 2351 chars
[claude-sdk] Query timed out after 900s — aborting
[claude-sdk] Completed: 51 turns, 35 tool calls, $0.0000, 0ms, tokens=0 (cache 0%: 0 read, 0 created) [TIMED OUT]
```

51 turns + 35 tool calls reported by `progress` lines (real work done), but the `Completed [TIMED OUT]` line writes `$0.0000 / 0ms / tokens=0`. Cost ledger gets $0.00 for an SDK query that actually consumed inference. Recurrence unknown — needs a sweep of `cost_ledger` rows joined with task durations to find other phantom-$0 entries.

**Open questions**:

1. Is `[TIMED OUT]` always the trigger, or does any abort path also zero out usage?
2. Is the SDK returning zero usage on timeout (an upstream bug), or are we discarding partial usage in our handler?
3. What's the actual spend over the 9-day image-missing window — were any retries / Sonnet warmup calls writing $0 silently?

**First step when picked up**: grep `mc.db` cost_ledger for `cost_usd=0 AND completion_tokens=0 AND duration_ms>60000` over the last 14d. If the count is non-trivial, this is a budget visibility gap. The 2026-05-23 daily-spend cap exceedance ($40+ on $30 cap) is a candidate side-effect of unreported real spend going through the budget tracker as $0.

Files of interest: `src/inference/claude-sdk.ts` (the `Completed:` log + cost emission), `src/inference/adapter.ts` (cost_ledger write site). Source-verify before assuming the SDK is the bug — the partial-usage path through our handler may be what's dropping the data.

---

## Added 2026-06-17 — evolution-log truncation follow-ups

Spun out of the `/diagnose` + fix for the 2026-06-17 evolution-log truncation (ritual called `file_write` — whole-file overwrite — as an "append" and destroyed the 45 KB log; June 11–15 were unrecoverable because uncommitted). Core fix shipped (drop file_write from ritual tools, mandate `cat >>` heredoc append, anti-git-recovery guard, shell.ts append-only gate, truth-restore from HEAD). Two residual items remain — see memory `feedback_evolution_log_truncation`.

### A. Evolution-log durability (the REAL backstop) — ✅ SHIPPED 2026-06-17

Done as a **weekly mechanical cron** (`scheduleEvolutionLogCommit` in `scheduler.ts` → `commitEvolutionLogIfDirty` in `evolution-log-commit.ts`, Sunday 03:00 MX): a pathspec-scoped `git add -- docs/EVOLUTION-LOG.md` + `git commit --no-verify -m … -- docs/EVOLUTION-LOG.md` so each week's appended entries become recoverable. NOT an LLM ritual — the git _tools_ throw on `main` (branch gate), so it runs as raw `execFileSync` git (round-1 audit C1). Pathspec scoping closes the dirty-index-sweep risk (C2). Local commit only (no push) — sufficient for truncation recoverability; leaves local `main` ahead of origin until the operator's next push (operator-runbook note). Two qa rounds: round-1 FAIL (LLM ritual dead-on-arrival) → mechanical rewrite → round-2 PASS.

### C. `git_commit` tool bare-commit (latent, security) — from round-1 audit C2

`gitCommitTool.execute` (`src/tools/builtin/git.ts:292,305`) does `git add <files>` then a **bare** `git commit -m <msg>` — committing the ENTIRE staged index, not just `<files>`. In the shared operator/Jarvis worktree, a pre-staged secret/source file gets swept in and pushed; `SENSITIVE_PATTERNS` only scans the `files` arg, never already-staged content. Fix: pathspec-scope the commit — `["commit", "-m", message, "--", ...files]` (or `--only`). Hardens all callers (`jarvis_dev`, autonomous-improvement). The new evolution-log-commit ritual does NOT use this tool (it's mechanical), so it's unaffected — but the tool bug remains for everyone else.

### B. WRITE_INDICATORS hardening (broader shell-guard gap, security)

The append-only gate on `RITUAL_WRITABLE_DOCS` lives inside the `WRITE_INDICATORS` regex loop in `src/tools/builtin/shell.ts`, so truncation primitives the regex doesn't capture skip it entirely and return `allowed:true`. Empirically-proven bypasses (round-2 qa-auditor, `.claude/agent-memory/qa-auditor/evolution-log-append-gate-audit.md`): `>|` clobber, `truncate -s 0`, `sed -i`, and **relative paths** (service cwd is the repo root, so `> docs/EVOLUTION-LOG.md` matches no absolute pattern). This is the SAME class as two-week-reflection #1 (narrow regex gates are structurally fragile) and affects ALL mission-control source write-protection, not just the log — a `truncate`/`sed -i` on any protected source slips the guard too. Scope: add `truncate`/`dd`/`sed -i`/`>|` to the write-detection (or `DENY_COMMANDS`), capture relative-path writes against the known cwd, and re-run the round-2 bypass list as tests. Do NOT whack-a-mole one primitive at a time — design the write-detection to be allowlist-shaped (deny writes to protected paths unless provably safe) rather than denylist-shaped. Deferred per 3-strike (stopped after 2 narrow-regex attempts on the log gate).

## Added 2026-06-17 — poison-detector / confabulated-tool-block follow-ups

Spun out of the `/diagnose` of "Jarvis seems forgetful / review tool failures." There are NO tool-handler failures; the "failures" are the router's `isPoisonedExchange` thread-purge stripping confabulated tool-refusals. 4 of 5 detections in 14d were Jarvis inventing a `don't ask mode` / `no tengo acceso` block for tools that ARE in scope + auto-approved (`shell_exec`, `mcp__supabase__query`, `gmail_send`). The detector-FP half (1 of 5) was fixed this session (Fix B, `router.ts` sentence-anchoring + `don't ask mode` signature). Two residual items — see memory `feedback_confabulated_permission_block_refusal`.

### A. Confabulated permission-block refusals — ✅ RESOLVED 2026-06-17 (was SCOPE, not persona)

`scope_telemetry` overturned the persona hypothesis: the tools were genuinely OUT of `tools_in_scope` (e.g. "Envía esto a javier@…" classified to `["intel"]`, `gmail_send` absent), and the confab was the model's explanation for a real gap. Fixed two layers (deploy=user): `scope.ts` shared `EMAIL_SEND_RE` (send verb + address → google) wired into BOTH the `DEFAULT_SCOPE_PATTERNS` rule (regex path) and the `scopeToolsForMessage` semantic-branch injection; `prompt-sections.ts` escape-hatch (name the missing capability so scope re-activates, never confab a block / dead-end — covers the gilda/`shell_exec` proper-noun case the scope regex can't safely match). 6 tests, qa PASS-with-W1-folded. Memory `feedback_confabulated_permission_block_refusal` (Root cause A RESOLVED section). **Residual watch (not blocking):** the scope classifier may misroute OTHER intents the same way (send/address was one instance) — if new confab classes appear, pull `scope_telemetry` first and add the analogous dual-path safety net rather than rewording the persona.

### A-original (superseded by the above). Confabulated permission-block refusals persist post-`bcb1894` (PRIMARY, behavioral)

The 06-16 persona fix (`bcb1894`: `confirmationSection` + poison patterns) REDUCED but did **not** eliminate the confabulation — **3 of the 4 true-positive strips occurred on 06-17, after that deploy.** The poison-detector strips the symptom from the thread buffer (so it can't reinforce), but the operator STILL SEES the refusal in real time — the strip only cleans future context, it doesn't un-send the message. This is the actual "tool failure" the operator notices: Jarvis declining in-scope tools and punting work back ("necesito que lo hagas tú"). Scope: strengthen the persona / tool-confidence so the model stops confabulating a `don't ask`/`no tengo acceso` gate for tools that are present + auto-approved under `permissionMode:"dontAsk"` — e.g. an explicit "if a tool is in your tool list it is callable; never claim it is blocked" clause, and/or a self-probe that lists callable tools before refusing. The detector is only the backstop; this is the cure. Measure via the write-time `Blocked poisoned exchange` log count trending to ~0.

### A-refinement (optional, from qa W1). Mid-clause self-error FN

Fix B anchored the self-error patterns (`error de configuración`, `problema técnico crítico/grave`) to sentence-start, which knowingly lets a genuine self-error phrased mid-clause through ("Hubo un error de configuración con Gmail, no pude enviar") with no sibling net. Accepted (over-stripping a real answer is worse). IF buffer-poisoning by mid-sentence self-errors is later observed, the middle-ground is a 1st-person/impersonal lead-in anchor (`(?:tuve|hubo|se produjo|hay) un …`) — but verify it does NOT re-match external descriptions (`"el cliente tuvo un error de configuración"`) before shipping. Tie-break toward root cause A (persona) over re-widening the regex.

---

## Deferred from 2026-06-20 monitoring/fix session (DENUE-chat watch)

Three fixes shipped this session (classifier lone-"repo" silent-save; shell-guard
blocks mc `.env` reads; overnight-tuning broadcasts directly instead of an LLM
"go send this" task). Residuals deliberately NOT done, in priority order:

1. **Cost-ledger blindness to chat/aux/streaming SDK calls** (P1, instrumentation).
   `cost_ledger` captured ~300 rows in 5 days while the journal logged 44
   completions in 10 min — the aux 0-tool + streaming calls bypass the ledger, so
   the $2/hr soft-cap can't see chat-driven burn (one DENUE turn = $1.08 / 1.5M
   tokens, unmetered). Route all SDK completions through `cost_ledger`. Same
   silent-state class as queue §3.
2. **Out-of-order / interleaved Telegram replies** (P2, UX). A 4-min streamed reply
   with an interim message posted mid-stream lands out of order (msgId 17361 sent
   before 17360 finalized). Suppress interim sends while a stream is in flight,
   and/or debounce rapid-fire messages per chat.
3. **DENUE-turn cost cap** (P2). Each turn reloads large `establecimientos` psql
   dumps into context ($1+/turn). Summarize/row-limit before it hits the model.
4. **shell-guard `.env` hardening** (P3, qa W3). Current guard = enumerated readers
   - literal mc path; misses `vim`/`python open()`/`while read`/`$HOME`/relative-cwd
     reads (the same limitation every sibling credential guard has). Broaden if a
     real bypass recurs.
5. **classifier W2** (P3, benign). A read task naming code ("explica el código")
   still sandboxes via the strong `código` signal — wastes a container, loses
   nothing. Low priority.

### Update 2026-06-20 (later) — fan-out chats → swarm WIRED + swarm validated

The "messaging router is blind to parallelism" gap is closed: `isFanOutTask`
(produce-verb AND per-item quantifier) routes a chat fan-out to **swarm**
(kill `MESSAGING_SWARM_ESCALATION`; → heavy if off). **swarm had 0 lifetime
runs — validated end-to-end first** (`scripts/validate-swarm.ts`, isolated DB
copy; PASS 3/3). NOT deployed (user-only). New follow-up:

- **W4 (UX, P3):** a swarm chat reply is the reflector **aggregate summary**, not
  the per-item sub-task outputs (those live in `parent_task_id` rows / saved
  artifacts). Fine for file-producing fan-outs; consider surfacing per-item
  results inline if a user expects them.

---

## Next session — system-hardening-sweep residuals (2026-07-05)

Sweep SHIPPED + deployed (`34fbb4f`→`a2e4da1`; plan `docs/planning/system-hardening-sweep-2026-07-05.md`; 6672 tests). **Residual close-out pass 2026-07-10** — see that date's PROJECT-STATUS entry for the eval-drift root cause (the headline outcome of this list):

**Operator actions:**

- ~~**Arm the eval-gate baseline**~~ ✅ **DONE 2026-07-06** (gate-native 60.35, `09ef105`) — then **RE-CAPTURED 2026-07-10**: the probe fix below invalidated it (60.35 was measured against a model that never saw the tools).
- ~~**`chmod 600 .env`**~~ ✅ **DONE 2026-07-10.**
- ~~**Delete stale `data/mc.db.bak-orphan-cleanup-20260526…`**~~ ✅ **DELETED 2026-07-10** (386 MB freed).
- **Quarterly `VACUUM INTO`** — ⚪ moot for now: freelist measured 35 pages (~140 KB) on 2026-07-10; the dead space got reused. Keep as a quarterly habit, no urgency.
- **AV key rotation** (pre-existing, blocks williams-radar) — still open.

**Code follow-ups:**

- ~~**Nightly eval drift 66.9→63.3**~~ ✅ **ROOT-CAUSED + FIXED 2026-07-10 — never a prod regression.** Since the 2026-05-10 SDK cutover, `infer()` on the claude-sdk path DROPPED `request.tools` (`queryClaudeSdkAsInfer` hardcoded `toolNames: []`, returned no `tool_calls`), so the eval's tool_selection (50% weight) measured "model never saw the tools" — pinned at an arithmetic silence floor (only `not_tools` cases pass). The visible drift was that floor decaying as case-miner's 90-day retention deleted old `not_tools` mined cases faster than mining replaced them. The 05-12 cliff (77.4→63.0) was the real break, absorbed as "new normal" for two months. Fixed with SDK selection-probe mode (tools passed as no-op stubs, first-turn tool_use returned as `tool_calls`); tool_selection 25.18→57.95 on the gate.
- **Resume auto-path is limited**: goals never persist as `FAILED` (failure is orchestrator-level), so `mc-ctl task-resume` needs an explicit `goalId`. To make auto-resume-from-failed-goal useful, persist goal-level failure status in the heavy runner (`dispatcher`/`heavy-runner` — writes `runs.goal_graph`). [[feedback_multi_agent_refactor_orchestration]]. — still open (capability work, not hardening).
- ~~**Mechanical-ritual silent-cease** (out-of-band wedge detector)~~ ✅ **DONE 2026-07-10** — `watchdog.sh` Check 9d: Prom query `time() - max(mc_ritual_last_success_timestamp)` > 1800s → Telegram. Detects a wedged scheduler externally even when in-process alert delivery rides the wedged process.
- ~~**scope.ts inert dead names** (audit I3)~~ ✅ **PRUNED 2026-07-10** (SOCIAL_TOOLS → tweet_* only; both tests updated, the scope.test negative retargeted to live names to stay non-vacuous).
- **container `--cap-drop=ALL` + `--memory 4g` watch** (audit I1): could bite a heavy nanoclaw build (npm postinstall `chown`, webpack OOM). The 8 working runs are small JS tasks. Loosen only if a real build fails. — watch-only, still standing.

## 2026-07-12 — V8.5 Phase 0+1 session follow-ups

**Context:** V8.5 plan committed (`d88ecdd`); Phase 0 shipped (`83b1fda`: security dep batch + `mc-ctl v83-promote`); Phase 1 shipped via 1b bisect (`6b271fd` + `41dc8a0`: SDK 0.3.207 + `settingSources: []` — the "regression" was the operator-config leak; full story PROJECT-STATUS 12a-c).

- ~~🔥 settingSources leak sweep across VPS SDK consumers (P1)~~ **DONE 2026-07-12** (3-lens `/sweep`): only ONE live leak — denue-data-analysis Sage provider (host tsx as root, both `query()` sites) — fixed + spec-pinned + deployed (`EurekaMD-net/eurekamd-denue-analysis` `3245676`). Pulso engine agent-runner's `settingSources: ['project','user']` is DELIBERATE nanoclaw design (containerized, HOME=/home/node, per-group synthesized `.claude` — no operator dotfiles reachable) and is dead code in the live CRM path anyway (entrypoint overridden to the OpenAI-compatible adapter). No `claude -p` CLI-spawn variants, no cron/systemd claude invocations anywhere. Residual: denue still on SDK 0.2.138 (works with `[]`; bump = separate deliberate action, no eval gate exists there).
- **claudeMd residual under isolation (P3)**: even with `settingSources: []`, a small `claudeMd` key appears in the turn system-reminder (deployed-config probe, 10.7k total — near-empty but nonzero). Identify its source (cwd repo CLAUDE.md? SDK scaffold?) during the sweep; decide if it matters.
- ~~Gate epsilon vs clean-population variance~~ **corpus ≥150 DONE 2026-07-12** (`c5a67b8`: tool_selection 55→172, incumbent re-baselined 65.75/35.43 on the harder mined-positive yardstick). REMAINING (P3): measure run-to-run variance at the new n (2-3 repeat gate runs, ~$5 each) before tightening epsilon below 2.0; also watch whether mined-positive noise (context-free replay of context-dependent tool sets) masks small regressions — if seed-only subscore diverges from mined subscore over time, consider seed-weighted gating.
- **Phase 4.4 remainder (P3, small — gap analysis done 2026-07-12)**: most Managed-Agents scheduled-run semantics already exist (schedule_runs = typed per-trigger records w/ status+result_summary; schedule.run_failed → operator notify; node-cron 4.6 DST; no-backfill is de-facto). Two narrow gaps: (a) a trigger that fails BEFORE task spawn writes no schedule_runs row (insertScheduleRun requires a taskId) — pre-spawn failures are invisible in the audit trail; (b) no auto-pause: a schedule that fails non-recoverably every tick keeps firing forever (only Telegram noise). Both are ~1h in src/rituals/dynamic.ts.
- ~~Shared-worktree concurrency (P1)~~ **FIXED 2026-07-12** (`54787ff`): Jarvis's mc git work isolated to the dedicated linked worktree `/root/claude/mission-control-jarvis` (jarvis_dev bootstraps; git_* tools reject the primary; shell denies worktree-state git on it). One-branch-one-worktree makes it structural.
- ~~Watchdog false-positive restart (P2)~~ **FIXED 2026-07-12** (`54787ff`): both restart paths require two failed probes 10s apart; health curl gains --max-time 10.
- **Operator decision pending — Jarvis communication style block**: Phase-1 isolation removed the operator's leaked no-sycophancy/no-emoji/concision rules that had been coaching Jarvis's tone for ~2 months; Jarvis now answers warmer/longer/with praise+emoji (e.g. task 7411). If the coached tone is wanted, port a deliberate style block into Jarvis's OWN system prompt (versioned, eval-gated) — the correct home per the isolation doctrine. Awaiting operator's pick of which rules apply.
- **WATCH — fast standard-tier now runs effort=medium (Phase 2.3, 2026-07-12)**: the gate's selection probe runs effort-unset, so this live-quality shift shipped gate-unverified. If fast-task delivery quality dips (STATUS parse failures, thin outputs, missed tool calls on routine chat), suspect this first — revert is the `tierToEffort` mapping in `fast-runner.ts` (standard→undefined restores SDK-default high). Side benefit to verify after a few days: standard-tier token/latency drop in cost_ledger.
- **Phase 3.1 cache-fix re-measure (blocker for that item)**: the cross-task cache-miss economics (~$754/mo estimate) were measured on the contaminated fat-prefix profile. 0.3.x changed everything: ~23k smaller stable prefix + 1h-TTL writes (2× write, 94-96% warm reads observed). Re-measure before choosing among the 3 drafted fix options — the problem may have shrunk or moved.
- **Operator note**: chat task 7379 ("Los errores siguen ahí. Diagnostica y corrige", 2026-07-12 03:23 UTC) died in the Phase-1 incident (npm swap under live service — `feedback_live_service_dep_swap`) and was never retried. Resend if still wanted.
- **Upstream (P3, optional)**: agent-SDK 0.2.x loaded filesystem settings with `settingSources` unset while its docs promised isolation-when-omitted; 0.3.207 types now document unset = load-all. Worth a GitHub issue for the 0.2.x docs/behavior mismatch if it still matters upstream.

## 2026-07-13 (late) — JME Phase 0 review + Jarvis coding-task walls removed

**Context:** Operator + Jarvis designed the **Jarvis Memory Engine** ("JME": episodic `jme_turns` + semantic `jme_facts`, hybrid Gemini-embed + FTS5 retrieval, per-category TTL) over Telegram (tasks 7473–7486, Mem0 evaluated and discarded); Jarvis then built Phase 0 solo (tasks 7487–7491). The code is good — independently verified 12/12 tests green + typecheck clean — but the run burned a watchdog kill, a full 55-turn budget, and three "Continúa" turns on **three structural walls, all fixed + deployed** (`d9187ae`, audit follow-up `e1c18bd`; qa-auditor verdict PASS-with-notes, no security regression — the local hook is typecheck+tests only and Jarvis's sensitive-file filters run pre-commit):

- `git_commit` (30s timeout) fired the pre-commit hook (full suite, ~3-5 min) → commit could NEVER succeed on `jarvis/*`. Now auto `--no-verify`, gated on the branch regex alone (audit finding: not on the GH token); CI on push/PR stays the enforced gate.
- `jarvis_dev action=pr` re-ran the suite it had ITSELF just passed (or cache-trusted) via the hook inside `GIT_TIMEOUT_MS=60s` → `--no-verify` on its commit.
- Stuck-task watchdog counts wall-clock from `started_at`; the nanoclaw→fast fallback inherited a 14-min-spent clock and was killed 2 min in → dispatcher resets `started_at` on fallback (documented tradeoff: the column no longer measures total task latency; dispatcher's own durationMs unaffected).

**Jarvis's JME branch (`jarvis/feat/jme-phase0` in the worktree — STAGED, not yet committed):**

- ⚠️ **Deploy-gate landmine:** his migration bumps `user_version` 2→3 but `scripts/deploy.sh` hardcodes the fresh-boot signature at 2 — the PR MUST update the gate expectation or the first post-merge deploy aborts.
- Branch base is stale (`24af61d`) — rebase onto main before PR.
- Minor: test file duplicates the migration DDL verbatim (drift risk — export a shared schema helper); `writeFacts()` doc says "single transaction" but it's a sequential loop.
- **Next:** operator tells Jarvis "Continúa" — commit + PR now take seconds. New Jarvis directive: `jarvis-kb/directives/coding-task-playbook.md` (phase-per-turn with durable checkpoints, commit-per-phase, scoped tests only, handoff note on low budget, 3-strike stop, migration⇒deploy-gate rule).

**Update 18:15 UTC — task 7493 ("Continue and land the pending PR") exposed two routing gaps:**

1. **Continuation-task nanoclaw misroute (P2):** a chat continuing prior HOST-runner work ("land the pending PR" — state = staged files in the host worktree, branch never pushed) was routed to nanoclaw AGAIN; the sandbox spent ~12 min / ~$1.5 rebuilding JME from scratch in its throwaway clone, failed its commit goals, and fell back. The foreign-repo guards can't fire (target IS mission-control); the gap is state-continuity: the sandbox can never see uncommitted host state. Candidate: classifier guard that keeps continuation phrasing ("continue", "land the PR", "lo que dejaste pendiente") + mc-coding on a HOST runner.
2. **Coding scope regex missed "PR" / English (P2):** the fast fallback ran with scope `[]` (29/217 tools) — "Continue and land the pending PR" trips no coding pattern, so git/shell tools were absent and Jarvis had to stop and ask the operator to activate them mid-task. Candidate: add `\bPR\b`/`pull request`/"land"+repo-noun to the coding scope patterns (both ES and EN).

Positive validations in the same run: dispatcher fallback + `agent_type='fast'` reattribution worked; the write-guard blocked Jarvis's attempt to patch the pre-commit hook (his plan was stale — recall surfaced the pre-fix conversation, not the new playbook); hook verified intact.

**Update 21:40 UTC — the 18:30/21:30 gate deaths root-caused (wall #5, the real one): self-killing sync gate.** `actionTest` ran the suite via `execFileSync`, freezing mission-control's event loop ~2 min → `/health` HTTP 000 on both watchdog probes (10s apart, both inside the freeze) → watchdog `systemctl restart` → cgroup SIGTERM killed the vitest child → reported as "vitest exceeded 300s" at ~107s. Watchdog log 21:30:02 is the smoking gun; the 18:30 "timeout" and the unexplained pid change (2796197→2850711) were the same event. Fixed `3cd8acd`: gate now awaits `promisify(execFile)` — event loop, health endpoint, and Telegram stay live during the suite. (`8cd6e66` maxBuffer 1MiB→32MiB was shipped first on a plausible-but-wrong hypothesis; kept — 774KB routine stdout vs 1MiB default was a real adjacent landmine, and its honest code/signal message is what disproved itself and exposed the watchdog.) Residual (P3): `actionPr`'s git push still blocks sync up to 60s — a slow network push could straddle one probe cycle; convert if it ever fires.

## 2026-07-13 (night) — JME Phase 0 audited: merge-ready; Phase-1 gate list

Tier-2 audit (R1 adversarial + R2 adjacent + R3 verdict) on PR #27 + independent E2E (migration v3 on a live mc.db snapshot: clean, idempotent, byte-identical to fresh boot; real-module harness 8/8). **Verdict: PASS WITH WARNINGS, CLOSURE-READY** — full findings in the PR #27 comment. Scoreboard: 0 Critical / 8 Warning (all bundle-authored) / 4 Info; 0 pre-existing repo bugs; production FTS/bm25 sites all verified CLEAN (R2 probes — sqlite-backend, jarvis-fs, v8-2 critic/decompose).

**Phase-1 GATE (trigger: before ANY code imports `src/memory/jme.js` outside tests):** fix W2 bm25 ranking inversion (copy sqlite-backend.ts:490 `abs/max` pattern) · W1 FTS5 bare-operator syntax errors (lowercase-before-filter like extractKeywords) · add `recall_audit` rows (source='jme') on every queryMemory call incl. failure branches · W7 getTurnsForTask oldest-vs-recent · W6 factsExpiringSoon window · W3 writeFacts doc. Watch-only: vector LIMIT 500 (alert at facts>400) · jme_turns unbounded (needs retention policy decision in Phase 2) · migration runner not per-migration transactional (comment before the next non-idempotent migration).

**Honesty finding (author-reporting, not code):** PR body claimed "fallos pre-existentes en email.test.ts y scheduler.test.ts — confirmado igual en main" — FALSE (93/93 pass in both trees; the 17 gate failures were load flake). Same class as [[jarvis_default_direction_discipline]]: gate FAIL → rationalized + shipped without verification. Playbook rule added.

## 2026-07-14 (early) — JME Phase 1 MERGED + DEPLOYED (PR #28, merge 044a494, pid 3591807)

**Audit scoreboard (Tier 1 on the wiring):** R1: 1 Critical (30s embed stall on every chat turn — fixed with withTimeout 1.5s) + 3 Warnings (owner-filter silent-dead warn-once · 2k lastMsg cap · router tests) + cleared-with-probes (no double-write, no cardinality issue, cache placement correct, transaction async-safe). R2 on the fixes: MERGE-READY, 0C/0W, 5 Info (post-timeout rejection proven safe empirically; specs proven non-vacuous). Fix authorship: Jarvis's orphaned container pushed 02fc208 (all 4 fixes) AFTER the watchdog killed its task — operator-authorized repair d8f01e4 fixed its 2 test bugs (fake-timer flush, mocked-dispatcher task id).

**LIVE from pid 3591807:** operator-telegram turns → jme_turns; JME facts injected ≤1,500 tok (cache-safe, 1.5s fail-fast); recall_audit source='jme' accumulating. **7-day utility clock started — success bar: ≥39% was_used; demote bar: <20% AND >50% latency tax** (same yardstick as Hindsight). Check anytime: `mc-ctl db "SELECT COUNT(*) FROM jme_turns"` + `mc-ctl recall-utility` (jme rows appear after the operator's next Telegram exchange).

**New routing-gap variant (fold into the queued P2 classifier item):** watchdog kills a >15min nanoclaw TASK while the CONTAINER keeps running → dispatcher fallback never fires (only triggers on a returned no-op) AND the zombie can still push to origin minutes after the task is dead (happened twice tonight: 7503's divergent push, 7507's post-mortem fix push). Ghost pushes landed fine tonight but are un-reviewed-by-anyone writes from a dead task — the P2 fix should also reconcile watchdog vs container lifetime (kill the container with the task, or give tasks with a live container a longer leash).

**Phase 2 next (Jarvis, own session):** consolidator + dedup/supersede + jme_turns retention decision + W3 echo-amplification guard (consolidator must ignore [JME MEMORY] restatements) — per jarvis-kb/projects/agent-controller/jme-plan-v2.md.

## 2026-07-14 — V8.5 Phase 4 CLOSED: 4.6 §17 no-verdict reminder + 4.7 excelente→flywheel auto-bridge (`a758454`, pid 3672552)

**4.6 (`src/briefing/no-verdict-reminder.ts` + 20:00 MX cron):** delivered brief still `pending` in the evening → ONE reminder with the exact whole-message vocabulary (`"sirve"`/`"descarta"` — quoted copy-paste resolves, VERDICT_STRIP_RE strips punctuation). Send-once guard = `safeguard_state[no_verdict_reminder_last_briefing_id]`, recorded only on ≥1-channel delivery (zero-delivery doesn't consume the brief's one shot). Directly feeds the §17 ruled-only denominator → §14 → L2 arming chain. First live fire: tonight 20:00 MX if today's brief goes unruled.

**4.7 (`src/tuning/flywheel-bridge.ts`, hooked at router `recordFeedbackWindowSignal`):** explicit positive (= "excelente", sole eval word) auto-pins the praised task's scope_telemetry as `flywheel-auto-<task_id>` (weight 1.0, source='flywheel', retention-exempt, INSERT OR IGNORE = double-praise safe), gated by the miner's focused-run filters (1-3 distinct tools, ≥5 words) so automatic weight-1.0 ground truth stays clean. Skips are logged with reasons, never silent.

**Audit scoreboard (Tier 1):** R1: 0 Critical / 2 Warning (unbounded retention-exempt growth; manual-pin namespace collision) / 3 Info — all folded. R2 on the folds: PASS, 0 Critical / 1 low Warning (fail-open COUNT — folded: fail-closed except missing-table) / 2 Info (ceiling_reached-masks-already_pinned label, documented deliberate; manual-pin ceiling exemption → >200 corpus-drift warn added). Prod-data regression probe clean (1 existing flywheel row uncounted under both old and new predicate). Bundle-regression catches: 3; pre-existing bugs: 0. No eval gate needed (no model/prompt/tool-description change).

**Watch:** first auto-bridge fire = next "excelente" on a task with a feedback window (`mc-ctl db "SELECT case_id, mined_from FROM mined_test_cases WHERE mined_from LIKE 'flywheel:excelente:%'"`). Deferred by design: manual `flywheel:task:%`/`flywheel:manual` pins stay ceiling-exempt; tripwire = nightly `[case-miner]` warn when active tool_selection >200.

**V8.5 remaining:** Phase 6 task-trace observability (next medium block) · Phase 5 arming runway (own sizing session) · cache-diag verdict ~07-16 (then remove `cache-diag.conf`) · tool_selection 32.30-vs-35.43 watch.

## 2026-07-14 (late) — V8.5 Phase 6 SHIPPED: task-trace observability (`f264d75`, pid 3754043)

**What exists now:** `task_trace_events` timeline per task_id — `task.started → tool.called / turn.completed → task.completed|failed`, plus `task.fallback` and `task.watchdog_failed` as distinct marks. Per-turn tokens (incl cache) + wall-clock latency at the queryClaudeSdk seam (fast runner; container runners fall back to lifecycle + runs summary). `mc-ctl trace <task_id>` renders the replay with a mid-flight forensic line ("trace ends at round N tool X") for dead tasks; `mc-ctl verdict-rate` gives §17 weekly + acceptance-window counts (labeled raw status, NOT the 6a metric). 30d retention on the daily tick. OTel-shaped single seam (`emitTraceEvent`) — Langfuse/exporter is a bolt-on if ever needed.

**Live E2E verified:** smoke task `c09ed268` traced end-to-end (started/flash → turn 1: 26,711 tok in, 3.2s → completed $0.161).

**Audit scoreboard (Tier 1):** R1: 0 Critical / 4 Warning (hot-path write amplification; warn spam; verdict-rate surface skew; untested dispatcher wiring) — all folded (prepared-stmt WeakMap cache + 60s throttle + exactly-one-terminal dispatcher specs). R2 on the folds: PASS — W1 cache verified airtight against the closed-handle silence class; **R2 caught R1's own W3 being mis-premised** (§17 6a is surface-agnostic; morning filter reverted, footer made honest). Bundle-regression catches: 5; pre-existing bugs: 0. No eval gate (no model/prompt/tool-desc change).

**Deferred (documented, no trigger needed):** reflection invocations write trace rows without a tasks row (30d prune bounds it); required-tools early-return terminals omit tokens/cost (consistent with the cost path); dispatcher test file's fire-and-forget async leak (pre-existing, masked).

**V8.5 remaining: Phase 5 ONLY** (Rule-of-Two audit, approval windows, shadow-Git (a)-lite — needs its own sizing session). Watches: cache-diag verdict ~07-16; tool_selection 32.30 band; §17 accrual (reminder live tonight 20:00 MX); JME 7-day clock.

## 2026-07-14 (wrap) — container image current + JME clock caveat + queued follow-up

- **Container image rebuilt from main 05:20** (post-Phase-6): nanoclaw/containerized-heavy now carry JME + traces + 4.6/4.7. Orphan `nanoclaw-agent:latest` (07-08, SDK 0.2.34, unreferenced) deleted. **Standing rule: deploy.sh does NOT rebuild the image** — after merging runner-relevant code, `docker build -t mission-control:latest .`; drift check = `docker images` CreatedAt vs last merge.
- **JME clock caveat for the 07-21 readout**: was_used = 0 BY CONSTRUCTION until Jarvis's Phase 2 consolidator writes facts (turns ARE captured — 10 on day 1 — and recall fires every chat turn). Judge utility from the date facts first exist.
- **Queued (small, P3)**: thread `trace: {taskId, runId}` through the 3 Prometheus queryClaudeSdk call sites (planner/executor/reflector) so heavy tasks get per-phase timeline depth — same pattern as fast-runner, ~30 min. Trigger: before Phase 5 sizing, or first heavy-task post-mortem that needs it.

## 2026-07-14 (incident) — node-cron 4.6 silent skips + verdict floor (pid 423876)

**Incident:** Phase 0's node-cron 3→4.6 (07-12) brought v4's `missedExecutionTolerance` default of 1000ms — a fire the event loop is blocked past by >1s is SKIPPED, not delayed. signal-intelligence's SDK spawn blocks the loop ~2s at 06:00 MX → morning-surface missed daily since 07-12 (no brief 07-12/13/14; 15+ other slots also skipped). Operator's bare "Sirve" then had no brief to resolve, fell through the intercept to the chat LLM, and Jarvis read it as a go-ahead → implemented JME Phase 2 uninvited (commit `5843f60` in his worktree, claimed "Registro el §17" falsely).

**Fixes:** (1) `src/lib/cron.ts` `scheduleCron(id, expr, fn, opts)` — 60s tolerance + `execution:missed` → `schedule.run_failed` phase `"missed"`; ALL 22 cron.schedule sites converted; CLAUDE.md invariant added. (2) Verdict floor in `interceptBriefingVerdict`: pure verdicts ALWAYS consumed — deterministic "No hay brief pendiente que resolver" when nothing is resolvable; a verdict token can never reach the chat LLM.

**Audit scoreboard (Tier 1):** R1 PASS-with-warnings: 0 Critical / 2 Warning (DB-outage floor message tradeoff — comment folded; missed-alert fan-out — accepted as intended signal). R2 PASS: 0 new findings; verified replacement path strictly louder than stock node-cron, DECLINE-side floor inert on triage/§17 state. Bundle-regression catches: 0; pre-existing bugs surfaced: 2 (the dep-bump semantics change; the intercept fall-through). No eval gate (no model/prompt/tool-desc change).

**Watches / follow-ups:**

- **Verify tomorrow's brief**: 06:00 MX 07-15 must produce a `proposed_briefings` row + delivery (`./mc-ctl db "SELECT briefing_id, delivered_at FROM proposed_briefings ORDER BY id DESC LIMIT 1"`). §17 earliest pass shifts ~07-20 → ~07-23 (3 lost fuel days; no backfill possible — delivery needs the in-process router).
- **Known residual (I1, pre-existing, shrunk)**: user dynamic schedules can still skip when the loop blocks >60s (poller matches wall clock, no replay) — now observable via `dynamic-schedules-poller` missed alert. Trigger to act: a missed-alert actually firing in prod.
- **NEXT SESSION (operator-approved): salvage review of Jarvis's JME Phase 2 commit `5843f60`** (`/root/claude/mission-control-jarvis`, upsertFact + consolidate, no PR) — audit against jme-plan-v2.md (dedup/supersede, echo-amplification guard, retention) before any merge.

## 2026-07-14 (late-2) — Jarvis Phase 2 R1 audit FAIL + self-dev classifier guard shipped (pid 626907)

**JME Phase 2 end-to-end audit (branch `jarvis/feat/jme-phase2-consolidator`, 3 commits, 480 LOC): R1 FAIL — PR blocked.** 3 Criticals: C1 `pruneStaleTurns` permanent no-op (ts in ms vs `unixepoch()` seconds; the test masks it by injecting seconds); C2 dedup thresholds (cosine-calibrated 0.85/0.95) applied to the FUSED hybrid score — proven live: with embed down, an unrelated fact sharing one keyword gets `skipped` (silent data loss); C3 per-message consolidation masquerading as session-close (one Haiku call per operator message, turns deleted per-exchange — session accumulation structurally impossible). Warnings: anti-echo regex INERT (wrong literal `[JME MEMORY]` vs real header + wrong surface — assistant paraphrase unaddressed); supersede-by-text-equality; confidence unclamped; FTS-only path untested. Refuted: his dedup tests are NOT vacuous; router rejection handling OK. Findings written to `jarvis-kb/projects/agent-controller/jme-phase2-review-2026-07-14.md` (syncs to his store 19:10). **PENDING OPERATOR DECISION (C3): consolidation granularity** — recommendation: nightly batch (drop the router hook, consolidate all unconsolidated turns in the nightly ritual, 1 Haiku/day, full-day context). Jarvis fixes C1/C2/W* after the ruling; THEN R2 on his fixes; THEN PR.

**Classifier guard shipped:** `referencesJarvisSelfDev()` excludes Jarvis self-dev chats (markers: `mission-control-jarvis`, `jarvis-kb`, `jarvis/{feat,fix,chore,docs,test}/`, `jme`, `jarvis_dev`) from BOTH nanoclaw gates — kills the 10-min sandbox detour (tasks 7529+7532). R1 audit PASS-with-warnings (stale-comment fold + safe-degradation spec added; FP/FN directions both probed safe — FPs land on host runners, FNs still rescued by the no-op fallback). Classification eval 100/100 (dry). R2 skipped deliberately: folds were comment+test only; R1 already executed live regex probes both directions — recorded honestly here.

## 2026-07-14 (late-3) — JME Phase 2 MERGED (`fee4203`, pid 708929)

Operator ruled: fix findings in-session + merge (C3 = nightly batch). All 3 R1 criticals + W1-W4 fixed on Jarvis's branch (`76494d9`, `2f38de4`); R2 PASS pre-merge (its empty-response finding folded). Consolidator now = `jme-consolidate` cron 02:45 MX. Jarvis notified via NEW KB file `jme-phase2-merged-2026-07-14.md`.

**Watches:** first consolidation run 07-15 02:45 MX (`grep jme-consolidate` + `jme_facts` count); JME utility clock measures for real once facts exist; morning brief 07-15 06:00 MX (cron-fix proof point). **Queued P3 (cost, not correctness):** upsertFact double-embed (once in upsertFact + once inside queryMemory) — trigger: Phase 3 work or embed-cost review. **R2 W3 noted:** DB-down during the cron could double-alert `jme-consolidate` (self-emit + cron catch) — likely collapsed by notify-once; glance if it ever fires twice. **Dead export:** `getTurnsForTask` has no production caller (tests only) — leave, it's Jarvis's Phase 1 API. **Container image NOT rebuilt** (jme/scheduler/router run host-side; nothing runner-relevant changed).

## 2026-07-14 (late-4) — @mexiconecesario 2-day tweet outage: playwright browsers missing (pid 730547)

Third silent casualty of the Phase 0 dep batch (after cron skips + image drift): playwright 1.61.1 bump required chromium_headless_shell-1228; `/root/.cache/ms-playwright/` topped out at 1224 — nobody ran `npx playwright install`. Schedule fired on time both days; `browserType.launch` failed identically (07-13 + 07-14, warn-level only). FIXED: browsers installed; today's tweet re-fired via temp cron nudge → **published** (tweetId 2077113007931314307), calendar self-marked `Sí (2026-07-14)`; 13-julio stays honestly unpublished (operator ruling: today only). Cron restored `0 13 * * 1-5`. **X probe ARMED** (`x-probe.conf`, 09:00 MX — its `withAuthedRequest` does a real `chromium.launch`, so it catches both missing binaries and cookie expiry 4h before the tweet slot). **Rule: any playwright/browser dep bump ⇒ `npx playwright install` + one live launch in the same session.**

## 2026-07-14 (late-3) — AV free-tier adaptation + radar Polygon cutover + VPS sweep (pid 888082)

**Shipped:** mc `8bfaae0` (AV 4/min + 22/day dual-window limiter w/ 24h boot seed; Polygon-primary daily+intraday dispatch; free TIME_SERIES_DAILY; watchlist ceiling 4,608). Radar `2bab4b5` (fetchTicker = cache → Polygon → AV-fallback, 13s pacing, holiday-week snap w/ 5 tests; scaffolds+harnesses `e83f736`). Key verified in radar service env (operator appended; probe authenticated). VPS sweep freed ~34GB (docker builder 31.5G, npm 3.5G, snaps 1.4G, go 770M, apt 520M, 06-28 backup pair) + dead-Grafana wiring removed (Caddy block, UFW 3001/9001) + swap flushed.

**Watches:**

- **Fri 07-17 ~18:00–19:25 MX — W29 first Polygon scan**: expect ~85 min (was ~10); compare Telegram summary vs W28 — pricePercentile/ranging recalibrate one-time on dividend payers (AO/AC proven identical). Check `journalctl -u williams-radar` for polygon errors / AV-fallback lines.
- **AV daily quota in practice**: `./mc-ctl db "SELECT status, COUNT(*) FROM api_call_budget WHERE provider='alpha_vantage' AND call_time > datetime('now','-1 day') GROUP BY status"` — rate_limited rows should drop to ~0 now that Polygon is primary. Weekly bars in mc still ride AV (Polygon weekly unimplemented in data-layer) — if AV weekly starts starving Jarvis's occasional weekly queries, implement Polygon weekly in `fetchWeeklyDispatch` (fetch-polygon.ts in the radar repo is the reference).
- **Operator-run cleanup still pending** (classifier requires the user to name shared/stateful targets): `rm -rf /tmp/tool-search-val-* /tmp/sdk-tool-vis-*` (3.9G, 07-12/13 experiment dirs) and `docker volume rm $(docker volume ls -f dangling=true -q)` (17 orphans ~1.6G incl. dead grafana-data). Candidates awaiting a ruling: qmd/bun (~1.1G, zero usage traces) and crm-hindsight image (6.4GB + ~300MB RAM — Hindsight demoted, but project_hindsight_fireworks_followup still open).

**Carry-over watches from 07-14 earlier entries remain live:** 02:45 MX first jme-consolidate; 06:00 MX brief on the fixed cron path; 09:00 MX first X-probe tick; 13:00 MX tweet; 20:00 MX no-verdict reminder should stay silent on a ruled day.

## 2026-07-15 — MCP isolation leak closed: strictMcpConfig (`4a5f3ca`, pid 2520693)

**Shipped:** operator MCP servers (`/root/claude/.mcp.json` playwright/supabase, claude.ai Gmail/Drive connectors, sequential-thinking) were merging into every Jarvis SDK subprocess — `settingSources: []` covers settings files only, NOT MCP config. Surfaced as Transhumanismo (delivery=telegram) shipping "no tengo herramienta de Telegram" complaints inside its deliverables since tool search armed (07-13): agents hunted for delivery tools, found the leaked connectors (07-14 even ATTEMPTED `mcp__claude_ai_Gmail__create_draft`; held by the allowedTools deny), found no Telegram, and narrated the failure into the broadcast text. Fix: `strictMcpConfig: true` (spec-pinned) + telegram deliveryInstructions hardened at both dynamic.ts sites. Live probe: jarvis-only manifest. **eval:gate PASS 66.65 vs 65.75 — tool_selection 36.88 resolves the Ph3.2 WATCH (32.30 was the leak, not tool search).**

**Watches:**

- **Next Transhumanismo run 12:00 MX**: expect clean content, `completed` (not `_with_concerns`), no ToolSearch delivery-hunting in toolCalls. Same check applies to the other 4 telegram-delivery schedules (Williams Journal, MexicoNecesario, Morning Sync, Química Básica).
- **Cache-diag window restarted AGAIN 07-15** (`4a5f3ca` changed the tool block; the 07-13 window's ~07-16 verdict is contaminated — discard it). New verdict ~07-18.
- The eval-baseline incumbent stays 65.75; candidate 66.65 was a gate check, not a re-baseline. If the next legit tool-surface change gates against 65.75 and the WATCH math matters, consider `--update-baseline` then.

## 2026-07-17 — JME Phase 3 merged + turn-exhaustion class closed (`a9fc14b` + `c7cfb06`, pid 1689123)

**Shipped:** JME Phase 3 (temporal dedup keep-newest, ceiling warn 400, `mc-ctl jme-stats`) merged via PR #30 after relanding the deduplicateFacts cluster fix (`a81e160` — Jarvis's own fix ran in nanoclaw and evaporated in the sandbox clone while self-reporting "completed"). Turn-exhaustion root cause: `gh_create_pr` had no `cwd` (ran in DEFAULT_CWD=cuatro-flor) + token-push set no upstream → unwinnable endgame thrash; fixed `c7cfb06` (cwd + --head + set-upstream), eval:gate PASS 66.87 (+1.12; tool_selection 37.41). #29 consolidator fix PROVEN in prod (120 turns → 8 facts; first was_used=1 hits). OAuth outage 16:26–16:31 UTC (dead refresh token; /login fixed).

**Queued (systemic, not yet built):**

- **Nanoclaw work-landing check**: an mc-coding task can "complete" in the sandbox with commits that never reach host/remote (task 595947aa). Needs a structural gate like the TARGET_NOT_IN_SANDBOX sentinel — e.g. worker verifies `git ls-remote` shows the claimed branch tip before reporting success, else `success:false`. Also decide whether chat-initiated mc-coding should route to nanoclaw at all now that the worktree+jarvis_dev host path exists.
- **Nanoclaw PlanParseError 15:12 07-17**: single unexplained instance (host auth was healthy; 16:26 twin was the OAuth outage). If it recurs, pull `LLMJsonParseError.rawSample` from container logs before theorizing.
- **Coding-playbook repo map**: ~7 turns of adcda0f2 went to hunting mc-ctl's location. One line in `jarvis-kb/directives/coding-task-playbook.md` ("mc-ctl is at repo root; tests colocated `src/**/*.test.ts`") kills that class.

**Watches:**

- **JME utility readout ~07-24** (7d from first facts 07-17): `./mc-ctl jme-stats` — success ≥39% was_used; demote <20% AND >50% latency tax. Nightly consolidator + dedup log line `[jme] temporal dedup: N → M` appears once same-topic fact versions accumulate.
- **Next fast-runner coding task**: expect commit→push→PR chain in ≤3 turns (no thrash); `mc-ctl trace <id>` histogram is the check.
- **Cache-diag (window restarted 07-15, verdict ~07-18)**: `c7cfb06` changed a DEFERRED tool description — coding-scope prompt lines perturbed; chat-scope lines (the main measurement) untouched. Read the verdict stratified by scope.
- strictMcpConfig watch RESOLVED: 07-16 Transhumanismo `completed` clean (07-14/15 were `_with_concerns`).
- Incumbent stays 65.75; candidates 66.65 (07-15) and 66.87 (07-17) were gate checks, not re-baselines.

## 2026-07-18 — Enhancer ASK enforcement + alias guard + W29 validation + §17 KB sync (pid 2338165)

**Shipped:** (1) `980297f` prompt-enhancer: CIRICD ASK rule enforced in CODE (ASK requires `risk==="high" && clarity<4 && context==="unresolved"`, else downgrade → ASSUME/PASS with an `ASK downgraded` log line), risk redefined (own-chat sends = low; high = irreversible/third-party/spend only), off-topic context ignored. Motivation: 16/16 ASKs operator-skipped over 30d (0% utility). Live-replay validated (3 real FPs → PASS; destructive control still ASKs). (2) `078cd16` dispatcher: `getForeignProjectNames()` folds `projects.config.aliases`; williams-entry-radar aliases `["williams","radar","journal","thewilliamsradar"]` registered in DB — closes the cf40a528 "Journal W29"→nanoclaw misroute (message named the project only colloquially). (3) Williams publish schedule 18:30→20:00 MX (`scheduled_tasks` 84ccc541) — it raced the now-85-min Polygon scan. (4) KB sync via `upsertFile` (4 files: plan-2027 pulso copy, williams README + scan-window rule, azteca-fsd copy, canonical pulso README → `192b97f`) attacking §17's unfixable 21.4% / resolver 93.8% blockers.

**Operator actions pending:**

- Re-send the W29 publish request to Jarvis ("Escribe el comentario del analista para el Journal W29 y publícalo siguiendo el SOP (publish-journal.mjs)") — routing is now alias-safe.
- Keep ruling the 06:00 brief with exact `sirve`/`descarta` — §17 acceptance needs ≥3 of each (currently 1/1).
- Carry-over: `/tmp/tool-search-val-* /tmp/sdk-tool-vis-*` (3.9G) + dangling docker volumes (~1.6G) cleanups.

**Watches:**

- ~~**Enhancer**~~ RESOLVED 2026-07-20 by REMOVAL (`cfbbb9e`): post-fix data settled it — 3 rule-conforming ASKs fired 07-20, operator skipped all 3 within seconds (lifetime: 13/13 skipped since June, `buildEnhancedPrompt` never ran once). Root cause architectural: the gate keys off the aux LLM's own risk/context self-scores, which are unreliable. Module + router interceptors deleted; `PROMPT_ENHANCER_ENABLED` inert.
- **§17 re-check ~07-25**: `./mc-ctl judgments` header — unfixable/resolver should trend toward thresholds as new judgments cite the synced KB; acceptance count with rulings.
- **Fri 07-24 20:00 MX**: first publish-schedule fire at the new time — must find `last-run.json` already stamped W30.
- Carry-over: JME utility readout ~07-24 (`jme-stats`, ≥39% was_used); cache-diag verdict ~07-18 (read scope-stratified); next fast-runner coding task ≤3 turns to PR.

**Queued (systemic, unchanged from 07-17):** nanoclaw work-landing check (`git ls-remote` gate); nanoclaw PlanParseError single instance; coding-playbook repo map one-liner.

## 2026-07-20 — Prompt enhancer REMOVED (pid 3145317)

**Shipped:** `cfbbb9e` (−1,118 lines) + `86038a2` (docs). Full diagnosis + rationale in PROJECT-STATUS 07-20 entry: post-fix ASK acceptance 0/3 (skipped within 5-9s), lifetime 13/13 skipped, `buildEnhancedPrompt` never executed in production, ~4-6s blocking aux LLM round on every ≥40-char inbound. Third strike on the fix class → removal per 3-strike rule. Deleted module + test, both router interceptors + `SKIP_ENHANCER_RE`, four protected-path entries, stale comments. `PROMPT_ENHANCER_ENABLED` inert; `AUX_HAIKU_ENABLED`/scope-classifier untouched. qa-auditor 5-check sweep: PASS clean, zero findings. Suite 6,949 green.

**Behavior notes (operator-visible):**

- Substantive Telegram/email messages now dispatch immediately — no analysis pause, no "🔍 Antes de proceder" questions, no "💡" assumption notes.
- "enhancer on/off/estado" is no longer a command — such text becomes a normal task.
- Truly ambiguous destructive requests are now judged by the downstream agent (which has the confirmation flow for high-risk tools) — the pre-dispatch gate is gone by design.

**Operator actions pending:** unchanged from 07-18 (W29 publish re-send; `sirve`/`descarta` rulings; /tmp + docker-volume cleanups).

**Watches:** unchanged from 07-18 minus the resolved enhancer watch (§17 re-check ~07-25; Fri 07-24 20:00 MX publish fire; JME utility ~07-24; cache-diag verdict; fast-runner ≤3 turns to PR).

## 2026-07-20b — DCB closed + citation-grounding fix (pid 3320604)

**Shipped:** `e395568` — `sourceGroundingSection()` (citation anti-fabrication, active when web_search/web_read in scope; eval:gate PASS 66.69 vs 65.75). Full rationale in PROJECT-STATUS 07-20b. KB paper `prompt-enhancement-redesign-2026-07-20.md` fact-checked + rewritten + CLOSED (Phase 0 gate failed: 1 context-poverty event/31d vs <2/wk; operator-ratified — do NOT build DCB phases unless the re-open trigger fires).

**Watches:**

- **Citation grounding (live behavior):** next research-backed writing task — every reference must carry an in-conversation URL or an explicit "(sin verificar — de memoria)" tag. If fabricated citations recur WITH the rule live, escalate: extend the community-reply audit gate to KB-bound artifacts.
- Carry-overs unchanged (§17 re-check ~07-25; Fri 07-24 20:00 MX publish fire; JME utility ~07-24; cache-diag; fast-runner ≤3 turns).

**Queued (new):** task 7755 anomaly — 13-min silent stall before `task.fallback` rescued it (21:56:50→22:09:44 UTC 07-20, zero trace events in the gap); diagnose what the primary attempt was doing and why no trace/timeout fired sooner.

**Queued (systemic, unchanged):** nanoclaw work-landing check (`git ls-remote` gate); nanoclaw PlanParseError single instance; coding-playbook repo map one-liner.

## 2026-07-22 — PDF-reading fix: attachment persistence + scope reachability (pid 1211610)

**Shipped:** 4-piece bundle (eval:gate PASS 66.43 vs 65.75) — telegram attachments persist to `/tmp/jarvis-downloads`; research regex matches filenames; google group carries `pdf_read`/`gemini_*` deferred; scope-classifier null fallbacks now logged. Full detail in PROJECT-STATUS 07-22. Root cause of note: **deferred ≠ reachable — ToolSearch searches only in-scope tools**; scope is the hard wall.

**Watches:**

- **PDF flow (live behavior):** next PDF the operator sends (Telegram or Drive link) must reach `pdf_read` → (if imageOnly) `gemini_upload`+`gemini_research` with NO shell improvisation. Check `task_trace_events` for the task: expect `pdf_read`/`gemini_*` tool.called rows, no pdftotext/pdftoppm shell_exec.
- **scope-classifier null rate:** `journalctl -u mission-control | grep scope-classifier` — the 02:15 incident silent-null is now visible; if nulls are frequent (>~10%/day), diagnose the classifier (timeout? unparseable format?) as its own item.

**Queued (small):** `/tmp/jarvis-downloads` has no reaper — slow accumulation (20MB cap/file, owner-only ingress; qa W2 residual). Fold a reap into the daily retention tick when convenient. Also noted: a PDF with `mime application/pdf` but no `.pdf` in the filename still misses the regex-fallback research scope (qa I2, rare — semantic classifier usually covers).

## 2026-07-27 — Graded-down heavy tasks deliver their answer (pid 3436394)

**Shipped:** 3-piece fix + 5 audit folds — heavy tasks whose goals ALL completed but whose reflection score fell below the 0.8 gate now promote to `completed_with_concerns` and deliver with a "⚠️ Completado con reservas" caveat; plain-`failed` heavy tasks with an extractable deliverable send it honestly framed instead of "No pude completar eso"; failed tasks persist `output`. Full detail in PROJECT-STATUS 07-27. Root causes of note: **grading ≠ execution — a reflector discount is not a failure when the deliverable exists**; and the failure path is a delivery path too (never discard produced work at the last hop; the memory retained "[Task failed] Unknown error" while the report existed).

**Watches:**

- **First promoted heavy task:** expect `completed_with_concerns` + caveat-prefixed answer on the operator channel, `runs.runner_status='DONE_WITH_CONCERNS'`. If a heavy task still lands `failed` with a real report in `tasks.output`, the promotion condition missed a shape.
- **Canary detector wake (qa I1):** `rituals/canary.ts`'s `failed`-status arm was dead (failed rows had `output=NULL`) and is now live — `deliveryMisses` may start counting; if `>2/24h` alerts fire, re-baseline the threshold rather than assume a delivery regression.
- **Recall-tier shift (qa I5):** promoted tasks retain as `outcome:concerns` (recallable, −0.05) instead of `outcome:failed` (dropped). By design; watch that unverified-criteria content doesn't pollute recall precedents.

**Queued:**

- **scope-classifier TIMEOUT rate — now its own item (07-22 watch tripped):** 14 `Classify failed → regex fallback: scope classifier timeout` lines on 07-27 alone. Every timeout degrades scope precision (regex fallback widened this task to 124 tools). Diagnose the classifier's timeout budget vs its actual latency distribution (aux Haiku call — provider slowdown? timeout too tight?).
- **pgvector 504 during task e6f3dfa0** (22:36:02) — KB vector search failing degrades goal executors into find/grep improvisation (feeds the looping penalty). Check supabase/pgvector health + the search's timeout/retry posture.
- **Command-layer `sendToChannel` replies** (router.ts ~1290, ~1693) predate the community write-gate and may carry LLM text on command flows; audit whether any command path is reachable from community email channels (sweep note from qa C1, out of scope this session).

## 2026-07-28 — Planner workload sizing (pid 3519028)

**Shipped:** PLAN_SYSTEM/REPLAN_SYSTEM workload-sizing rules (batch goals of 3-5 items, mutual independence, no ALL-quantified criteria, overflow + small-task floor) + checkReplan timeout vote (a timed-out goal fires a hard replan whose reason carries the timeout so the replanner splits it instead of retrying it whole). Substitute gate `scripts/validate-planner-sizing.ts` PASS ×2 (eval:gate corpus is blind to PLAN_SYSTEM). Full detail in PROJECT-STATUS 07-28.

**Watch: ✅ VERIFIED same night (task 68f8847b, 00:19)** — operator re-sent the incident PDF: 4 mutually-independent slide-range batches + consolidation, all batches concurrent, clean `completed` in 4m42s, 11-chunk delivery, zero penalties. The watch is closed; if a FUTURE large plan regresses to monolithic per-item goals, capture the exact task text as the prompt regression case.

**Queued:**

- **Executor unbounded concurrent fan-out (qa W2):** `executeGraph` runs ALL ready goals via `Promise.allSettled` with no concurrency limiter — fine at 4 batches, but a 13-batch plan = 13 concurrent Opus calls → rate-limit burst → breaker opens → per-goal CB_COOLDOWN sleeps consume the ceiling. Add a small semaphore (3-4) if a wide plan ever trips the breaker.
- **`GOAL_TIMEOUT_MS` is dead config (qa I5):** `config.ts` defines it but both consumers read `defaultConfig()` (types.ts 120_000). Either wire the env through or delete it; the prompt's "~2 minutes" now hardcodes the same number in a third place.
- **Self-assess output window vs batch size (qa W5):** `windowOutputForAssess` caps at 5000 chars (head 3000/tail 2000) — a 5-item batch with rich prose can elide middle items' evidence and fail per-batch criteria. Prompt now asks for structurally checkable criteria; if unverified-criteria grades recur on batch goals, widen the window or assess per-section.

## 2026-08-20 — Sandbox-blind host-DB writes: nanoclaw ingestion dead-end (task 8094e37c)

**Incident (no code shipped — queued for Jarvis or next session):** Operator sent the AMN Plan 2027 PDF (17.6MB, `/tmp/jarvis-downloads/AMN_el_lugar_donde_todos_llegan_mas_lejos_2027_V4.pdf`). Task classified **nanoclaw** → ran inside the OpenSandbox container. The agent shell-improvised the ingestion (uploaded to Gemini **twice**, both ACTIVE), then every attempt to persist the handle failed with `attempt to write a readonly database` — `nanoclaw-runner.ts:90` mounts `/root/claude/mission-control:ro` (deliberate), and `data/mc.db` lives under it. The agent burned its 10-turn cap diagnosing **file permissions** (`ls -l` shows rw) when the wall was the **mount**; the heuristic scorer then marked 6/6 goals "completed". Handle recovered manually 23:1x via Gemini `files.list` → `gemini_files` row 6 (`files/y7x7jp7rusme`, expires 08-22 21:51Z). This **trips the 07-22 watch** ("next PDF must reach `pdf_read` → `gemini_upload`+`gemini_research` with NO shell improvisation") — the improvisation moved into the sandbox, where the correct host-side tool (`gemini_upload` → `upsertGeminiFile`, host process, writable DB) was in scope but not chosen.

**Fix directions (tool contract / routing — NOT a turn-cap bump; classes: sandbox-runner-blind-to-host-attachments, turn-exhaustion-unwinnable-endgame):**

1. **Signal at the wall:** nanoclaw's in-container system prompt / env note (`src/runners/nanoclaw-env-note.ts`) must state: "the mission-control tree (incl. `data/mc.db`) is a READ-ONLY mount — DB writes only succeed via host-side tools (`gemini_upload`, `jarvis_file_write`, …); do not script SQLite/file writes under `/root/claude/mission-control`." Wiring test: assert the note text reaches the nanoclaw prompt (mutation-verify: remove note → RED).
2. **Route ingestion host-side:** classifier steering — tasks with fresh attachments in `/tmp/jarvis-downloads` (or explicit "ingesta") should classify **fast** (host runner) where `gemini_upload` persists correctly; nanoclaw is for coding/sandboxed work. Check `classifyTask` prompt + regex fallback.
3. **Optional fail-loud:** in-sandbox shell writes targeting `data/*.db` could be intercepted with an explanatory error naming the host tool (cheap string check in the sandbox shell wrapper).

**How to execute (Jarvis):** coding task → nanoclaw writable clone workspace → `jarvis/*` branch → PR (host tree is `:ro`; the clone+push path in `nanoclaw-worker.ts setupCodingWorkspace` is the sanctioned route). Scoped vitest on touched files; the pre-commit hook runs the full suite.

**Watch:** next operator PDF ingestion must show `gemini_upload` tool.called rows in `task_trace_events` (host path) and a same-day `gemini_files` row — zero sandbox shell improvisation.

## 2026-08-21 — Content-filter 400 delivered as a "completed" reply (task 38dca557) — FIXED, watch open

**Incident:** "Me regalas un poema de Rumi?" → the API's output filter rejected the final turn (`400 Output blocked by content filtering policy`) because the SOP's "texto EXACTO" rule had Jarvis reproducing a full copyrighted Coleman Barks translation. The SDK appended the error to already-streamed prose (no newline) inside a `success` result; `status.ts` only matched `^API Error`, so the row went `completed` and the raw error shipped to Telegram. First fire in 27 deliveries — no change on our side; the filter is probabilistic/length-sensitive.

**Shipped `d9f1f4a` (deployed pid 1694867):** tail-anchored `API_ERROR_TAIL_RE` in `src/runners/status.ts` (mutation-verified) · SOP v1.1 PASO 2b in `jarvis_files` (public-domain sources verbatim — Nicholson/Whinfield/Redhouse, Arberry is NOT PD; ≤4–6 lines from copyrighted translations; never retry the same text after a filter 400) · Rumi INDEX reconciled (#27 folded from `rumi-delivery-log.md`, stale "8/8" state fixed, undelivered _Where Everything Is Music_ recorded as still available) · task 8586 reclassified `failed`.

**Watch:** next operator "poema de Rumi" turn must deliver a poem from a PD source or an attributed excerpt and register #28; any future filter 400 must land as `status='failed'` with `statusSource=api_error` — never as a delivered reply. Cheap check: `sqlite3 data/mc.db "SELECT id,status FROM tasks WHERE output LIKE '%content filtering policy%'"` → every row must be `failed`.

**Not done (deliberate):** no retry/fallback on the filter 400 inside `claude-sdk.ts` — the refusal fallback keys on `stop_reason: "refusal"`, which this error does not carry. Revisit only if the filter fires on non-verbatim content; the SOP change should make the case disappear.

## 2026-08-23 — Usability plan Phase 0 SHIPPED; Phase 1 is next

- Plan: `docs/planning/jarvis-usability-plan-2026-08-22.md` (§7 = ship record). Review artifact: https://claude.ai/code/artifact/805801b6-49f5-4493-946d-d4f7e07f0d3d.
- **Phase 1 SHIPPED 2026-08-23** (plan §8): sticky scope + scope-miss auto-widen + prompt. Watch: `journalctl -u mission-control | grep scope-miss`; `./mc-ctl usability 7` → scope-asks 0 / incantations ≤ 3. 1.3 allow-always has no population (no read-only tool is gated) — not built.
- **Phase 2 SHIPPED 2026-08-23** (plan §9): read-back gates for KB/Sheets/Docs/schedules; `⚠️ No quedó` / `✔ Verificado` lines; `mc-ctl gates summary` stratified. Watch: RB-* rows; any false `No quedó` = verifier fixture.
- **Phase 3 SHIPPED 2026-08-23** (plan §10): typed evidence corpus + claim detector; `(sin verificar)` inline in chat; KB/Sheets/Docs writes REJECT unsourced figures; `finance` routed; `data_summarize`; citation checker (latent population). Watch: `provenance.checked` rejections on operator-typed figures = corpus bug; Sunday 05:00 README rewrite must pass; first "precio de <ticker>" must call `market_quote`. NEXT = **Phase 4** (thread pin, auto-retry, hard stop; closes 2.3), then Phase 5 (`/rituales`), Phase 6.
- **Phase 5 SHIPPED 2026-08-24** (plan §12): sent-before ledger (URL/title key + Jaccard rewording; seam filter for signal-intelligence, `## YA ENVIADO` prompt block for every schedule), `no_new_items` silence, skill-evolution suppressed + nightly-close capped (one evening push), ≥10 % signal lead line, `/rituales` (list · pausa · reanuda · silencio hasta · completo <id>), plan-literal reading budget 4/700 with anchor slot reservation, per-push caps and phone handles for everything cut or deferred. 3 audit rounds, 27 mutations RED. 14-day replay: 3.7 pushes / 551 words a day (from 8.9 / 3,289), 0 days over. **Two rulings pending (plan §12):** fire-order priority among the optional rituals (06:00 scans win; tweet/EOD/Posthumanismo lose most days — `/rituales pausa …` or shorten Posthumanismo ≤250 w) · words not reserved for the close. Watch: `grep -E "\[rituals\]|\[schedules\]|\[rituales\]"`; first `/rituales`; first Morning Sync after a deferral → `deferral(s) consumed`; `./mc-ctl usability 1`. **NEXT = Phase 6** (graduated autonomy, `/que-puedes`, draft-first, second lane).
- **Phase 4 SHIPPED 2026-08-24** (plan §11): thread pins (`## FIJADO`, confirmed figures + URLs), image expiry, auto-resume + auth-retry on the PRODUCTION claude-sdk branch, hard stop (`Detenido: N…`, thread-scoped), 2.3 confirmed-model diff in the Sheets/Docs read-back, flailing-guard diagnostic exemption (journalctl flag ALLOW-list after 4 failed deny rounds). 5 audit rounds R1→R5, every fold mutation-pinned. Watch: `grep -E "recovery|hard-stop|thread-pins"` in the journal; CONTINUITY section in `./mc-ctl usability 7`.
- **NEXT = Phase 5 — ritual diet and phone-side control** (plan §2 Phase 5): sent-before ledger, change-only delivery, nightly trio merge, signal digest memory, `/rituales`, reading budget (≤4 pushes / ≤700 words/day). Then Phase 6 (allow-always groups, draft-first identities, second lane).
- Watch (week 1): `./mc-ctl usability 7`; `journalctl -u mission-control | grep deliverable-filter`; `ritual_deliveries` rows at 07:00 MX; Química row `active` flips to 0 on its first run (`./mc-ctl schedule-resume cbd14c88` to resume); Morning Sync PASO 4 produces ≤2 evolution lines or nothing.
- Known residue: the filter leaves pure-English replies untouched (flagged only); the PM fingerprint still sees the model's own 178-vs-198 rejection wording as "changed" — Phase 5's sent-before ledger will compare normalized items, not numbers.

## 2026-08-23 — Preview publishing self-serve (ant-colony incident) — SHIPPED; 1 observation queued

**Incident:** 4 chat rounds failed to publish a Three.js demo. Root cause chain: per-demo `/etc/caddy` edits required an operator paste → the panel-terminal heredoc paste silently never executed (Caddyfile mtime proved it) → Jarvis "verified" with `http://` → 308, which Caddy returns for ANY hostname (control-tested) → confident wrong diagnosis (LE rate-limit) → flailing guard then blocked its `journalctl` checks ("colony" token shared with the failed curls). Every prior demo vhost (intel-demo, bariatrica, …) had been pasted by operator sessions — Jarvis never had this ability; the relay just hadn't failed visibly before.

**Shipped (host-side, no mission-control deploy):** `scripts/preview-caddy-sync.sh` → `/usr/local/bin` + `preview-caddy-sync.path`/`.service` watching `/root/claude/previews/`; fixed-template vhost generation (only the sanitized dir name crosses the trust boundary), `caddy validate` + revert gate, reload on change; `import /etc/caddy/previews-generated.caddy` in the Caddyfile (backup `Caddyfile.bak-preview-sync-20260823-213248`). Installer: `scripts/install-preview-sync.sh`. KB: `directives/preview-publishing.md` (conditional) — recipe + "a 308 on http:// proves nothing" doctrine + never hand the operator `/etc` edits. **Verified e2e:** ant-colony LIVE over HTTPS with real content; smoke-test dir auto-published in ~12 s (404 = live vhost, no index yet) and auto-unpublished on rmdir.

**Queued observation — flailing guard blocks novel diagnostics:** `src/tools/flailing-guard.ts` strikes on any token shared with ≥3 recent failures, so after repeated failing `curl https://ant-colony…` it also blocked _first-attempt_ `journalctl … ant-colony` — the exact command that would have revealed the truth. Consider exempting read-only diagnostic commands (journalctl/systemctl status/grep of logs) from strike _enforcement_ (keep recording), or keying strikes on command-shape, not shared tokens. Fold into usability Phase 4 (recovery) scoping.
| 2026-08-28 | **Memory-architecture plan v2.0 — Jarvis's v1.0 reviewed (4/10) and rewritten; Tracks 2+3 SHIPPED.** v1.0 refuted claim-by-claim against `main` (JME already stores preferences; the extractor would read turns the consolidator deletes; sibling context already injected; `/tmp` invisible in the sandbox; ALS cannot cross `submitTask`; confirm-rate gate = the Phase-4 shape). Shipped: `jme_signals` (migration v5) + tagger in `writeEpisodic`, inferred-preference prompt rule, preferences-first injection, `mc-ctl jme-preferences`; planner ownership rule + `## Shared findings` forwarding in `buildSubTaskDescription`; `directives/user-data-sources.md` (enforce). New section "Memory-architecture plan v2.0 — follow-ups" queues the Jarvis Track 1 audit, the secrets ruling and the two readouts. |
| 2026-08-28b | **Memory plan v2.0 — audit R2 FAIL folded (R3 pending at write time).** R2 caught a regression my R1 fold introduced: the anchored `## Shared findings` parser ran over `tracker.output`, which is the JSON-stringified task blob, so Track 3 forwarding would never have fired in production and the `— Result:` slice kept laundering sibling figures. Folds: unwrap via `extractDeliverableText` before parsing; `stripForwardedSiblingFindings` cuts the whole sibling tail; `execute()`-seam test; CommonMark code shielding; fail-safe `inferred` clamp; confirmed preferences never superseded; `mc-ctl jme-preferences` migration guard + lower-bound note. Track 2 counter downgraded to advisory. Lesson for the queue: a parser fold must be exercised on the PRODUCTION input shape (JSON blob), not on the markdown the tests hand-write. |
| 2026-08-28c | **Memory plan v2.0 — audit R3 FAIL folded.** Two one-liners, both mine: the R2 indented-code shielding corrupted live deliverables (nested placeholder; 5/3,032 in replay) and was redundant with the `^ {0,3}##` anchor → deleted, and the strip is now verified as an IDENTITY over every live output; the sibling-tail strip missed `auditNumbers`' second caller (`provenance-gate.ts`, the path that persists to Sheets/Docs/KB) → moved to `numbers.ts`, applied at both, pinned by a wiring test. Also: flagless preferences pinned as inferred; model-authored preferences capped at 0.99 so 1.0 stays the operator's mark. Lesson: a text filter that runs on EVERY item ships only after an identity replay over the live corpus, not a fixture. |
| 2026-08-28d | **Track 1 run by Jarvis + KB registry drift fixed.** Jarvis executed Track 1 (`e8a1b038`, heavy, 6 goals) cleanly and surfaced five rulings (see the follow-ups section). He read plan **v1.0**: the operator rewrite had only touched the FS mirror; `jarvis_files` (source of truth) kept the old row because `kb-reindex` upserts MISSING paths only. Fixed by `upsertFile` from the mirror file. Rule: a KB edit meant for Jarvis goes through `upsertFile`/`jarvis_file_write`, never a bare file write. |

## NanoClaw v2.0–v2.3 review deferreds (2026-09-01)

From `docs/planning/nanoclaw-upstream-review-2026-09-01.md` (memory `reference_nanoclaw_upstream`). Shipped: sandbox executes the host's deployed `dist/` (RO mount, was a 7-week-old baked copy), image↔lockfile label + refuse-on-drift + `deploy.sh` auto-rebuild, `:ro`-only mount gate on both backends with boundary-safe prefixes, `--init`, unknown `SANDBOX_BACKEND` refuses at boot, egress allow-list ON (item 0 below, done), image npm 10.9.9, `.npmrc` key fix. Every deferral below carries its trigger.

0. ~~▶ **OPERATOR: flip the egress allow-list**~~ **DONE 2026-09-01 06:44 UTC (operator-approved in-session): drop-in line active, deployed pid 1034295, `mc-ctl sandboxes` → `Egress allow-list set: yes`, `mc-ctl smoke sandbox` PASS 42 s (task `b3053a69`) with the egress sidecar `sandbox-egress-c8bd991c…` attached — inference through `*.anthropic.com`/`*.claude.com` from inside the box.** The line that ran:
   `sed -i 's|^# Environment=SANDBOX_EGRESS_ALLOW=.*$|Environment=SANDBOX_EGRESS_ALLOW=*.anthropic.com,*.claude.com,github.com,api.github.com,*.githubusercontent.com,registry.npmjs.org|' /etc/systemd/system/mission-control.service.d/opensandbox.conf && ./scripts/deploy.sh --drain 900 && ./mc-ctl sandboxes && ./mc-ctl smoke sandbox`
   Rollback: re-comment the line + restart.

1. ~~**Claude Agent SDK 0.3.207 → 0.3.252**~~ **DONE 2026-09-01 07:10 UTC as 0.3.245** — `latest` 0.3.252 (published 08-31) is refused by the repo's `min-release-age=7`; 0.3.246–0.3.252 become eligible 09-01…09-07 (→ #19). Evidence: `npm run eval:gate -- --run` PASS (376 cases / 187 tool_selection, candidate 66.05 vs incumbent 65.75, +0.30, threshold 63.75; 1,029 s, $5.61) · `npx tsx scripts/validate-tool-search.ts --run` PASS (all five phases) · fast smoke 6 s · sandbox smoke 39 s (`78d5452b`) · R1 audit PASS-W, 0 Critical (W3 folded as a `TerminalReason` type pin in `claude-sdk.ts`; W1/W2/W5 → #13–#15) · image rebuilt by `deploy.sh` on lockfile drift (`c03d24003c6f`). **Cost of the bump:** the install ran under the live service against memory `feedback_live_service_dep_swap` → the 07:00 tuning run scored 85 cases on Haiku (`spawn ETXTBSY`) and was killed by the restart; run 83 closed as `aborted` by hand (→ #12). Full account: review doc §"SDK bump".
2. ~~**Host npm upgrade**~~ **DONE 2026-09-01 06:53 UTC** — operator ran `npm install -g npm@12` → npm 12.0.2 (node 22.23.2). Verified: `npm config get min-release-age` → `7`; `npm install --dry-run @anthropic-ai/claude-agent-sdk@0.3.252` refused ("No matching version found … with a date before 8/25/2026"), resolves with `--min-release-age=0`; npm's vendored tar now 7.5.19 (host GHSA-23hp-3jrh-7fpw cleared). Side effect to keep in mind: every `npm install`/`update` on this box now resolves to versions ≥ 7 days old — a same-day security fix needs `--min-release-age=0` on that one command.
3. **systemd restart backoff** (upstream startup circuit-breaker analog; `StartLimitBurst=5/10s` can never trip at `RestartSec=5s`): `printf '[Service]\nRestartSteps=6\nRestartMaxDelaySec=15min\n' > /etc/systemd/system/mission-control.service.d/restart-backoff.conf && systemctl daemon-reload`. `NRestarts=0` today. **Trigger:** first `NRestarts > 3` within an hour.
4. **Watchdog vs container lifetime** (folds into the queued P2 classifier item): a watchdog-failed task keeps its heartbeating sandbox alive (TTL renewed) → ghost pushes. Reference design: upstream's claim fence ("never adopt or finalize without the claim fence — fail closed", 08-25). **Trigger:** next ghost push.
5. **Mid-deploy `dist/` rewrite race:** the sandbox now mounts host `dist/`; `npm run build` during a running sandbox can hand a lazy `await import()` a half-written module (pre-existing for the host process's own dynamic imports). Window ≈ build seconds × 6 tasks/30 d. **Trigger:** a task error naming an ESM/syntax error in `dist/` during a deploy → build into a staging dir + atomic rename.
6. **Egress list growth:** a nanoclaw task failing with `ENOTFOUND`/`EAI_AGAIN` for a host = a needed egress not on the list. Add ONE host with its reason to `opensandbox.conf`. Known-but-unevidenced: `codeload.github.com`, `objects.githubusercontent.com`. Rollback of the whole policy: comment the `SANDBOX_EGRESS_ALLOW` line + restart. **Trigger:** the first such failure.
7. **One-off probe scripts leak their sandbox until TTL** — `spawnOpenSandbox` resolves `result` before `finally → teardown()`, so a script that `process.exit`s on result never sends `kill` (observed twice today; TTL reaped both). No live impact (the runner never exits). **Trigger:** writing any new one-off that awaits `handle.result` — await the kill or accept the TTL.
8. **Non-root container identity** (upstream `--user uid:gid` + `HOME=/home/node`): deliberate divergence, documented on `buildDockerRunArgs` (root reads the mode-600 SDK credentials). **Trigger:** credentials move to a vault/proxy pattern.
9. **Same class, other repo (R2 adjacent-surface sweep):** `Pulso-Aura-Upfront/crm/container/Dockerfile:14 COPY src/` bakes the CRM agent code into `agentic-crm-agent:latest`, and `engine/src/container-runner.ts:316` builds `docker run` args with no `--init`, no `--cap-drop`, opt-in `readonly`. Not touched here (out of repo). **Trigger:** next Pulso session — run the same three checks (image `Created` vs last relevant commit; `--init`/cap-drop parity; a lock label or a source mount).
10. **Dead config advertising a non-existent image:** `NANOCLAW_IMAGE=nanoclaw-agent:latest` in `docker-compose.yml:15`, `.env.example:25`, `env.example:32`, `README.md:365` is read by no code (the runners use `HEAVY_RUNNER_IMAGE`, default `mission-control:latest`); `Dockerfile.nanoclaw` + `scripts/build-nanoclaw.sh` (`nanoclaw-coding:latest`) are dead too. Left in place (not this bundle's scope). **Trigger:** next docs/config hygiene pass — delete all five references and the two files.
11. **Observation (verification window, 06:34 UTC): `unattended-upgrades` restarted mission-control AND opensandbox-server twice** (with fail2ban/fwupd/rsyslog/ssh — a library upgrade's dependent-service restart), while a nanoclaw smoke was in flight. The task survived on the 30 s in-flight shutdown grace, and `NRestarts=0` (clean stops, not crashes). Neither the `deploy.sh` in-flight guard nor the drain applies to this path, and a mid-task server restart pauses TTL renewals for the running sandbox. **Trigger:** a task failure whose window overlaps an `apt.systemd.daily` run (`journalctl -u apt-daily-upgrade`) → consider `Unattended-Upgrade::Automatic-Reboot "false"` is already the case; the knob is `needrestart`'s service list (`/etc/needrestart/conf.d/`) to exclude `mission-control.service` / `opensandbox-server.service` from automatic restarts, or a `--drain`-aware restart wrapper.
12. **`tune_runs` orphan reconciliation at boot (resilience gap, exposed 2026-09-01).** A restart mid-run leaves the row `running` forever: run 83 `tune-1788246000451` was killed at experiment 3/25 by the SDK-bump deploy (07:10:24 UTC) and sat as `running` until closed by hand via `updateRun()` (`aborted`, report states the cause). No `WHERE status='running'` guard exists, so a stuck row does not block the next 07:00 run — it only lies in `mc-ctl tuning`. Fix: at boot (or at the start of `runOvernightTuning`), `UPDATE tune_runs SET status='aborted', completed_at=now, report='orphaned by restart' WHERE status='running'` through `updateRun`. **Trigger:** next session touching `src/tuning/` or `src/rituals/overnight-tuning.ts`.
13. **Resize budget windows on post-0.3.239 cost figures BEFORE arming `BUDGET_ENABLED` (SDK R1 W1).** 0.3.239 makes `total_cost_usd` / `modelUsage.costUSD` include the 1.1× US-only-inference multiplier when the response reports `inference_geo: "us"`. Enforcement is off today (`/health` → `budget.enabled=false`), so the step is observability-only — but the daily window read `spend 44.84 / limit 50` at 07:1x UTC on 09-01 (including ~$7.5 of harness + the aborted tuning run). Arming on historical rows would refuse every SDK call, chat included, at ~$49.3. **Trigger:** the session that arms `BUDGET_ENABLED`; first check whether our responses carry `inference_geo` at all.
14. **`src/dispatch/classifier.ts:761` — hard cost literal `avg_cost_usd > 0.05` (SDK R1 W2).** A 10% inflation moves the effective trigger to ~$0.0455 pre-multiplier (gated on `total >= 5 && success_rate < 0.5`, so low impact). Make it a ratio or re-derive with #13. **Trigger:** with #13.
15. **`src/inference/claude-sdk.ts:368` `/aborted/i` is unanchored (SDK R1 W5, not firing).** 0.3.211 puts the CLI's stderr into process-exit errors; stderr noise containing "aborted" would route a crash into the no-retry branch. Inert today because the `catch` at `:1104` swallows every SDK error into a degraded result. **Trigger:** any change to that catch.
16. **`scripts/opensandbox-e2e.ts:76,225` spawn the production image WITHOUT `RUNTIME_CODE_MOUNTS` or the drift/assets pre-flights (R3 Info)** — the e2e probes the image's baked `dist/`, not the deployed one; harmless (no host dist mounted) but it is an ungated `spawnOpenSandbox` caller and its result does not prove what a task run proves. **Trigger:** next use of the e2e script.
17. **Dockerfile builder stage runs bare `npx tsc` (R3 Info):** the baked-dist fallback (bare `docker run`) lacks `dist/tuning/seed-cases.json` (`schema.sql` is COPYed at line 55, seed-cases is not) — degraded, not equivalent, and nothing uses it today. One `COPY src/tuning/seed-cases.json ./dist/tuning/seed-cases.json`. NOTE a Dockerfile-only change does NOT trigger `deploy.sh`'s rebuild (the label pins the lockfile, not the Dockerfile) — run `bash scripts/build-mc-image.sh` by hand. **Trigger:** next Dockerfile edit.
18. **`volumeRefusal` validates only the host side (R3 Info):** `mountPath` is never checked for absoluteness or `..`. Specs are runner literals today. **Trigger:** the first non-literal mount spec (operator/config-supplied).
19. **Claude Agent SDK 0.3.245 → 0.3.252+.** 0.3.246–0.3.252 were <7 days old on 09-01 (`min-release-age=7` refused them — correctly); eligible 09-01…09-07. Of interest: `costBasis` on `modelUsage` (which price table produced the 1.1× — useful if #13 arms), `user_message_uuid`, `perTaskStopAffordance` (no value, one-shot), per-server MCP `timeout` (marginal). Same checklist as the 0.3.245 bump: STOP the service first ([[feedback_live_service_dep_swap]]), `npm run eval:gate -- --run`, `npx tsx scripts/validate-tool-search.ts --run`, deploy, both smokes. **Trigger:** bundle with the 2026-10-01 upstream reviews.
20. ~~**npm 12 `allowScripts` — VPS-wide hazard, mission-control fixed, three trees are not.**~~ **DONE 2026-09-01 08:16 UTC.** Truth-checked with `npm install-scripts ls` + a `hasInstallScript` scan of every lockfile before approving — the queued package lists were partly wrong: neither `sharp` (Pulso) nor `bcrypt` (very-light-cms) exists in those trees; every tree's Linux install-script set is exactly `better-sqlite3` + `esbuild` (`fsevents` is darwin-only and never installs here). Applied name-only (`npm install-scripts approve --no-allow-scripts-pin better-sqlite3 esbuild`): Pulso-Aura-Upfront root + `engine/` (the `agentic-crm` unit runs `npx tsx engine/src/index.ts` from the root, so both trees resolve), very-light-cms, vlved; `engine/` additionally denies `@whiskeysockets/baileys` (preinstall = Node ≥ 20 check) and `protobufjs` (postinstall = stderr version warning), mirroring mission-control. `crm/container/agent-runner/package.json` got `better-sqlite3: true` by hand (`approve` needs an installed tree; the image's nanoclaw-agent npm 10.9.8 ignores the field today — forward defense for a base-image bump). `engine/container/agent-runner` has no install scripts. All four `ls` runs now say "No packages with unreviewed install scripts."; bindings/binaries present in every tree; no lockfile changed, nothing installed, no service touched. Commits: Pulso `6ae137e`, very-light-cms `e53b8c6`, vlved `9c858fb` (all pushed). Residual: the field is host-only — a container built by an npm ≥ 12 base image honors it, npm 10 images ignore it either way.
21. **`npm audit` baseline (measured 2026-09-01, identical before/after the SDK bump): 23 vulnerabilities — 3 critical, 13 high, 5 moderate, 2 low; ALL pre-existing.** Critical: `@whiskeysockets/baileys` (via `libsignal-node` → `protobufjs` code-injection GHSA; WhatsApp path), `protobufjs` 6.8.8 itself, `vitest` (UI server arbitrary-file read — dev-only, the UI server never listens here). High incl. `sharp`/libvips CVE-2026-333xx, `ws`, `vite`, `path-to-regexp`, `picomatch`, `postcss`, `adm-zip`, `fast-uri`, `ip-address`, `nanoid`, `@xmldom/xmldom`. `fixAvailable` on every entry, several are semver-majors. Not folded into the SDK bump on purpose (own blast radius, own gate). Reproduce: `npm audit --json | node -e '…'` (see scratchpad recipe in the 09-01 session wrap) or plain `npm audit`. **Trigger:** a dedicated security-dep batch session (V8.5 Phase 0 style: `npm audit fix` non-major first, then majors one at a time with scoped tests), or the first exploit advisory naming a live path (baileys/sharp/ws).
22. **Nanoclaw's `TARGET_NOT_IN_SANDBOX` backstop is keyed on the model echoing a literal token in `finalAnswer`/summary — a PARAPHRASED refusal scores as success (task 8954, 2026-09-01 16:03 UTC).** Follow-up chat "Use Shell y activa git para lo que quedó pendiente" (thread = the new `/root/claude/trustr` scaffold) named no path and no registered project, so `classifier.ts:470-485` (coding → nanoclaw unless `targetsForeignRepo` / `referencesForeignProject` match `detectionText` — the BARE message, `router.ts` ~2373) sent it to the sandbox, which mounts only `/root/claude/mission-control:ro` (`nanoclaw-runner.ts:136-146`). The `[SANDBOX SCOPE]` prompt guard WORKED — 5 read-only tool calls, no edits, no evasion — but the agent's `finalAnswer` read "No `trustr` directory exists anywhere in the sandbox… **Goal achieved**" (`score:1`) and the literal token appeared only in `learnings`. `nanoclaw-worker.ts:193-194` (`emittedTargetNotInSandbox(summary) || …(finalAnswer)`) therefore returned `success:true`, so `dispatcher.ts:731-735` (`!result.success && !result.error && agentType==='nanoclaw' && messaging`) never rescued it to the host runner; the operator received an English "Goal achieved" (`deliverable-filter … english_leading`) for work that was never attempted. This is the 06-24 `dcb5eb3` layer-3 backstop (`feedback_foreign_repo_sandbox_routing` Bug A2) defeated one level up: a "structural" check whose INPUT is the model's compliance with a `reply EXACTLY with <token>` instruction is still a prompt-enforced property. Fixes, smallest first: (a) `emittedTargetNotInSandbox` scans the WHOLE worker result (finalAnswer + summary + learnings) AND the paraphrase family (`not (in|mounted in|present in) (this|the) (nanoclaw )?sandbox`, `confirmed absent from this sandbox`) — corpus-replay against every `tasks.output` where `agent_type='nanoclaw'` before shipping (text filter rule); (b) a signal the model cannot paraphrase: the worker's pre-flight resolves any `/root/claude/<x>` or registered-project name found in the task text + thread pins against `/workspace`; absent ⇒ `success:false` before the agent runs (no inference spent); (c) classifier: a follow-up whose thread's PREVIOUS task ran on a HOST runner for a foreign project inherits that routing (router already stores prior scope per `tk=`), or the thread's pinned URLs/paths join `detectionText`. Scan for recurrences: `sqlite3 -readonly data/mc.db "select id,title from tasks where agent_type='nanoclaw' and status='completed' and (output like '%absent from this sandbox%' or output like '%not in this%sandbox%' or output like '%TARGET_NOT_IN_SANDBOX%')"`. Class: structural-safety-gate (the resolver must not depend on model text) + completion-is-a-claim + sandbox-runner-blind-to-host-attachments.
23. **Scope-classifier LLM call times out on EVERY chat turn → the regex fallback is the de-facto classifier (found 2026-09-01 during the shell_exec /diagnose).** `src/messaging/scope-classifier.ts:13` `SCOPE_CLASSIFIER_TIMEOUT_MS = 3_000`; `HAIKU_MODEL_ID` is NOT in the live env, so the `...(HAIKU_MODEL_ID !== undefined && { model: HAIKU_MODEL_ID })` spread at :170 leaves the call on the primary (claude-sonnet-4-6) — a Haiku-sized budget for a Sonnet call. Journal 2026-09-01: 17× `[scope-classifier] Classify failed → regex fallback: scope classifier timeout`, 0 successes, 13 `[router] Scope groups (regex fallback)`. No user-visible damage today (the regex picked `coding` for the trustr asks via `shell_exec\b`), but the semantic classifier is dead weight paying a 3 s latency tax on every turn. Options: (a) set `HAIKU_MODEL_ID` in the mission-control drop-in so the call runs on the model the timeout was sized for (measure p50/p95 of a Haiku classify BEFORE trusting 3 s); (b) raise the timeout (costs turn latency); (c) keep the regex and delete the LLM hop (it has been carrying production since at least today's window — check how long: `journalctl -u mission-control --since '30 days ago' -o cat | grep -c 'scope classifier timeout'`). **Trigger:** the next misroute or chat-latency session. **Verify:** the timeout line disappears from the journal AND `mc-ctl usability 7` scope-asks stays 0.
24. **One ToolSearch miss is treated as proof a tool does not exist — and a BLOCKED reply bypasses the scope-miss interceptor.** Under `TOOL_SEARCH_ENABLED` the model learns about `deferred: true` tools only through the SDK's `ToolSearch`. Task 8959 retried 6 times and found `shell_exec` (15 calls, trustr pushed); 8958/8961/8962/8963 (same 51-tool scope, same `toolsHash 13857b89`) searched ONCE, missed, and replied «Necesito `shell_exec` para esto.» + `STATUS: BLOCKED`. Two layers: **(a) prompt** — nothing in Jarvis's prompt mentions ToolSearch or deferred tools; candidate always-on line: «si ToolSearch no encuentra una herramienta, reformula 2–3 veces (verbo, sinónimo, inglés) antes de decir que no la tienes; nunca reportes BLOCKED por una herramienta sin ≥2 búsquedas». System-prompt change ⇒ `npm run eval:gate -- --run` before ship. **(b) interceptor blind spot** — `detectScopeMiss` runs only on the task-COMPLETED path (`router.ts:2858`); `STATUS: BLOCKED` → `dispatcher.ts:850` `taskStatus="blocked"` → the router's failed/blocked branch (`router.ts:3322`) delivers the generic line and never widens/re-runs. Same day, task 8950 (a completed reply asking for shell_exec) WAS intercepted and re-run as 8951 — the mechanism works, the BLOCKED status is the hole. Fix shape: run `detectScopeMiss` on the blocked/needs_context branch too (the "strong ask for a tool it already had → correction-note re-run" case is exactly this), pinned with a fixture built from 8963's reply. The structural fix shipped this session (un-defer the core coding set, `core-coding-always-loaded.test.ts`) removes the head of the distribution from the failure mode; the long tail (`file_delete`, `gh_*`, `code_search`, ~140 other deferred tools) still depends on one search. **(c) the `[DEFERRED TOOLS]` catalog lies on the SDK path (qa R1 #6):** `fast-runner.ts:726` builds it unconditionally and its last sentence (`registry.ts:250`: "Call any of them by name — the system will return the parameter schema so you can retry") is the OpenAI adapter's contract (`adapter-openai.ts:1699`); under tool search an unloaded tool is NOT callable by name, so every scoped chat tells the model a false affordance for the ~140 deferred tools. Fix = keep the name list (it tells the model what to search for) and swap the sentence for a ToolSearch instruction when `toolSearchEnabled()` (`fast-runner.ts` never consults it today) — same eval:gate run as (a). **Trigger:** any `status='blocked'` task whose `metadata.tools` contains the tool its reply asked for — `sqlite3 -readonly data/mc.db "SELECT id, title FROM tasks WHERE status='blocked' AND created_at > datetime('now','-7 days')"` then read the `conversations` row for the reply.
25. **Same class, other heads of the distribution (qa R1 #8 sweep, 30 d `task_trace_events` × static `deferred: true` parse — 143 deferred / 45 core of 188 builtin objects; counts are SUCCESSFUL calls, so an undiscovered tool is invisible to this metric — lower bound).** Still deferred: `intel_query` 140 calls / 75 tasks (the `intel` head); `project_get` 43 / 13 and `project_update` 10 — both in `MISC_TOOLS` (`scope.ts:144-145`, in scope on EVERY turn) and the justifying comment at `scope.ts:140-143` ("deferred, so always-on cost is name + 1-line summary only (~30 tokens)") is false under tool search — they are invisible, not cheap, and the same comment cites the 04-21 "model claimed false-unavailable" archetype this fix closes; `gdocs_read_full` 33, `calendar_list` 32, `gdocs_write` 31, `gdrive_create` 26, `gdocs_replace` 25 (google). Population: of 470 tasks that called ToolSearch in 30 d, 247 (52.6 %) searched exactly once. Decision per tool = schema tokens on every in-scope turn vs. head-of-distribution risk; `project_get`/`project_update` first (fix the comment either way). Also stale (qa #9): `docs/TOOL-HIERARCHY.md:15,19,107,122` still say "82 of 172 deferred / ~16,400 tokens saved" and the "How Deferral Works" walkthrough (:109-124) documents the OpenAI-path round-trip as if live. **Trigger:** the next un-defer or scope session; measure with the sweep query in the 2026-09-01 R1 audit (`name='tool.called'`, `agent_type='fast'`, non-ritual).
26. **New-project isolation: Jarvis put a NEW project's schema into the SHARED production Supabase `postgres` DB, `public` schema, via passwordless `docker exec supabase-db psql -U postgres` (task 8964 / trustr, 2026-09-01 23:21 UTC — the very task that verified the un-defer fix).** It created 6 tables + role `trustr_app` (CREATEDB, weak password logged in clear in the journal), mutated a test row, and left two orphan `nohup tsx` runs on `*:3010`/`*:3011` because the shell-guard BLOCKED `pkill -f "tsx src/index.ts"` (correctly — that spelling would have killed salones-wa/gilda.mx PROD, xpoz-pipeline and both eurekams-intelligence-ui servers). Then crystal/extractor STORED the workarounds as lessons: "create a DB user via the unix socket — prefer over guessing passwords" (`lessons/2026-09-01-c99a13c3bc45666b.md`), "when a process cannot be killed due to tool restrictions, start a replacement on another port" (`…0f5cb82dca2be942.md`), execution-pattern "try default local Supabase creds first" (`knowledge/execution-patterns/2026-09-01-7b0a1eb3.md`) — the learning loop rewarded working around a guard. Operator remediation 2026-09-02: tables snapshotted + dropped from `postgres.public`, dedicated `trustr` DB owned by `trustr_app`, password rotated + NOCREATEDB, the 5 lessons/facts removed via `deleteFile`/`pgDelete` (scratch script). **Fix shape (needs eval:gate — prompt change):** (a) coding-section rule for new projects: own database (`CREATE DATABASE <proj> OWNER <proj>_app`), never `postgres.public`, and a shell-guard deny for `docker exec supabase-db psql -U postgres` (the passwordless superuser is a trust-boundary hole, not a convenience); (b) crystal/extractor admission filter: a lesson whose text names a blocked tool/guard/restriction as the thing being worked around ("cannot be killed", "tool restrictions", "instead of <blocked op>") is a guard-evasion pattern → reject + log, never store ([[guardrail_inverts_priority]] — the learner turned a guard into a lesson); (c) the shell-guard already protects `pkill -f` — add the REASON text naming the safe spelling (`kill <pid>` after `pgrep -af <own dir>`) so the model does not spawn a duplicate instead. **Trigger/verify:** `docker exec supabase-db psql -U postgres -d postgres -Atc "SELECT tablename FROM pg_tables WHERE schemaname='public'"` after any Jarvis coding task that touches a DB; `kb_entries WHERE type='pattern' AND content ~* '(cannot be killed|restriction|bypass)'` should stay at 0.
