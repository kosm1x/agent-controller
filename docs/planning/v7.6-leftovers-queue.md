# v7.6 — Post-Closure Watchlist

> **Status**: Watchlist, NOT actionable backlog.
> **Origin**: Created at v7.6 closure (2026-05-18), per V7.6-GUIDE.md Spine 7 ritual.
> **Sibling for v7.5**: `docs/planning/v7-leftovers-queue.md`.
>
> Items here have explicit re-open triggers. Until those fire, nothing in this file requires engineering effort. If a trigger fires, the item moves to the active queue (`docs/planning/next-sessions-queue.md`) or the relevant phase plan.

---

## Tier A — Open queue items inherited from `next-sessions-queue.md`

| Item                                               | Status                   | Re-open trigger                                                                                                                |
| -------------------------------------------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------ |
| #2 — AV key rotation                               | P0, dormant              | Operator provisions new AV API key. ~1-2h work once unblocked. Independent of all other items.                                 |
| #18 — NorthStar compass-reframe operationalization | P1, needs operator align | Operator confirms the 4-decision matrix from 2026-05-09 brief. Folds naturally into v7.7 S5 narrative if not picked up sooner. |

`next-sessions-queue.md` itself stays as the canonical execution-ordered list. 16/18 of its items are closed; the 2 open ones are mirrored here for closure-period visibility.

---

## Tier B — Anti-mission deferrals (v7.7+ or v8.0+)

v7.6 declared these out-of-scope by anti-mission (V7.6-GUIDE §32). Each has a forward-anchor.

| Item                                               | Forward-anchor / re-open trigger                                                                                        |
| -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| New runners (6th+)                                 | Not planned. "Five is enough." Re-open only if a class of workload genuinely doesn't fit fast/nanoclaw/heavy/swarm/a2a. |
| New skill SOURCE authoring                         | v7.7 Spine 3 (S5 stored procedures) registers existing scaffolding; authoring new skills is v8.0+ surface work.         |
| F7 financial-pipeline runner wire-up               | L4-L6 modules are library-only by design through v7.7. Wire-up is a v8.x capability phase.                              |
| `kb_entries.layer` schema add                      | v7.7 substrate (S5 may surface need) or v8.0 capability work. Specced 2026-04-30; deferred from v7.6 explicitly.        |
| Substrate scout adoption (datagen/voyager/letta/…) | v7.7 spines compose patterns from these references but do NOT adopt the libraries. Adoption is v8.0+.                   |
| Prometheus loop architectural changes              | Plan-Execute-Reflect stays as-is. Re-open only if a measured planner/executor/reflector contract failure recurs.        |
| Closure-ritual automation (`mc close-phase`)       | v7.8 placeholder per V7.7-PREPLAN §267. By third manual closure, automation case will be empirically grounded.          |

---

## Tier C — Hindsight escape hatches (mechanism preserved, currently dormant)

The Spine 3 DEMOTE verdict (queue #15, 2026-05-15) flipped `HINDSIGHT_RECALL_ENABLED=false` as the documented default. Two primitives stay preserved so a future re-evaluation can be done without rebuilding.

| Item                                                             | State                                                                                                                                                                                                                                 |
| ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `RecallOptions.withRerank: true` (queue #10 mechanism)           | Code path preserved. Inert under current dual-flag (`HINDSIGHT_ENABLED=false` AND `HINDSIGHT_RECALL_ENABLED=false`). To reactivate for analysis-task opt-in: flip `HINDSIGHT_ENABLED=true` AND keep `HINDSIGHT_RECALL_ENABLED=false`. |
| `HINDSIGHT_RECALL_DISABLED_BANKS=csv` (per-bank surgical demote) | Primitive intact. Empty list under current global demote.                                                                                                                                                                             |

**Re-evaluation triggers** (from `feedback_hindsight_demote_verdict_2026_05_15`):

- mc-jarvis SQLite-hybrid utility drops below 25% for 7 consecutive days (signal that L4 layer needs rebuild)
- A new memory-product candidate appears worth a REPLACE evaluation (not "patch Hindsight")

Until one of those fires, no Hindsight architecture work is in scope.

---

## Tier D — R3 hygiene residuals from SDK cutover (queue #16)

R3-1, R3-2, R3-3 closed 2026-05-13 (commit `2e56e9b`). R3-4 accepted as-is (swarm-runner intentionally has no cross-subtask aggregation; doc drift only). No re-open trigger.

---

## Tier E — Budget alarm (informational, not a v7.6 deliverable)

Monthly spend $411 / $400 cap **exceeded** as of 2026-05-18. Today's `5a9ab6c` raised the cap; cost-plumbing fix surfaced the true Opus number (previously underreported as $0 in nightly cost_ledger).

| Item                               | Re-open trigger                                                                                                            |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| Monthly cost cap re-tune           | If cap exceeded again before month-end after the today bundle's plumbing fix. Then either raise cap or audit cost drivers. |
| Hindsight cost-pull job (queue #4) | Stays active. Frozen Hindsight container still does observation extraction on already-written data; cost_ledger reflects.  |

---

## Tier F — X-posting brittle path (pre-existing concern, no closure required from v7.6)

No native X tool in agent-controller. Tweets go via hand-written Playwright + hardcoded `auth_token`/`ct0` cookies (~30d lifetime). Headless cookie-refresh blocked by X anti-bot. Recurrence expected ~monthly.

| Item                                   | Re-open trigger                                                                                           |
| -------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| Cookie-refresh ritual (cheap option)   | Next cookie expiry. Pre-write the ritual now; consider scheduled task with manual-trigger fallback.       |
| X API v2 ($100/mo Basic) — paid option | Operator decision: pay-to-eliminate-toil vs cookie-refresh-ritual. Defer-decision until next expiry hits. |

Pin: `feedback_x_posting_brittle_path`.

---

## Tier G — Stability re-benchmark calendar item

V7.6-GUIDE §205 listed "No regression in the 5 stability dimensions from the freeze" as a closure criterion. The 5 dimensions (efficiency / resilience / security / speed / tool-scoping) were baselined 2026-04-22 at `docs/audit/2026-04-22-*.md`. The full re-benchmark was scheduled for 2026-05-22, 4 days after v7.6 close. v7.6 closed on the weaker stand-in "0 incident reports during the 10-day phase + audit cadence catching pre-existing bugs" — see CLOSURE.md "Note on closure criterion §205".

| Item                              | Re-open trigger                                                                                                                                              |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-05-22 stability re-benchmark | Date arrives. Re-run the 5 dimensions against the 2026-04-22 baseline. If any regression maps to a v7.6 commit, re-open v7.6 per CLOSURE.md §247 trigger #2. |

---

## How to use this file

- **Engineering input**: nothing here is actionable until a trigger above fires.
- **When a trigger fires**: move the item to `docs/planning/next-sessions-queue.md` with priority + estimate; reference back to this watchlist line for context; update this file with the move (status → "promoted to active queue YYYY-MM-DD").
- **Adding new items**: only when v7.7+ work explicitly defers something with a documented trigger. Do not add "TODO" or "nice to have" items — those belong in the active queue or get dropped.
- **Removing items**: when an item ships in v7.7+ or its trigger becomes obsolete (e.g., the surface it referenced was removed). Note the removal commit + date inline.

---

## Cross-references

- `docs/V7.6-CLOSURE.md` — origin doc for this watchlist
- `docs/V7.6-GUIDE.md` — phase plan, anti-mission, operating rules
- `docs/V7.7-PREPLAN.md` — next phase, where most Tier-B items have forward-anchors
- `docs/planning/next-sessions-queue.md` — active execution-ordered list (16/18 closed at v7.6 close)
- `docs/planning/hindsight-strategic-options.md` — Tier-C verdict block + escape hatches
- `docs/planning/v7-leftovers-queue.md` — v7.5 sibling (same template)
