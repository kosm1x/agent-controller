# v7.7 Spine 5 — Bundle 1 Audit Log (self-defining cohort + operator-profile skeleton)

> **Spine**: 5 of 7 (Conway Pattern 2 — self-defining cohort + Q3 operator-profile skeleton)
> **Bundle**: 1 of 3 — schema + deterministic roll-up engine
> **Tier**: 1 (~330 LOC code + 15 tests)
> **Spec authority**: `reference_conway_2005_sms.md` §Pattern 2 + V7.7-GUIDE/PREPLAN Q3

## Bundle scope

| File                                  | Change                                                                                                |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `src/db/index.ts`                     | Additive migration: `self_defining_cohort` + `operator_profile` + indexes                             |
| `src/cohort/self-defining.ts`         | NEW — `rollUpCohort` (deterministic), `getCohort/getCohortMember/getOperatorProfile`, scoring helpers |
| `src/cohort/self-defining.test.ts`    | NEW — 15 tests                                                                                        |
| `docs/audit/v7.7-spine-5-bundle-1.md` | This file                                                                                             |

## Design notes

- The roll-up is **deterministic** — pure data aggregation (activity counts, priority, recency), no LLM. Cohort derivation is substrate; semantic judgment is V8.1/V8.2 capability.
- `operator_profile` is the Q3 carve-out skeleton: `written_by` CHECK admits only `'cohort-rollup'` (structural single-writer enforcement); `attribute_key`/`attribute_value` are opaque TEXT so V8.2 can redefine value-side semantics with zero migration (anti-mission test passes).
- `member_kind` CHECK admits `'thread'` as documented forward-compat headroom; the v7.7 roll-up populates only `project` + `objective` (only 3 coarse system conversation banks exist — no meaningful thread signal).

## qa-auditor R1 — PASS WITH WARNINGS

0 Critical · 4 Warning · 3 Recommendation. W2/W3/W4 + R3 folded in-bundle; W1/R2 are Bundle 2 scope; R1 no action.

| ID  | Finding                                                                                          | Disposition                                                                                                           |
| --- | ------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------- |
| W1  | `rollUpCohort` has no caller — cohort never populated in production                              | NOT A FOLD — Bundle 2 wires the daily cron. Expected substrate staging.                                               |
| W2  | `operator_profile` rows for a dropped-out candidate leaked monotonically (roll-up never DELETEd) | FOLDED — roll-up now `DELETE`s operator_profile rows for non-candidates; +1 test                                      |
| W3  | COHORT_MAX boundary ties resolved non-deterministically (sort had no tiebreaker)                 | FOLDED — `member_id.localeCompare` stable tiebreaker added                                                            |
| W4  | No FK from `operator_profile.cohort_member_id` to `self_defining_cohort.member_id`               | FOLDED — added `FOREIGN KEY ... ON DELETE CASCADE` (member_id is UNIQUE → valid FK target)                            |
| R3  | Test gaps: empty-candidate branch, negative `age_days`, malformed `updated_at`, the W2 leak      | FOLDED — +4 tests (empty set, future-dated objective, malformed date, dropout cleanup)                                |
| R1  | `getCohort` uses `SELECT *`                                                                      | NO ACTION — codebase-consistent (skills, general_events retrieval all do); runtime-safe (better-sqlite3 keys by name) |
| R2  | No metric on `RollUpResult`                                                                      | NOT A FOLD — Bundle 2 ships the Prom counter alongside the cron wiring                                                |

**Auditor-verified sound**: `written_by` CHECK genuinely blocks a non-rollup INSERT (`SQLITE_CONSTRAINT_CHECK`, reproduced); anti-mission opaque-TEXT contract holds; bound modifier in `datetime('now', ?)` works; `julianday()` of a malformed date → NULL → `age_days` null → recency 0 as designed; future `updated_at` → negative age clamped by `Math.max(0,…)`; transaction nesting sound; empty-`allIds` branch deactivates everything correctly; `first_seen_at` preserved across runs.

## R2 decision

**Not run.** 0 Criticals; folds are one `DELETE`, a sort tiebreaker, an FK clause, and 4 additive tests — no contract drift, no adjacent surface. Spine 3/4 cadence.

## Verification

- `npx vitest run src/cohort/self-defining.test.ts` → 15/15 pass
- `npm run typecheck` → 0 errors

## Next

Bundle 2 — daily roll-up cron (node-cron, mirror `src/skills/test-sweep.ts`) + `mc-ctl cohort {list,show,rollup}` operator surface + `mc_cohort_rollup_total` Prom counter. Bundle 2 resolves W1 (wiring) and R2 (observability).
