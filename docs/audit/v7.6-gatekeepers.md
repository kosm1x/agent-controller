# v7.6 Spine 1 — Gatekeeper Audit Pass

**Started**: 2026-05-08
**Status**: In progress (2 / 9 candidates audited)
**Lens**: 5-criterion check from `V7.6-GUIDE.md` — applied to every LLM-routing parser in the codebase.

The orienting bug: `prompt-enhancer-leakage` (2026-05-08, see `feedback_prompt_enhancer_leakage.md`). LLM returned one of N enum decisions → parser had a `return raw` legacy fallback → user saw raw blob shipped as a "27 questions" payload. Spine 1's job: find the others.

---

## Audit lens (apply to each candidate)

| #   | Criterion                              | What it prevents                                                                          |
| --- | -------------------------------------- | ----------------------------------------------------------------------------------------- |
| 1   | **Typed enum return shape**            | No `string` returns that could carry raw LLM content.                                     |
| 2   | **Parse-failure = hard-default**       | Never `return raw` or echo unparsed content to the user.                                  |
| 3   | **Log counter coupling**               | RC4-style producer/consumer test pins log claims to actual emission.                      |
| 4   | **Cold-start guard**                   | First-call/empty-state path returns a sane default, not bare LLM output.                  |
| 5   | **Risk carve-out on ergonomic relief** | Every "fast path" / "[]-on-greeting" / "skip-on-low-confidence" has a high-risk override. |

**Per-candidate scoring**: ✅ pass / ⚠️ partial / ❌ fail / N/A.

---

## Candidate roster (9)

| #   | Module                                         | Status                                                    |
| --- | ---------------------------------------------- | --------------------------------------------------------- |
| 1   | `messaging/scope-classifier.ts`                | ✅ Audited 2026-05-08                                     |
| 2   | `dispatch/classifier.ts`                       | ✅ Audited 2026-05-08 (heuristic; lens partially applies) |
| 3   | `messaging/confirmations.ts`                   | ⏳ Pending                                                |
| 4   | `messaging/fast-path.ts`                       | ⏳ Pending                                                |
| 5   | `runners/fast-runner.ts` (hallucination guard) | ⏳ Pending                                                |
| 6   | `messaging/post-filter.ts`                     | ⏳ Pending                                                |
| 7   | `prometheus/reflector.ts`                      | ⏳ Pending                                                |
| 8   | `prometheus/planner.ts`                        | ⏳ Pending                                                |
| 9   | `messaging/normalize.ts`                       | ⏳ Pending                                                |

---

## #1 — `messaging/scope-classifier.ts`

**Audited**: 2026-05-08
**Verdict**: ⚠️ Partial — strong on return-shape and carve-outs; gap on observability.

| Lens                                  | Result | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Typed enum return                  | ✅     | Returns `Set<string> \| null`. Each member round-trips through `VALID_GROUPS` allowlist (`scope-classifier.ts:131-137`). Raw LLM content cannot leak into the set.                                                                                                                                                                                                                                                                                                                                |
| 2. Parse-failure hard-default         | ✅     | All parse failures → `return null`. Outer try/catch on infer error → `return null` (line 115-117). `null` triggers regex fallback in `scope.ts:1081` (else branch). No `return raw` path.                                                                                                                                                                                                                                                                                                         |
| 3. Log counter coupling               | ❌     | No counter at all. Caller (`router.ts:1669-1671`) only logs the resulting `activeGroups`, not whether the semantic classifier returned `null` / `[]` / groups, nor whether regex fallback fired. RC4-style coupling test absent. **Spine 2 territory.**                                                                                                                                                                                                                                           |
| 4. Cold-start guard                   | ✅     | `withTimeout(infer(...), 3_000)` → cold-start latency triggers timeout → `null` → regex fallback.                                                                                                                                                                                                                                                                                                                                                                                                 |
| 5. Risk carve-out on ergonomic relief | ✅     | The "Short follow-ups → []" prompt rule is the ergonomic-relief branch. **Carve-outs already exist downstream** in `scope.ts:953-1080`: Google URL injection, Google content carryover, SEO injection, NorthStar noun injection, coding/SQL/DENUE safety net (post-2026-05-06), `jarvis_write` personal-learning safety net (post-2026-05-07), and a final `pattern.test(priorText)` inheritance pass (line 1073-1080). Five layers of override before the empty-set is treated as authoritative. |

**Findings**:

- **F1 (MEDIUM)** — No producer/consumer log coupling. When the operator reports "scope didn't fire on X", there's no journalctl evidence of what the classifier actually returned vs what the safety nets added vs what regex fallback contributed. Add structured log line: `scope.classify result=<json> source=<llm|regex|inheritance> safety_nets=[...]`. Spine 2 will catch this on the sweep, but flagging here so it's tracked.
- **F2 (LOW, cosmetic)** — File header still says "v6.4 CL1.1" (line 4). Reality: scope.ts has been heavily extended through v7.5+. Header is stale doc, not a bug.

**No fix bundle this round.** Both findings are observability gaps, not gatekeeper holes. Defer F1 to Spine 2's sweep.

**Side-discovery (material to friction-pickup #1)**:

`scope.ts:1177-1180` comment confirms: _"JARVIS_WRITE_TOOLS — 2026-05-07 promoted to MISC_TOOLS (always-on) per operator directive. Gating block removed; the `jarvis_write` regex group still fires for telemetry/inheritance purposes but no longer changes the tool surface."_

→ The friction-pickup brief's question _"verify whether the always-on promotion landed for `jarvis_file_write` specifically"_ is **answered: yes, it landed**.

→ Therefore the operator-reported friction class ("Procede" / "Dale" don't activate `jarvis_file_write`) **cannot be scope-loss for the write tools** — those tools are already in scope on every message. The friction must be at a different layer:

- Possibility A: the LLM in the fast runner has `jarvis_file_write` in its tool list but doesn't decide to call it (decision-layer issue, not scope-layer).
- Possibility B: a different write tool (not `jarvis_file_write`) is the one missing — e.g. `northstar_create_task`.
- Possibility C: the scope of a NorthStar-write was lost while jarvis_write surface stayed available, and the operator conflated the two in the report.

The friction-pickup brief should be amended to reflect this discovery before #1 work begins. Adding a 1-paragraph addendum at the bottom of `next-session-friction-pickup.md` is the right next step — keep the brief honest.

---

## #2 — `dispatch/classifier.ts`

**Audited**: 2026-05-08
**Verdict**: ⚠️ Partial — heuristic (not LLM-routed), so the prompt-enhancer-leakage class doesn't apply directly. The lens still surfaces one undocumented carve-out worth confirming.

| Lens                                  | Result | Evidence                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1. Typed enum return                  | ✅     | Returns `ClassificationResult` with `agentType: AgentType` (5-member enum) and `modelTier: ModelTier` (3-member). Explicit override gated by `VALID_AGENT_TYPES.has(...)` (line 130). The `reason: string` field is a debug log artifact, not a routing input. No raw LLM content path — this classifier is keyword/score-based, not LLM-driven. |
| 2. Parse-failure hard-default         | N/A    | Heuristic — no parse step.                                                                                                                                                                                                                                                                                                                       |
| 3. Log counter coupling               | ⚠️     | `reason: string` is built up from append-as-you-go; no producer/consumer coupling test asserts that the reasons array length matches the score deltas applied. Low priority — drift here would only show up in telemetry, not user-visible routing.                                                                                              |
| 4. Cold-start guard                   | ✅     | `getOutcomeAdjustments` returns `ZERO` when `totalOutcomes < 5` (line 299). Per-signal guards inside also require `s.total >= 3` or `>= 5`. Outcome-adjustment never moves the score with insufficient data.                                                                                                                                     |
| 5. Risk carve-out on ergonomic relief | ⚠️     | **Lines 145-153 unconditionally route messaging tasks to `fast`**, including high-stakes data prompts that DO get `modelTier: "capable"` but stay on the `fast` runner. This is intentional — fast can use DENUE/coding tools and the fast→heavy escalation cost would dominate — but it is **not documented as a deliberate carve-out**.        |

**Findings**:

- **F3 (LOW)** — Messaging fast-routing (line 145-153) is an undocumented carve-out. A messaging task with `denue` + `top 50 + scoring` keywords gets `modelTier: "capable"` but `agentType: "fast"`. If fast lacks the bandwidth for a 50-row scoring loop, the task would silently underperform. The current behavior is plausibly correct (fast has the tools), but it should be either (a) reaffirmed by a comment block, or (b) carved-out for "very high stakes" via an additional escalation path. **Recommend**: comment block in this audit round; revisit if telemetry shows messaging+high-stakes-data tasks underperforming.
- **F4 (LOW)** — `reason: string` is a free-form telemetry field with no schema. If a downstream consumer parses it, they'll grep on substrings. No consumer found in this audit, so deferring.

**No fix bundle this round.** Heuristic classifier, no leakage class found.

---

## #3 — `messaging/confirmations.ts`

**Audited**: 2026-05-08
**Verdict**: ❌ Real finding — producer/consumer drift across two parallel regex sets. Canonical RC4-style mismatch.

| Lens                                  | Result | Evidence                                                                                                                                                                                                                                                              |
| ------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Typed enum return                  | ✅     | `"confirm" \| "decline" \| null` (line 110-112). Strict union, no string passthrough.                                                                                                                                                                                 |
| 2. Parse-failure hard-default         | ✅     | No-match → `null`. Caller treats null as "not a confirmation" and proceeds to normal routing.                                                                                                                                                                         |
| 3. Log counter coupling               | ❌     | No counter. **More importantly: a SECOND CONFIRM_PATTERN exists at `runners/fast-runner.ts:104`** for the deletion-confirmation flow, and the two diverge. See F5 below.                                                                                              |
| 4. Cold-start guard                   | N/A    | Stateless regex.                                                                                                                                                                                                                                                      |
| 5. Risk carve-out on ergonomic relief | ⚠️     | 60-char threshold (line 115) is uniform. A high-stakes destructive operation (delete N tasks, send mass email) gets the same lax matcher as a benign one. No carve-out for `requiresConfirmation && destructiveHint` tools to require stricter / longer-form replies. |

**Findings**:

- **F5 (HIGH — real bug class)** — Two parallel CONFIRM_PATTERNs exist in the codebase, serving overlapping but divergent purposes:

  | Location                                     | Pattern coverage                                                                                                                                                         |
  | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
  | `messaging/confirmations.ts:99`              | `s[ií]\|dale\|hazlo\|procede\|adelante\|ok\|okey\|va\|confirmo\|env[ií]a(lo)?\|m[aá]ndalo\|ejecuta\|claro\|por\s*favor\|yes\|go\|confirm\|send\s*it\|do\s*it\|approved?` |
  | `runners/fast-runner.ts:104` (deletion-only) | `s[ií]\|confirmo\|dale\|ok\|yes\|hazlo\|adelante\|procede\|proceed\|confirm\|bórrala\|bórralas\|elimínalas?\|go ahead\|do it\|haz(?:lo)?\|claro`                         |

  Drift directions:
  - `confirmations.ts` has `m[aá]ndalo, env[ií]a(lo)?, ejecuta, send_it, approved` (production verbs).
  - `fast-runner.ts` has `bórrala, bórralas, elimínalas, proceed, go ahead, haz(lo)?` (deletion-clitic forms).
  - **Neither has** `súbelo/súbela, créalo/créala, lánzalo/lánzala, tráelo/tráela, ágregálo/agrégala, cámbialo/cámbiala` — common Spanish imperative-clitic confirmations the user naturally produces in response to LLM prompts like "¿Subo el archivo?" / "¿Creo el evento?" / "¿Lanzo el experimento?".
  - **No coupling test** asserts that the LLM-side prompts (system prompt + tool descriptions that elicit confirmation questions) align with what either regex set will accept.

  This is the canonical RC4 mismatch: producer (LLM-asked confirmation) and consumer (regex matching user reply) drift silently. When they diverge, the user sees Jarvis asking "¿Lo subo?", they reply "Súbelo", and the confirmation matcher returns `null` → the pending operation never executes → user retypes longer phrasing → friction tax.

  **Adjacent context**: This is the bug class implied by friction-pickup #1's "short Spanish imperatives" pattern, but `procede`/`dale`/`hazlo` already match in BOTH regexes. So friction-pickup #1's specific incident is NOT this F5 — it's a different layer (LLM didn't gate on CONFIRMATION_REQUIRED, or the user message wasn't routed to the confirmation matcher because no pending was stashed). F5 stands as its own real bug.

- **F6 (MEDIUM)** — Uniform 60-char threshold. Destructive ops (delete, send, push) should require stricter matching — e.g. exact-anchor or longer-form reply — to prevent typo'd "noo k" or "no estoy seguro pero ok" from accidentally confirming a high-risk operation. Carve-out missing.

- **F7 (LOW)** — No log counter for which pattern matched / which path was rejected. Spine 2.

**Fix bundle scope** (proposed, not shipped this round — operator should weigh in on regex changes that touch destructive-op confirmation flow):

1. Unify the two regexes behind a shared `CONFIRMATION_VERBS` constant + small variants per call site.
2. Add the missing imperative-clitic forms (súbelo, créalo, lánzalo, tráelo, etc.).
3. Add a coupling test in `confirmations.test.ts` that:
   - Pins the LLM-side prompt forms (a representative sample harvested from journalctl + tool descriptions that elicit confirmation).
   - Asserts every natural reply to those forms matches CONFIRM_PATTERNS.
   - Fails the test when a new tool with `requiresConfirmation: true` adds a producer-side phrasing not covered by the consumer.
4. Add risk carve-out: if the pending op's tool has `destructiveHint: true`, require a stricter match (full-word anchor, no substring; minimum reply length 2 chars; no character-class slop).

**Recommend**: surface F5 + F6 to operator before regex change. Confirmation surface is touchy; a wider regex risks accidentally confirming on incidental reply text.

### F5 + F6 fix shipped 2026-05-08

Operator approved fix bundle. Implementation:

- **New file** `src/messaging/confirmation-verbs.ts` — shared verb vocabulary, exports `buildConfirmRegex("lax" | "strict")` and `buildDeclineRegex()`. Single source of truth for both call sites.
- **Updated** `src/messaging/confirmations.ts` — imports shared regex builders. `detectConfirmationResponse(text, options?)` now accepts `{ strict?: boolean }`. Strict mode drops broad action verbs + clitic forms, requires `≤30` chars (vs `60` lax).
- **Updated** `src/runners/fast-runner.ts:104` — `CONFIRM_PATTERN` now built from the shared `buildConfirmRegex("lax")` so it can no longer drift from the messaging-side regex.
- **Updated** `src/messaging/router.ts:1413-1422` — looks up `pendingConf.toolName` in `toolRegistry`, passes `strict: true` when `destructiveHint === true`. F6 carve-out wired.
- **Verb additions** (clitic forms previously missing from BOTH regexes): subir/crear/lanzar/traer/guardar/agregar/añadir/cambiar/modificar/escribir/actualizar/programar/descargar/compartir/publicar/notificar/enviar/mandar/borrar/eliminar/quitar/remover/pautar/habilitar/deshabilitar/activar/desactivar.
- **Tests added**: 47 new assertions in `confirmations.test.ts` covering (a) lax-mode clitic coverage, (b) strict-mode rejection of action verbs, (c) strict-mode 30-char tightening, (d) **producer/consumer coupling pins** — 8 representative LLM prompt forms × natural reply corpus.

**Verification**: typecheck clean. 4451 / 4451 tests pass (+47 new). Zero regressions in `fast-runner.test.ts` (110 passing) or `router.test.ts` (25 passing).

### Round 2 audit findings + fixes shipped 2026-05-08

qa-auditor returned **FAIL** with 2 critical issues. Both legitimate, both addressed:

**C1 — `va` / `go` over-fire in strict mode** (`confirmation-verbs.ts:19` pre-fix)

- Pre-fix: `GENERIC_CONFIRM_SRC` included bare `va` and `go`. Strict mode draws only from GENERIC, so `"va para allá"`, `"go away"`, `"go home"` all confirmed when a destructive op was pending — exact bug class the audit was built to find.
- Fix: removed `va` and `go` from GENERIC. `vale` moved to ACTION (lax-only) since it can appear mid-sentence as filler. `go ahead` is in EN_CONFIRM_SRC; bare `go` is no longer accepted.
- Tests: 5 new C1 regression-guard cases (`"va para allá"`, `"va a casa"`, `"go away"`, `"go to hell"`, `"go home"`) all assert `null` in strict mode.

**C2 — Strict mode rejected legitimate destructive confirmations** (`confirmation-verbs.ts:93-104` pre-fix)

- Pre-fix: strict mode used `[GENERIC_CONFIRM_SRC]` only. `"Bórralo"` (delete-clitic confirming a delete op) was rejected → user sees no acknowledgment, retries, gives up. UX regression on the most common destructive flow.
- Fix: introduced `DESTRUCTIVE_CLITIC_CONFIRM_SRC` (verbs semantically aligned with destructive ops: `b[oó]rr|elim[ií]n|qu[ií]t|remu[eé]v|desact[ií]v|deshabil[ií]t`). Strict mode now accepts GENERIC + this subset, so `"Bórralo"` confirms but `"Súbelo"` (verb/op-type mismatch) still doesn't.
- Tests: 13 new positive cases for destructive clitics in strict mode + 5 new negative cases for non-destructive clitics in strict mode.

**W3 — `procédelo` not covered**: added `proced(?:elo|ela|elos|elas)` to ACTION_CONFIRM_SRC (lax-only, since strict already covers the destructive semantics it would otherwise satisfy).

**W1 — strict-mode false-positive coverage**: closed by C1 regression-guard tests above.

**W2 — Router integration test for `destructiveHint` lookup chain**: deferred. The chain `pendingConf.toolName → toolRegistry.get → ?.destructiveHint` is short and the fallback (no tool found → no `destructiveHint` → lax mode) is safe. Worth adding eventually but not blocking this bundle.

**Final verification**: typecheck clean. **4470 / 4470 tests pass** (+66 new vs pre-Spine-1 baseline). qa-auditor 2-round cadence captured what the 1-round bundle missed — exactly the pattern documented in `feedback_strict_mirror_sync_pattern.md` (round 1 catches concrete bugs; round 2 catches contract-level issues).

### Round 2 follow-up audit (post-commit `90ebc31`) — additional findings shipped 2026-05-08

A second qa-auditor pass on the committed bundle found 2 more critical issues + 1 standards violation. All addressed:

**C3 (load-bearing) — `hasUserConfirmedDeletion` regressed to lax** (`runners/fast-runner.ts:107`)

- Bundle's stated purpose: eliminate drift between the two confirmation regexes. `fast-runner.ts:104`'s pre-bundle hand-written regex was narrow and destructive-aligned (`bórrala/elimínalas`). Post-bundle it pointed at `buildConfirmRegex("lax")` — accepting `súbelo`, `créalo`, `lánzalo` etc. for a deletion two-step. This is the exact verb/op-mismatch the strict-mode carve-out was added to prevent. Drift moved, not closed.
- Fix: changed to `buildConfirmRegex("strict")`. Deletion two-step is inherently destructive; strict mode is the right vocabulary.

**C3 cascading refinement** — round-1 strict mode was over-aggressive

- Investigating C3 surfaced an existing test `runners/fast-runner.test.ts:723` that expects `Dale` to confirm a deletion two-step. Round-1's strict mode dropped ALL action verbs, which would have broken this. Reframe: action verbs (`dale`, `hazlo`, `procede`, `adelante`) are op-INDIFFERENT — they mean "go ahead with whatever you proposed" regardless of op type. The verb/op-mismatch class only applies to verb-typed clitics (`súbelo` to delete op).
- Fix: refined `buildConfirmRegex("strict")` to be `GENERIC + ACTION + DESTRUCTIVE_CLITIC` (was `GENERIC + DESTRUCTIVE_CLITIC` post-round-1). Existing fast-runner test passes; non-destructive clitics still gated.
- `vale` was considered for ACTION but EXCLUDED — same false-positive risk as `va`/`go` removed in C1 ("vale la pena" / "no vale" read as filler).

**C4 — `para` decline over-fires on Spanish preposition** (`confirmation-verbs.ts:128` pre-fix)

- Pre-fix: `DECLINE_SRC` included bare `para`. In Spanish overwhelmingly the preposition "for/towards" — `"para mí está bien"` (24 chars) registered as decline.
- Fix: replaced bare `para` with compound `para\s+ya`. Imperative "stop" still covered by `detente`, `alto`, `stop`. Added `alto` (Spanish "halt").
- Tests: 4 new C4 regression-guard cases (`"para mí"`, `"para allá"`, `"para que veas"`, `"para él"`) all assert `null`.

**S1 — Audit doc inflated hit-rate framing** (this section, pre-update)

- Pre-update: scoreboard claimed "3 real bugs in 3 audits = 100%". But C1 and C2 were SELF-INFLICTED regressions introduced by the round-1 bundle, not pre-existing bugs. F5 was the only pre-existing bug.
- Honest scoreboard:
  - F5 (1) — pre-existing bug found in audit
  - C1 / C2 (2) — bundle-regressions caught in round-2 before commit
  - C3 / C4 / refined-strict (3) — bundle-regressions caught in round-2 follow-up post-commit
- True audit value: 1 pre-existing bug + 5 bundle-regression catches across 2 audit rounds. The 2-round cadence is doing the work; the bundle iteration is the cost of a cleanly-shipped change.

**W1 — "producer/consumer coupling" test was misnamed** (`confirmations.test.ts:265`)

- Pre-fix: the test pinned hardcoded LLM-prompt → reply pairs but only ever asserted `detectConfirmationResponse(reply)`. The `llmPrompt` field was never passed through any producer code. It was a duplicate of the lax positive list with prose context.
- Fix: renamed `describe("producer/consumer coupling")` → `describe("reply vocabulary by op family")` with comment explaining what would be needed for a real coupling test (extract LLM-prompt strings from tool descriptions; assert each elicits a matching reply via mocked LLM or grep-extracted fixtures).

**W2 — Tool-annotation snapshot drift**: deferred with explicit note. `pendingConfirmations.ts` stores `{toolName, args, timestamp, summary}` only; `router.ts:1418` re-reads `destructiveHint` at confirm time. With v7.6 Spine 4 (MCP annotation push) coming, snapshot the destructive-hint at stash time instead. Tracked as a v7.6 leftover.

**R2 — Syntactic regex-source guards**: 3 new tests (`confirmations.test.ts:323-365`) trip CI if a future diff:

- Adds non-destructive clitic stems back into strict mode
- Removes destructive clitic stems from strict mode
- Resurrects bare `para` in DECLINE_SRC

**Final verification (round 2 follow-up)**: typecheck clean. **4479 / 4479 tests pass** (+9 vs round-1, +75 vs pre-Spine-1 baseline). Coverage parity with the old `fast-runner.ts:104` regex confirmed (bórrala/elimínalas/dale/hazlo/sí all match in strict mode now).

---

## #4 — `messaging/fast-path.ts`

**Audited**: 2026-05-08
**Verdict**: ✅ Pass — defensive pre-filter; false-negatives explicitly accepted by design.

| Lens                                  | Result | Evidence                                                                                                                                                                                                                                                    |
| ------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Typed enum return                  | ✅     | `isConversationalFastPath` returns `boolean`. `fastPathRespond` returns `string` (LLM raw content from infer), but it only runs after the conservative pre-filter has already gated entry — and the pre-filter requires ≤2 words AND a SAFE_PATTERNS match. |
| 2. Parse-failure hard-default         | ✅     | `result.content \|\| "👋"` fallback covers the empty-content case. Regex pre-filter has no parse step.                                                                                                                                                      |
| 3. Log counter coupling               | N/A    | No counter in this module.                                                                                                                                                                                                                                  |
| 4. Cold-start guard                   | ✅     | `wordCount >= 3` → reject (line 61). Conservative; multi-word inputs always go through the full pipeline regardless of pattern match. ≤2-word inputs that aren't in SAFE_PATTERNS also reject (line 67-69).                                                 |
| 5. Risk carve-out on ergonomic relief | ✅     | TOOL_TRIGGERS list (lines 32-46) is the carve-out. Even ≤2-word messages with tool-related vocabulary reject the fast-path. Bilingual coverage (ES/EN). The whole module is "ergonomic relief from the full pipeline" with the TOOL_TRIGGERS escape hatch.  |

**No findings.** False-negatives (full-pipeline routing for a fast-path-eligible message) are explicitly accepted in the file header comment ("false negatives are acceptable; false positives are NOT"). Architecture is sound.

---

## #5 — `runners/fast-runner.ts` (hallucination guard)

**Audited**: 2026-05-08
**Verdict**: ⚠️ Risk carve-out gap — fixed in this round.

| Lens                                  | Result | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Typed enum return                  | ✅     | `detectsHallucinatedExecution` returns `boolean`.                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2. Parse-failure hard-default         | N/A    | Regex-based detection.                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 3. Log counter coupling               | ✅     | Diagnostic logs (lines 342, 382, 463) emit on detection with `failedWriteTools` / `toolsCalled` arrays. Producer/consumer share the same arrays at-emit. No drift class.                                                                                                                                                                                                                                                                             |
| 4. Cold-start guard                   | N/A    | Stateless.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 5. Risk carve-out on ergonomic relief | ❌→✅  | **F8 (MEDIUM, fixed)** — Layer 2 verification/read carve-out (lines 405-410 pre-fix) skipped the entire write-claim block when `isVerificationRequest \|\| isReadRequest`. First-person past-tense verbs ("escribí", "actualicé", "I wrote") were exempted along with passive observation. Now: first-person carve-out always fires when no write tool was called, regardless of verification/read intent. Mirror of the W1 confirmations carve-out. |

**Findings**:

- **F8 (MEDIUM, fixed)** — Risk carve-out gap on Layer 2 verification/read exemption. A user message containing "verifica" or "lista" exempted ALL write claims, including first-person past-tense ("escribí", "actualicé", "I wrote"). User-visible failure mode: LLM responds to "verifica las tareas y actualízalas" with "Verifiqué y actualicé las 5 tareas" while only calling `jarvis_file_read` — the hallucination ships unflagged.

**Fix shipped**: extracted FIRST_PERSON_WRITE_RE / FIRST_PERSON_WRITE_EN_RE constants. Added a dedicated check before the Layer 2 exemption: if no write tool was called AND the response contains a first-person past-tense write verb, flag immediately. Verification/read exemptions still apply to passive observation ("están actualizadas") and detached state reports ("no las veo registradas").

**Tests added** (`fast-runner.test.ts`): 7 new cases — 4 positive (ES + EN first-person under verify/read intent) and 3 negative (passive observation under verify; detached state report under verify; first-person when write tool actually called).

---

## #6 — `messaging/post-filter.ts`

**Audited**: 2026-05-08
**Verdict**: ✅ Pass — detect-only with internally-consistent counter.

| Lens                                  | Result | Evidence                                                                                                                                                                         |
| ------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Typed enum return                  | ✅     | Returns `PostFilterResult` typed object. Detect-only — `text` field is unmodified user-visible content.                                                                          |
| 2. Parse-failure hard-default         | N/A    | Regex match only.                                                                                                                                                                |
| 3. Log counter coupling               | ✅     | Line 130-132 emits `flagCount` then `flags.slice(0,5).join(", ")`. Both derived from the same `result` object. Single source. (Spine 2 cross-check confirmed this is non-drift.) |
| 4. Cold-start guard                   | N/A    |                                                                                                                                                                                  |
| 5. Risk carve-out on ergonomic relief | N/A    | No ergonomic-relief branch — module never modifies user-visible content.                                                                                                         |

**No findings.** Detect-only architecture is sound.

---

## #7 — `prometheus/reflector.ts`

**Audited**: 2026-05-08
**Verdict**: ✅ Pass — strong gatekeeper. Minor parseLLMJson leak path closed by the same fix as #8.

| Lens                                  | Result | Evidence                                                                                                                                                                                                                                          |
| ------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Typed enum return                  | ✅     | Returns `{ result: ReflectionResult; usage: TokenUsage }`. `sanitizeDimensions` (line 91-110) drops malformed entries, clamps scores, rejects unknown dimension names. No raw LLM content path.                                                   |
| 2. Parse-failure hard-default         | ✅     | `try { parseLLMJson(content) } catch { heuristicFallback(graph) }` (line 156-185). Graceful degrade; never throws to caller. Model citizen.                                                                                                       |
| 3. Log counter coupling               | ✅     | Multiple console.log lines (213, 238, 256, 289) are derivation-style (computed scores), not counters of "things processed". Producer/consumer share data structure at-emit.                                                                       |
| 4. Cold-start guard                   | N/A    | Per-task.                                                                                                                                                                                                                                         |
| 5. Risk carve-out on ergonomic relief | ✅     | `scoreOverridden` flag (lines 209/219/237/255/288) drops `assessment.dimensions` from output if any heuristic overrode the LLM score. Prevents shipping LLM rationale that contradicts the kept score (already addressed by audit W1 — line 207). |

**Findings**:

- **F9 (LOW, fixed by #8 fix)** — `console.warn` at line 182 included `err.message` directly when parse failed. With pre-fix `parseLLMJson` embedding raw content in `.message`, this would print up to 500 chars of raw LLM output to journalctl. Operator-only (not user-visible), but still a hardening target. Fixed by the same `LLMJsonParseError` typed-error introduction in #8: reflector now branches on `err instanceof LLMJsonParseError` and uses `.diagnosticDetail()` — generic message + structured detail, never bare raw content.

---

## #8 — `prometheus/planner.ts`

**Audited**: 2026-05-08
**Verdict**: ❌→✅ Real bug — fixed in this round (HIGH severity, user-visible leak).

| Lens                                  | Result | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Typed enum return                  | ✅     | `plan()` and `replan()` return `{ graph: GoalGraph; usage: TokenUsage }`. `parseGoalGraph()` returns `GoalGraph` (typed object).                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2. Parse-failure hard-default         | ❌→✅  | **F10 (HIGH, fixed)** — `parseLLMJson` (`types.ts:247-280` pre-fix) embedded `Raw (first 500 chars): ${text}` in the thrown `Error.message`. Caller `planner.ts:171`'s exception bubbled to `orchestrator.ts:129` which wrapped: `"Planning failed: ${err.message}"`. That wrapper landed in `RunnerOutput.error`, persisted to `tasks.error`, and emitted in `task.failed` events. Two user-visible surfaces: `router.ts:2321` (background-agent failure reply) and `rituals/dynamic.ts:574` (scheduled-task failure broadcast to all channels). |
| 3. Log counter coupling               | ✅     | Diagnostic logs (lines 295, 319) are pin-with-data (dropped deps, validation errors). No counter drift class.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 4. Cold-start guard                   | N/A    |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 5. Risk carve-out on ergonomic relief | ⚠️     | Force-add path (line 282-310) silently commits goals with dropped refs after a stall pass. Diagnostic warning logged but graph still built. Internal-only (executor downstream gates destructive ops); not user-visible.                                                                                                                                                                                                                                                                                                                          |

**Findings**:

- **F10 (HIGH, fixed)** — Same prompt-enhancer-leakage class as the original Spine 1 motivator: error path leaking unparsed LLM output to the user. Up to 500 chars of raw LLM content (potentially containing prompt-injected instructions, hallucinated content, or internal reasoning) propagated through `Planning failed` to user-facing surfaces.

**Fix shipped**: introduced `LLMJsonParseError` typed error class (`types.ts`) with:

- Generic `.message = "LLM returned unparseable JSON"` — never carries raw LLM content
- `.rawSample: string` — first 500 chars, operator-visible only via `.diagnosticDetail()`
- `.stage: "no-object" | "extracted-invalid"` — discriminator for log analysis
- `.innerMessage?: string` — JSON.parse error detail when applicable

`planner.ts:parseGoalGraph` now catches the typed error, emits operator-only `console.warn` with `err.diagnosticDetail()`, and rethrows. Reflector applies the same pattern. Orchestrator's `Planning failed` wrapper inherits user-safety because it reads `err.message` — which is now generic.

**Tests added** (`types.test.ts` +6, `planner.test.ts` +1, `orchestrator.test.ts` +1):

- `error.message NEVER contains raw LLM content` (no-object stage)
- `error.message NEVER contains raw LLM content` (extracted-invalid stage)
- `rawSample is operator-visible and bounded to 500 chars`
- `stage discriminates no-object vs extracted-invalid`
- `diagnosticDetail() is operator-only (separate from .message)`
- Syntactic regression guard: `error.message must be exactly "LLM returned unparseable JSON"`
- Planner-level: `error.message does NOT leak raw LLM content (Spine 1 G8 invariant)`
- Orchestrator-level: `Planning failed wrapper does NOT leak raw LLM content (Spine 1 G8)`

---

## #9 — `messaging/normalize.ts`

**Audited**: 2026-05-08
**Verdict**: ✅ Pass — deterministic word-level replacement, no LLM call.

| Lens                                  | Result | Evidence                                                                                             |
| ------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------- |
| 1. Typed enum return                  | ✅     | `normalizeForMatching` returns `string` (transformed input). `wasNormalized` returns `boolean`.      |
| 2. Parse-failure hard-default         | ✅     | TYPO_MAP lookup; if no match, returns word unchanged. `text.normalize("NFC")` is safe on all inputs. |
| 3. Log counter coupling               | N/A    | No log counter.                                                                                      |
| 4. Cold-start guard                   | N/A    |                                                                                                      |
| 5. Risk carve-out on ergonomic relief | N/A    | No ergonomic-relief branch — purely a defense-in-depth normalization helper.                         |

**No findings.** Module is a small, well-scoped pure function with explicit "Used for matching only — does NOT modify the user message for display" caller convention.

---

## Round 1 — qa-auditor on `228c0cd` (PASS WITH WARNINGS → all addressed)

Audit found 1 critical bundle-regression (C1) + 2 warnings + 1 suggestion. C1 was a real bundle-regression (introduced by this bundle, not pre-existing); per `feedback_round2_audit_pattern.md`, bundle-regressions are catches but represent COST not finds.

**C1 (CRITICAL bundle-regression) — `FIRST_PERSON_WRITE_RE` lacks Spanish negation guard** (`fast-runner.ts:399-401` pre-fix)

- The new regex matches `escribí|actualicé|...` with `\s` token-boundary on both sides but no negation guard. Test case: `"He revisado las tareas — ya estaban actualizadas, no escribí nada nuevo"` would fire as hallucination → `task.failed` with generic-but-disruptive error. Symmetric to W1 (which the bundle says it mirrors), but Spanish allows "no" as a separate token before the verb, so the existing `\s` boundary doesn't protect.
- **Fix**: added variable-width JS lookbehind `(?<!\b(?:no|nunca|tampoco|jam[aá]s)\s+)` on the ES regex. EN side is structurally safe because the literal `I\s+` anchor doesn't match "I didn't" or "I never". Added `\b` start anchor on both regexes (W3).
- **Tests added (4)**: `"no escribí nada"`, `"No actualicé"`, `"Nunca subí"`, `"Tampoco envié"` — all assert non-flagging under verify/read intent.

**W1 (test gap) — no negative coverage for negation case**

- Closed by the 4 negation tests added for C1.

**W2 (test depth) — no syntactic regex-source guard for first-person carve-out**

- **Fixed**: 2 new behavior-driven syntactic guards added:
  - `regex-source guard: negation lookbehind catches all 4 ES negators` — iterates `["no", "nunca", "tampoco", "jamás"]` and asserts each blocks first-person verb flagging
  - `regex-source guard: every operator-typed verb stem still flags first-person` — iterates 12 verb stems and asserts each fires the carve-out
- These trip CI if anyone removes the lookbehind, weakens the verb list, or breaks the start anchor.

**W3 (\b start anchor) — `FIRST_PERSON_WRITE_RE` could theoretically match within concatenated tokens**

- Added `\b` at the start of both regexes (low-risk hardening; EN already had `I\s+` literal anchor so no behavior change).

**Verification**: typecheck clean. **4511 → 4517 tests pass** (+6 from C1+W2 fixes). All audit items closed.

### Honest scoreboard update post round-1

- 3 pre-existing bugs (F5 + F8 + F10) found across 9 audits
- 1 bundle-regression caught at round-1 (C1 — Spanish negation false-positive)
- Bundle-regression cost: 1 catch + 6 new tests + 1 round of audit-and-fix
- Without round-1 cadence, C1 ships to production and operators see noisy `task.failed` events on legitimate state reports

## Round 2 — qa-auditor on `a9ac208` (PASS WITH WARNINGS → all addressed)

Round 2 found 1 real gap (W1 — same C1 class, one negator further out) + 2 doctrine-hygiene warnings + 2 suggestions. All 3 material findings closed in this round.

**W1 (round-2 finding) — `ni` correlative negation missed by lookbehind**

- `"Ni escribí ni borré nada"` (correlative `ni…ni`) and `"Ni siquiera escribí"` (negation intensifier) both fire as hallucination. Same class as round-1 C1, just one negator further out.
- **Fix**: added `ni` and `siquiera` to the alternation. Full set now: `(?<!\b(?:no|ni|nunca|tampoco|jam[aá]s|siquiera)\s+)`.
- Tests added (2): `Ni escribí ni borré` correlative case + `Ni siquiera escribí` intensifier case.

**W2 (round-2 finding) — syntactic guard test enumerated only 12 of 22 verb stems**

- The "every operator-typed verb stem flags" test was a partial enumeration; if a future diff dropped a stem from the regex, only the 12 covered stems would trip the guard.
- **Fix**: parameterized over the FULL 22-verb-stem list (`escribí, actualicé, publiqué, subí, eliminé, borré, envié, configuré, instalé, activé, desactivé, limpié, creé, modifiqué, edité, guardé, programé, completé, marqué, empujé, commiteé, comiteé`) plus separate cases for the multi-word `hice push` / `hice commit` forms.

**W3 (doctrine hygiene) — honest scoreboard not yet updated for round-1 results**

- **Fixed**: this section + scoreboard below.

**S1 (suggestion, addressed) — comment naming Node engines floor**

- Added inline comment naming the `engines.node` ≥22.0.0 floor that supports JS variable-width lookbehind. Inoculates against future engine downgrade.

**S2 (suggestion, addressed) — pin "distant `no` does NOT block flagging"**

- Added test asserting that `"Hoy no hice mucho, pero escribí un documento"` correctly flags `escribí` (positive claim despite earlier `no`). Behavior pin against a future "let me make the lookbehind more permissive" diff.

**Verification**: typecheck clean. **4517 → 4520 tests pass** (+3 from round-2 additions: Ni correlative + Ni siquiera + distant-no behavior pin). Lookbehind variable-width verified working on Node 22.

### Honest scoreboard — Spine 1 final

| Phase                          | Pre-existing finds | Bundle-regression catches | Tests added |
| ------------------------------ | ------------------ | ------------------------- | ----------- |
| Initial audit (G1–G3)          | 1 (F5)             | 0                         | +47         |
| Round-1 / round-2 (F5 bundle)  | 0                  | 5 (C1/C2/C3/C3-r/C4)      | +28         |
| Initial audit (G4–G9)          | 2 (F8 + F10)       | 0                         | +15         |
| Round-1 audit (F8/F10 bundle)  | 0                  | 1 (C1 — ES negation gap)  | +6          |
| Round-2 audit (post-C1 commit) | 0                  | 1 (W1 — `ni` correlative) | +3          |
| **Spine 1 total**              | **3**              | **7**                     | **+99**     |

**Hit-rate sanity check**: 3 pre-existing bugs in 9 audits = 33% — matches V7.6-GUIDE.md projection ("~33% hit-rate"). Predicted +30-50 tests; actual +99 because audit-and-bundle iteration cost more rounds than the original projection assumed. The 7 bundle-regressions were caught BEFORE shipping to production — that is the cost of clean delivery on regex/parser surfaces.

**Lessons captured for round-2 audit pattern memory** (already in `feedback_round2_audit_pattern.md`):

- Bundle-regressions on negation guards come in classes — catch one form ("no"), the next form ("ni") follows in the next round (round-1 C1 → round-2 W1)
- Syntactic regex-source guards must enumerate the FULL alternation, not a sample, or future diffs slip through (round-2 W2)
- Distant-vs-immediate negation is a semantic distinction worth pinning explicitly (round-2 S2)
- 2-round audit cadence on regex bundles consistently turns up 1-2 catches per round; budget for 3 rounds when the regex is in destructive-op or LLM-error territory (this bundle: round-1 found 4 issues, round-2 found 5 minor issues — 9 total catches across 2 rounds)

## Spine 1 — CLOSED

All 9 gatekeepers audited. 3 pre-existing bugs found and fixed (F5 + F8 + F10). Spine 2 (observability backfill) shipped 2026-05-08. Recommended next: Spine 4 (MCP annotation push) or Spine 5 (skill engine surface). Spine 3 (Hindsight A/B/C) data-ready 2026-05-21.

---

## Running scoreboard (Spine 1 closure)

| Metric                                          | Count                                                                                                                                           |
| ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| Candidates audited                              | 9 / 9 ✅                                                                                                                                        |
| Pre-existing bugs found                         | 3 — F5 (CONFIRM regex drift), F8 (G5 verification carve-out gap), F10 (G8 LLM-content leak via planner errors)                                  |
| Bundle-introduced regressions caught pre-merge  | 2 (C1 `va`/`go` over-fire; C2 strict mode rejected legitimate destructive)                                                                      |
| Bundle-introduced regressions caught post-merge | 3 (C3 fast-runner regressed to lax; C3-refined `dale` rejection; C4 `para` decline)                                                             |
| Risk carve-outs flagged                         | 3 (F3 documented; F6 fixed; F8 fixed)                                                                                                           |
| Observability gaps logged                       | 3 (F1, F4, F7) — Spine 2 territory; F1/F7 closed by Spine 2 sweep                                                                               |
| Tests added (Spine 1 total)                     | 90 (75 from F5 bundle + 15 from F8/F10 bundle: 6 LLMJsonParseError invariants + 1 planner regression + 1 orchestrator wrapper + 7 G5 carve-out) |
| Side-discoveries material to other work         | 1 (friction-pickup #1 amendment shipped)                                                                                                        |

**Spine 1 hit-rate**: 3 pre-existing bugs in 9 audits = ~33%. Matches the V7.6-GUIDE.md projection ("6-9 audits × ~1h each. Likely +30-50 tests, +2-4 real bugs caught at the audit-bundle 33% hit-rate"). Tests came in higher (+90) but the bug count landed exactly in the predicted 2-4 range.

**Lessons captured for round-2 audit pattern memory** (already in `feedback_round2_audit_pattern.md`):

- Bundle that unifies parallel regexes can MOVE drift instead of closing it (F5 → C3)
- Strict mode must distinguish op-INDIFFERENT verbs from op-TYPED verbs (C3-refined)
- Decline-side regex is its own audit surface (F5 vs C4)
- "Coupling test" naming is load-bearing; misnamed tests give false confidence (W1)
- Risk carve-outs need carve-outs of their own (F8 — verify exemption needed first-person carve-out)
- Error-message leak class is its own audit-target distinct from JSON-parse correctness (F10)

Spine 1 closure: ready for v7.6 Spine 2 (DONE) → Spine 4 (MCP annotations) progression. Spine 3 (Hindsight A/B/C) data-ready 2026-05-21.
