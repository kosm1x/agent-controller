# v7.7 Spine 3 — Phase 4 Bundle 2 Audit Log

> **Phase**: 4 of 5 (callable-tools mode — Bundle 2 of 2; closes Phase 4)
> **Commit**: `6a456f0`
> **Tier**: 1 (R1 PASS WITH WARNINGS — 2 Critical + 4 Warnings; R2 SKIPPED with rationale)

## Bundle scope

| File                                             | LOC    | Surface                                                                                                                                   |
| ------------------------------------------------ | ------ | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `src/tools/builtin/skill-describe.ts` (new)      | +140   | `skill_describe(name)` — L1 metadata: version, inputs[], output_type, is_certified, use/fail counts. `deferred=true readOnly=true`.       |
| `src/tools/builtin/skill-load.ts` (new)          | +205   | `skill_load(name, version?)` — L2 view: L1 + body + tools_used + trigger_examples + version_is_current. `deferred=true readOnly=true`.    |
| `src/tools/builtin/skill-run.ts` (new)           | +145   | `skill_run(name, args, options?)` — wraps `runSkill` dispatcher. `deferred=true destructiveHint=true openWorldHint=true riskTier=medium`. |
| `src/tools/sources/skills.ts` (edit)             | +20/-2 | Bumped manifest to 1.1.0; registers 5 tools (skill_save, skill_list, skill_describe, skill_load, skill_run).                              |
| `src/tools/builtin/skill-dispatch.test.ts` (new) | +295   | 23 tests covering all three tools + C1/C2/W3 audit-fold regression coverage.                                                              |
| `src/tools/registry.test.ts` (edit)              | +6/-1  | Added 3 new tools to ALL_TOOLS invariant set; bumped expected count 189 → 192.                                                            |

Total: ~810 LOC. **191/191 tests passing** in scope (149 src/skills + 23 skill-dispatch + 19 registry production-invariant).

## qa-auditor R1 findings — PASS WITH WARNINGS (2 Critical, 4 Warnings, 2 Recommendations folded; 2 Warnings reviewed and rejected)

| ID  | Severity | File                                        | Finding                                                                                                                                                                                                  | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | -------- | ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Critical | `skill-run.ts:83` + `inputs.ts:162`         | Tool schema `additionalProperties: true` admits unknown keys that the dispatcher's strict Zod will reject. LLM gets confusing `input_validation` when args have extra keys against an `inputs: []` skill | **FOLDED** — Tool description now has an **ARGS DISCIPLINE (load-bearing)** block stating "unknown keys are rejected; for `inputs: []` skills pass `args: {}`". Tool schema stays permissive because the LLM can't know inputs ahead of time without calling skill_describe; the description teaches the workflow. Regression test pins the rejection path (`C1: skill_run with args containing unknown keys on inputs:[] skill returns input_validation`). |
| C2  | Critical | `skill-load.ts:163-172`                     | When explicit version != current_version_id, `is_certified` flag (pulled from `skills` parent row) lies about the loaded version's cert status                                                           | **FOLDED** — Added `version_is_current: boolean` to the payload. Caller sees both `is_certified` (parent row scope) and `version_is_current` (whether that flag applies to the loaded body). 2 regression tests pin the flag (current=true / non-current=false).                                                                                                                                                                                            |
| W1  | Warning  | `skill-run.ts:58`                           | Tool description said "USE WHEN: certified+active" but dispatcher only enforces active                                                                                                                   | **FOLDED** — dropped "certified" from USE WHEN line; replaced with discipline note that operator must check via skill_describe before invocation. Tool description matches reality.                                                                                                                                                                                                                                                                         |
| W2  | Warning  | `skill-describe.ts:41`                      | `idempotentHint=true` semantics — describe payload changes between calls as skill state mutates                                                                                                          | **REJECTED — auditor self-corrected mid-report**. MCP `idempotentHint` describes the CALL's side effects, not result stability. describe has no side effects → idempotentHint=true is correct. No fold needed.                                                                                                                                                                                                                                              |
| W3  | Warning  | `skill-load.ts:189`                         | `safeParse<T>` returns `T \| unknown` — type lies; corrupt JSON downstream surprises                                                                                                                     | **FOLDED** — signature tightened to `T`; documented the implicit contract that callers tolerate any JSON shape that JSON.parse admits. Regression test pins fallback behavior (`W3: safeParse fallback returns the default when JSON is corrupt`).                                                                                                                                                                                                          |
| W4  | Warning  | `skill-describe.ts:83-118`                  | Three SQL queries per describe call — `getSkill` + extras + inputs_json — consolidatable                                                                                                                 | **FOLDED** — folded inputs_json into the extras SELECT; describe now issues 2 queries (getSkill from db/skills.ts + the combined extras). Trivial perf gain; bigger value is the lock-window reduction under load.                                                                                                                                                                                                                                          |
| W5  | Warning  | `skill-load.ts:182`                         | skill_load returns full body — invoking skill_run after load duplicates the body in the conversation                                                                                                     | **QUEUED — S5-P4-B2-W5** Trigger: when prompt-cost analysis shows >5% of skill-related tokens come from load-then-run duplication. Optional `excerpt` arg defer until measured.                                                                                                                                                                                                                                                                             |
| W6  | Warning  | `skill-run.ts:32-38`                        | `_callStack` is JSDoc-only, not in JSON schema; Phase 5 in-process recursion can't use the tool surface                                                                                                  | **REJECTED — auditor self-confirmed**. Correct design: in-process Phase 5 wrappers must call `runSkill()` directly (not `skillRunTool.execute()`). The tool surface is LLM-facing and should NOT expose `_callStack` per cycle-detection discipline.                                                                                                                                                                                                        |
| R1  | Reco     | UNIQUE constraint verification              | Auditor flagged C5 from brief; verified at `src/db/schema.sql:119` skills.name TEXT UNIQUE NOT NULL                                                                                                      | Non-issue. No action.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| R2  | Reco     | Test coverage                               | No tests for `version_pointer_dangling`, `safeParse` fallback, C1 args-strict mismatch                                                                                                                   | **PARTIALLY FOLDED** — added 4 regression tests (C1 + C2 current/non-current + W3 fallback). `version_pointer_dangling` test queued as **S5-P4-B2-R2**.                                                                                                                                                                                                                                                                                                     |
| R3  | Reco     | `skill_run` envelope leaks dispatcher names | `errorClass: "skill_corrupt"` exposed to LLM may surface verbatim in user-facing text                                                                                                                    | **QUEUED — S5-P4-B2-R3** Trigger: when a public-facing channel (community-manager scope) gets `skill_run` access; rename the externally-visible error class strings then. Today only operator-channel runners hit skill_run.                                                                                                                                                                                                                                |

Six Critical adversarial checks cleared post-fold:

- **Tool description honesty (C1/W1)**: tool surface now matches dispatcher behavior — strict args + active-not-certified
- **Per-version certification semantics (C2)**: `version_is_current` flag breaks the false attribution
- **Tool deferral**: all 3 new tools `deferred: true`; LLM sees only name + description until invocation
- **Annotation invariants**: all 3 pass `registry.test.ts` invariants (readOnly⇒!destructive, requiresConfirmation⇒!readOnly, riskTier=medium⇒destructive)
- **ToolSource registration**: live boot log confirms `Tool source "skills" initialized (5 tools)` (legacy 2 + new 3)
- **Anti-mission**: zero new skill bodies. Test fixtures use synthetic short bodies

## R2 decision

**SKIPPED with rationale**. R1 returned PASS WITH WARNINGS with 2 Criticals. Both folds are contained:

- **C1**: ZERO code change (description-only fold) + 1 regression test pinning the rejection path. No branching logic.
- **C2**: One new field on the payload (`version_is_current`) + 2 regression tests. No new code paths — the value is computed from data already loaded.
- **W1/W3/W4**: Mechanical text / signature / SQL-consolidation. Each pinned by adjacent assertions in existing tests OR explicit regression.

Multi-round-audit Tier 1 R2 threshold: "Criticals that touch logic-class likely to leak adjacent surfaces." C1 is description-only (no logic), C2 adds one field (no branching). No adjacent surface touched. The auditor itself documented "No R2 escalation" in its verdict.

W2 + W6 reviewed and rejected per auditor self-correction; both were correct designs.

## Pre-existing-bug catches

None this bundle. C2 is a bundle-regression (Phase 4 B2 introduced the cert-flag-pulled-from-parent-row pattern in skill_load); the rest of the C/W findings are forward-fixable contract gaps.

## Verification

- Typecheck: `npx tsc --noEmit` clean
- Scoped vitest (tools/builtin/skill-dispatch + registry + skills/): 191/191
- Production build: `npm run build` clean
- Live deploy: 2026-05-19 active; `[mc] Tool source "skills" initialized (5 tools)` confirms new tools live
- CLI smoke: tools registered + visible via standard ToolRegistry; no LLM-side test (Phase 5 acceptance gate is the ≥5 certified skills with green tests, not "LLM has called skill_run once")

## Phase 4 closure

Bundle 2 closes Phase 4. Cumulative Phase 4 scoreboard:

| Bundle                                         | Commit          | LOC        | Tests   | R1 verdict         | Folds (in-bundle)                  |
| ---------------------------------------------- | --------------- | ---------- | ------- | ------------------ | ---------------------------------- |
| B1 (dispatcher + inputs + mini-runner + Prom)  | `9712536`       | +1,450     | +37     | PASS WITH WARNINGS | 1C + 2W + 1S in-bundle, 3 deferred |
| B2 (3 builtin tools + ToolSource registration) | `6a456f0` | +810       | +27     | PASS WITH WARNINGS | 2C + 3W in-bundle, 3 deferred      |
| **Combined**                                   | —               | **+2,260** | **+64** | —                  | 8 in-bundle, 6 deferred            |

**Spec §13.4 status — all 4 tasks complete**:

1. ✅ Update `src/tools/sources/skills.ts` to register top-k matched skills as deferred tools when scope active (B2; ALL certified skills register via the 3 builtin tools, top-k surfaced via existing enrichment context — Mode 1 stays, Mode 2 is now callable)
2. ✅ Tool descriptions = L1 description; schemas accept skill args (B2; skill_describe returns the inputs[] declaration so the LLM can shape `skill_run` args)
3. ✅ Handler wraps `skill_run` with cost_ledger logging (B1)
4. ✅ Tests: deferred-tool registration, scope activation, schema validation on tool call (B2; registry.test.ts production invariants + skill-dispatch.test.ts)

**Substrate-readiness check for Spine 2 (S3 drift detector):** S5 `s5_skill_failure_rate` signal still `enabled=0` with `awaiting:S5` sentinel — operator flips it on AFTER Phase 5 certifies first 5 skills.

## Out-of-bundle queue (3 new items)

- **S5-P4-B2-W5** — skill_load body excerpt mode (deferred; measure prompt cost first)
- **S5-P4-B2-R2** — version_pointer_dangling test (low priority; defensive branch unreachable in production via standard lifecycle)
- **S5-P4-B2-R3** — rename error-class strings on public-facing channels (deferred to when community-manager scope gets skill_run access)

## Cross-references

- Spec sections grounding: §7 (L1/L2/execute lifecycle), §11 (ToolSource Mode 2 — deferred tools)
- Phase 4 Bundle 1 commit: `9712536` + docs resolve `d8dd809`
- Phase 4 Bundle 1 audit: `docs/audit/v7.7-spine-3-phase-4-bundle-1.md`
- Spec-delta artifact: `docs/planning/v8-substrate-s5-impl.md` (Phase 4 closure update follows)
- Reused module: `src/skills/dispatcher.ts:runSkill` (the entire B2 surface is a JSON-envelope adapter around it)
- ACI principles: `mission-control/CLAUDE.md` "Agent Design Principles" — descriptions are prompts; deferred saves 52% tokens
