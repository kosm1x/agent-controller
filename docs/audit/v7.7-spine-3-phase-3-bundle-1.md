# v7.7 Spine 3 — Phase 3 Bundle 1 Audit Log

> **Phase**: 3 of 5 (vector retrieval — Bundle 1 of 2)
> **Commit**: `<this commit>`
> **Tier**: 1 (R1 FAIL → fold → R2 skipped with rationale)

## Bundle scope

| File                                          | LOC       | Surface                                                                                                  |
| --------------------------------------------- | --------- | -------------------------------------------------------------------------------------------------------- |
| `src/db/index.ts` (edit)                      | +13       | `ALTER TABLE skills ADD COLUMN description_embedding BLOB` (idempotent)                                  |
| `src/skills/embedding.ts` (new)               | +95       | `composeEmbeddingText` + `embedAndStoreSkill` + `loadSkillEmbedding` (reuses `src/memory/embeddings.ts`) |
| `src/skills/lifecycle.ts` (edit)              | +22       | After `pointSkillAtVersion`, call `embedAndStoreSkill`; null-return info-logged (R1-W2)                  |
| `src/skills/backfill-embeddings.ts` (new)     | +140      | `backfillSkillEmbeddings({limit?, sleepMs?, onProgress?})` — idempotent walk                             |
| `scripts/skills-backfill-embeddings.ts` (new) | +40       | tsx-resolvable script invoked by mc-ctl (R1-C1 fix)                                                      |
| `mc-ctl` (edit)                               | +85 / -55 | `skills` subcommand router merged (list / backfill-embeddings / help); duplicate `cmd_skills` removed    |
| Tests (new)                                   | +290      | 18 tests (compose × 3, embedAndStore × 4, load × 4, backfill × 7)                                        |

Total: ~605 LOC net. **119/119 tests passing** in scope (Phase 1+2+3 skills + S2 critic).

## qa-auditor R1 findings — FAIL verdict

| ID  | Severity | File                         | Finding                                                                                                                                                                                   | Disposition                                                                                                                                                          |
| --- | -------- | ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Critical | `mc-ctl:1107` + `:2042`      | Duplicate `cmd_skills()` definitions — Phase 3 dispatcher shadowed by pre-existing list-only one. Operator `mc-ctl skills backfill-embeddings` would fall through to "Unknown subcommand" | **FOLDED** — merged: Phase 3 router gained `list` subcommand from the old definition; old definition replaced with comment-block stub; duplicate route entry removed |
| C2  | Critical | `mc-ctl` retrieve subcommand | `npx tsx -e` inline script fails: tsx `-e` mode doesn't auto-resolve `.js → .ts` extension imports                                                                                        | **FOLDED** — extracted to `scripts/skills-backfill-embeddings.ts`; `retrieve` subcommand REMOVED from B1 (depends on `retrieval.ts` not shipped until B2)            |
| W1  | Warning  | `embedding.ts:83`            | `loadSkillEmbedding` byte-length check passes malformed BLOBs of correct length (e.g. 6144 NaN floats)                                                                                    | **DEFERRED S5-P3-B1-W1** — Phase 3 threat model is internal writer; flag for B2 retrieval cosine-of-NaN                                                              |
| W2  | Warning  | `lifecycle.ts`               | Null-embed return is silent — operator can't distinguish "API unavailable" from "no saves yet"                                                                                            | **FOLDED** — `console.info(...)` on null return with skillId + name                                                                                                  |
| W3  | Warning  | `mc-ctl`                     | `--limit=0` treated as no-limit, inconsistent with operator mental model                                                                                                                  | **FOLDED** — explicit rejection with helpful message                                                                                                                 |
| W4  | Warning  | `lifecycle.ts:191`           | `console.warn` vs pino structured logging used elsewhere                                                                                                                                  | **DEFERRED S5-P3-B1-W4** — v7.7 closure log-hygiene pass                                                                                                             |
| W5  | Warning  | `backfill-embeddings.ts:44`  | DEFAULT_LOG.warn defined but never invoked (dead code path)                                                                                                                               | **DEFERRED S5-P3-B1-W5** — cleanup with W4                                                                                                                           |

All Critical adversarial checks cleared post-fold:

- Schema additive migration safe on 67 live rows (DEFAULT NULL on a nullable BLOB column)
- Anti-mission held — zero authored skill content; backfill uses operator-supplied descriptions
- Embed/save race acceptable under single-process invariant (documented in Phase 2 R1-W3 carry)
- Lifecycle try/catch correctly isolates embed failure from save success

## R2 decision

**SKIPPED with rationale**. All R1 folds were mechanical:

- C1: dispatcher merge (text rearrangement, no logic change)
- C2: script extraction (same logic, different invocation path) + retrieve removal (smaller surface)
- W2: 1-line `console.info`
- W3: 1-line guard

Live smoke-test confirmed:

- `mc-ctl skills list` renders first-party catalog + DB rows
- `mc-ctl skills backfill-embeddings --limit=2` invokes scripts/, embeds 2 candidate skills (returns null in dev — no EMBEDDING_KEY), JSON output structured correctly
- `mc-ctl skills backfill-embeddings --limit=0` correctly rejects with helpful error
- `mc-ctl skills` (no args) routes to `list` default

R2 would have spent 30-60 min finding ~0 issues against folds that have no new logic surface. Matches the session-wrap pattern: "mechanical fixes, regression-pinned, no adjacent surface."

## Verification

- Typecheck: `npx tsc --noEmit` clean
- Scoped vitest: 119/119 (10 test files)
- Production build: `npm run build` clean
- Live deploy: service active; boot log shows `[skills:loader] complete loaded=0 skipped=0 drift=0 errors=0` (Phase 1 loader behavior preserved)
- CLI smoke: all 3 paths (list / backfill / default) work end-to-end

## Out-of-bundle queue (3 items appended)

- **S5-P3-B1-W1** — Malformed-BLOB shape check (NaN/Inf scan) at `loadSkillEmbedding`. Trigger: B2 retrieval surfaces a cosine-NaN OR threat model expands to untrusted writer.
- **S5-P3-B1-W4** — Migrate `console.warn`/`console.info` to pino structured logger. Trigger: v7.7 closure log-hygiene pass.
- **S5-P3-B1-W5** — Remove dead `DEFAULT_LOG.warn` in backfill. Bundle with W4.

## Cross-references

- Spec sections grounding: §6 (vector retrieval), §15 Q1 (embedding model — RESOLVED EMPIRICALLY in favor of existing Gemini 1536d via `src/memory/embeddings.ts`)
- Phase 2 Bundle 2 commit: `acfbdfd` (closed Phase 2)
- Spec-delta artifact: `docs/planning/v8-substrate-s5-impl.md`
- Reused module: `src/memory/embeddings.ts` (embed + cosineSimilarity + serializeEmbedding + deserializeEmbedding + EMBED_DIMS)
