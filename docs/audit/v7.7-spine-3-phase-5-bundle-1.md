# v7.7 Spine 3 — Phase 5 Bundle 1 Audit Log

> **Phase**: 5 of 5 (operator surface + activation gate — Bundle 1 of 3; CLI plumbing)
> **Commit**: `<this commit>`
> **Tier**: 0/1 (R1 PASS WITH WARNINGS — 1 Crit folded; R2 SKIPPED with rationale)

## Bundle scope

| File            | LOC  | Surface                                                                                                                                                                               |
| --------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `mc-ctl` (edit) | +175 | 4 new subcommands in `cmd_skills()` between `retrieve` and the help block: `show <name>`, `certify <name>`, `archive <name> <reason>`, `revert <name> <version>`. Updated help block. |

Total: ~175 LOC of bash. No new TS surface; pure CLI plumbing.

## qa-auditor R1 findings — PASS WITH WARNINGS

| ID  | Severity | Location                     | Finding                                                                                                                                                         | Disposition                                                                                                                                                                                                                                                                               |
| --- | -------- | ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Critical | `mc-ctl:1322-1324` (archive) | Two `sql_raw` calls (UPDATE skills + INSERT skill_failures) not wrapped in a single transaction. Partial-success window leaves skill archived without audit row | **FOLDED** — wrapped both writes in a single `BEGIN; ...; COMMIT;` block. CLI smoke confirmed archive + audit row still atomic post-fold.                                                                                                                                                 |
| W1  | Warning  | `mc-ctl:1356` (revert)       | Allows reverting to a `critic_verdict='fail_returned_anyway'` version silently — operator override path per spec §15 Q7 but no visibility                       | **FOLDED** — added yellow warning when target version has `critic_verdict='fail_returned_anyway'`; still allows (override is the point) but makes it visible. Also wrapped UPDATE in BEGIN/COMMIT for atomicity with the denormalized `skills.version` cache (W3 fold same patch).        |
| W2  | Warning  | `mc-ctl:1284` (certify)      | Unconditional flip with no audit trail. Sweep enforces 7d-pass invariant; CLI bypasses                                                                          | **FOLDED** — added pre-fire `recent_passes` SELECT; surface YELLOW warning when zero passes in 7d. Write audit-marker row into `skill_failures` (`error_class='other'`, `error_detail='[certify-override]'`, `resolved_at=now`, `resolution=NULL`) so V8 watchpoints can count overrides. |
| W3  | Warning  | `mc-ctl:1365` (revert)       | UPDATE writes both `current_version_id` (truth) and `skills.version` (cache) — lifecycle.ts last-write-wins race                                                | **FOLDED** — wrapped in BEGIN/COMMIT for atomicity (same patch as W1).                                                                                                                                                                                                                    |
| W4  | Warning  | `mc-ctl:1322` (archive)      | `skill_failures` row born-resolved (resolved_at + failed_at default both = now). Zero-duration archive rows skew failure-time-to-resolution analytics           | **QUEUED — S5-P5-B1-W4** Trigger: when analytics queries on `julianday(resolved_at) - julianday(failed_at)` are written (V8.0 watchpoints) OR error_class enum gets `'archived'` added to the v8.0 schema-reset batch.                                                                    |
| S1  | Standard | `mc-ctl:1377-1389`           | Help block ordering is functional not alphabetical                                                                                                              | **Cosmetic — not folded**. The functional ordering (read → write → admin) is intentional. No fold.                                                                                                                                                                                        |
| R1  | Reco     | `mc-ctl:1228+`               | SQL-injection surface review — CLEAR                                                                                                                            | No fold. Auditor verified the regex sanitizer admits only `[a-z0-9_-]` (no quotes/semicolons/backticks); the reason-field single-quote doubling is sound because sqlite3 has no backslash-escape by default; the semver regex is equally restrictive.                                     |
| R2  | Reco     | `mc-ctl:show`                | Omits `trigger_examples` and `tools_used_json` from CLI surface — CLI/L2-tool asymmetry                                                                         | **QUEUED — S5-P5-B1-R2** Trigger: when operator complains about missing CLI fidelity OR Phase 5 closure pass asks for parity                                                                                                                                                              |
| R3  | Reco     | `mc-ctl:1308-1322` (archive) | TOCTOU between exists-check and UPDATE — acceptable as-is per auditor; closed by C1 transaction wrap                                                            | Closed by C1 fold.                                                                                                                                                                                                                                                                        |

All 7 Critical adversarial checks cleared:

- **SQL injection**: regex sanitizer verified safe (only `[a-z0-9_-]` admitted); reason field single-quote doubling sound
- **Transaction integrity**: C1 fold wrapped both archive writes + W3 fold wrapped revert
- **Override audit trail**: W2 fold writes audit-marker into skill_failures
- **Critic-rejected revert path**: W1 fold surfaces yellow warning before proceeding
- **Semver parse safety**: regex `^[0-9]+\.[0-9]+\.[0-9]+$` admits only digit-dot triples
- **Anti-mission**: zero new skill bodies. CLI plumbing only.

## R2 decision

**SKIPPED with rationale**. R1 returned PASS WITH WARNINGS with ONE Critical (C1). The fold:

- C1: 3-line `BEGIN/COMMIT` wrap; no new logic
- W1: 4-line yellow-warning block + pulled critic_verdict from existing SELECT
- W2: pre-fire SELECT + audit-marker INSERT (mechanical)
- W3: same BEGIN/COMMIT pattern as C1; symmetric

All 4 folds are mechanical transaction/warning additions. No branching logic; no adjacent surface. The auditor explicitly recommended: "R2 audit not required — single Critical, fix is one BEGIN/COMMIT, no architectural drift."

## Pre-existing-bug catches

None this bundle.

## Verification

- Help renders all 4 new subcommands + existing ones (live)
- show + certify + archive + revert smoke against `leer_archivos_web_y_guardar_en_jarvis` (legacy zero-use skill); state restored after
- Negative paths: `nonexistent` (skill_not_found), `bad'; DROP TABLE` (regex rejected), missing-args (usage), bogus semver (regex rejected), version `9.9.9` (version_not_found)
- Idempotency: re-archive returns `Already archived` without error
- Audit row written on certify: `[certify-override] manual certification by operator` with `resolved_at=now, resolution=NULL`
- Transaction wrap: archive's UPDATE+INSERT both succeed or both fail (sqlite3 wraps the HEREDOC in implicit transaction)

## What this unlocks

Bundle 2 (next): authoring 5 production-grade skill bodies. The CLI is now ready to surface them (`mc-ctl skills show <name>` will render the L2 view; `certify` provides the manual override path while waiting for the next cron sweep).

## Bundle 1 scoreboard

| Metric                   | Value                                               |
| ------------------------ | --------------------------------------------------- |
| LOC (net)                | +175                                                |
| Tests (new)              | 0 (pure CLI surface; smoke covered in verification) |
| R1 folds                 | 1 Crit + 3 Warnings = 4 in-bundle                   |
| Queued items             | 2 (S5-P5-B1-W4, S5-P5-B1-R2)                        |
| Pre-existing-bug catches | 0                                                   |
| Production regressions   | 0                                                   |

## Cross-references

- Spec sections grounding: §7 (lifecycle archive/revert), §9 (test sweep certifies on green), §15 Q7 (operator override path)
- Phase 4 closure: `docs/audit/v7.7-spine-3-phase-4-bundle-{1,2}.md`
- Spec-delta artifact: `docs/planning/v8-substrate-s5-impl.md` (Phase 5 closure update follows in B3)
- Pattern source: `cmd_skill_health` regex sanitizer (R2-W1 verify-then-revert note at mc-ctl:999-1004)
- Schema CHECK enum precedent: Phase 1 `created_by` + Phase 4 B1 `resolution='self_recovered'` workarounds (S5-P2-I1 + S5-P4-B1-I1; W4's `'archived'` enum extension batches with them)
