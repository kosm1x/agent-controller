# v7.7 Spine 2 Bundle 2 — Audit Log

**Date**: 2026-05-19 · **Bundle**: S3 alerts delivered into morning brief + S3-I2 counter fold · **Cadence**: Tier-1 (R1 only — R2 not warranted; see scoreboard rationale)

## Bundle scope

| File                              | Status | Net change                                                                 |
| --------------------------------- | ------ | -------------------------------------------------------------------------- |
| `src/lib/s3/delivery.ts`          | new    | ~215 LOC                                                                   |
| `src/lib/s3/delivery.test.ts`     | new    | ~320 LOC                                                                   |
| `src/rituals/morning.ts`          | edit   | +37 LOC (optional `alertSection` arg + verbatim-copy directive)            |
| `src/rituals/morning.test.ts`     | edit   | +45 LOC (5 regression tests)                                               |
| `src/rituals/scheduler.ts`        | edit   | +25 LOC (compute alerts before `createMorningBriefing`)                    |
| `src/observability/prometheus.ts` | edit   | +35 LOC (`mc_s3_evaluator_errors_total{cadence,kind}` counter, S3-I2 fold) |
| `src/lib/s3/scheduler.ts`         | edit   | +30 LOC (3 wire-points for counter + cadence-typed `noticeError`)          |
| `src/db/index.ts`                 | edit   | +12 LOC (drift_alerts schema NOTE block, R1-I4 fold)                       |

**Total**: ~720 LOC across 8 files. 105 tests passing scoped (`src/lib/s3/` + `src/rituals/morning.test.ts` + `src/observability/prometheus.test.ts`). typecheck clean. dist/ builds. **Live deploy**: PID 3034044 active; `[s3] Seed signals: 0 inserted, 13 skipped` + `mc_s3_evaluator_errors_total` registered on `:8080/metrics`.

## R1 audit (qa-auditor)

**Findings**: 0 Critical / 4 Warning / 3 Info → 5 in-bundle folds + 3 deferred.

### Folded in-bundle (Warnings)

| #     | Severity | Surface                                                                                                                                                                            | Fold                                                                                                                                                                                                                                                                                             |
| ----- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --- | ----------- | --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1-W1 | Warning  | `morning.ts:44` — contradictory placement directives ("COPIA VERBATIM al final del email" vs "ANTES de Racha actual"). LLMs are unreliable at "move from here to there in output." | Replaced both with single unambiguous: "Al final del email, DESPUÉS de la línea Racha actual, incluye exactamente la siguiente sección. Cópiala tal cual..." Section is appended at the very bottom of the description; LLM just copies in-place.                                                |
| R1-W2 | Warning  | `delivery.ts:loadActiveAlertsForBrief` — `ORDER BY a.triggered_at DESC` with no `LIMIT` fetches all active alerts (10k worst-case during a burst) to render top 30.                | Pushed truncation to SQL: `LIMIT ${ALERT_SECTION_CAP + 1}`. The +1 row lets `formatAlertSection` detect overflow without a separate `COUNT(*)`. Overflow footer now reads "Más alertas activas — mostrando primeras 30" (no specific count, because we capped the fetch). Regression test added. |
| R1-W3 | Warning  | `drift_alerts.signal_id` has no `REFERENCES drift_signals(id)` clause. `foreign_keys=ON` doesn't enforce missing FKs. JOIN silently drops alerts whose signal was deleted.         | Switched to `LEFT JOIN` + `COALESCE(s.signal_name, '<deleted signal '                                                                                                                                                                                                                            |     | a.signal_id |     | '>')`in`delivery.ts`. Orphaned alerts now render visibly with a placeholder. Regression test seeds + deletes + asserts the alert still appears. Schema FK option deferred (S3-W3-fk in queue). |
| R1-W4 | Warning  | No tests for the W2 LIMIT pushdown or W3 LEFT JOIN orphan-tolerance.                                                                                                               | Added 2 regression tests: "SQL LIMIT caps fetch at ALERT_SECTION_CAP+1 rows" + "alert whose signal was deleted still surfaces with placeholder name." Both target the specific changes; would catch a future refactor that reverts them.                                                         |

### Folded in-bundle (Info)

| #     | Surface                                        | Fold                                                                                                                                          |
| ----- | ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1-I4 | Spec §8 has `delivery_status`/`acknowledged_at | by`/`resolution_kind`fields. Bundle 2 doesn't mutate any of them. Next maintainer reading the schema would assume`pending` means undelivered. | Added 8-line NOTE block in `src/db/index.ts` above the `drift_alerts` CREATE TABLE: documents that Bundle 2 doesn't touch `delivery_status`, that `pending` ≠ undelivered (just "not yet resolved/suppressed"), and that `signal_id` deliberately lacks `REFERENCES` (R1-W3 fold rationale). |

### Deferred (3 items)

| #     | Surface                                                                                                                                                      | Why deferred                                                                                                                                     |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| R1-I1 | Replace `---` section delimiters with explicit `<!--S3-VERBATIM-START-->` / `END` markers + LLM-eval regression assertion in `gmail_send` output.            | Cross-cuts the LLM-eval surface; bigger than this bundle. Queued as `S3-I1-verbatim-markers`.                                                    |
| R1-I2 | Integration test for `getTaskTemplate("morning-briefing")` wiring delivery → createMorningBriefing in order.                                                 | Components independently tested; integration is straightforward. Add as a hot-spot detector in Bundle 3 if needed.                               |
| R1-I3 | Tests that `recordS3EvaluatorError` actually increments via injected throws in scheduler.ts (mock evaluateSignal → throw → assert counter +1 for each kind). | Counter is registered live on `:8080/metrics` ✓. Mocking dynamic imports inside try/catch is awkward in vitest. Queued as `S3-I3-counter-tests`. |

## Scoreboard

```
Bundle:  v7.7 Spine 2 Bundle 2 (S3 morning-brief delivery + S3-I2 counter)
Rounds run: R1
Tier: 1 (R1+R2 — R2 SKIPPED, see rationale)

Findings:
- R1: 0 Critical, 4 Warning, 3 Info — 5 in-bundle, 3 deferred (with triggers)

Pre-existing bugs found: 1 (drift_alerts.signal_id has no REFERENCES; FK pragma
  on but schema lacks the clause — caught by R1-W3, folded via LEFT JOIN
  + COALESCE).
Bundle-regression catches: 3
  - W1 contradictory placement directive (LLMs unreliable at "move from here
    to there" — would have produced inconsistent email layouts)
  - W2 unbounded SQL fetch (10k rows for 30-row render at alert storm peak)
  - W4 missing regression tests for the W2+W3 fixes

Total rounds: 1

Verdict: PASS WITH WARNINGS (0 Crits + 4 W folded + 3 I deferred with triggers)
Closure-ready: Bundle 2 ships; Bundle 3 (P0 push + suppression + baseline
  aging) is the next ship to fully close Spine 2.
```

## Tier-1 → R1-only downgrade rationale

R2 catches "bugs INSIDE R1 fixes." Post-R1 surface examination:

- **W1 fix** (single placement directive): 3-line description edit. No code logic. No new failure surface.
- **W2 fix** (SQL LIMIT cap+1): mechanical SQL change. Updated test and overflow-footer wording to match. Pre-existing test "truncates at ALERT_SECTION_CAP" was updated to assert the new "no specific count" footer.
- **W3 fix** (LEFT JOIN + COALESCE): one SQL change. Pinned by a new regression test that seeds → deletes → asserts orphan-with-placeholder. Any future refactor that reverts to INNER JOIN would fail that test.
- **W4 fix** (tests added): new tests, no new code surface.
- **I4 fix** (schema NOTE block): documentation, no logic change.

R2 would have spent ~30-60 min finding ~0 new issues. Where R2 WOULD be mandatory: Bundle 3 (touches `drift_alerts.delivery_status` + adds P0 push notification + adds suppression API — three new state-mutation surfaces).

## Live verification

- ✅ Service active PID 3034044
- ✅ S3 cron jobs re-registered: `[s3] Registered 4 cron jobs (timezone America/Mexico_City)`
- ✅ All 13 signals seeded (re-seed idempotent, 0 inserted / 13 skipped)
- ✅ `mc_s3_evaluator_errors_total{cadence,kind}` counter live on `:8080/metrics`
- ⏳ First morning-briefing tick fires at 07:00 MX. Until then no alert section will inject because:
  - 3 enabled signals (s1_tool_cache_read_ratio, s2_critic_unfixable_rate, cost_per_brief_drift) haven't run yet today (nightly+weekly cadences)
  - Even when they run, none should trip in shadow mode — baselines are placeholders; observed values should be within tolerance
- Activation gate ("env edit triggers visible alert within 60s of next service interaction") is **not yet wired** — that requires Bundle 3's P0 push hook. Today's bundle delivers the morning-brief path only.

## Cross-references

- Predecessor: Bundle 1 audit `docs/audit/v7.7-spine-2-bundle-1.md`
- Predecessor lesson applied: Spine 1 P2a R1-C2 morning_brief retry-on-required-tool-skip pattern — `requiredTools` left unchanged in this bundle
- Spec §8 (alert delivery): `docs/planning/v8-substrate-s3-spec.md`
- Deferreds queued: S3-I1, S3-I2, S3-I3, S3-W3-fk-and-cascade in `docs/planning/next-sessions-queue.md`
