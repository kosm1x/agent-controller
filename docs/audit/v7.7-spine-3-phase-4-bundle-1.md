# v7.7 Spine 3 — Phase 4 Bundle 1 Audit Log

> **Phase**: 4 of 5 (callable-tools mode — Bundle 1 of 2; dispatcher + cost-ledger + cycle detection + Prom counter)
> **Commit**: `9712536`
> **Tier**: 1 (R1 PASS WITH WARNINGS — 1 Critical + 5 Warnings; R2 SKIPPED with rationale below)

## Bundle scope

| File                                     | LOC     | Surface                                                                                                                    |
| ---------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------- |
| `src/skills/inputs.ts` (new)             | +190    | `buildArgsSchema(inputs_json)` + `validateSkillArgs(...)` — runtime Zod from frontmatter inputs[]                          |
| `src/skills/mini-runner.ts` (new)        | +205    | Shared LLM prompt harness extracted from `test-runner.ts` (RUNNER_PREFIX + JSON-only contract + usage exposure)            |
| `src/skills/test-runner.ts` (refactor)   | +60/-95 | Switched to `runSkillPrompt` from mini-runner; mini-runner status → TestRunOutcome mapping replaces inlined `infer()`      |
| `src/skills/dispatcher.ts` (new)         | +480    | `runSkill(name, args, options)` — full lifecycle: lookup, validate, dispatch, mutate skills + skill_failures + cost_ledger |
| `src/observability/prometheus.ts` (edit) | +38     | `mc_skills_run_total{name, result}` Counter + `recordSkillRun()` with cardinality clip                                     |
| `src/skills/inputs.test.ts` (new)        | +175    | 17 tests covering all 7 supported types + duplicate names + malformed JSON + identifier regex                              |
| `src/skills/dispatcher.test.ts` (new)    | +395    | 20 tests covering 8 errorClass paths + dryRun + C1 cost-ledger regression + W3 skill_corrupt regression                    |

Total: ~1,545 LOC. **49/49 tests passing** in immediate scope (inputs/dispatcher/test-runner); **149/149 in `src/skills/`**.

## qa-auditor R1 findings — PASS WITH WARNINGS

| ID  | Severity | File                         | Finding                                                                                                                                                        | Disposition                                                                                                                                                                                                                                                            |
| --- | -------- | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Critical | `dispatcher.ts:506-515`      | Cost-ledger row lies about tokens AND task linkage. Wrote `promptTokens:0 completionTokens:0 taskId:""`. Spec §12 per-skill cost analysis would return garbage | **FOLDED** — mini-runner now exposes `usage: MiniRunUsage` (promptTokens/completionTokens/model); dispatcher threads it into cost_ledger. Empty-string taskId replaced by `randomUUID()` fallback when caller omits. 2 regression tests pin: real tokens + UUID format |
| W1  | Warning  | `dispatcher.ts:425-436`      | `consecutive_failures` is unbounded; no auto-archive ceiling                                                                                                   | **QUEUED — S5-P4-B1-W1** Trigger: when an operator complains about a forever-broken skill in retrieval blocklist, ship auto-archive at threshold (spec §10 implies but doesn't fix the value)                                                                          |
| W2  | Warning  | `dispatcher.test.ts:122-138` | Cycle-detection test passes today but the contract (`_callStack` mutation by callers) isn't pinned for Phase 5 body-side recursion                             | **QUEUED — S5-P4-B1-W2** Trigger: when Phase 5 wires a skill tool that calls `runSkill(... _callStack: [...prev, currentName])`, add a chained-dispatch sentinel test                                                                                                  |
| W3  | Warning  | `dispatcher.ts:235-247`      | Corrupt `inputs_json` (DB-side damage) was classified as `input_validation`, burning anti-list strikes against an innocent skill                               | **FOLDED** — added `errorClass: "skill_corrupt"` (matches reason prefix `"skill inputs_json corrupt"`); `finalizeFailure` skips `incrementFailure` + `writeSkillFailure` for this class. Regression test pins: 0 consecutive_failures, 0 skill_failures rows           |
| W4  | Warning  | `dispatcher.ts:529-537`      | `bumpRunCounter` dynamic-import error path uses void/then — chained future error within `recordSkillRun` body wouldn't be caught                               | **DEFERRED — out of scope** Current code IS the documented counter-recovery-path pattern (`feedback_prometheus_counter_recovery_path`). The hypothetical future drift requires a separate guard at modification time. Audit-by-grep won't catch it; queue rejected     |
| W5  | Warning  | `dispatcher.ts:539-545`      | `safeJson` fallback `{_unserializable:"object"}` lossy — no key names                                                                                          | **FOLDED** — fallback now emits `{_unserializable, keys:Object.keys(...).slice(0,20)}` for forensics                                                                                                                                                                   |
| S1  | Standard | `dispatcher.test.ts:207-229` | Test title said "as self_recovered" but assertion only checks `resolved_at IS NOT NULL` (matching real workaround for CHECK enum gap)                          | **FOLDED** — renamed to "resolves open skill_failures rows on success (resolution stays NULL pending v8.0 schema reset / S5-P4-B1-I1)"                                                                                                                                 |
| R1  | Reco     | `dispatcher.ts:38`           | `MAX_SKILL_CALL_DEPTH = 3` should be env-overridable                                                                                                           | **QUEUED — S5-P4-B1-R1** Trigger: when spec §15 Q4 closes one way or the other; minor                                                                                                                                                                                  |
| R2  | Reco     | `dispatcher.test.ts`         | No test asserts that `mc_skills_run_total` counter actually increments                                                                                         | **QUEUED — S5-P4-B1-R2** Trigger: when `recordSkillRun` is touched again; standalone counter-snapshot test pattern is established elsewhere                                                                                                                            |

Six Critical adversarial checks cleared post-fold:

- **C1 (cost-ledger truthfulness)**: addressed by usage threading + UUID taskId fallback; 2 regression tests
- **Concurrency / TOCTOU**: SQLite better-sqlite3 prepare-and-run is serialized; no `BEGIN/COMMIT` needed for the success/failure UPDATE
- **Cycle detection bypass at depth 3**: regression test covers boundary; W2 queued for Phase 5 body-side recursion contract
- **Anti-list filter regression**: spec §10 says retrieval-side filtering, not dispatch-side reject. Dispatcher correctly increments without rejecting (mirrors spec semantics)
- **`writeSkillFailure` CHECK enum drift**: mapping verified against `index.ts:678` enum — `wrong_output | tool_unavailable | timeout | critic_runtime_fail | other`. Dispatcher emits `wrong_output | tool_unavailable | timeout | other`. `critic_runtime_fail` is critic-side, not dispatcher-side. Mapping defensible
- **Anti-mission**: zero new skill bodies. Test fixtures only

## R2 decision

**SKIPPED with rationale**. R1 returned PASS WITH WARNINGS with ONE Critical (C1). The fold:

- Threads `usage` through one new mini-runner field
- Replaces empty-string taskId with `randomUUID()` fallback at one call site
- Pinned by 2 explicit regression tests (real tokens + UUID format)
- Touches no adjacent surface — `writeCostLedger` is the only callsite of `recordCost` in the dispatcher

This matches the multi-round-audit rule "R2 mandatory only when R1 surfaces ≥1 Critical that touches a logic-class likely to leak adjacent surfaces." The C1 fold is a contained data-flow change pinned by regression — no branching logic, no new error class, no new caller. Audit cost (~30 min R2) doesn't compound on this surface.

W3 (skill_corrupt) introduced a new errorClass but is also regression-pinned and touches one branch in `finalizeFailure`. W5 is a 3-line forensic improvement. S1 is a test rename.

## Pre-existing-bug catch

R1 surfaced one pre-existing schema CHECK enum gap during implementation (`skill_failures.resolution` doesn't admit spec §10's `'self_recovered'`). Workaround: leave resolution NULL, set resolved_at only — the anti-list invariant is encoded by `resolved_at IS NULL`. Tracked as **S5-P4-B1-I1** for v8.0 schema reset (parallel to Phase 1 / Phase 2 B1's `created_by='operator-override'` workaround — same `S5-P2-I1` schema-reset batch).

## Verification

- Typecheck: `npx tsc --noEmit` clean
- Scoped vitest: 49/49 (inputs + dispatcher + test-runner refactor)
- Skills scope: 149/149 (12 files)
- Adjacent scope: 27/27 (S2 critic + enrichment) — refactor preserved S2's contract via shared `src/lib/critic-verdict.ts`
- Production build: `npm run build` clean
- Live deploy: service active 2026-05-19; boot log preserved
- CLI smoke deferred to Bundle 2 (no `mc-ctl skills run` exists yet; B2 adds builtin tools as the operator surface)

## Bundle 1 scoreboard

| Metric                   | Value                                                          |
| ------------------------ | -------------------------------------------------------------- |
| LOC (net)                | +1,545 / -95 = +1,450                                          |
| Tests (new)              | +37 (17 inputs + 20 dispatcher)                                |
| Total scoped passes      | 49/49 + 149/149 + 27/27                                        |
| R1 folds                 | 1 Critical + 2 Warnings + 1 Standard = 4 in-bundle             |
| Queued P3 items          | 3 (S5-P4-B1-W1, W2, R1/R2)                                     |
| Pre-existing-bug catches | 1 (schema CHECK enum gap; S5-P4-B1-I1 → batched with S5-P2-I1) |
| Production regressions   | 0                                                              |

## What this unlocks

Bundle 2 (next): wraps the `runSkill` dispatcher in 3 deferred builtin tools (`skill_describe`, `skill_load`, `skill_run`) and wires them through `src/tools/sources/skills.ts`. The dispatcher is the blocker; B2 is one ToolSource-registration patch + 3 tool definitions + tests.

Phase 5 (later): operator certification surface + activation gate (≥5 certified skills with green tests in last 7 days). Authoring those 5 skill bodies is Phase 5 work, not Phase 4.

## Cross-references

- Spec sections grounding: §7 (lifecycle), §10 (anti-list / failure handling), §11 (ToolSource Mode 2), §15 Q4 (cycle detection / depth 3)
- Phase 3 closure: `docs/audit/v7.7-spine-3-phase-3-bundle-{1,2}.md`
- Reused module: `src/skills/test-runner.ts` (its `runOneTest` → `runSkillPrompt` from `mini-runner.ts`)
- Reused module: `src/budget/service.ts` (`recordCost` + `CostRecord`)
- Schema CHECK enum precedent: `feedback_session_2026_05_19_pm_v77_spine_3` (Phase 1 `created_by='operator-override'` workaround)
- Auditor empirical-claim discipline: `feedback_session_2026_05_18_install_batch` rule #3 — applied at the auditor C1 cost-ledger claim (verified `infer()` does NOT call `recordCost` before folding)
