# v7.7 Spine 4 — Bundle 2 Audit Log (general-events hierarchical retrieval)

> **Spine**: 4 of 7 (Conway Pattern 1 — general-events middle layer)
> **Bundle**: 2 of 3 — hierarchical retrieval (read path)
> **Tier**: 1 (new substrate module, ~400 LOC code + 19 tests)
> **Spec**: `docs/planning/v8-capability-1-spec.md` §5

## Bundle scope

| File                                  | Change                                                                                                                        |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `src/events/retrieval.ts`             | NEW — `retrieveGeneralEvents` (Layer 1), `retrieveForBriefing` (Layer 2 descent), `resolveEpisodic`, `aggregateGeneralEvents` |
| `src/events/retrieval.test.ts`        | NEW — 19 tests                                                                                                                |
| `src/db/index.ts`                     | Comment-only — honest note on `idx_general_events_active` (R1-W1)                                                             |
| `docs/audit/v7.7-spine-4-bundle-2.md` | This file                                                                                                                     |

## qa-auditor R1 — PASS WITH WARNINGS

0 Critical · 3 Warning · 4 Recommendation. W2 + W3 folded as code; W1 folded as an honest comment; R1/R2/R3 folded as tests; R4 no action.

| ID  | Finding                                                                                                                                        | Disposition                                                                                                                                                                          |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| W1  | `idx_general_events_active` is walked as a SCAN, gives no seek benefit to the window predicate; `ORDER BY start_at DESC` is a temp B-tree sort | FOLDED (comment) — migration comment now states this honestly. **This is the honest resolution of Bundle-1 I1**: the index is touched, not leveraged for a seek; fine at ~30-50 rows |
| W2  | A wrong-dimension stored embedding makes `cosineSimilarity` return `NaN`, which escapes the un-embedded partition and sorts unpredictably      | FOLDED (code) — `Number.isFinite` guard treats a non-finite score as un-embedded → recency tail; +1 test                                                                             |
| W3  | Doc comment overstated "most recent N" — `linked_at` has 1s resolution, same-second batches tie-break on insertion order                       | FOLDED (comment) — comment now says "last-linked N as a recency proxy" with the same-second caveat                                                                                   |
| R1  | Test gap: no NaN/mismatched-embedding test                                                                                                     | FOLDED — `treats a wrong-dimension stored embedding as un-embedded`                                                                                                                  |
| R2  | Test gap: `resolveEpisodic` never-throws only tested for missing-row, not end-to-end deleted-source resilience                                 | FOLDED — `descent survives a deleted source row without throwing`                                                                                                                    |
| R3  | Test gap: zero-width / boundary-touching window                                                                                                | FOLDED — `handles boundary-touching and zero-width windows inclusively`                                                                                                              |
| R4  | `retrieveGeneralEvents` candidate SELECT is unbounded (no SQL LIMIT)                                                                           | NO ACTION — intentional: cosine ranking happens in JS so SQL cannot pre-limit; bounded by cohort scale; all other loops bounded by k/episodicPerEvent                                |

**Auditor-verified sound (no defect)**: window-overlap predicate (`start ≤ window.end ∧ (end IS NULL ∨ end ≥ window.start)` is the correct inclusive-overlap test); ranking partition (`[...withScore, ...withoutScore]` — an un-embedded event can never outrank an embedded one); integer-ref TEXT-bind affinity (better-sqlite3 coerces `"7"` to match `INTEGER PRIMARY KEY` 7 — verified live); `resolveEpisodic` never throws on any branch; week-Monday bucketing across the Sunday case and the 2026-01-01→2025-12-29 year boundary; `k<=0` / `episodicPerEvent<=0` guards.

## Bundle-1 I1 — resolved

Bundle 1's open Improvement I1 asked whether `idx_general_events_active` is exercised by Bundle 2 retrieval. **Answer (honest, per R1-W1)**: the index _is_ used — SQLite walks it for the `archived_at IS NULL` partial predicate — but its `end_at` key provides no seek acceleration to the window predicate, and `ORDER BY start_at DESC` always builds a temp B-tree. At the ~30-50 row cohort this is correct and cheap; the migration comment now records this rather than overstating the index's value. No code change beyond the comment.

## R2 decision

**Not run.** 0 Criticals; folds are one `Number.isFinite` guard, two comment corrections, and three additive tests — no contract drift, no adjacent surface. Per the Spine 3 cadence, R2 is reserved for Critical-bearing or contract-drift bundles.

## Verification

- `npx vitest run src/events/retrieval.test.ts` → 19/19 pass
- `npm run typecheck` → 0 errors

## Next

Bundle 3 — operator surface (`mc-ctl events`), manual seed script, Prom counters, Spine 4 closure (GUIDE row, impl-delta doc, Spine 5 entry brief).
