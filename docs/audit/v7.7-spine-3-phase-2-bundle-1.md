# v7.7 Spine 3 — Phase 2 Bundle 1 Audit Log

> **Phase**: 2 of 5 (critic + lifecycle, Bundle 1 of 2)
> **Commit**: `5866f49`
> **Tier**: 1 (R1 only, R2 skipped with rationale)

## Bundle scope

| File                                 | LOC      | Surface                                                                                                            |
| ------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------ |
| `src/lib/critic-verdict.ts` (new)    | +88      | Shared JSON-extraction parser; extracted from S2's critic so S5 can reuse without duplication                      |
| `src/audit/critic.ts` (refactor)     | -65/+2   | `parseVerdict` + `extractBalancedObjects` deleted; imports from `lib/critic-verdict.ts`                            |
| `src/skills/critic.ts` (new)         | +160     | `runSkillCritic` mirroring S2's call shape; skill-specific 5-check system prompt (DUPLICATION deferred to Phase 3) |
| `src/skills/storage.ts` (new)        | +173     | Extracted `ensureSkillRow`/`recordVersion`/`pointSkillAtVersion` from Phase 1 loader                               |
| `src/skills/loader.ts` (refactor)    | -147/+25 | Imports storage helpers; loader-specific code shrinks to the walk + per-file try/catch                             |
| `src/skills/lifecycle.ts` (new)      | +201     | `skillSave` / `skillRevise` wrap critic + persist on pass + operator override                                      |
| `src/skills/critic.test.ts` (new)    | +142     | 9 tests (pass/fail/empty/non-JSON/throw/pre-aborted-signal/prompt-shape/body-cap)                                  |
| `src/skills/lifecycle.test.ts` (new) | +222     | 12 tests (critic-pass + critic-fail + critic-error + override + version-conflicts + skillRevise)                   |

Total: ~860 LOC net. 21 new tests + 38 existing S2 critic tests + 21 existing Phase 1 skills tests = 80 passing in scope.

## qa-auditor R1 findings

**Verdict**: PASS WITH WARNINGS — 1 Critical / 3 Warning / 2 Recommendations.

| ID  | Severity | File:Line                                           | Finding                                                                                                                                                                               | Disposition                                                                                                                                                                  |
| --- | -------- | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Critical | `lifecycle.ts:13` + `:54` + `lifecycle.test.ts:146` | Docstrings claimed `created_by='operator-override'` is written on override; code actually keeps `created_by` under caller's value because Phase 1's schema CHECK lacks the enum value | **FOLDED** — rewrote module docstring + `forceOperatorOverride` JSDoc + test description to match the actual `critic_verdict='fail_returned_anyway'`-as-sole-signal behavior |
| W1  | Warning  | `lifecycle.ts:161-169`                              | `SkillSaveResult.kind='unchanged'` coalesces no-op success into `ok:false` — caller can't distinguish from rejection                                                                  | **DEFERRED S5-P2-W1** — split into `ok:true, kind:'unchanged'` variant once Phase 4 producer retry loop ships                                                                |
| W2  | Warning  | `critic.ts:103`                                     | Body excerpt cap fixed at 8000 chars; no env tunable, no surface on result                                                                                                            | **DEFERRED S5-P2-W2** — no skill body exceeds 8k today                                                                                                                       |
| W3  | Warning  | `lifecycle.ts:140`                                  | Override-on-`critic_error` stores raw error text as `critic_critique`; ambiguous in `mc-ctl skill-health`                                                                             | **DEFERRED S5-P2-W3** — prefix with `[override-on-error]` once mc-ctl skill-health lands in Bundle 2                                                                         |
| R1  | Reco     | `lifecycle.ts:192-200`                              | `skillRevise` is a 9-line wrapper over `skillSave` with `createdBy='refiner'` default; could collapse                                                                                 | Not folded — spec §7 lists both as distinct tool surfaces; keeping call-site readability                                                                                     |
| R2  | Reco     | `lifecycle.test.ts`                                 | No test pinning that override is ignored when critic passes                                                                                                                           | **FOLDED** — added regression test "override is a no-op when critic passes"                                                                                                  |

All 5 Critical adversarial checks cleared:

- Abort/timeout cleanup in `critic.ts:158-161` mirrors S2's pattern; listener removed + handle cleared on every exit path
- Verdict-error vs content-fail correctly distinguished in lifecycle (`critic_failed` vs `critic_error` kinds)
- `fail_returned_anyway` grep confirms ONLY `lifecycle.ts:138` sets it on `skill_versions` rows (the `src/audit/submit-report.ts` mention writes to the separate `reports` table — different CHECK constraint, no contamination)
- Loader→storage refactor preserves Phase 1's `boot-scan` + `skipped` defaults bit-identically; 21/21 Phase 1 tests pass post-refactor
- Anti-mission held — zero authored skill content; only critic + lifecycle + scaffolding

## R2 decision

**SKIPPED with rationale**. All 4 R1 folds were:

- C1: 3-string-literal change across 3 files (no logic change)
- R2: 1 new regression test pinning the existing-and-correct guard at `lifecycle.ts:133-135`
- W1/W2/W3 deferred with explicit triggers

No new logic surface introduced. R2 would have spent 30-60 min finding ~0 issues against folds that are pure documentation + one regression test. Matches the session-wrap pattern of "mechanical fixes, regression-pinned, no adjacent surface."

## Pre-existing-bug catch (Phase 1 schema gap)

While implementing the override path, discovered Phase 1's CHECK constraint on `skill_versions.created_by` does NOT include `'operator-override'`, despite spec §8 mandating it. Bundle 1 worked around by carrying override via `critic_verdict='fail_returned_anyway'` (unreachable except via override → unambiguous). Real fix requires SQLite destructive table re-create; queued as **S5-P2-I1** for v8.0 schema reset.

## Verification

- Typecheck: `npx tsc --noEmit` clean
- Scoped vitest (`src/skills/` + `src/audit/critic.test.ts`): 82/82 pass (21 Phase 1 + 21 Phase 2 B1 + 38 S2 + 2 misc)
- Production build: `npm run build` clean
- Live deploy: `sudo systemctl restart mission-control` → active; boot log:
  ```
  [skills:loader] complete loaded=0 skipped=0 drift=0 errors=0
  ```
  Refactor preserves Phase 1's empty-namespace behavior end-to-end.

## Out-of-bundle queue (4 items appended to `docs/planning/next-sessions-queue.md`)

- **S5-P2-I1** — Extend `skill_versions.created_by` CHECK to include `'operator-override'`. Trigger: v8.0 schema reset.
- **S5-P2-W1** — `unchanged` kind disambiguation. Trigger: Phase 4 producer retry loop OR operator confusion.
- **S5-P2-W2** — Body excerpt cap tunability. Trigger: first skill body >8k.
- **S5-P2-W3** — Override-on-error critique provenance. Trigger: Phase 5 `mc-ctl skill-health`.

## Cross-references

- Spec sections grounding this bundle: §8 (critic), §7 (lifecycle tool surface), §15 Q6 (discovery→critic loop), §15 Q7 (operator override)
- Entry brief: `docs/planning/v7.7-spine-3-entry-brief.md`
- Phase 1 commit: `422d985`
- Phase 1 audit log: `docs/audit/v7.7-spine-3-phase-1.md`
- Spec-delta artifact (updated this bundle): `docs/planning/v8-substrate-s5-impl.md`
- Shared with S2: `src/audit/critic.ts` (still operational; tests pass post-refactor)
