# v7.7 Spine 3 — Phase 2 Bundle 2 Audit Log

> **Phase**: 2 of 5 (Bundle 2 of 2 — test harness + cron sweep + operator surface)
> **Commit**: `acfbdfd`
> **Tier**: 1 (R1 → FAIL → fold → R2 → PASS WITH WARNINGS)

## Bundle scope

| File                                     | LOC  | Surface                                                                           |
| ---------------------------------------- | ---- | --------------------------------------------------------------------------------- |
| `src/skills/test-runner.ts` (new)        | +400 | Mini-LLM-driven test executor + result judging + counter bump + is_certified flip |
| `src/skills/test-sweep.ts` (new)         | +140 | Every-6h cron sweep over certified+active skills; reaffirms or decertifies        |
| `src/index.ts` (edit)                    | +18  | Boot wire for sweep cron with `JARVIS_SKILLS_TEST_SWEEP_DISABLED` env gate        |
| `src/observability/prometheus.ts` (edit) | +18  | `mc_skills_test_runs_total{result}` Counter + `recordSkillTestResult()` export    |
| `mc-ctl` (edit)                          | +120 | `skill-health` subcommand + override summary + activation gate snapshot           |
| `src/skills/test-runner.test.ts` (new)   | +250 | 13 tests including W1 abort-preservation regression                               |
| `src/skills/test-sweep.test.ts` (new)    | +130 | 6 tests for sweep aggregation across multiple skills                              |

Total: ~1075 LOC. 19 new tests + 21 P1 + 21 P2 B1 + 38 S2 + 2 catalog = 101 in scope, all passing.

## qa-auditor R1 findings — FAIL verdict

| ID  | Severity | File:Line                            | Finding                                                                                                                       | Disposition                                                                                                             |
| --- | -------- | ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| C1  | Critical | `mc-ctl --name=` interpolation       | SQL injection via `--name="x'; DROP TABLE skill_test_runs; --"` — HEREDOC executes multi-statement                            | **FOLDED** — regex sanitizer `^[a-z0-9][a-z0-9_-]{0,62}[a-z0-9]$` at entry                                              |
| C2  | Critical | `test-runner.ts:106`                 | `body + RUNNER_SUFFIX` lets adversarial body override the harness ("ignore the above") → self-certification bypass            | **FOLDED** — renamed to `RUNNER_PREFIX`, prefix BEFORE body; added explicit override-detection clause                   |
| W1  | Warning  | `test-runner.ts:181`                 | Caller abort mid-loop → all remaining tests get `result='error'` → `allPassed=false` → decertifies previously-certified skill | **FOLDED** — `aborted` flag + early break + preserve existing `is_certified` from DB; regression test pins the contract |
| W2  | Warning  | `test-runner.ts:464-468`             | Counter dynamic-import failure silently swallowed (counter-recovery-path discipline)                                          | **FOLDED** — `console.warn` on import failure                                                                           |
| W3  | Warning  | `mc-ctl` activation gate             | Counted `is_certified=1 AND active=1` without spec §14's 7-day recency subclause → stale-cert drift                           | **FOLDED** — added `EXISTS (SELECT 1 FROM skill_test_runs WHERE result='pass' AND ran_at >= datetime('now','-7 days'))` |
| W4  | Warning  | `deepPartialMatch` null vs undefined | Author writing `expect.output_match: {y: null}` means "expect null", not "expect absent" — undocumented                       | **DEFERRED S5-P2-B2-W4** — document in test-runner JSDoc next bundle                                                    |

## qa-auditor R2 findings — PASS WITH WARNINGS

R2 verified all 5 folds end-to-end. Adjacent-surface check confirmed:

| ID                   | Finding                                                                                             | Disposition                                                                                                                                                                                                                                                                                                                                                  |
| -------------------- | --------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| R2-W1                | Bash regex allowed `_` while frontmatter `NAME_RE` does not → contract drift                        | **FOLDED then EMPIRICALLY REVERTED** — `mc-ctl skill-health --name=health_check` (a real legacy DB skill) failed under the tightened regex. Reverted to underscore-allowing superset with comment explaining the legacy-DB-compat reason. R2 auditor's reasoning was incorrect; live DB has 67 legacy skills with underscores predating NAME_RE enforcement. |
| R2-W2                | S2 critic `src/audit/critic.ts:112` user-controlled draft in `user` role — adjacent surface         | **NO ACTION** — `user` role + JSON.stringify discipline = safe                                                                                                                                                                                                                                                                                               |
| R2-W3                | S5 critic `src/skills/critic.ts:111` same pattern — adjacent surface                                | **NO ACTION** — same safety argument                                                                                                                                                                                                                                                                                                                         |
| R2 EXISTS index cost | Activation-gate EXISTS subquery has `idx_skill_test_runs_skill(skill_id, ran_at DESC)` from Phase 1 | **VERIFIED** — well-indexed                                                                                                                                                                                                                                                                                                                                  |

## R3 decision

**NOT WARRANTED**. Tier 2's R3 is for verdict-verification on closure bundles (multi-surface, ~10k+ LOC). This is a single phase-bundle ship. R2 PASS verdict with one empirical fix (the bash regex revert was caught at the moment of fold, not in production). Stop at R2.

## Pre-existing-bug catch

Empirical reality check during R2-W1 fold caught a contract-drift in the auditor's recommendation itself: the auditor assumed frontmatter NAME_RE applies to ALL skills, but 67 legacy DB skills predate that enforcement and have underscores. Verified by running `mc-ctl skill-health --name=health_check` against the live DB — the proposed fold REJECTED a valid legacy name. This is the "verify auditor empirical claims" discipline pinned in `feedback_session_2026_05_18_install_batch.md` rule #3, applied at the auditor-output boundary.

## Verification

- Typecheck: `npx tsc --noEmit` clean
- Scoped vitest: 101/101 pass (8 test files in `src/skills/` + `src/audit/critic.test.ts`)
- Production build: `npm run build` clean
- Live deploy: `sudo systemctl restart mission-control` → active; boot log shows:
  ```
  [skills:loader] complete loaded=0 skipped=0 drift=0 errors=0
  registered test sweep cron (0 2,8,14,20 * * *, America/Mexico_City)
  ```
- `mc-ctl skill-health` smoke test against live DB renders all 67 legacy skills + activation gate snapshot reporting `0 of ≥5 certified active`
- `mc-ctl skill-health --name="bad'; DROP TABLE"` correctly rejects with `Invalid --name:` (SQL-injection sanitizer active)

## Out-of-bundle queue (1 item appended)

- **S5-P2-B2-W4** — document `deepPartialMatch` null-vs-undefined semantics in test-runner JSDoc + a test case asserting current behavior. Trigger: first author-confusion observed OR Phase 4's typed-output discipline pass.

## Cross-references

- Spec sections grounding this bundle: §9 (test harness), §14 (activation gate)
- Phase 2 Bundle 1 commit: `5866f49`
- Phase 2 Bundle 1 audit log: `docs/audit/v7.7-spine-3-phase-2-bundle-1.md`
- Spec-delta artifact: `docs/planning/v8-substrate-s5-impl.md`
- Counter-recovery-path discipline source: `feedback_prometheus_counter_recovery_path`
- Auditor-empirical-claim-verification discipline: `feedback_session_2026_05_18_install_batch.md` rule 3
