# v7.7 Spine 5 — Bundle 2 Audit Log (roll-up cron + operator surface + observability)

> **Spine**: 5 of 7 (Conway Pattern 2)
> **Bundle**: 2 of 3 — daily cron, `mc-ctl cohort`, Prom metrics
> **Tier**: 1 (~330 LOC: cron + operator surface + metrics + 4 tests)

## Bundle scope

| File                              | Change                                                                               |
| --------------------------------- | ------------------------------------------------------------------------------------ |
| `src/cohort/rollup-cron.ts`       | NEW — `registerCohortRollupCron`/`stop` + `runCohortRollup` (never throws)           |
| `src/index.ts`                    | +1 block — registers the daily roll-up cron at boot                                  |
| `src/observability/prometheus.ts` | NEW `mc_cohort_rollup_total` counter + `mc_cohort_size` gauge + `recordCohortRollup` |
| `scripts/cohort-ctl.ts`           | NEW — backs `mc-ctl cohort rollup`                                                   |
| `mc-ctl`                          | NEW `cmd_cohort` (`list/show/rollup`) + dispatch route                               |
| `src/cohort/rollup-cron.test.ts`  | NEW — 4 tests                                                                        |

Resolves Bundle-1 W1 (`rollUpCohort` now has a caller — the daily cron) and R2 (observability — counter + gauge).

## qa-auditor R1 — PASS WITH WARNINGS

0 Critical · 4 Warning · 7 Improvement. W3 + W4 folded in-bundle; W1/W2 no action (faithfully-mirrored pre-existing patterns); I1-I7 were verified-sound notes.

| ID    | Finding                                                                                                                 | Disposition                                                                                          |
| ----- | ----------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| W3    | `mc_cohort_size{kind="thread"}` is a perpetually-0 series (thread is v7.7 headroom)                                     | FOLDED — comment added; flat-0 series is intentional (avoids a dashboard "no data" gap)              |
| W4    | The gauge-not-overwritten-on-failure invariant had no test                                                              | FOLDED — +1 test: ok→error→ok asserts the gauge holds last-known-good + the error counter increments |
| W1    | `mc-ctl cohort rollup` prints no explicit "FAILED" line on a non-zero exit                                              | NO ACTION — identical to `cmd_events`; faithfully-mirrored, auditor-blessed as acceptable            |
| W2    | `npx --no-install tsx` hard-fails without `node_modules`                                                                | NO ACTION — pre-existing pattern, mirrors `cmd_events`/`cmd_skills`                                  |
| I1-I7 | Failure-faithfulness, cron fidelity, index.ts wiring, injection regex, list degradation, cohort-ctl.ts, gauge staleness | NO ACTION — all verified sound by the auditor                                                        |

**Auditor-verified sound**: injection regex `^[a-z]+:[A-Za-z0-9:/._-]+$` rejects every quote/space/semicolon payload (the looser-than-`cmd_events` regex is necessary — member_ids carry paths — and still safe; `${id//\'/\'\'}` is correct redundant defense); cron expr `"0 5 * * *"` valid; `registerCohortRollupCron` faithfully mirrors `test-sweep.ts` (the missing `.catch()` on the callback is correct — `runCohortRollup` never throws by contract); `index.ts` wiring cannot crash boot; gauge re-set per fixed-kind tuple so no staleness; `cohort-ctl.ts` DB path resolved via `import.meta.url` (Spine 4 W1 lesson applied).

## R2 decision

**Not run.** 0 Criticals; folds are one comment + one test. Spine 3/4 cadence.

## Verification

- `npx vitest run src/cohort/rollup-cron.test.ts` → 4/4 pass
- `npm run typecheck` → 0 errors · `bash -n mc-ctl` → clean

## Next

Bundle 3 — Spine 5 closure: impl-delta doc, V7.7-GUIDE Spine 5 → CLOSED, Spine 6 entry brief. Deploy + first live roll-up + `mc-ctl cohort` verification.
