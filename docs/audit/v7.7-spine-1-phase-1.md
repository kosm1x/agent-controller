# v7.7 Spine 1 Phase 1 — Audit Log

**Date**: 2026-05-19 · **Bundle**: S2 self-audit-before-reporting harness · **Cadence**: Tier-1 (R1 only, per `multi-round-audit` skill)

## Bundle scope

| File                                    | Status | LOC  |
| --------------------------------------- | ------ | ---- |
| `src/audit/report-schema.ts`            | new    | ~215 |
| `src/audit/critic.ts`                   | new    | ~210 |
| `src/audit/submit-report.ts`            | new    | ~275 |
| `src/audit/report-schema.test.ts`       | new    | ~245 |
| `src/audit/critic.test.ts`              | new    | ~225 |
| `src/audit/submit-report.test.ts`       | new    | ~365 |
| `src/db/index.ts`                       | edit   | +30  |
| `docs/planning/v8-substrate-s2-spec.md` | edit   | +2   |

107 audit tests passing. `npx tsc --noEmit` clean. `npm run build` clean. No deploy (Phase 1 is library code, zero production callers).

## R1 audit (qa-auditor)

**Findings**: 0 Critical / 8 Warning / 4 Info → 11 in-bundle folds + 1 deferred.

### Folded in-bundle

| #   | Severity | Surface                                                                  | Fold                                                                                                                                                              |
| --- | -------- | ------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| W1  | Warning  | `report-schema.ts:18` SHA256_HEX lowercase-only                          | Added documentation comment; producers MUST normalize to lowercase (matches git/crypto.subtle convention).                                                        |
| W2  | Warning  | All 8 citation variants accept unknown fields silently                   | Switched `z.object` → `z.strictObject` per variant; unknown fields now reject, satisfying spec §3 forensic-reconstruction intent.                                 |
| W3  | Warning  | Surface enum drift (`closure_doc` + `community_email` not in spec)       | Added code comment citing V7.7-GUIDE Spine 1 Phase 2 retrofit deltas + amended spec §6 in same commit.                                                            |
| W4  | Warning  | `critic.ts` greedy `{...}` regex breaks on double-JSON responses         | Replaced with balanced-paren walker `extractBalancedObjects` (handles nested objects + escaped quotes); first valid verdict wins.                                 |
| W5  | Warning  | Caller signal already aborted = wasted critic call                       | Pre-check at top of `runCritic` short-circuits with reason in `audit_failed` concern.                                                                             |
| W7  | Warning  | `ON CONFLICT DO NOTHING` swallows producer report_id collisions silently | Added `console.warn` when `result.changes === 0` so producer bug (UUID reuse) surfaces in logs.                                                                   |
| W8  | Warning  | Schema-vs-spec drift: FK omission + no size cap on report_json           | Added code comment for FK rationale (mc.db convention) + 256 KB cap with skip-log fallback.                                                                       |
| I1  | Info     | Brittle `CRITIC_SKIP_FOR` mutation pattern in tests                      | Lifted into `withAllowlisted(surface, fn)` helper — assertion throws inside fn still restore state.                                                               |
| I2  | Info     | `ReadonlySet` is compile-time only; runtime is mutable                   | Added doc comment flagging the gap; Phase 2 candidate to freeze.                                                                                                  |
| I3  | Info     | Missing test for `signal` + `timeoutMs` combo cleanup path               | Added test exercising both.                                                                                                                                       |
| I4  | Info     | `reviseFn` throwing propagates as unhandled rejection                    | Wrapped in try/catch; throw is folded into `audit_failed` concern (preserves spec §4 "never silently skip" semantics for producer-side failures too). Added test. |

### Deferred (1 item)

**S2-W6 — Hoist prepared statement out of `persistReport`**. R1 verbatim: "Phase 1 has zero producers so this is moot; flag for Phase 2 retrofit before `morning_brief` wires in." **Trigger**: Phase 2 morning_brief wire-in. Queue: `docs/planning/next-sessions-queue.md` → "v7.7 Spine 1 Phase 1 deferreds" section.

## Scoreboard

```
Bundle:  v7.7 Spine 1 Phase 1 (S2 harness)
Rounds run: R1
Tier: 1 (R1+R2, downgraded to R1-only — 0 Crit + no nervous signals)
Reason for R2 skip: 0 Criticals + all 8 Warnings were either documentation gaps
  or one-liner correctness improvements; none touched a logic class likely
  to leak into adjacent surfaces.

Findings:
- R1: 0 Critical, 8 Warning, 4 Info — 11 fixed in-bundle, 1 deferred (with trigger)

Pre-existing bugs found: 0   (new code, no pre-existing surface)
Bundle-regression catches: 8 Warning + 4 Info  (entire bundle is new — every find is a bundle-regression catch)
Total rounds: 1

Verdict: PASS WITH WARNINGS (all folded except S2-W6 with explicit trigger)
Closure-ready: Phase 1 ships; Phase 2 wiring is the next ship.

Deferred queue entries created: S2-W6 (Phase 2 morning_brief wire-in trigger)
```

## Tier-1 → R1-only downgrade rationale

Per the `multi-round-audit` skill: R2 catches "bugs INSIDE R1's fixes" and "adjacent surfaces that share the bug class." This bundle's surface mix:

- **Parser** (Zod schema, `parseVerdict`): R1 caught the greedy-regex bug AND the unknown-field strip. The fix (balanced-paren walker + `strictObject`) is mechanical and well-tested. No "adjacent surface" to sweep — there's no other Zod schema or LLM-response parser in this bundle.
- **Async/concurrency** (`runCritic`): R1 caught the already-aborted gap. The fix is a 4-line pre-check; no class of "follow-up" bugs likely.
- **SQL persistence** (`persistReport`): R1 caught the silent-dedupe + missing size cap. Fixes are additive (log + early-return).

R2 would have spent ~30-60 min finding ~0 new issues. Re-test pre-existing-surface sweep wasn't applicable (this is all new code).

If Phase 2 retrofits land touching this code: R2 mandatory on that bundle.

## Cross-references

- Spec: `docs/planning/v8-substrate-s2-spec.md`
- Operational guide: `docs/V7.7-GUIDE.md` §"Spine 1"
- Phase 1 entry brief: `docs/planning/v7.7-spine-1-entry-brief.md`
- Skill applied: `~/.claude/skills/multi-round-audit/SKILL.md` (Tier-1 with R1-only downgrade rationale per skill)
- Sibling primitive: `src/audit/self-audit.ts` (`mc-ctl audit-claim`, commit `095647b`)
