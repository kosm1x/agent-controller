# v7.7 Spine 3 — Phase 3 Bundle 2 Audit Log

> **Phase**: 3 of 5 (vector retrieval — Bundle 2 of 2; closes Phase 3)
> **Commit**: `144dc42`
> **Tier**: 1 (R1 PASS WITH WARNINGS; R2 skipped with rationale)

## Bundle scope

| File                                         | LOC      | Surface                                                                                                                 |
| -------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------- |
| `src/skills/retrieval.ts` (new)              | +160     | `retrieveSkills(taskContext, options)` — cosine ranking + threshold + anti-list filter + keyword fallback               |
| `src/intelligence/enrichment.ts` (edit)      | +20 / -8 | `getMatchingSkills` async; routes through `retrieveSkills({k:3})`; rich-block placeholder for new-schema skills (R1-W3) |
| `scripts/skills-retrieve.ts` (new)           | +50      | tsx-resolvable CLI bridge with stderr path log (R1-W4)                                                                  |
| `mc-ctl` (edit)                              | +30      | `retrieve` subcommand with --k / --threshold validation                                                                 |
| `src/skills/retrieval.test.ts` (new)         | +205     | 14 tests including W1 dim-mismatch fallback regression                                                                  |
| `src/intelligence/enrichment.test.ts` (edit) | +35      | Mock retrieveSkills + getSkill (R1-W2 fold); positive-case regression                                                   |

Total: ~500 LOC. **141/141 tests passing** in scope (Phase 1+2+3 skills + S2 critic + enrichment).

## qa-auditor R1 findings — PASS WITH WARNINGS

| ID  | Severity | File                         | Finding                                                                                                                                | Disposition                                                                                                                                                                                 |
| --- | -------- | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------ |
| W1  | Warning  | `retrieval.ts` post-loop     | Dim-mismatch silent black-hole defeats fallback — all-BLOB-mismatched would return empty when keyword fallback should fire             | **FOLDED** — `mismatched` counter + post-loop guard: `if (scored.length === 0 && mismatched > 0 && fallback) return toKeywordResults(...)`. Regression test added pinning the new behavior. |
| W2  | Warning  | `enrichment.test.ts`         | Mock targeted `findSkillsByKeywords` after refactor routed through `retrieveSkills` — tests asserted catch-path instead of integration | **FOLDED** — added `vi.mock("../skills/retrieval.js", ...)` + `getSkill` mock; new positive-case test verifies retrieveSkills hits propagate to matchedSkillIds + contextBlock              |
| W3  | Warning  | `enrichment.ts:340`          | `JSON.parse(s.steps)` on Phase 1 schema's empty `'[]'` rendered empty "Pasos:" section                                                 | **FOLDED** — placeholder `Pasos: (cargar con skill_load <skill_id>)` when steps empty; same pattern for tools                                                                               |
| W4  | Warning  | `scripts/skills-retrieve.ts` | CLI "(no matches)" indistinguishable across vector-empty / kw-empty / API-down                                                         | **FOLDED** — stderr `[retrieval] path=<vector                                                                                                                                               | keyword | empty> matches=N` before stdout rows |

All 6 Critical adversarial checks cleared:

- `getMatchingSkills` async-prop: only one caller (`enrichContext`) — already async, just added await
- Fallback semantics under partial state: today's 0-embedded/0-certified state routes to keyword; verified via `mc-ctl skills retrieve "publicar wordpress imagen"` returning `kw_*` rows for legacy skills
- Cosine of zero-magnitude vectors: returns 0 via `cosineSimilarity` denominator check
- Threshold negative interaction with `Number.isFinite` guard: NaN coerces to 0; dim-mismatch skipped pre-cosine
- CLI threshold validation: regex `^-?[0-9]+(\.[0-9]+)?$` accepts negatives (intentional — allows explicit no-filter); rejects NaN
- Anti-mission: zero authored content; test fixtures use synthetic short descriptions

## R2 decision

**SKIPPED with rationale**. All 4 R1 folds are mechanical:

- W1: 3-line guard + `mismatched` counter — pinned by new regression test
- W2: test mock surface swap — no production change
- W3: 2 placeholder strings
- W4: 2-line stderr log

R1 returned PASS WITH WARNINGS (no Criticals), and W1's branching logic is the only meaningful surface change — pinned by an explicit regression test (`falls back to keyword when ALL embedded rows have wrong dim (R1-W1 regression)`).

## Pre-existing-bug catch

R1-W2 surfaced that the enrichment test mock had been masking the integration path post-refactor; the catch-path was asserting empty results instead of the rich-block format. Caught + folded with a positive-case regression that verifies retrieveSkills → getSkill → matchedSkillIds wiring end-to-end.

## Verification

- Typecheck: `npx tsc --noEmit` clean
- Scoped vitest: 141/141 (12 test files: Phase 1+2+3 skills + S2 critic + enrichment)
- Production build: `npm run build` clean
- Live deploy: service active; boot log preserved
- CLI smoke: `mc-ctl skills retrieve "publicar wordpress imagen" --k=3` returns 3 keyword-source matches for legacy Spanish skills; stderr `[retrieval] path=keyword matches=3` confirms W4 fold

## Phase 3 closure

Bundle 2 closes Phase 3. Cumulative Phase 3 scoreboard:

| Bundle                                          | Commit          | LOC       | Tests   | R1 verdict              | Folds                                      |
| ----------------------------------------------- | --------------- | --------- | ------- | ----------------------- | ------------------------------------------ |
| B1 (schema + embed helpers + backfill)          | `0d0bd5c`       | +605      | +18     | FAIL → folded → R2 skip | 2C + 2W in-bundle, 3 deferred              |
| B2 (retrieval + replace keyword + CLI retrieve) | `144dc42` | +500      | +14     | PASS WITH WARNINGS      | 4W in-bundle, 1 regression test pinning W1 |
| **Combined**                                    | —               | **+1105** | **+32** | —                       | 6 in-bundle, 3 deferred                    |

**Spec §13.3 status — all 5 tasks complete**:

1. ✅ Migration: `description_embedding` BLOB column (B1)
2. ✅ Backfill: operator-triggered via `mc-ctl skills backfill-embeddings` (B1) — legacy 67 skills addressable
3. ✅ Replace `findSkillsByKeywords` with `retrieveSkills` (B2) — kept as fallback per graceful-degradation discipline
4. ✅ Re-embed hook in `skillSave` (B1, via `embedAndStoreSkill`)
5. ✅ Tests covering ranking + threshold + anti-list + fallback (B1 + B2, 32 new)

**Substrate-readiness check for Spine 2 (S3 drift detector):** S5 `s5_skill_failure_rate` signal remains `enabled=0` with `awaiting:S5` sentinel — operator can flip it on after Phase 5 certification.

## Out-of-bundle queue (0 new items)

No items deferred from B2. The B1 queue items (S5-P3-B1-W1/W4/W5) carry forward unchanged.

## Cross-references

- Spec sections grounding: §6 (vector retrieval), §10 (anti-list filtering at query layer)
- Phase 3 Bundle 1 commit: `0d0bd5c`
- Phase 2 close: `acfbdfd`
- Spec-delta artifact: `docs/planning/v8-substrate-s5-impl.md` (Phase 3 closure update)
- Reused module: `src/memory/embeddings.ts` (embed + cosineSimilarity)
- Counter-recovery-path discipline carry: W4 stderr log echoes the `feedback_prometheus_counter_recovery_path` pattern at the CLI layer
