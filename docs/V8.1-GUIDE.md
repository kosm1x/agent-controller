# V8.1 — Operating Guide (Proactive Context Engine)

**Status**: **OPEN** (opened 2026-05-20).
**Predecessor**: [`V7.7-CLOSURE.md`](./V7.7-CLOSURE.md) (tag `v7.7-closed`) — the substrate phase.
**Spec**: [`docs/planning/v8-capability-1-spec.md`](./planning/v8-capability-1-spec.md) — the full design; this guide is the operating layer over it.
**Vision**: [`V8-VISION.md`](./V8-VISION.md) §4-V8.1.
**Operating principle**: _v8.0 is the first capability phase — substrate becomes behaviour._

This is the **first v8.0 capability phase**. v7.7 shipped the foundation; V8.1 is the first item to consume it.

## Phase progress

| Phase                                                  | Status                                                   | Spec ref         | Commits / audit                                                                                                                                                                                       |
| ------------------------------------------------------ | -------------------------------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A — Substrate reconciliation                           | ✅ **Done** (2026-05-20)                                 | §12 P1-3 vs v7.7 | `1a9fc63` (item 2) + item-1 bundle; R1+R2 audited                                                                                                                                                     |
| 4 — Reflection runner + cursor + bounded-diff scope    | ✅ **Done** (2026-05-20)                                 | spec §12 Phase 4 | `src/reflection/` cursors+scope+runner; R1 PASS-with-warnings, folded                                                                                                                                 |
| 5 — Detection algorithms                               | ✅ **Done** (2026-05-20)                                 | spec §12 Phase 5 | `src/detection/` 4 detectors; R1 PASS-with-warnings, folded                                                                                                                                           |
| 6 — Briefing schema + judgment prompt + S2 integration | ✅ **Done** (2026-05-20)                                 | spec §12 Phase 6 | `src/briefing/` schema+storage+prompt+construct; R1 FAIL→fold→R2 PASS                                                                                                                                 |
| 7 — Triggers (N-turn + cron + idle)                    | ✅ **Done** (2026-05-20)                                 | spec §12 Phase 7 | `src/triggers/` n-turn+morning-surface+idle-detect; R1 PASS, W1/W3/W4 folded                                                                                                                          |
| 8 — Surface delivery                                   | ✅ **Done** (2026-05-20)                                 | spec §12 Phase 8 | `src/briefing/` render+delivery+promote; router `sendBriefingToOwner`; R1 PASS, W1/W3/W5 folded                                                                                                       |
| 9 — Activation & curation                              | 🟢 **Activated 2026-05-20** — 7-day live run in progress | spec §12 Phase 9 | `src/briefing/activation-gate.ts` + `recordReflectionCost` + `mc-ctl briefing-gate`; R1 PASS. `V81_BRIEF_DELIVERY_ENABLED=true` flipped 2026-05-20. **Live run + curation in progress** (see runbook) |

---

## Mission

Ship V8.1 — the **Proactive Context Engine**. The shift V8-VISION §4-V8.1 names: from a once-daily ritual that _summarizes data_ into a continuous reflection process that _constructs judgments_. The morning brief becomes one surface of that engine — "this is at risk, this has momentum, this is today's highest-leverage move" — not "here is what happened."

Orienting metric: **the operator stops needing to ask "what should I be looking at?"** — the brief already said it. Activation gate (§13 below): cache-read ratio ≥80% over 24h with brief generation included, promote-rate ≥60% on the morning surface within 30 days.

## Substrate already shipped (v7.7 — NOT v8.1 deliverables)

V8.1 spec §12 lists Phases 1-3 as Conway-pattern infrastructure. **v7.7 shipped all three** (Spines 4-6). Phase A reconciles them — they shipped with deliberate deviations from the spec §12 phrasing:

| Spec §12 phase                 | v7.7 delivery                                                       | Deviation V8.1 must account for                                                                                                                                                                           |
| ------------------------------ | ------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Phase 1 — general-events layer | v7.7 Spine 4 (`general_events` + `retrieveForBriefing`)             | BLOB embeddings not `vec0`; 12-event manual seed (spec wanted 30-50); auto-discovery deferred (`S4-B1-W4`).                                                                                               |
| Phase 2 — self-defining cohort | v7.7 Spine 5 (`self_defining_cohort` + `operator_profile` skeleton) | A denormalized table, NOT the `jarvis_files.qualifier` enum extension. **The S1 prompt-wiring (spec Phase 2 step 2) was NOT done** — V8.1 Phase A must wire the cohort into the stable prompt section.    |
| Phase 3 — recall modes         | v7.7 Spine 6 (`recallMode` + `recall_audit.mode` + drift signal)    | Additive, not a rename. The correspondence audit is a drift signal, not a cron. **`recall_audit` is dormant** (`S6-recall-audit-dormant`) — the audit produces no live data until recall logging resumes. |

## The seven phases

V8.1 phases are **sequential** (unlike v7.7's parallel spines) — each consumes the prior. Spec §12 is the authoritative per-phase design; this guide is the operating layer.

### Phase A — Substrate reconciliation (~1-2 days)

Confirm the v7.7-shipped Conway substrate is V8.1-ready and close the gaps the spec §12 phases assumed:

1. **Cohort → prompt wiring** — ✅ **DONE** (2026-05-20). The self-defining cohort is surfaced in `buildJarvisSystemPrompt`'s stable (P2) section via a new pure `cohortSection()` — Conway Pattern 2's grounding payoff. **Security:** the cohort is operator-private (business names, health objectives), so `cohortSection` takes a required `ownerChannel` param (default-deny) and the router gates it with `isOwnerChannel()` — community-manager/public channels get nothing. R1 audit caught the leak as a Critical; R2 verified the fix. Live-verified: owner channel renders 26 members, public channel renders `""`.
2. **`recall_audit` dormancy** (`S6-recall-audit-dormant`) — ✅ **DONE** (bundle `V8.1-PA-recall-logging`, 2026-05-20). Decision: restore `logRecall` — wired into `SqliteMemoryBackend`'s primary path via an `instrument` constructor flag (not `HINDSIGHT_ENABLED=true`, which would reverse the queue #15 demote verdict). `recall_audit` receives fresh `source='sqlite-primary'` rows again; live-verified post-deploy. **Remaining sub-item 2b**: the Spine 6 coherence-drift signal stays `enabled=0` until its baseline is recomputed from ~7d of fresh rows (the old 0.0406 was Hindsight-era) — then flip `enabled=1`. Scheduled: routine `trig_012BrPZ5VGXZPaqcPW6f8vt4`, fires 2026-05-27.
3. **General-events seed sufficiency** — ✅ **DECIDED** (2026-05-20, accepted-with-rationale). Live count: 12 `general_events` rows, all `created_by='seed'` — thin vs the spec's 30-50. Decision: **do NOT speculatively bulk-seed.** Hand-authored seed events are guesses; the designed population path is auto-discovery (`S4-B1-W4` — nightly LLM clustering of episodic memory into general events), which produces events derived from real data. Auto-discovery is **NOT in Phase A scope** (it is a capability bundle, not reconciliation) but is **promoted to a Phase 6 prerequisite**: it must land before Phase 6's briefing retrieval, OR Phase 6 entry re-evaluates seed sufficiency against the then-concrete `retrieveForBriefing` shape. 12 seed events is accepted for Phases 4-5 (which do not consume briefing retrieval).

**Done when**: the three gaps are closed or explicitly accepted-with-rationale, and `retrieveForBriefing` returns a usable cohort + general-events set. ✅ **All three closed** — 1 + 2 shipped, 3 decided. Phase A complete.

### Phases 4-9 — spec §12, verbatim

The remaining six phases follow spec §12 exactly — reflection runner + cursor (Phase 4), detection algorithms (5), briefing schema + judgment prompt + S2 integration (6), triggers (7), surface delivery (8), activation & curation (9). See `docs/planning/v8-capability-1-spec.md` §12 for each phase's task list and §6-§11 for the detailed design.

**Phase 4 carry-overs into Phase 5/6** (from the Phase 4 R1 audit; each is flagged with an inline `PHASE 5/6 NOTE` comment at the code site):

- **Phase 5** — `scope.ts` `idle-detect` bounds the stalled subset by `id > cursor`, but staleness is a time property, not an insertion-order one; a task that stalls below the cursor is invisible. Phase 5 decides whether idle-detect is cursor-bounded at all (audit R2). Phase 5 also enriches `deltaEvents` beyond the task skeleton (`conversations`/`cost_ledger`/`general_events`/`recall_audit`).
- **Phase 6** — the real judgment prompt MUST keep an explicit `STATUS:`-line contract: `fastRunner` gates `output.success` on it, and a status-less reflection silently stalls the cursor (audit W4). Phase 6 must also empirically verify the role-reframe holds against the `getEssentialFacts` mc-jarvis identity injection on the chat path (audit W3).

**Phase 5 carry-overs into Phase 6** (from the Phase 5 live verification):

- The detectors are deliberately **high-recall / low-precision** — Phase 6's judgment is the precision filter. The live run produced 10 stalled / 4 dormant / **183** implicit-deadline candidates / 3 recurring-blocker clusters. Phase 6's judgment prompt must rank and discard, not echo: most of the 183 "deadlines" are dates in report titles, not commitments (the documented `extractDates` FP class).
- The dormant-objective task↔objective link (`tasks.metadata LIKE '%path%'`) is **weak** — the live run flagged all 4 objectives as "no task activity on record" because task metadata does not carry objective paths. Phase 6 should weight `dormant_objective` signals cautiously, or a follow-up should strengthen the link (e.g. a real `task→objective` foreign key).
- `recurring_blocker` is the highest-signal detector (live: `mission-control:latest` image-not-found, container timeouts — real, actionable). Lead with it.

**Phase 6 carry-overs into Phase 7/8** (deferred sub-pieces + the Phase 6 audit):

- **`triage_policies` + the learned-policy loop → Phase 8.** The §9 table and the LLM policy-rewrite-on-promote/discard belong with surface delivery. Phase 8 must also wire the §10 prompt's `triage_policies.policy_text` read into `renderJudgmentPrompt` (Phase 6 ships the prompt without it).
- **Promote/discard transitions → Phase 8.** `proposed_briefings` rows are written `pending` and supersede prior pending; the `promoted`/`discarded`/`expired` transitions need operator interaction. `recentlyDiscarded` is wired through `renderJudgmentPrompt` as `[]` until then.
- **Formal S5 skill registration deferred.** The judgment prompt is a code-module template; `skill__construct_morning_brief` certification (S5 lifecycle) is a follow-up.
- **`signal_id` is judgment-minted, not a detector FK** (audit W1). `DetectionSignal` has no id; the LLM mints each judgment's `signal_id`, and `subject` is the only linkage back to a detected row. If Phase 7/8 needs to correlate a judgment to its detector row, give detectors stable ids and thread them through.
- **Item-level evidence is deferred.** `verified_against` is source-level (4 citations: tasks/objectives/general_events/cohort); per-signal citations are a refinement.
- **`constructBriefing` is built but un-triggered** — Phase 7 wires the N-turn / cron / idle triggers. Live-verified only at the prompt-render layer (no inference run).
- **Seed-sufficiency gate (Phase A item 3) — resolved at Phase 7, not Phase 6.** Phase A item 3 set a gate: auto-discovery (`S4-B1-W4`) lands before Phase 6 briefing retrieval, OR Phase 6 re-evaluates seed sufficiency. Auto-discovery did not land; Phase 6 shipped `constructBriefing` **un-triggered**, so `retrieveForBriefing` never ran against a live brief — the gate's real decision point is **Phase 7 entry** (when a trigger first fires retrieval for real). 12 seed `general_events` are accepted for the un-triggered Phase 6; Phase 7 must re-evaluate seed sufficiency before enabling any trigger that generates a brief operators see (closure audit W1).
- **`recurring_blockers.named_by_briefing_id` is a soft (un-enforced) reference → Phase 8.** Unlike `proposed_briefings.superseded_by_briefing_id`, it carries no `REFERENCES` FK. The column is unwritten until Phase 8; the Phase 8 writer must either enforce the briefing-id link at the application layer or do a migration to add the FK (closure audit W2).

**Phase 7 carry-overs into Phase 8** (deferred sub-pieces + the Phase 7 audit):

- **Triggers fire, but produce only NON-operator-facing artifacts.** Phase 7 wires all three triggers (`src/triggers/`): N-turn (`task.completed` subscription, every 5th `spawn_type='root'` completion → `runReflection`), cron morning-surface (06:00 → `constructBriefing`, persists a `pending` `proposed_briefings` row), idle-detect (30-min cron → `runReflection` when 4h-idle + a stalled task, throttled 1/12h). Delivery to the operator is Phase 8 — a Phase 7 trigger's job ends at a persisted pending briefing / a logged reflection pass (anti-mission held).
- **`rituals/morning.ts` is NOT touched.** Spec §6 Trigger 2 says cron-morning "replaces the existing `rituals/morning.ts`"; §12 Phase 8 item 3 is the actual ritual migration. Phase 7's morning-surface runs ALONGSIDE the existing `morning-briefing` ritual (which still emails the operator); Phase 8 collapses the old ritual into a thin wrapper / removes it. Two morning paths run concurrently until then — only the new one is non-delivering.
- **Seed-sufficiency entry gate (Phase A item 3 / closure audit W1) — honored by DEFERRAL.** Phase 7 keeps the brief non-operator-facing, so a thin retrieval layer (12 seed `general_events`, auto-discovery `S4-B1-W4` not landed) cannot reach an operator. `constructBriefing` self-flags a thin window with a `stale_data` producer concern, carried on the persisted row. **Phase 8 MUST re-check seed sufficiency before wiring delivery** — that is the gate's true decision point.
- **`triage_policies` + promote/discard + S5 skill registration** — still Phase 8 (unchanged from the Phase 6 carry-overs above).
- **§14-Q4 cost ceiling is enforced** — `trigger_runs` (DB-backed) caps reflection runs at ≤10/24h (n-turn + idle-detect, rolling window) and throttles idle-detect to 1/12h. The N-turn counter itself is in-memory (approximate cadence; a deploy restart loses ≤4 counts — harmless). `V81_TRIGGERS_ENABLED=false` is the kill switch.
- **First live fires are the production verification.** Phase 7 verified: clean deploy, all 3 triggers registered, `trigger_runs` table + CHECK constraint live. The first natural fires — n-turn on foreground traffic, idle-detect 30-min cron, morning-surface 06:00 — are observable via `SELECT * FROM trigger_runs`. Phase 8 entry should confirm they fired as expected.
- **Audit W2 was a verified false positive.** The qa-auditor flagged a check-then-act race on the cost ceiling; the budget check and the `recordTriggerRun` are sequential synchronous statements (better-sqlite3 is sync) with no `await` between them, so the ceiling is a hard bound, not best-effort. A code comment in `n-turn.ts` pins this — do not insert an `await` between the check and the row write.

**Phase 8 carry-overs into Phase 9** (deferred sub-pieces + the Phase 8 audit):

- **Seed-sufficiency gate — CLEARED at Phase 8 entry.** The gate (Phase A item 3, carried through Phases 6/7 above) said re-check `general_events` before operator-facing delivery. Live count at Phase 8 entry: **36 seed rows** — within the spec §5 target band (30-50). The "12" in the Phase A / Phase 6 / Phase 7 notes above was the count at those earlier dates; it is now 36. Gate cleared; delivery is built behind `V81_BRIEF_DELIVERY_ENABLED` regardless, and `constructBriefing` still self-flags a thin retrieval window via a `stale_data` producer concern. Auto-discovery (`S4-B1-W4`) remains the designed long-term population path.
- **Delivery is flag-gated OFF (`V81_BRIEF_DELIVERY_ENABLED`).** Phase 8 ships the full delivery path — `renderBriefing` (Briefing → Spanish markdown), `deliverBriefing` (router `sendBriefingToOwner` → Telegram/WhatsApp/owner-only email), promote/discard on the operator's next reply (`resolveBriefingOnOperatorReply`, hooked into `router.handleInbound`, owner-channel-gated), `triage_policies` counters. **Phase 9 activates it** (flip the flag after a shadow run), per the spec's Phase 8-build / Phase 9-activate split. The first real operator delivery is the Phase 9 live-verify.
- **Shadow-run is intentional (audit C1).** With the flag off, the morning-surface trigger generates a briefing daily WHILE the legacy `morning-briefing` ritual still emails the operator — two LLM generations/day. This is the deliberate shadow window: the generated `pending` briefings are what Phase 9 curation evaluates. When the flag flips on, `scheduler.ts` skips the legacy ritual → exactly one delivered brief in every flag state.
- **Single-operator assumption (audit C2).** `sendBriefingToOwner` fans out to every owner-only channel; `getResolvablePendingBriefing` is a global singleton. Correct for one operator (Fede). A multi-operator deployment would need per-operator briefing scoping — a Phase 9+ concern, documented in the `router.ts` method.
- **`triage_policies` LLM rewrite loop → deferred.** Phase 8 ships the table + promote/discard counters; the LLM `policy_text` rewrite-on-outcome (spec §9 LangChain-ambient port) needs real promote/discard history to be meaningful — it belongs after delivery has produced data (Phase 9+). `policy_text` defaults to `''`.
- **Promoted-brief → `general_events` write-back → deferred.** Spec §9 says a promoted briefing becomes a Pattern-1 `general_events` entry. That memory write-back needs the embedding pipeline; Phase 8 only transitions `status`. Discarded briefings are already retained (`status='discarded'` rows) for correspondence-mode recall.
- **`recurring_blockers.named_by_briefing_id` soft-FK** — still unwritten (carried from Phase 6). Naming a recurring blocker to a briefing would happen when a `recurring_blocker` judgment is promoted; not wired in Phase 8.
- **Expiry sweep added (audit W1).** `expireStalePendingBriefings()` runs in the daily morning-surface trigger so a delivered briefing the operator never replied to reaches `status='expired'` (the §13 metrics depend on it); `promote.ts` also expires lazily on the next reply.

## Phase 9 — Activation runbook (operator + calendar)

Phase 9's three spec items split into a **build** part (shipped 2026-05-20) and an **operate** part (operator-driven, calendar-gated). The build part:

- **`recordReflectionCost`** (`src/budget/service.ts`) — tags reflection / briefing inference into `cost_ledger` as `agent_type='reflection:<surface>'`. Before this the §13 cache-read query had zero rows; `runReflection` and `constructBriefing` run outside the dispatcher, so the dispatcher's `recordCost` never saw them. Recorded for any completed inference round-trip (success or failure).
- **`evaluateActivationGate`** (`src/briefing/activation-gate.ts`) + **`mc-ctl briefing-gate`** — the §13 gate: cache-read ratio ≥80% over ≥5 reflection runs (24h) + morning-brief promote-rate ≥60% (7d). Pure read; verdict `pass` / `fail` / `insufficient_data` (exit 0 / 1 / 2).

> **STATUS — activated 2026-05-20.** `V81_BRIEF_DELIVERY_ENABLED=true` was
> flipped via `activate-briefing-delivery.sh` and verified live (legacy ritual
> superseded, var confirmed in the running process). Step 1 is **done**; the
> 7-day live run is **in progress** — currently at steps 2-3.

The operate part — **what the operator does, in order:**

1. ✅ **Flip the flag** (done 2026-05-20). Run `sudo ./scripts/activate-briefing-delivery.sh on` — it backs up `.env`, sets `V81_BRIEF_DELIVERY_ENABLED=true`, restarts `mission-control`, verifies the PID transition, and confirms the var reached the running process. This activates `deliverBriefing` in the morning-surface trigger AND makes `scheduler.ts` skip the legacy `morning-briefing` ritual — the operator's morning email becomes the new judgment briefing. Reversible: `sudo ./scripts/activate-briefing-delivery.sh off` falls back to the legacy ritual. (It is a code-free env flip — no rebuild; `./scripts/deploy.sh` is not needed.)
2. **Run live 7 days** (in progress — started 2026-05-20; first delivery at the next 06:00 MX). The morning-surface cron delivers a briefing each 06:00; the operator promotes/discards by replying. n-turn + idle-detect reflection accrues `reflection:*` cost rows. **Watch the first delivery** — every V8.1 phase shipped a bug only the first live run caught.
3. **Check the gate.** `mc-ctl briefing-gate` — during the run it reads `insufficient_data`; after ~7 days it returns `pass` or `fail`.
4. **Curate** (spec §12 Phase 9 item 1) — review the `general_events` seed (36 rows) and the self-defining cohort against what the briefings actually leaned on; prune/add. `mc-ctl events list` / `mc-ctl cohort list`.
5. **Tune detection thresholds** (item 2) — the 6 thresholds (stalled/dormant/deadline-window/blocker constants in `src/detection/*.ts`) are still code constants; if the 7-day run shows a detector is too noisy or too quiet, adjust + redeploy. (Making them env-tunable was offered and declined for this bundle — revisit if tuning churns.)
6. **Declare V8.1 active** once `mc-ctl briefing-gate` reads `pass` and the promote-rate holds ≥60% — that closes Phase 9 and V8.1.

**Deferred past Phase 9** (need real promote/discard history first): the `triage_policies` LLM `policy_text` rewrite loop; the promoted-brief → `general_events` write-back; `reflection_followups` §10.5 self-scheduled checkpoints.

## Anti-mission — what V8.1 will NOT do

- **No V8.2 / V8.3 work.** V8.1 is the brief; V8.2 is strategic proposals; V8.3 is autonomous execution. If a task drifts toward "the agent proposes an initiative" or "the agent acts," it is V8.2/V8.3 — defer.
- **No new runners.** The reflection runner is the fast-runner with a reflection-specific prompt + config (Letta same-class pattern) — not a 6th runner.
- **No memory-architecture redesign.** pgvector + the v7.7 Conway substrate are the memory layer. Compose against them.
- **The brief constructs judgments, it does not just summarize.** A brief that reads as "here is what happened" has failed the mission — `noted`-posture >60% is the §13 watchpoint that says so.
- **No autopilot.** Briefings are proposals (`proposed_briefings` with promote/discard); the operator's first morning interaction promotes or discards. Reversibility from day one.

## Phase sizing

| Phase                               | Estimate |
| ----------------------------------- | -------- |
| A — reconciliation                  | 1-2d     |
| 4 — reflection runner               | 3d       |
| 5 — detection algorithms            | 2d       |
| 6 — briefing schema + judgment + S2 | 3d       |
| 7 — triggers                        | 2d       |
| 8 — surface delivery                | 2d       |
| 9 — activation & curation           | 1d       |

**Sum**: ~14-15d focused. v7.7's actual cadence (the 4-week projection landed in ~3 days of focused work via audit-in-bundle) suggests faster elapsed; do not over-commit a date — V8.1 is the largest single capability ship and Phase 6 (judgment) is genuinely hard.

## Operating rules

All v7.7 rules carry forward. Especially load-bearing for V8.1:

1. **Audit every bundle** — qa-auditor R1 per ship; R2 on Critical-bearing/contract-drift bundles. v7.7's ~33% real-bug rate held across ~24 audits.
2. **Verify auditor empirical claims** — reinforced ~7× across v7.7. An auditor's "X does not exist" needs checking against the authoritative source (git, the live DB), not a proxy.
3. **Verify live before claiming done** — deploy + run it + read the output. Three v7.7 spines shipped a bug only the first live run exposed.
4. **Spec-vs-codebase reconciliation** — Phase A IS this rule applied to V8.1's own substrate. The spec is design intent; verify against what v7.7 actually shipped.
5. **Construct, do not summarize** — the mission discriminator. Every briefing-related bundle is judged against it.

## Activation gate & measurement

Per spec §13 / V8-VISION §4-V8.1:

- **Cache-read ratio ≥80%** over a 24h window with brief generation included.
- **Promote-rate ≥60%** on the morning surface within 30 days.
- **Audit-cycle rate → 0** within 60 days (the S2 critic should make "Audited?" unnecessary).
- Watchpoints: `noted`-posture >60% (summarizing not judging), promote-rate <30% (briefs not useful), cache <70% (cohort/general-events sizing).

## Cross-references

- Spec: `docs/planning/v8-capability-1-spec.md` (§5 general-events, §6 trigger model, §7 detection, §8 briefing schema, §9 judgment prompt, §12 phasing, §13 measurement, §14 open questions)
- Substrate predecessors: `docs/planning/v7.7-spine-{4,5,6}-impl.md` (Conway 1-3)
- v7.7 closure: `docs/V7.7-CLOSURE.md` · Watchlist: `docs/planning/v7.7-leftovers-queue.md`
- Vision: `V8-VISION.md` §4-V8.1 + §10

---

**Status at authoring (2026-05-20)**: V8.1 NOT STARTED. v7.7 substrate complete. Recommended entry: **Phase A** (substrate reconciliation) — it surfaces what the spec §12 phases assumed but v7.7 left as gaps, before the sequential capability build begins.
